# raybend 浏览模式规格（BROWSE.md）

> 本文件是**浏览模式（Browse）界面与交互**的事实来源。来自用户 2026-09-15 的完整口述，
> Agent 负责整理、指出冲突与待确认项，**不擅自改语义**。
>
> 相关：`LIBRARY.md`（库与导入）、`DESIGN.md`（视觉与配色）、`AGENTS.md` §11（术语表）。
> 状态：📝 **规格已记录；界面设计进行中；实现排期待用户开 plan 后重排**。
>
> 建立时间：2026-09-15

---

## 0. 一句话定位

浏览模式是**看片、挑片、打标**的主战场：左边选库与目录，中间看图，右边看信息。
它与导入模式的**根本区别**：导入模式是「把照片弄进库」，浏览模式是「对库里的照片做评估与组织」。

---

## 1. 结构总览

仍然是三列（与导入模式同构，但内容与职责不同）：

```text
┌──────────────────────────────────────────────────────────┐
│ titlebar（三键、主题、密度…）                              │
├──────────────────────────────────────────────────────────┤
│ flowbar（工作流切换 + 图片信息 + 磁吸）                    │
├──────────────────────────────────────────────────────────┤
│ toolsbar（浏览模式专属功能：筛选 / 标记 / 标签 / 锁）       │ ← 见 §3
├────────────────┬───────────────────────────┬─────────────┤
│ 左：库目录选择器 │ 中：照片 tiles / 看图      │ 右：信息栏   │
│ （§4）          │ （§5）                     │ （§6）      │
└────────────────┴───────────────────────────┴─────────────┘
```

---

## 2. toolsbar 在浏览模式下的功能（§3 的入口）

浏览模式的 `toolsbar` 承载四组能力，**全部只在浏览模式下出现**：

| 组 | 内容 | 语义 |
| --- | --- | --- |
| **筛选开关** | 一个可按下式开关 | 打开后，**同一行后面的标记/标签/锁全部从「设置」变成「筛选」**（见 §3.1） |
| **标记系列** | 旗标 / 移除所有旗标 / 标星 / 标色 / 喜欢 | 对选中照片打标，支持批量（§3.2） |
| **设置标签** | 打开标签弹窗 | 单个可增可删；批量**只能加**（§3.3） |
| **设置锁** | 两级锁 | 一级=不可删，二级=不可编辑（§3.4） |

---

## 3. toolsbar 的四组能力（详细语义）

### 3.1 筛选状态（**这是本模块最独特的机制**）

| 规则 | 说明 |
| --- | --- |
| 开启方式 | `toolsbar` 上的一个可按下式开关（形态与「吸附」「按时间」一致：关=平，开=主色底） |
| 开关打开后 | 后面的**标记系列、标签、锁**全部切换成**筛选模式**：它们不再是「给选中图片设置值」，而是「**对当前 tiles / 胶片带做筛选**」 |
| 影响范围 | **只筛选当前的 tiles 或胶片带内容**；不改变库、不改变目录 |
| **不影响看图** | 看图模式下开筛选**不影响当前正在看的那张图** —— 哪怕它不在筛选范围内，也只是**在胶片带里选不到它**，看图状态本身不变 |
| 典型用法（用户点名的"很爽"场景） | 选中一张或多张图片时，上面的标记/标签/锁会显示成**这些图片的状态**；此时打开筛选 → **直接筛出同类图片**（例如选一张 3 星的红标图 → 开筛选 → 所有 3 星或红标图留下） |

> 待确认（§10 问题 1）：多选状态下开筛选时，**多个条件之间是「或」还是「与」**？
> 直觉上应当是「与」（更精确），但用户描述的例子读起来像「或」。

### 3.2 标记系列（对选中图片打标，支持批量）

| 标记 | 取值范围 | 持久化 | 备注 |
| --- | --- | --- | --- |
| **旗标** | 开 / 关（只有两态，明确） | ❌ **不持久化，只在内存中** | **跨库、跨目录**都有效；用来「临时标记一堆要处理的东西」；**一定程度上替代未来规划的 bucket**（见 §11） |
| **移除所有旗标** | — | — | 清空内存中的**所有**旗标（效果类似关掉软件重开）；**点击要确认**（`easy destroy` 范式） |
| **标星** | 0–5 星 | ✅ | 在已选中且为 1 星时**再点一次 → 扣成 0 星** |
| **标色** | 红 / 黄 / 绿 / 蓝 / 紫 / 无 共 6 态 | ✅ | — |
| **喜欢** | 喜欢 / 不喜欢 / 取消（三态） | ✅ | 点赞图标 |

**所有标记共同遵循的状态规则**（这条决定了组件的视觉语言）：

| 选择状态 | 标记组件的显示 |
| --- | --- |
| **未选图片** | 全部处于**无状态**（默认态，没有任何值被点亮） |
| **选中单张** | 显示该图的真实状态（例如 3 星就点亮 3 颗），**并且可以直接调整** |
| **选中多张且取值不一致** | 显示为**类似"未选中"的中性态**（既不是有值也不是无值的"混合态"），方便批量改；**改完统一后恢复正常显示** |

> 这就是「三态控件」：`无值 / 有值 / 混合`。混合态必须与无值态**视觉可区分**（§10 问题 2）。

### 3.3 设置标签（tagging）

| 场景 | 行为 |
| --- | --- |
| **单张图片** | 弹窗内可**增加**也可**移除**已有标签；标签以 **flex 横向瀑布流**列出，**每个标签右边带一个叉**（点叉移除） |
| **多张图片（批量）** | 弹窗内**只能添加**（不能移除）；添加时**自动去重** |
| 标签输入组件 | 见 §7（输入即搜、0.6s 防抖、回车创建新标签） |

### 3.4 设置锁（lock）

| 级别 | 含义 |
| --- | --- |
| **一级锁** | **不能删除** |
| **二级锁** | **不能编辑**（更严；显然也不可删） |

- 支持**批量**，批量的状态规则与 §3.2 的三态规则一致；
- 锁状态在**图片上要有明确显示**（tiles 上、看图时、胶片带上都要能看出来）；
- 底部状态栏里：看图模式下**文件名右边紧挨着就是锁状态**（§5.8）。

---

## 4. 左列：库目录选择器

从上到下**三个部分**：

### 4.1 搜索条

- **本期（简单版）**：只搜**库名 / 目录名**；
- **后续**：扩展到**照片信息**（标签 / 注释 / 文件名 / 日期 / EXIF 品牌等），登记见 §11；
- **结果的作用是「过滤」而不是「跳转」**：界面内的**库 / 库下目录 / 右侧照片 tiles 都只显示符合条件的内容**；
- **多层级目录**：若命中的是深层目录，则该目录的**整条树分支链路都要显示**（否则用户看不出它在哪）。

### 4.2 紧缩库列表（compact library list）

纵向排列的库列表，**长条形**显示，组件与导入模式几乎一样（含离线徽标、齿轮改导入模版、点离线检查在线）。

**截断规则（用户明确，必须照做）**：

```text
可见高度 = 最多 3.5 个库卡片
  ├─ ≤ 3 个库：正常全部显示，区域随内容撑开（用户感知不到"有限制"）
  └─ > 3 个库：第 4 个只显示「半个」
                └─ 那半个里**不是库信息**，而是只在**被看见的那半部分**显示「查看所有库」
                   （用户点击 → 库列表展开到「除搜索条外的全部空间」，
                     并**完全吃掉**下方库内目录树的显示区域）
```

**展开态**：

- 展开后的库列表**支持滚动**，显示**所有**库；
- **每次点击的库都会排到最顶部**；
- **进入浏览模式时默认进入最顶部那个库**；
- **导入模式下每次导入照片，也会把该库在浏览界面中移到顶部** ——
  这样「最常用的库 / 最近导入的库」自然浮在顶部，被默认进入或方便点击；

**移顶规则的例外（用户特别强调，别做错）**：

| 库列表状态 | 点击库的行为 |
| --- | --- |
| **展开态** | 点中的库**移至顶部**并选中；同时列表**收缩**，下方显示该库的目录树 |
| **收缩态** | 在**顶部三个库之间来回切换不会移动位置**（避免日常切换时列表乱跳） |
| **导入行为** | **不管库当前处于什么位置、列表是什么状态，都强制把该库移顶** |

**动画**：展开/收缩**要有弹性动画**（用户要求）—— 动画库选型见 §8.3。

**多导入模版**：将来每个库可设**多个**导入模版，导入时选一个。**优先级不高，先保持简约** → §11。

### 4.3 库内目录树

- 选中库后，库列表**收缩**并把选中的库放到**顶部并选中**，同时下方显示该库的目录树；
- 就是一个目录树：**选中会自动展开**；**右边有展开/收起的图标**；
  **点选（选中）与展开/收起必须是两个不同的动作**（点名字=选中，点图标=展开）—— 不要合并成一个；
- **右侧三点菜单**（`⋯`）提供：

| 菜单项 | 行为 |
| --- | --- |
| **删除空目录** | 选项**一直在**；目录不为空时**暗掉不可点**。该功能**支持同步删除其下所有空子目录** ⇒ 点开时要**做一次深度检索**（判断"是否真的没有文件"） |
| **创建子目录** | 弹**中央模态窗**输入子目录名；**创建时检查重名**，重名要提示 |

---

## 5. 中列：照片 tiles / 看图 / 对比

### 5.1 与导入模式的复用关系

- **组件基于导入模式的照片网格**（`PhotoGrid` / `Tile`），但**功能更强大**；
- 差异点：浏览模式下有评级标记的显示、有看图/对比模式、右栏内容不同。

### 5.2 选择逻辑（**所有工作流通用**，tiles 与胶片带一致）

| 操作 | 行为 |
| --- | --- |
| **点击** | 选中该图（**先移除原有选中**，再选中当前这张） |
| **Shift + 点击** | **区间翻转**（见下方严格语义） |
| **Ctrl + 点击** | 自由多选（**包含反选**：已选中的再点一下取消） |
| **在「按日期」模式下** | 可直接点**时间范围内的选中**（点时间片/日组即选中该范围内全部） |
| **无修饰键的点击** | **任何时候**都是「移除原有选中 + 选中当前」 |
| **Esc** | 取消所有选中 |

**Shift 区间翻转的严格语义（用户逐字描述，必须照做）**：

```text
ABCDE 五张连续图片：
  1. 点 A（A 选中，A 成为「上一次的图」）
  2. 按住 Shift 点 C → A..C 之间（B 和 C）的选中状态**翻转**；
     注意**不包括**上一次的图（A），**包括**这一次点的图（C）；
     然后 C 成为新的「上一次的图」
  3. 按住 Shift 点 E → 从 C 开始计，D 和 E 翻转
  4. 按住 Shift 再点 A → A B C D 全部翻转
```

- 这样做的理由（用户原话）：**逻辑简单、无需保存复杂状态、操作又灵活**；
- **Ctrl / Shift 的所有选中操作都是「翻转」而不是单纯的「置为选中」**；
- **暂不支持跨目录选择** → 未来用**图片筒（picture bucket）**实现跨目录批量操作（§11）。

### 5.3 进入 / 退出看图

| 操作 | 结果 |
| --- | --- |
| **双击图片** 或 **回车** | 进入**看图**（不再是「放到全屏观看」） |
| **看图模式** | 下方形成**胶片带**（横向滚动选图），上方显示照片 |
| **Esc** 或 **回车** | 退回 tiles |
| **选中多张时按回车** | 进入**图片对比模式**（§5.7） |

### 5.4 看图模式的三种显示状态（`Tab` 循环）

| 状态 | 内容 |
| --- | --- |
| ① 默认 | 左边的库目录选择器 + 右边信息栏都在 |
| ② 关闭左右 | 只有中间看图部分最大化（**包括**下面的胶片带）；**顶部区域不受影响** |
| ③ 关闭胶片带 | 只关胶片带（左右保持上一状态？见 §10 问题 3） |

- **按回车切回 tiles 时，左右栏都会回来**；
- **tiles 状态不做「tiles 区最大化」功能**（即最大化只在看图模式存在）。

### 5.5 胶片带

- 横向滚动选图；
- **选择逻辑与 tiles 完全一致**（§5.2 的点击 / Shift / Ctrl / Esc 全部适用）；
- 胶片带中多选 → **自然进入多图对比**；反选图片 → **自然从对比中移除**；
- **对比模式下的特殊状态**：只要胶片带开启，**再按一次回车** → 胶片带**只显示当前对比的图片**，
  此时胶片带区域显示**一个主色细边框**表示进入了这种状态；
  - 在此状态下，**即使不按 Ctrl**，点击胶片带中的图也是「带 Ctrl 的效果」而不是简单选中
    ⇒ **点什么图就把什么图移出对比**；
  - 直到**只剩一幅图**时：**退出此状态**，胶片带恢复显示全部图片，并**在胶片带中定位到这最后一张图的位置**。

### 5.6 看图操作

| 操作 | 行为 |
| --- | --- |
| **滚轮** | 缩放 |
| **双击** | 在 **100%** 与 **适合窗口** 之间切换 |
| **← / →** | 左右切图（**所有界面通用**，不限于浏览模式） |

### 5.7 对比模式

| 项 | 规则 |
| --- | --- |
| **缩放** | **比例同步**（所有对比图一起缩放） |
| **位移** | **同步，且按百分比换算**（不是按像素） |
| **画幅比例不一致时** | **以第一幅图的画幅比例为准**，对后面的图**扣等比例区域**做位移同步。例：第一张 4:3、后面是 3:2 → 只能对比「3:2 区域中间抽出来的 4:3」那部分 |
| **点击哪张图** | 视为**当前实际选中的图**，这决定**右侧信息栏显示谁的内容** |

### 5.8 底部状态栏（看图模式与 tiles 不同）

| 模式 | 布局 |
| --- | --- |
| tiles | （同导入模式：计数 / 当前目录 / 视图控制等） |
| **看图** | **左边**：文件名；**紧挨着文件名右边**：**锁状态**；**右边**：当前图片的**标记**（星/色/旗/喜欢） |

### 5.9 全工作流通用的操作（不限于浏览）

| 操作 | 行为 |
| --- | --- |
| **Delete** | 删除文件，**有确认**；支持**批量** |
| **Shift + Delete** | 删除文件，**无确认**；支持批量 |
| **← / →** | 看图时左右切图（§5.6） |

> ⚠️ 与 `AGENTS.md` §11.3 的术语约定冲突检查：那里写「**删除**（delete）：真的从磁盘删文件 —— **第一阶段不做**」。
> 本规格要求浏览模式提供删除（含 `Shift+Delete` 免确认）⇒ **需要用户确认这条是否解除**（§10 问题 4）。

---

## 6. 右列：信息栏

内容随中间的状态不同而不同：

| 模式 | 内容 |
| --- | --- |
| **tiles 模式** | **Exif 详细信息** —— 要**支持滚动**（内容可能很长） |
| **看图模式** | ① **全图缩略图**（放大时要显示**当前看的那块区域的框**）② **直方图** |

**下面这些两种模式下都显示（通用）**：

| 分组 | 字段 |
| --- | --- |
| **标签信息** | 该图的所有标签（点击/右键可增删，见 §7） |
| **文件基础信息** | 文件名 / 创建日期 / 所在路径 / 所属库 / **作者**（可编辑）/ **Description**（可编辑） |
| **地理信息** | GPS 经纬 / Country / Province/State / City / Sublocation（**均可编辑**） |

> 作者字段附注：**将来支持「某库导入时自动设置 author」**（按库配置）→ §11。

---

## 7. 标签体系

### 7.1 存储与检索

- **用过的标签会存进 `app.db`**（全局标签词典，跨库可用 —— 与 `AGENTS.md` §6.4 的 `app.db` 定位一致）；
- 标签**附到图片上**（每库的 `catalog.db` 里记关联），便于搜索。

### 7.2 输入组件（对标 Vue Element 的「带查询的 select input」）

| 行为 | 说明 |
| --- | --- |
| **一边输入一边搜索** | **0.6 秒防抖**（用户建议用 `es-toolkit` 的 debounce） |
| **选中已有标签** | 搜索出结果后可用**上下键**或**鼠标点击**选中 |
| **创建新标签** | 输入完成后，**即使已有匹配结果，只要不选而直接回车** → **把当前输入内容创建为新标签** |
| **移除** | 单图弹窗里标签右带**叉**（§3.3） |

> **标签库的管理界面本期不做**（改名、合并、批量删除等）→ §11。

---

## 8. 两个新的通用浮层（用户 2026-09-15 新增要求）

### 8.1 中央模态浮窗（通用）

- **原则上遮挡后面所有界面**；
- 使用场景：**创建库、导入过程（进度弹窗）、创建目录** 等等；
- 导入进度弹窗的具体内容见 `LIBRARY.md` §4.4。

### 8.2 右上角非遮挡提示（toast）

| 项 | 要求 |
| --- | --- |
| **位置** | **`titlebar` 正下方、右上角** |
| **形态** | 一个**清晰的框，可以带边框** |
| **边框颜色表状态** | 成功 = **主色**（`$brand`）；警告/错误 = **danger**（`$danger`） |
| **底色** | 浅深色 / 深浅色（即 `$surface-bar` / `$surface-layer` 这一档） |
| **时长** | **约 5 秒自动消失** |
| **不阻塞** | 除了自身显示，**不阻挡界面上任何操作** |
| **占用空间** | 大致是「图片信息 / 磁吸控制」那块的位置；可能会**稍微压到 `toolsbar` 一点点**，但 `toolsbar` 是**居中**的，所以影响很小（用户判断：目前空间非常充足） |
| **为什么放这里** | ① 不挡操作（右上角是空白拖拽区）；② **将来可以在 `titlebar` 右侧放「消息中心」**，把消息作为列表塞进去 → §11 |

### 8.3 弹性动画的库选型（用户提问，Agent 调研结果）

需求：**库列表展开/收缩**要有**弹性动画**，最好有靠谱的、**能用在 Solid 上**的库。

| 候选 | 版本（2026-09-15 实测 npm） | 适配 Solid | 评价 |
| --- | --- | --- | --- |
| **`@formkit/auto-animate`** | 0.10.0 | ✅ **不需要适配层**（框架无关，`autoAnimate(element)` 一行） | **首推**：FLIP 式自动动画，列表增删/重排/容器尺寸变化都管，约 3KB，正好覆盖「展开收缩 + 移顶重排」两个场景。<https://auto-animate.formkit.com/> |
| **`motion`**（motion.dev，framer-motion 的继任者） | 13.3.0 | ⚠️ 官方适配器只有 React/Vue/Svelte，**但核心是框架无关的**（`import { animate, spring } from "motion"` 直接操作 DOM） | 想要**真弹簧物理**与编排（stagger/sequence）时用它；代价是 API 面向 DOM，需要自己接 Solid 的 ref。<https://motion.dev/> |
| **`solid-transition-group`** | 0.3.0 | ✅ Solid 官方生态 | 只做**进入/离开**过渡（CSS 过渡驱动），**没有弹簧物理**；适合面板显隐，不适合"弹性展开"。<https://github.com/solidjs-community/solid-transition-group> |
| **`@solid-primitives/animation`** | 1.0.0-**next**.1 | ✅ | ⚠️ **尚在预发布**（WAAPI 封装、含 FLIP），生产不宜依赖 |
| **`animejs`** | 4.5.0 | ✅ 框架无关 | 老牌、API 直白、v4 支持 spring 缓动；比 auto-animate 重，适合复杂时间线 |
| **`solid-motionone`** / `@motionone/dom` | 1.0.4 / 10.18.0 | ✅ Solid 封装 | ⚠️ **已停止维护**（Motion One 已并入 `motion`） |

**Agent 建议**：先用 **`@formkit/auto-animate`** 满足「展开/收缩 + 移顶重排」；
若之后发现需要真弹簧手感（例如拖拽跟手、惯性），再评估 `motion`。
**注意与现有动效纪律的冲突**：`src/styles/motion.css` 规定「时长 ≤150ms、只动 transform/opacity」，
而**展开/收缩必然要动高度**（会触发布局）—— 这是**有意的例外**，
要在 `DESIGN.md` 的动效章节里明确写出来（§10 问题 5）。

---

## 9. 与现有文档 / 代码的关系

| 项 | 关系 |
| --- | --- |
| **照片网格 / Tile** | **复用导入模式的组件**（`src/components/ui/Tile.tsx`、`PhotoGrid`），浏览模式在其上加评级与看图 |
| **`app.db`** | 标签词典（§7.1）；`AGENTS.md` §6.4 已把「全局标签词典」放在那里 ✓ |
| **`catalog.db`** | 每库的评级/标记/标签关联/锁/作者/描述/地理信息 |
| **`DESIGN.md`** | 新增的控件（三态标记、树、toast、模态）要在 §7/§9 补规格 |
| **`LIBRARY.md`** | 导入进度弹窗（§8.1 的中央模态的一个实例）在那边已写 |
| **界面设计** | 待做：`design/browse-*.pen` + 同名 `.md`（见 §12） |

---

## 10. 待你确认

1. **筛选模式下多个条件之间是「与」还是「或」？**（§3.1）
2. **「混合态」的视觉表达**：与「无值态」如何区分？（建议：无值=图标暗；混合=图标半亮/加一个短横或方点），需要你定
3. **`Tab` 三态的循环顺序**：②「关闭左右」与 ③「关闭胶片带」是**并列的两态**（Tab 在 ①②③ 间循环），还是 ③ 是在 ② 基础上的**叠加**？
4. **删除文件**：`AGENTS.md` §11.3 原写「第一阶段不做真删」，本规格要求 Delete/Shift+Delete —— **是否解除该限制**？
5. **弹性动画与既有动效纪律**：确认「展开/收缩约 200–260ms + 弹性曲线、允许动高度」作为**有意例外**写进 `DESIGN.md`
6. **旗标不持久化**：确认"关软件即清空"是**期望行为**（它是临时工作集，不是元数据）
7. **直方图**是否需要 RGB 三通道叠加/分离切换（本期先做单张合成直方图？）
8. **对比模式最多几张**？（建议 2–4 张，超过就只同步第一张的比例）

---

## 11. 明确登记到 `FUTURE.md` 的部分

| # | 事项 |
| --- | --- |
| 1 | **每库多个导入模版**（导入时选一个）—— 用户明确"优先级不高，先简约" |
| 2 | **标签库管理界面**（改名、合并、删除、批量整理） |
| 3 | **`titlebar` 右侧的消息中心**（toast 的历史列表） |
| 4 | **图片筒（picture bucket）**：跨目录 / 跨库的收藏式批量操作，取代「旗标」的临时工作集角色 |
| 5 | **跨目录选择**（依赖 picture bucket） |
| 6 | **每库导入时自动写 `author`** |
| 7 | **搜索扩展到照片信息**（标签 / 注释 / 文件名 / 日期 / EXIF 品牌…） |
| 8 | 对比模式的更多能力（同步方式、多图网格对比等） |

---

## 12. 界面设计计划（**用户要求：先出设计，设计确认后再开 plan 排期**）

设计稿拟放在 `design/browse.pen` + `design/browse.md`（同建成对，`AGENTS.md` §5.1）。
⚠️ **Agent 无法自行新建 `.pen`** —— 需要用户在 Pencil 里新建这个文件并打开（`design/main.md` §6.1 已记录该限制）。

拟画的帧（按优先级）：

| # | 帧 | 内容 |
| --- | --- | --- |
| 1 | `Shell / Browse / Tiles`（主稿，深色紧凑） | 三列：搜索条 + 紧缩库列表（3.5 截断态）+ 目录树 / tiles 网格（含选中与标记显示）/ 右栏 Exif（可滚动） |
| 2 | `States / 标记三态` | 无选中 / 单选（3 星 + 红标）/ 多选不一致（混合态）/ 筛选开启态：标记、标签、锁各一行 |
| 3 | `Shell / Browse / View` | 看图：上方照片 + 下方胶片带 + 右栏（缩略图含视口框 + 直方图 + 标签/文件/地理信息）+ 底部状态（文件名+锁 左，标记 右） |
| 4 | `Shell / Browse / Compare` | 2–3 图并排对比 + 胶片带（细边框态）+ 右栏 |
| 5 | `Shell / Browse / Tiles / ExpandedLibs` | 库列表展开态（吃掉目录树区域、滚动、查看所有库） |
| 6 | `Components / 浮层` | 中央模态（以「创建子目录」为例）+ 右上角 toast（成功主色边框 / 警告 danger 边框） |
| 7 | `States / 选择逻辑` | 点击 / Shift 区间翻转 / Ctrl 多选 / 日期模式区间选 的选中态示意 |
| 8 | `Shell / Browse / View / Focus` | `Tab` 的「关闭左右」最大化态 |

画完后由用户核对，确认无误后再开 plan 排实现顺序（用户明确要求）。
