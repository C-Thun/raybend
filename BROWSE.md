# raybend 浏览模式规格（BROWSE.md）

> 本文件是**浏览模式（Browse）界面与交互**的事实来源。来自用户 2026-09-15 的完整口述，
> Agent 负责整理、指出冲突与待确认项，**不擅自改语义**。
>
> 相关：`REPOSITORY.md`（库与导入）、`DESIGN.md`（视觉与配色）、`AGENTS.md` §11（术语表）。
> 状态：📝 **规格已记录；界面设计进行中；实现排期待用户开 plan 后重排**。
>
> 建立时间：2026-09-15

---

## 0. 一句话定位

浏览模式是**看片、挑片、打标**的主战场：左边选库与目录，中间看图，右边看信息。
它与导入模式的**根本区别**：导入模式是「把照片弄进库」，浏览模式是「对库里的照片做评估与组织」。

---

## 1. 结构总览

仍然是三列（与导入模式同构，但内容与职责不同）：

```text
┌──────────────────────────────────────────────────────────┐
│ titlebar（三键、主题、密度…）                              │
├──────────────────────────────────────────────────────────┤
│ flowbar（工作流切换 + 图片信息 + 磁吸）                    │
├──────────────────────────────────────────────────────────┤
│ toolsbar（浏览模式专属功能：筛选 / 标记 / 标签 / 锁）       │ ← 见 §3
├────────────────┬───────────────────────────┬─────────────┤
│ 左：库目录选择器 │ 中：照片 tiles / 看图      │ 右：信息栏   │
│ （§4）          │ （§5）                     │ （§6）      │
└────────────────┴───────────────────────────┴─────────────┘
```

---

## 2. toolsbar 在浏览模式下的功能（§3 的入口）

浏览模式的 `toolsbar` 承载四组能力，**全部只在浏览模式下出现**：

| 组 | 内容 | 语义 |
| --- | --- | --- |
| **筛选开关** | 一个可按下式开关 | 打开后，**同一行后面的标记/标签/锁全部从「设置」变成「筛选」**（见 §3.1） |
| **标记系列** | 旗标 / 清空旗标 / 标星 / 标色 / 喜欢 | 对选中照片打标，支持批量（§3.2） |
| **设置标签** | 打开标签弹窗 | 单个可增可删；批量**只能加**（§3.3） |
| **设置锁** | 两级锁 | 一级=不可删，二级=不可编辑（§3.4） |

---

## 3. toolsbar 的四组能力（详细语义）

### 3.1 筛选状态（**这是本模块最独特的机制**）

| 规则 | 说明 |
| --- | --- |
| 开启方式 | `toolsbar` 上的一个可按下式开关（形态与「吸附」「按时间」一致：关=平，开=主色底） |
| 开关打开后 | 后面的**标记系列、标签、锁**全部切换成**筛选模式**：它们不再是「给选中图片设置值」，而是「**对当前 tiles / 胶片带做筛选**」 |
| 影响范围 | **只筛选当前的 tiles 或胶片带内容**；不改变库、不改变目录 |
| **不影响看图** | 看图模式下开筛选**不影响当前正在看的那张图** —— 哪怕它不在筛选范围内，也只是**在胶片带里选不到它**，看图状态本身不变 |
| 典型用法（用户点名的"很爽"场景） | 选中一张或多张图片时，上面的标记/标签/锁会显示成**这些图片的状态**；此时打开筛选 → **直接筛出同类图片**（例如选一张 3 星的红标图 → 开筛选 → 所有 3 星或红标图留下） |

> 待确认（§10 问题 1）：多选状态下开筛选时，**多个条件之间是「或」还是「与」**？
> 直觉上应当是「与」（更精确），但用户描述的例子读起来像「或」。

### 3.2 标记系列（对选中图片打标，支持批量）

| 标记 | 取值范围 | 持久化 | 备注 |
| --- | --- | --- | --- |
| **旗标** | 开 / 关（只有两态，明确） | ❌ **不持久化，只在内存中** | **跨库、跨目录**都有效；用来「临时标记一堆要处理的东西」；**一定程度上替代未来规划的 bucket**（见 §11） |
| **清空旗标** | — | — | 清空内存中的**所有**旗标（效果类似关掉软件重开）；**点击要确认**（`easy destroy` 范式） |
| **标星** | 0–5 星 | ✅ | 在已选中且为 1 星时**再点一次 → 扣成 0 星** |
| **标色** | 红 / 黄 / 绿 / 蓝 / 紫 / 无 共 6 态 | ✅ | — |
| **喜欢 / 不喜欢** | 喜欢 / 不喜欢 / **取消**（三态） | ✅ | 点赞 / 踩图标；**互斥**，重复点同一个 = **取消**（发 `null`，见下） |

> **赞 / 踩的「再点一次 = 取消」**（人类 2026-09-20 报过「取消不了、右上角提示没有需要改动的照片」）：
> 重复点同一个时必须把该字段**清空**（请求里 `value = null`），不能原样再发一次 ——
> 后端会判定「没有改动」。与「再点一次已锁级别 = 解锁」是同一条口径。

**所有标记共同遵循的状态规则**（这条决定了组件的视觉语言）：

| 选择状态 | 标记组件的显示 |
| --- | --- |
| **未选图片** | 全部处于**无状态**（默认态，没有任何值被点亮） |
| **选中单张** | 显示该图的真实状态（例如 3 星就点亮 3 颗），**并且可以直接调整** |
| **选中多张且取值不一致** | 显示为**类似"未选中"的中性态**（既不是有值也不是无值的"混合态"），方便批量改；**改完统一后恢复正常显示** |

> 这就是「三态控件」：`无值 / 有值 / 混合`。混合态必须与无值态**视觉可区分**（§10 问题 2）。

### 3.2.1 tiles 上的「信息」档位（`i` 开关）

`toolsbar` 最右侧那颗 `信息` 按钮（快捷键 `i`，**只在 tiles / film 下生效**）三态循环：

| 档位 | 状态条按钮底色 | 未选中 / 未悬浮的 tile | 选中 / 悬浮的 tile |
| --- | --- | --- | --- |
| `off`（默认） | 无底色 | 什么都不显示 | **标准**：顶部标记条 + 底部文件名条（半透背景） |
| `marks` | 辅色底 | **强制显示顶部标记**（无底纹、文字加反色勾边） | **标准**（同上） |
| `marks-name` | 主色底 | 强制显示顶部标记 **+ 底部文件名**（同样无底纹、带勾边） | **标准**（同上） |

> **关键口径（人类 2026-09-20 明确）**：强制显示只针对「**没有选中、也没有悬浮**」那种情形；
> 一旦选中/悬浮就回到**原来那套标准方案**（半透背景条 + 正常文字，此时**不要勾边** ——
> 有底纹就不需要描边了）。这个概念与照片右下角的 `RAW` / `+RAW` 角标**完全一致**：
> 都是「没有指向/选中时才用加强显示」。
>
> **顶部条上有哪些标记**：星标 / 色标 / 旗标 / **赞踩**
> （人类 2026-09-20 新增赞踩 —— 与工具条同一套三态：`like` 朝上、`dislike` 朝下、无值不显示）。
> 强制显示时**文字与图案都要反色勾边**：文字走八向 `text-shadow`（`.tile-info-text`）、
> 图案走 `paint-order: stroke`（`.tile-info-icon`）、色点走 1px `box-shadow` 环（`.tile-info-dot`）。

> `infoMode` 与「按时间」一样是**设备级、import 与 browse 共用的持久偏好**
> （`lib/display-prefs.ts`）—— 切工作流、重开软件都还原。

### 3.3 设置标签（tagging）

| 场景 | 行为 |
| --- | --- |
| **单张图片** | 弹窗内可**增加**也可**移除**已有标签；标签以 **flex 横向瀑布流**列出，**每个标签右边带一个叉**（点叉移除） |
| **多张图片（批量）** | 弹窗内**只能添加**（不能移除）；添加时**自动去重** |
| 标签输入组件 | 见 §7（输入即搜、0.6s 防抖、回车创建新标签） |

### 3.4 设置锁（lock）

| 级别 | 含义 |
| --- | --- |
| **一级锁** | **不能删除** |
| **二级锁** | **不能编辑**（更严；显然也不可删） |

- 支持**批量**，批量的状态规则与 §3.2 的三态规则一致；
- 锁状态在**图片上要有明确显示**（tiles 上、看图时、胶片带上都要能看出来）；
- 底部状态栏里：看图模式下**文件名右边紧挨着就是锁状态**（§5.8）。

---

## 4. 左列：库目录选择器

从上到下**三个部分**：

### 4.1 搜索条

- **本期（简单版）**：只搜**库名 / 目录名**；
- **后续**：扩展到**照片信息**（标签 / 注释 / 文件名 / 日期 / EXIF 品牌等），登记见 §11；
- **结果的作用是「过滤」而不是「跳转」**：界面内的**库 / 库下目录 / 右侧照片 tiles 都只显示符合条件的内容**；
- **多层级目录**：若命中的是深层目录，则该目录的**整条树分支链路都要显示**（否则用户看不出它在哪）。

### 4.2 紧缩库列表（compact library list）

纵向排列的库列表，**长条形**显示，组件与导入模式几乎一样（含离线徽标、齿轮改导入模版、点离线检查在线）。

> **2026-09-18 人类逐条重述**（画布是对的、实现做错了；下面是完整口径，**以实现为准**）。

**两种状态：缩起 / 展开。缩起是常态。**

#### 缩起态

| 库数 | 行为 |
| --- | --- |
| **≤ 3** | 正常全部显示；**区域高度只够实际内容**（有几张就多高），**不显示「查看所有库」**，也**不能进入展开态**（只有缩起一种状态）—— 腾出来的纵向空间全部让给下方 `photos/` 的目录树 |
| **> 3** | 第 4 个位置放一张**伪卡片**（见下）；列表容器高度**钉在 3.5 张卡片**的高度上 |

**伪卡片**（第 4 个位置，只在 >3 时出现）：

- 与真库卡片**同宽同高、同样的圆角与面**（8px 圆角、`$surface-bar` 底、`--card-h` 60/72）；
- 内容**只有一行** `查看所有库`，**垂直靠卡片顶部**（上方留一点 padding，不要贴边）；
- 容器 `overflow: hidden` + 高度 = 3.5 张卡 ⇒ 伪卡片**只露出上半截**：看起来就是「上边两个圆角、下边两个直角」，`查看所有库` 几个字完整可见；
- **无阴影**（我们不用阴影）—— 「被切掉一半」这件事靠**卡片的几何**表达，不靠投影。

#### 展开态（只有 >3 时才能进）

- 点伪卡片 → **先把数据读齐（app.db 里的全部库 + 排序），构造完再换界面**（不差这一两秒）；
- 允许动画（现在**可以不做**，以后再加）：例如「查看所有库向下淡出 → 列表展开」；
- 展开后的列表**支持滚动**，显示**所有**库；
- **下面的目录树不消失，而是缩到最小**（定一个最小值，例如只留一行高或者几百像素内的一个固定值）—— 不强迫用户在「选目录」与「管库」里二选一；
- **排序**：库在 `app.db` 的总表里有一个排序，**前 3 名才有资格出现在缩起态**；
- **点库卡片本身 = 选中该库 + 把它移到排序第一**（点齿轮、以及以后别的功能按钮**不算**「选取」，不触发移顶）；
- **移顶只在展开态生效**：缩起态在顶部那（至多）3 张卡之间来回切**不移动位置**（避免日常切换时列表乱跳）。

#### 什么会收起库列表（**三个条件，三选一**）

点库、点目录、点选照片都**不会**自动收起 —— 收起只由这三件触发：

1. **15 秒内没有再次点选任何一个库**（从最后一次点库算起）；
2. **用户在 `browse mid` 里点了一下**：tiles 里的某张照片、`film` 的预览图或胶片带、`view` 的预览图—— 总之从他体感上就是「去看照片了」；
3. **切换工作流**（flow）。

> 三条都是「用户离开库选择这件事」的信号；不用「点一下就收」是因为那样会让人来不及连续换库。

**多导入模版**：将来每个库可设**多个**导入模版，导入时选一个。**优先级不高，先保持简约** → §11。

### 4.3 库内目录树

- 选中库后，库列表**收缩**并把选中的库放到**顶部并选中**，同时下方显示该库的目录树；
- 就是一个目录树：**选中会自动展开**；**右边有展开/收起的图标**；
  **点选（选中）与展开/收起必须是两个不同的动作**（点名字=选中，点图标=展开）—— 不要合并成一个；
- **右侧三点菜单**（`⋯`）提供：

| 菜单项 | 行为 |
| --- | --- |
| **删除空目录** | 选项**一直在**；目录不为空时**暗掉不可点**。该功能**支持同步删除其下所有空子目录** ⇒ 点开时要**做一次深度检索**（判断"是否真的没有文件"） |
| **创建子目录** | 弹**中央模态窗**输入子目录名；**创建时检查重名**，重名要提示 |

---

## 5. 中列：照片 tiles / 看图 / 对比

### 5.1 与导入模式的复用关系

- **组件基于导入模式的照片网格**（`PhotoGrid` / `Tile`），但**功能更强大**；
- 差异点：浏览模式下有评级标记的显示、有看图/对比模式、右栏内容不同；
- **中列 = `TilesShell`（网格 + 底部状态条）**，与导入侧用的是同一个组件
  （人类 2026-09-19：tiles 在业务上不可分割，配置统一传进去）——差异只走显式配置，
  见 §5.8 那张表。

### 5.2 选择逻辑（**所有工作流通用**，tiles 与胶片带一致）

| 操作 | 行为 |
| --- | --- |
| **点击** | 选中该图（**先移除原有选中**，再选中当前这张） |
| **Shift + 点击** | **区间翻转**（见下方严格语义） |
| **Ctrl + 点击** | 自由多选（**包含反选**：已选中的再点一下取消） |
| **在「按日期」模式下** | 可直接点**时间范围内的选中**（点时间片/日组即选中该范围内全部） |
| **无修饰键的点击** | **任何时候**都是「移除原有选中 + 选中当前」 |
| **Esc** | 取消所有选中 |

**Shift 区间翻转的严格语义（用户逐字描述，必须照做）**：

```text
ABCDE 五张连续图片：
  1. 点 A（A 选中，A 成为「上一次的图」）
  2. 按住 Shift 点 C → A..C 之间（B 和 C）的选中状态**翻转**；
     注意**不包括**上一次的图（A），**包括**这一次点的图（C）；
     然后 C 成为新的「上一次的图」
  3. 按住 Shift 点 E → 从 C 开始计，D 和 E 翻转
  4. 按住 Shift 再点 A → A B C D 全部翻转
```

- 这样做的理由（用户原话）：**逻辑简单、无需保存复杂状态、操作又灵活**；
- **Ctrl / Shift 的所有选中操作都是「翻转」而不是单纯的「置为选中」**；
- **暂不支持跨目录选择** → 未来用**图片筒（picture bucket）**实现跨目录批量操作（§11）。

### 5.2.1 全选（`Ctrl/Cmd + A`）

* **只在 tiles 下接**（纯看图/对比不接：那时全选会把对比集合一起换掉，不是用户要的）；
* 选的是**整个范围**里每一张照片，**包括虚拟化没渲染出来的部分**（人类 2026-09-20）；
  实现上浏览侧用**时间线的全量 id 清单**（列表是按页取的，只选已加载的那页是错的），
  导入侧的网格本来就握着整目录；
* 记得 `preventDefault()`：否则浏览器会把整页文字选中、蓝一片。

### 5.3 进入 / 退出看图

| 操作 | 结果 |
| --- | --- |
| **双击图片** 或 **回车** | 进入**看图**（不再是「放到全屏观看」） |
| **看图模式** | 下方形成**胶片带**（横向滚动选图），上方显示照片 |
| **单张 view：Esc 或回车** | 退回 tiles |
| **compare：Esc / 回车** | Esc 退回 tiles；回车在胶片带“仅对比集 / 全目录”之间切换 |
| **选中多张时按回车** | 进入**图片对比模式**（§5.7） |

### 5.4 看图模式的四种显示状态（`Tab` 循环）

| 状态 | 内容 |
| --- | --- |
| ① 默认（`film+左右`） | 左边的库目录选择器 + 右边信息栏都在；下面有胶片带 |
| ② `film+right` | **只收起左栏**，右边信息栏与下面胶片带保留 |
| ③ `film only` | 收起左右两栏，只剩中间看图 + 下面胶片带；**顶部区域不受影响** |
| ④ `view only` | **只看图**：左右两栏与胶片带一起收起，整列让给照片 |

- 四个状态按上表固定循环，从 ④ 再按 `Tab` 一步回到 ①；
- **单张 view 按回车切回 tiles 时，左右栏都会回来**；compare 用 Esc 退出，回车留给胶片带范围切换；
- **tiles 状态不做「tiles 区最大化」功能**（即最大化只在看图模式存在）；
- **import 与 browse 共用同一套**（人类 2026-09-19）：同一个 `lib/viewer-chrome.ts` 循环、
  同一个 `PhotoViewingStage` / `PhotoViewingController`，内部再装同一个
  `PhotoGrid` / `Viewer` / `FilmStrip` / `CompareView`。网格进入任何看图态都只隐藏、绝不卸载，
  因而虚拟列表滚动位置与退出后的键盘焦点接续只有一份实现。

### 5.5 胶片带

- 横向滚动选图；**尺寸**：tile 高度 17 档约 **96–240px**，宽度保持 6:5，
  默认档 **158×132px**；胶片带总高度 = tile 高度 + **上下各 8px**（默认 148px）。
  尺寸与外框由同一份数学推导，禁止只把胶片带加高、缩略图仍保持旧尺寸；
- **没有原生横向滚动条**（2026-09-20）：它占高度且时有时无，一出现就把缩略图挤小 ——
  位置由贴底的 **2px 无感滚动条**指示（`components/ui/SubtleScrollbar.tsx`：
  左滚到头不显示、右滚到头占满全宽、装得下时不可见），
  **纵向滚轮在这个区域内横向滚**，触屏左右划动走原生；
- **隐藏缩放**：鼠标在胶片带内时 `Ctrl + 滚轮` 调整上述 17 档，一次滚轮只走一档；
  不增加界面按钮。缩放前后保留眼前照片的像素锚点，不能横跳；
- import / browse 的胶片带尺寸是**两份独立偏好**，分别写入 `app.db.settings`；连续缩放期间
  只更新内存，最后一次变化静止 **2 秒**后才写数据库，避免滚轮过程中持续写盘；
- **装得下就居中**（人类 2026-09-20）：整条胶片带的宽度小于显示范围时，缩略图要**居中**；
  宽度超出时**与原来一样**（从左排起、横向滚动）——
  实现是内层一行 `w-max mx-auto`（`components/ui/viewer/FilmStrip.tsx`），
  **不是** `justify-center`（flex 居中对溢出会两头截，起头那几张滚不回来）；
- **选择逻辑与 tiles 完全一致**（§5.2 的点击 / Shift / Ctrl / Esc 全部适用）；
- 胶片带中多选 → **自然进入多图对比**；反选图片 → **自然从对比中移除**；
- **进入对比时默认是全目录胶片带**（2026-09-20 修正：原来一选到第二张就自动切进
  「只看对比图」，选不了第三张，得额外回车退出来才能继续选）：
  按**回车**才进入 focus 态（胶片带只显示当前对比的图片，整条主色细边框提示），
  再按回车在“仅对比集 / 全目录”之间来回；
  - 在此状态下，**即使不按 Ctrl**，点击胶片带中的图也是「带 Ctrl 的效果」而不是简单选中
    ⇒ **点什么图就把什么图移出对比**；
  - 直到**只剩一幅图**时：**退出此状态**，胶片带恢复显示全部图片，并**在胶片带中定位到这最后一张图的位置**。

### 5.6 看图操作

| 操作 | 行为 |
| --- | --- |
| **滚轮** | 缩放 |
| **双击** | 在 **100%** 与 **适合窗口** 之间切换 |
| **← / →** | 左右切图（**所有界面通用**，不限于浏览模式） |

### 5.7 对比模式

**虚拟画布（2026-09-20 人类改的口径，取代旧的「以第一幅比例扣等比例区域」）**：

```text
┌─ 栏区（窗口）────────────────────────────────┐  ← 2 张一排 / 3 张一排 / 4 张 2×2 分到的那一格
│  ┌─ 虚拟画布（各图最大宽 × 最大高）────────┐  │     = 可见/裁剪边界，放大后画布铺满整格
│  │      ┌─────┐                          │  │
│  │      │ 竖图 │  ← 每张图按**原尺寸居中** │  │
│  │      └─────┘                          │  │
│  │  ┌───────────┐                        │  │
│  │  └───────────┘                        │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

| 项 | 规则 |
| --- | --- |
| **画布** | 所有对比图放进**同一张虚拟画布**：宽 = 各图最大宽、高 = 各图最大高（单位就是原图像素） |
| **图怎么放** | 每张图按**自己的原图尺寸、居中**贴进画布 —— **不裁、不缩、不拉伸**；小的图两头自然留空 |
| **比例相同** | 画布就是那张图的尺寸，等于「正常并排对比」 |
| **缩放** | **统一倍率**（画布像素 → CSS 像素，1 = 100% = 1:1）；所有图跟着同一个倍率，不存在「各按各的比例」 |
| **位移** | 按**画布坐标**同步；顶多有些图被移出窗口，但总有图在窗口里（人类 2026-09-20：可以接受） |
| **进入 / 换图集** | 重算画布 → 画面**重新居中** → 按**像素数最小**的那张算「适合窗口」的倍率 |
| **双击** | **适合窗口 ↔ 100%**。适合窗口按**被双击的那张图**算（其他图跟着这个倍率）；100% = 画布 1:1 |
| **滚轮** | 以**光标**为锚缩放（与单张看图共用同一份按帧合并的实现） |
| **点击哪张图** | 视为**当前实际选中的图**，这决定**右侧信息栏显示谁的内容** |

> ❌ **已废弃**：「画幅比例不一致时以第一幅比例为准、对后面的图扣等比例区域」。
> 原因：被裁掉的部分**一放大就露馅**（人类原话：「这一缩就露馅了」）——
> 竖图打头时横图被裁成竖比例，看着没问题，但用户想看被裁掉的那部分。
> 现在谁都不裁：画布按最大宽高取，小图居中留空。

### 5.8 底部状态栏（看图模式与 tiles 是同一条）

| 模式 | 布局 |
| --- | --- |
| tiles | `components/ui/tiles/TilesControlBar.tsx`（人类 2026-09-19 统一）↓ |
| **看图** | **同一条栏**（同一个 `BarFrame`，`PhotoStatusBar`）：**左边**：文件名；**紧挨着文件名右边**：**锁状态**；**右边**：当前图片的**标记**（星/色/旗/喜欢） |

> **它在中列的最底那一格**（人类 2026-09-20）：workspace **只有纵向分列、没有跨列行**，
> 每一列各自到底（`AGENTS.md` §11.1、`DESIGN.md` §8.7）。
> 看图时中列从下往上是：**状态栏** → 胶片带 → 照片区；
> 以前那条三列下面全宽的 `ViewerStatusBar` 已删除（既跨列、又重复了一份“当前照片”）。

**tiles 状态条的统一口径**（人类 2026-09-19 逐条点名，两个工作区都按这个来）：

```text
[ 1 248 张  已选 3 ]   ←弹性→   库名 / 最后一级目录 · MY0001   ←弹性→   [信息] [按时间] [排序] [⇤━━━⇥]
```

| 元素 | 口径 |
| --- | --- |
| 计数 | **含选中数**（导入侧本来就有，浏览侧补上）；未选中时不显示那一段 |
| 中间 | **容器 + 当前那张的文件名**：浏览侧容器是**库名 / 最后一级目录**（不放完整路径），导入侧是**目录**（`PathText` 缩写）；文件名多选时取**锚点**那张（§5.10） |
| 九点图标 | **去掉**（人类原话：不知所云） |
| 高度 | 用**矮的那档**（`h-8`，够操作就行） |
| 「按时间」 | **图标保留**（它是重要功能，不是装饰） |
| 排序 | **可配置项**：浏览侧开着（键 + 升降）；导入侧不传 ⇒ 整块不出现。以后导入想开就传同一份 props |
| 缩放 | 用**导入侧那档**：两端带加减号图标、**17 档**离散（2026-09-20：400 封顶、级差约 7.4%） |
| 「信息」三态 | 两侧都有（`i` 键 / 按钮，`components/ui/tile-info.ts` 单例） |

> **组件纪律**：tiles 在业务上是**不可分割的整体** —— 网格 + 下面是同一个 `TilesShell`
> （`components/ui/tiles/`），两个工作区各自把配置**传进去**，不各写一份状态条。
> 各自的缩放档位仍然分别记录（人类 2026-09-19：可以不一样，但组件必须是同一个）。

### 5.9 全工作流通用的操作（不限于浏览）

| 操作 | 行为 |
| --- | --- |
| **Delete** | 删除文件，**有确认**；支持**批量** |
| **Shift + Delete** | 删除文件，**无确认**；支持批量 |
| **← / →** | 看图时左右切图（§5.6） |

> ⚠️ 与 `AGENTS.md` §11.3 的术语约定冲突检查：那里写「**删除**（delete）：真的从磁盘删文件 —— **第一阶段不做**」。
> 本规格要求浏览模式提供删除（含 `Shift+Delete` 免确认）⇒ **需要用户确认这条是否解除**（§10 问题 4）。

---

### 5.10 当前文件（anchor）：多选时右栏显示谁

多选状态下必须能回答「**现在这一屏的信息是给哪张看的**」。规则（2026-09-15 用户要求补）：

| 情形 | 当前文件是 |
| --- | --- |
| 单击某张 | 就是它（= 锚点） |
| `Shift` 区间翻转后 | **本次点中的那张**成为新锚点（与 §5.2 的语义一致：每次点中的都成为"上一次的图"） |
| `Ctrl` 加选 | **新加的那张**成为锚点 |
| 取消锚点本身 | 回退到**最早选中的**那张 |
| 全部取消 | 无当前文件 → **右栏显示空态** |
| 看图 / 对比模式 | 看图就是它；对比模式是**点击的那张**（§5.7） |

- **视觉**：锚点在「选中底」之外再加 **1px 主色内描边**（`stroke: $brand`，`inner`）——
  一眼看出「选了一堆，但当前看的是这张」；
- **底部状态栏**：中间显示 `库名 / 最后一级目录名 · 文件名`（**不放完整路径**，用户明确要求），
  库名与目录名用 `$fg-3`、文件名用 `$fg-2` —— 文件名是这块里最要紧的信息。

## 6. 右列：信息栏

内容随中间的状态不同而不同：

| 模式 | 内容 |
| --- | --- |
| **tiles 模式** | **Exif 详细信息** —— 要**支持滚动**（内容可能很长） |
| **看图模式** | ① **全图缩略图**（放大时要显示**当前看的那块区域的框**）② **直方图** |

> **预览框的比例是固定 `4:3`**（人类 2026-09-20：「比例改成 4:3，不要 3:2，这样对纵图支持更好」）：
> 照片按原图比例在框里**铺满一条边**（横图铺宽、纵图铺高），框本身不跟着照片变。
> 常量与纯函数在 `lib/preview-frame.ts`；视觉规则见 `DESIGN.md` §13.2。

**下面这些两种模式下都显示（通用）**：

| 分组 | 字段 |
| --- | --- |
| **标签信息** | 该图的所有标签（点击/右键可增删，见 §7） |
| **文件基础信息** | 文件名 / 创建日期 / 所在路径 / 所属库 / **作者**（可编辑）/ **Description**（可编辑） |
| **地理信息** | GPS 经纬 / Country / Province/State / City / Sublocation（**均可编辑**） |

> 作者字段附注：**将来支持「某库导入时自动设置 author」**（按库配置）→ §11。

---

## 7. 标签体系

### 7.1 存储与检索

- **用过的标签会存进 `app.db`**（全局标签词典，跨库可用 —— 与 `AGENTS.md` §6.4 的 `app.db` 定位一致）；
- 标签**附到图片上**（每库的 `catalog.db` 里记关联），便于搜索。

### 7.2 输入组件（对标 Vue Element 的「带查询的 select input」）

| 行为 | 说明 |
| --- | --- |
| **一边输入一边搜索** | **0.6 秒防抖**（用户建议用 `es-toolkit` 的 debounce） |
| **选中已有标签** | 搜索出结果后可用**上下键**或**鼠标点击**选中 |
| **创建新标签** | 输入完成后，**即使已有匹配结果，只要不选而直接回车** → **把当前输入内容创建为新标签** |
| **移除** | 单图弹窗里标签右带**叉**（§3.3） |

> **标签库的管理界面本期不做**（改名、合并、批量删除等）→ §11。

---

## 8. 两个新的通用浮层（用户 2026-09-15 新增要求）

### 8.1 中央模态浮窗（通用）

- **原则上遮挡后面所有界面**；
- 使用场景：**创建库、导入过程（进度弹窗）、创建目录** 等等；
- 导入进度弹窗的具体内容见 `REPOSITORY.md` §4.4。

### 8.1.1 命令面板与快捷键设置（M2-W3 新增）

| 项 | 口径 |
| --- | --- |
| **命令面板** | `Ctrl+K`（macOS `⌘K`）。输入即搜（中文/英文/命令 id 都行）；每行右侧**当前键位**；空查询 = **最近使用**；`↑↓` 选、`Enter` 跑、`Esc` 关。只列**此刻适用**的命令（不适用的不显示 —— 那是启动器，不是能力清单） |
| **快捷键设置** | `Ctrl+,`（或 帮助菜单）。按分组列出全部命令；点键位 → 按下新键（`Backspace` 解绑、`Esc` 取消）；冲突红字拦截；**导入 / 导出 JSON**（要么全过、要么不动）；全部恢复默认 |
| **菜单** | 标题栏五个（文件/编辑/视图/窗口/帮助）；**每一项都来自命令注册表**，所以「菜单项同时是可搜索命令」是结构保证；右侧显示当前键位 |
| **键位存储** | 设备级 `localStorage["raybend.shortcuts.v1"]`（`{version, overrides, recent}`）；换机器靠导出导入 |
| **内建键位** | `Enter`（网格进看图 / 单张 view 返回）、列表里的 `Tab` 与方向键、弹窗与菜单内部 —— 不走注册表、不可改；compare 下的 `Enter` 是可改键命令，用来切换胶片带范围 |

> 后续新增用户动作时必须检查是否适合进入命令注册表；适合就让命令面板、快捷键设置与菜单共用同一动作，
> 不适合（纯组件内部导航等）则留在组件内并在实施记录说明理由。

### 8.2 右上角非遮挡提示（toast）

| 项 | 要求 |
| --- | --- |
| **位置** | **`titlebar` 正下方、右上角** |
| **形态** | 一个**清晰的框，可以带边框** |
| **边框颜色表状态** | 成功 = **主色**（`$brand`）；警告/错误 = **danger**（`$danger`） |
| **底色** | 浅深色 / 深浅色（即 `$surface-bar` / `$surface-layer` 这一档） |
| **时长** | **约 5 秒自动消失** |
| **不阻塞** | 除了自身显示，**不阻挡界面上任何操作** |
| **占用空间** | 大致是「图片信息 / 磁吸控制」那块的位置；可能会**稍微压到 `toolsbar` 一点点**，但 `toolsbar` 是**居中**的，所以影响很小（用户判断：目前空间非常充足） |
| **为什么放这里** | ① 不挡操作（右上角是空白拖拽区）；② **将来可以在 `titlebar` 右侧放「消息中心」**，把消息作为列表塞进去 → §11 |

### 8.3 弹性动画的库选型（用户提问，Agent 调研结果）

需求：**库列表展开/收缩**要有**弹性动画**，最好有靠谱的、**能用在 Solid 上**的库。

| 候选 | 版本（2026-09-15 实测 npm） | 适配 Solid | 评价 |
| --- | --- | --- | --- |
| **`@formkit/auto-animate`** | 0.10.0 | ✅ **不需要适配层**（框架无关，`autoAnimate(element)` 一行） | **首推**：FLIP 式自动动画，列表增删/重排/容器尺寸变化都管，约 3KB，正好覆盖「展开收缩 + 移顶重排」两个场景。<https://auto-animate.formkit.com/> |
| **`motion`**（motion.dev，framer-motion 的继任者） | 13.3.0 | ⚠️ 官方适配器只有 React/Vue/Svelte，**但核心是框架无关的**（`import { animate, spring } from "motion"` 直接操作 DOM） | 想要**真弹簧物理**与编排（stagger/sequence）时用它；代价是 API 面向 DOM，需要自己接 Solid 的 ref。<https://motion.dev/> |
| **`solid-transition-group`** | 0.3.0 | ✅ Solid 官方生态 | 只做**进入/离开**过渡（CSS 过渡驱动），**没有弹簧物理**；适合面板显隐，不适合"弹性展开"。<https://github.com/solidjs-community/solid-transition-group> |
| **`@solid-primitives/animation`** | 1.0.0-**next**.1 | ✅ | ⚠️ **尚在预发布**（WAAPI 封装、含 FLIP），生产不宜依赖 |
| **`animejs`** | 4.5.0 | ✅ 框架无关 | 老牌、API 直白、v4 支持 spring 缓动；比 auto-animate 重，适合复杂时间线 |
| **`solid-motionone`** / `@motionone/dom` | 1.0.4 / 10.18.0 | ✅ Solid 封装 | ⚠️ **已停止维护**（Motion One 已并入 `motion`） |

**Agent 建议**：先用 **`@formkit/auto-animate`** 满足「展开/收缩 + 移顶重排」；
若之后发现需要真弹簧手感（例如拖拽跟手、惯性），再评估 `motion`。
**注意与现有动效纪律的冲突**：`src/styles/motion.css` 规定「时长 ≤150ms、只动 transform/opacity」，
而**展开/收缩必然要动高度**（会触发布局）—— 这是**有意的例外**，
要在 `DESIGN.md` 的动效章节里明确写出来（§10 问题 5）。

---

## 9. 与现有文档 / 代码的关系

| 项 | 关系 |
| --- | --- |
| **照片网格 / Tile** | **复用导入模式的组件**（`src/components/ui/Tile.tsx`、`PhotoGrid`），浏览模式在其上加评级与看图 |
| **`app.db`** | 标签词典（§7.1）；`AGENTS.md` §6.4 已把「全局标签词典」放在那里 ✓ |
| **`catalog.db`** | 每库的评级/标记/标签关联/锁/作者/描述/地理信息 |
| **`DESIGN.md`** | 新增的控件（三态标记、树、toast、模态）要在 §7/§9 补规格 |
| **`REPOSITORY.md`** | 导入进度弹窗（§8.1 的中央模态的一个实例）在那边已写 |
| **界面设计** | 待做：`design/browse-*.pen` + 同名 `.md`（见 §12） |

---

## 10. 已收敛（M2-W2 结项，2026-09-19）

> 原来这里是「待你确认」，八条**全部有结论**了。结论同时落到了 `DESIGN.md` §14、
> 本文档对应小节与代码里；实现记录见 `implementations/2026-09-19_w2-stage3-6_toolbar-tags-toast-delete.md`。

| # | 问题 | 结论 |
| --- | --- | --- |
| 1 | 筛选模式下多个条件之间是「与」还是「或」 | 引擎两种都支持（`store/query.rs` 的 `Combinator`）；**UI 默认「或」**，并给「任一 / 全部」切换（两个以上条件时才出现） |
| 2 | 「混合态」的视觉表达 | 无值 = 全暗 `fg-3`；取值一致 = 显示该值；**混合 = 半亮 `fg-2` + 一个短横**。硬要求：混合**不能**看着像无值 |
| 3 | `Tab` 四态的循环顺序 | ① `film+左右` → ② `film+right`（只关左）→ ③ `film only` → ④ `view only` → ①。退回 tiles 时左右必定回来；import 与 browse 同一套 |
| 4 | 删除文件（`Shift+Delete` 无确认？） | **删除永远确认**（人类 2026-09-19 定）。理由：删除支持多选批量，而 `easy destroy` 那套是给「不做批量界面、又要连续快速删单张」准备的 —— 能批量的操作不需要它。`AGENTS.md` §11.3 的定案保持 |
| 5 | 弹性动画与动效纪律 | 库列表的展开/收缩（约 200–260ms + 弹性曲线、允许动高度）是 `DESIGN.md` 的**唯一有意例外**，已写进 §14 |
| 6 | 旗标不持久化 | **确认**：只在内存、关软件即清空（它是临时工作集，不是元数据）；「清空旗标」是全局动作、点击要确认 |
| 7 | 直方图要不要通道分离 | **已更新（2026-09-20）**：86 点三通道完整曲线；底部 R/G/B 一次选一个、再点取消回到全部；悬停显示 0..255。完整口径见 `DESIGN.md` §13.1 |
| 8 | 对比模式最多几张 | **只对比最近选中的 4 张**（人类 2026-09-19 定）：按选择先后滚动窗口、锚点必含、超出时最早那张被挤出；摆放为 2 张一排 / 3 张一排 / **4 张 2×2** |

### 10.1 本轮另外定下的两条（口述之外的实现口径）

* **`U` 键只清旗标**（不是把星级/色标一起抹掉）：旗标是最常反悔的那一项，而星级与色标是持久元数据，
  各自另有「再点一次同一个值 = 清零」的入口。
* **数字键打星只在网格里生效**：看图里的 `0` / `1` 是既有的缩放快捷键（适配 / 100%），
  不让打星覆盖它；看图时打标用 `P` / `X` / `U` 与工具条。

## 11. 明确登记到 `FUTURE.md` 的部分

| # | 事项 |
| --- | --- |
| 1 | **每库多个导入模版**（导入时选一个）—— 用户明确"优先级不高，先简约" |
| 2 | **标签库管理界面**（改名、合并、删除、批量整理） |
| 3 | **`titlebar` 右侧的消息中心**（toast 的历史列表） |
| 4 | **图片筒（picture bucket）**：跨目录 / 跨库的收藏式批量操作，取代「旗标」的临时工作集角色 |
| 5 | **跨目录选择**（依赖 picture bucket） |
| 6 | **每库导入时自动写 `author`** |
| 7 | **搜索扩展到照片信息**（标签 / 注释 / 文件名 / 日期 / EXIF 品牌…） |
| 8 | 对比模式的更多能力（同步方式、多图网格对比等） |

---

## 12. 界面设计计划（**用户要求：先出设计，设计确认后再开 plan 排期**）

设计稿拟放在 `design/browse.pen` + `design/browse.md`（同建成对，`AGENTS.md` §5.1）。
⚠️ **Agent 无法自行新建 `.pen`** —— 需要用户在 Pencil 里新建这个文件并打开（`design/main.md` §6.1 已记录该限制）。

拟画的帧（按优先级）：

| # | 帧 | 内容 |
| --- | --- | --- |
| 1 | `Shell / Browse / Tiles`（主稿，深色紧凑） | 三列：搜索条 + 紧缩库列表（3.5 截断态）+ 目录树 / tiles 网格（含选中与标记显示）/ 右栏 Exif（可滚动） |
| 2 | `States / 标记三态` | 无选中 / 单选（3 星 + 红标）/ 多选不一致（混合态）/ 筛选开启态：标记、标签、锁各一行 |
| 3 | `Shell / Browse / View` | 看图：上方照片 + 下方胶片带 + **中列最底的状态栏**（与 tiles 同一条；文件名+锁 左，标记 右）+ 右栏（缩略图含视口框 + 直方图 + 标签/文件/地理信息） |
| 4 | `Shell / Browse / Compare` | 2–3 图并排对比 + 胶片带（细边框态）+ 右栏 |
| 5 | `Shell / Browse / Tiles / ExpandedLibs` | 库列表展开态（吃掉目录树区域、滚动、查看所有库） |
| 6 | `Components / 浮层` | 中央模态（以「创建子目录」为例）+ 右上角 toast（成功主色边框 / 警告 danger 边框） |
| 7 | `States / 选择逻辑` | 点击 / Shift 区间翻转 / Ctrl 多选 / 日期模式区间选 的选中态示意 |
| 8 | `Shell / Browse / View / Focus` | `Tab` 的「关闭左右」最大化态 |

画完后由用户核对，确认无误后再开 plan 排实现顺序（用户明确要求）。
