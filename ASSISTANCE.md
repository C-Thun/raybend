# 待你做的事（ASSISTANCE.md）

> 这里**只放「现在就必须由你做、不做会卡住我」的事**，一条一句话。
> 做完告诉我，我验证后**直接从本文件删掉** —— 不写总结、不留归档（历史在 `implementations/` 与 git log）。
> **越短越好，空着就是最好的状态。** 未来才做的（验收、拍板、授权）不属于这里。

## 一、现在需要你做（阻塞中）

**（空）** —— 当前没有阻塞我的事，照常推进。

原本挡住 W1 最后一步的三件已全部清掉（2026-09-18）：

- **A / A′（Windows GPU 逐项验证 + 跨屏 DPI 判定）**：✅ **2026-09-19 真机跑完**
  （125% / 200% 两档 DPI + 性能基线 + 透明/命中/窗口状态/跨屏）。
  结论：**透明挖洞成立、坐标契约成立、性能远超需要**；三处遗留（设备恢复、后端回退未测成、跨屏过渡闪烁）
  见 `implementations/2026-09-19_windows-spike-verified-and-device-loss.md`。
  该能力**只服务编辑模块**，M2 不受影响。
- **B（Pencil 连接）**：已恢复，W2 的画布补帧已完成 —— 见下面第三节第 3b 条（该您过目）。

## 二、绕过去了的问题（攒着等外援）

> **这一节不阻塞任何工作**：每条都写清了「怎么绕的」与「风险是什么」，
> 所以可以攒着 —— 直到某一条**再也绕不过去**（继续走会踩空），它才升到上面「一、现在需要你做」。
> 修好之后同样**直接删掉**（历史在 `implementations/` 与 git log）。
>
> 规则见 `AGENTS.md` §2.12。

1. **中文在小控件里看着偏高（0.5–0.8px）** —— 已做度量修正（`vite.config.ts` 的
   `cjkMetricsOverride` 把 CJK 字体的 ascent/descent 改成方块字 em 盒，实测已生效），
   但**像素偏移一点没变**，判断是光栅化/基线吸格（本环境测不出来源）。
   → 你在真机上看一眼：**如果还是偏高就说一声**，那条改动一条 revert 就能撤
   （涉及三处：`vite.config.ts` 的插件、`src/index.tsx` 的字体 import、`scripts/ui-smoke.mjs` 的两条断言）。
   量法与数据见 `implementations/2026-09-17_exclude-chain_radius_language_font-metrics.md` §二。

2. **RAW 缩略图 134 ms/张（目标曾是 <10ms）** —— 瓶颈在 rawler 的
   `preview_image()` 实际走的是完整图像解码路径，拿不到「只抠 JPEG 段」的便宜。
   绕法：接受现状（比 JPEG 的 53ms/张慢，但比完整解码 764ms 快得多），
   缩略图队列本来就是后台跑、有缓存。
   风险：RAW 占比高的库里，首次导入后的缩略图阶段会明显慢于 JPEG。
   真要压到 10ms 级，得绕开 rawler 自己解析 TIFF/BMFF 里的预览偏移 —— 那是另一个工作量，
   记在 `FUTURE.md` 待办里（尚未登记）。

3. **契约/类型检查器偶发「陈旧快照」误报** —— 表现为报某个刚加的字段/键「不存在」，
   而 `npx tsc --noEmit` 全项目 0 错。已用决定性实验证伪一次（把键从源文件删掉才复现），
   并已用 `lens_diagnostic_mark` 标为误报。
   绕法：**先跑权威编译器**，不一致就以编译器为准；必要时提交一次让快照刷新。

   **2026-09-19 复现两次（同一天、同一根因）**：都是「i18n 加了新键，LSP 仍按旧键集报错」——
   `features/browse/labels.ts` 报 8 个色标/喜欢/锁的键不存在、`i18n/en-US.ts` 报
   `browse.filterAny` 不在 `Record<MessageKey, string>` 里。
   两次都动过 `zh-CN.ts`（`MessageKey` 的来源）**也没能让缓存刷新**，标了误报才清掉。

   **验证配方**（比 grep 硬，10 秒出结果）：临时写一个文件

   ```ts
   // src/i18n/__keyproof.ts
   import type { MessageKey } from "./index.ts";
   const proof: MessageKey[] = ["browse.filterAny", /* …有疑问的键 */];
   export default proof;
   ```

   跑 `npx tsc --noEmit`（**退出码 0 = 键确实在联合类型里**），然后把它删掉。
   `MessageKey = keyof typeof zhCN`、`enUS: Record<MessageKey, string>` 两层约束意味着：
   只要这个文件过编译，键就既在中文包里、也在英文包里。

4. **`cargo test` 偶发链接失败**（`rust-lld: undefined hidden symbol`）——
   今天第三次之后已按早先的建议**关掉测试档的增量编译**（`[profile.test] incremental = false`），
   根因是增量编译的陈旧目标文件。代价：改完源码后测试二进制整份重编。
   若你嫌慢，可以改回增量 + 遇到时 `cargo clean -p raybend`。

5. **渲染线程的 panic 会静默冻住画面**（2026-09-19 真机发现，主因已修）——
   设备恢复时 `recover()` 复用旧设备的资源（第一次是 bind group layout、补掉之后又暴露
   uniform buffer）→ wgpu 校验错 → panic 打死渲染线程；表现是「界面照常响应、图永远不动」。
   **已按结构性方式修完**：跟设备绑定的资源收进单一入口 `build_device_resources`，
   `new()` / `recover()` / 离屏三处共用；并加了离屏回归测试（两块真实设备上各建一整套）。
   **仍未做**：装 logger、`recreate_surface()` 兜底、`configure` 的 error scope；
   surface 侧（用新设备重配曾绑给死设备的 surface）只能在真机复跑确认。风险：**编辑模块必须给渲染线程加「panic 捕获 + 重启 + 上报」**，
   否则用户看到的就是一张冻住的图。

6. **跨屏/DPI 过渡有闪烁**（2026-09-19 真机确认）——原生窗口事件与 DOM 事件是两条独立队列，
   过渡期间先后到达。按 Astro 指南 §4，产品化时用**带递增 geometry revision 的整包布局事务**收口。
   风险：只影响过渡那一两帧的观感，不影响稳态正确性。

---

不属于这里的：设计取舍 → 各文档的「待决」小节；目视/真机验收 → 下面第三节；
工具约束 → `design/main.md` §6.1。

---


## 三、人类验证清单（**不阻塞开发**，到点我会提醒）

> 与 `PLAN.md` 的「人类验收清单」是一回事，但这里只放**当前这一波新产生的**。

| # | 事项 | 怎么做 | 卡住什么 |
| --- | --- | --- | --- |
| 1 | **M1 验收**（`PLAN.md` 第 1–4 项，含 3b–3f） | 跑 `C:\rb-target\raybend\debug\raybend-desktop.exe`（**需先重建**，见下）逐项过 | M1 签字 |
| 2 | **浏览工作区目视**（M2-W1 新交付；可与第 5 条一起过） | 切到「浏览」工作流：左列能看到库与目录树、中列网格出真图（含 RAW！）、点照片右栏出信息；**点库不出图、要点到目录才出图**（2026-09-18 的新口径）；点「按时间」出分组标题 | W2 开工前确认地基可用 |
| 3 | ~~**Windows GPU 验证**（M0-2 全部项 + M0-6 帧率）~~ | ✅ **2026-09-19 已完成**（见 §一 归档条与本文件底部记录）。遗留三件：① **设备恢复**（真因已定位并修了主因，下次真机复跑确认）；② **后端回退三选一**（脚本的 `WSLENV` bug 已修，可直接重跑 `WGPU_BACKEND=dx12 pnpm spike:win`）；③ 跨屏过渡闪烁（已知，产品化按 geometry revision 收）。**M0-6 网格帧率仍无真机数字**（库里照片太少），不阻塞 | 已完成 |
| 5 | **W2 看图与对比目视**（阶段 1–2 交付） | 切到浏览、点到目录出图后：**双击进看图**（下面有胶片带、右栏是「预览 + 直方图」）→ `Tab` 循环三态（关左右 / 关胶片带）→ `Esc` 退回；`Ctrl` 多选 2–4 张**自动进对比**（第一幅带主色内描边；滚轮缩放时所有画幅同步）；**选中超过 4 张时**应当只剩「最近选中的 4 张」并有一句说明；4 张时是 **2×2**；对比态**再按回车** → 胶片带只显示对比图（整条主色细边框），点一张 = 移出对比 | W2 签字 |
| 6 | **W2 标记 / 筛选 / 排序 / 标签 / 提示**（阶段 3–4 交付） | 选一张 → 工具条打星 / 色标 / 喜欢 / 锁（⚠️ **这几个按钮在本波之前是死的**，麻烦重点确认它们真的生效）→ 开「筛选」开关应当**直接筛出同类**（chips 可逐条摘掉、「任一 / 全部」可切）→ 底部控制条换排序键与升降；点「标签」开弹窗（输入即搜 0.6 秒防抖、**没选结果时回车 = 建新标签**、单张每个标签带叉、多选时不带叉）；右上角 toast 不挡操作、带「撤销」 | W2 签字 |
| 7 | **W2 删除与键盘**（阶段 5 交付） | 选一张按 `Delete` → **先弹确认**（删除不给 Shift 快通道）→ 确认后看提示与回收站；被锁的照片要报出来、删除失败的清单要逐条列；网格里 `←`/`→` 移动「当前那张」（⚠️ 画面要跟着滚，不滚就是 bug）、`0`–`5` 打星、`P` / `X` / `U` 旗标；拖一下左右列之间的**细长条**（也可以聚焦它后按方向键），列宽应当**重启后还在** | W2 签字 |
| 8 | **真机顺手度复核**（本波新交付的手感） | 三处主观项：① 标签弹窗的 **0.6 秒防抖**是不是偏慢；② toast **5 秒**够不够读完一句、位置挡不挡事；③ 侧栏宽度上下限 **220–520px** 够不够用。都只是调常量 | 不卡开发 |
| 3b | **画布过目**（2026-09-18 批次：画布 0.3.1–0.3.6） | 打开 `design/browse.pen`，或看导出的截图 `C:\src\tmp\raybend-canvas-2026-09-18\`（6 张）。四个看点：① `States / 浏览标记与选择` 的 7 个选择态 tile 已换成新外形（正方外框 + 覆盖信息条，锚点带主色内描边）；② `States / 浏览状态（…）` 四格：命令面板 / 筛选（**新增排序 + 「任一·全部」**）/ Tab 的**两态**（新增「关胶片带」）/ **未选目录空态**；③ **新帧 `Components / 标签弹窗`**（单张带叉 / 批量无叉）；④ 按「实现优先」改的面用法（tile 图区改回 `$surface-main`）与对比帧胶片带（12 张→2 张）。说明见 `design/browse.md` §5.00–§5.3 | 不卡开发；有问题我改画布 |
| 4 | **网格水印的观感 + 「等缓存」的等待体感**（2026-09-17 支线） | 真机跑一次：① 打开一个**外部目录** —— 那段「印在底纹上的大图标 + 文字」的**呼吸/扫光浓度与速度**是否「不抢眼也不显得卡」；② 双主题 × 两档密度各看一遍；③ 打开一个大目录（几百上千张），确认「等头部缓存再铺照片」的等待可接受（这是那个人类定下的取舍）；④ 空态：找一个空目录 / 空库，看那块水印；⑤ 切到英文（帮助菜单 → English）确认这几处文案真的跟着变 | 不卡任何开发；只影响要不要调那两个数字（`motion.css` 的 `rb-watermark-*`、`StateWatermark.tsx` 的不透明度） |

**重建 Windows 产物的命令**（照抄；细节见 `AGENTS.md` §5.3）。

> 现在有更省事的一条：**`pnpm spike:win`** 等价于「`pnpm build` → Windows 构建 → `pnpm check:win` → 开窗」
> 这几步的合体（外加 `--dry-run` 只构建不开窗）。下面这段是它的展开形态，出错时照它手跑：

```bash
pnpm build                                  # 必须先做：dist 是编译期嵌进 exe 的
export CARGO_TARGET_DIR='C:\rb-target\raybend'
export WSLENV='CARGO_TARGET_DIR'
cd /home/andares/repos/c-thun/raybend
cmd.exe /c 'pushd \\wsl.localhost\Ubuntu-24.04\home\andares\repos\c-thun\raybend & cargo build -p raybend-desktop -p raybend --features custom-protocol'
pnpm check:win                              # 验产物比 dist 新、资源名对得上
```

> ⚠️ 这次构建要**同时构建 `raybend` crate 的 bin**（`-p raybend`）——
> RAW 解码跑在独立的 `raybend-raw-worker` 进程里（`AGENTS.md` §6.3 的进程隔离）。
> 只建 `raybend-desktop` 的话，主程序会回退成「用标记参数重启自己」那条路径（也能工作，
> 但那是发布形态，开发期走独立 bin 更清楚）。
