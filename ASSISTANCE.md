# 待人类协助事项（ASSISTANCE.md）

> **最后核对：2026-09-15**（Agent 每次动工前必须重核「一、阻塞中」是否仍为空）

## 铁律（同时写在 `AGENTS.md` 开头）

- **只有「一、阻塞中」非空时**，Agent 才必须立即停下、提示人类处理；**该节为空即可照常推进**。
- 「二、待你定夺」与「三、需要你目视/真机验证」的条目**按各自标注的时机**处理，**不阻塞**日常推进；
  Agent 到该时机要**主动提示**，不要拖到收尾才说。
- 人类完成后告知 Agent；Agent **逐项验证**、回填证据，然后把该条**压缩**成一行移入「五、已完成」，
  细节留在 `implementations/` 或对应文档里（本文件保持短小才有分量）。

> **为什么改成这样（2026-09-15 用户指出）**：原规则是「本文件只要有内容就必须停下」。
> 结果非阻塞条目（已完成的、只是要人看一眼的、历史留痕的）越积越多 ——
> **「停」变成了常态，提示也就没人当回事了**，铁律反而失效。
> 所以现在按「是否真的阻塞」分级：**阻塞项零容忍，非阻塞项排队等时机。**

---

## 一、阻塞中（必须停下处理）

**当前：无。** M1 可继续推进。

---

## 二、待你定夺（不阻塞，但会卡住收尾）

| # | 事项 | 何时需要你 | 未定前我按这个走 |
| --- | --- | --- | --- |
| **D1** | **浅色主题下主色当文字 / 焦点环**：`#52C6AB` on `#F6F8F4` = **1.96:1**，不达 AA（`DESIGN.md` §13 已知问题 2） | 浏览模式设计定稿前 | **焦点环在浅色主题改用 `--fg-1`**（深色，肯定看得见）；主色只做填充。备选：派生 `--brand-ink`，候选 `#1F7A64`（4.88:1 过 AA） |
| **D2** | **MiSans 字体选型**：观感更现代，但其协议禁止「再分发字体软件」，桌面应用内嵌是否构成再分发**存在解释空间** | 发布前 | 继续用 **Inter + Noto Sans SC**（SIL OFL 1.1，零风险） |
| **D3** | **`easy copy` 气泡形态**：现状是「两层文案叠在同一个气泡里」，设计文描写的是**两个独立小窗** | M1-1 验收时一起看 | 保持现状，你看观感再定 |
| **D4** | **浏览模式的 8 条交互待确认**（`BROWSE.md` §10：筛选条件的与/或、混合态视觉、`Tab` 三态语义、**是否解除「第一阶段不做真删」**、弹性动效例外、旗标不持久化、直方图通道、对比张数上限） | 画图过程中逐步问；**真删那条要在实现前定** | 我按 `BROWSE.md` 里写的默认值先画，你在图上纠正 |

---

## 三、需要你目视 / 真机验证（Agent 做不到的事）

| # | 事项 | 时机 |
| --- | --- | --- |
| **V1** | **M1-1 陈列室目视比对**：`pnpm dev` → <http://localhost:1420/dev/kitchen-sink>，顶部可切主题/密度/语言，逐项比对 `design/main.md` | 你有空即可，**M1 收尾前必须做** |
| **V2** | **M1-4 外壳验收 8 项**（拖拽、双击最大化、边缘缩放、菜单悬停、三键、主题密度持久化、`toolsbar` 随工作流显隐、高 DPI 清晰度） | **M1 收尾前**（验收表见 `plans/M1-4.md` §7） |
| **V3** | **Windows 侧 GPU 与渲染验证**（M0-2 全部验证项 + M0-6 帧率；WSLg 是软件渲染，结论无效） | **M2 开工前**，不阻塞 M1 |
| **V4** | 各单元完成后的目视验收（M1-3 缩略图观感 / M1-5 导入工作区 / M1-6 导入执行） | 各单元完成时 |

---

## 四、Agent 侧约束备忘（依赖人类，但不是「任务」）

- **Pencil 只能操作「编辑器里已打开」的 `.pen`**，且 Agent **不能新建 `.pen`**。
  需要新画布时请你新建并保持打开。当前已开：
  - `design/main.pen` —— 导入外壳（已与实现同步，见 `design/main.md` §9.3）
  - `design/browse.pen` —— 浏览模式（空画布，待画）
- 画布的主题（`mode`）与密度（`density`）是**主题轴**，Agent 不能自行切换 —— 需要时请你切。

---

## 五、已完成（归档，一行一条）

| 日期 | 事项 | 结论 | 细节在哪 |
| --- | --- | --- | --- |
| 2026-09-15 | **A1 美术设计阶段** | ✅ 规格已全部给出（外壳三行 / 导入三列 / 浏览模式），设计稿产出转为 Agent 常规工作，不再需要人类「授权开工」 | `design/main.md`、`BROWSE.md` |
| 2026-09-15 | **A3 RAW / JPG 样本** | ✅ `/mnt/c/src/tmp/pic`（Windows `C:\src\tmp\pic`）：**301 文件 / 4.2 GB，159 JPG + 142 RW2 同名成对** | `BROWSE.md`/`LIBRARY.md` 的规格以它为验证样本 |
| 2026-09-15 | **A4 中间色在浅色主题偏淡** | ✅ 已按候选 ① 落地：浅色 `--fg-2 = #6E6866`（5.12:1，过 AA）；`tokens.css` 里注明了出处 | `DESIGN.md` §13 已知问题 1 |
| 2026-09-15 | **A5 Pencil 连接测试标记** | ✅ 已处理（标记未保存，无需清理） | 记忆（tool-quirk） |
| 2026-09-15 | **A7 画布与实现同步** | ✅ 变量层 + 三帧全部对齐（色 / 字号 / 条带高度 / 窗口三键 / 库卡片齿轮与离线徽标）。仍缺画布的两件小事已转成 Agent 待办 | `design/main.md` §9.3 |
| 2026-09-15 | **B1 Windows 工具链与 UNC 构建** | ✅ VS BuildTools + Windows SDK `10.0.26100.0` + Rust 1.98.1 MSVC；UNC 全链路构建通过（坑与命令已入 `AGENTS.md` §5.3） | `implementations/2026-09-15_M0-1_windows-build-path.md` |
| 2026-09-15 | **B2 WSLg 窗口目视** | ✅ 正常（窗口、三栏、深色令牌、中文字体） | — |
| 2026-09-15 | **B3 Pencil MCP 连接与多窗口稳定性** | ✅ 读写双向通；3 个窗口下可读回并修改同一节点（未覆盖「更多窗口」的极端场景） | 记忆（tool-quirk：`TakeScreenshot` 必须与写入分开调用） |

---

## 验证记录

| 日期 | 事项 | 验证方式 | 结论 |
| --- | --- | --- | --- |
| 2026-09-15 | B2 WSLg 窗口 | 人类目视 | ✅ 正常 |
| 2026-09-15 | B1 Windows 工具链与 UNC 构建 | `vswhere` + SDK 目录 + `rustc -vV` + 实跑 `cargo build` / exe | ✅ 通过 |
| 2026-09-15 | B3 Pencil 连接 | `pencil_get_app_state` + 写入探针 + 截图 | ✅ 双向通 |
| 2026-09-15 | B3 多窗口稳定性 | 单窗口写入 → 3 窗口读回并修改同一节点 → 截图 | ✅ 通过 |
| 2026-09-15 | A7 画布同步 | 画布变量表读取 + 三帧逐项截图核对 | ✅ 与实现一致 |
