# M5-W2：手动触发的签名更新
开工：2026-09-26，承接已授权的 M5 并行准备。

复用 Tauri 官方 updater（Rust 端）、现有设置与弹窗/命令；不另写下载签名算法，不在启动后台联网。界面先在 main.pen 的 OBgSA 定义、main.md 说明。稳定/预览/关闭/自定义 HTTPS 源及其签名公钥；关闭是缺少官方公钥的默认值。第三方可提供独立公钥或禁用，但不跳过校验。

流程分为检查、下载并校验、确认安装。稳定拒绝 prerelease。失效检查不得保留旧待安装对象；配置更换须丢弃旧下载。安装前原生检查未结束导入/运行导出；前端另检查编辑脏状态/迁移。失败不宣布回滚成功；旧应用仍可用的下载失败和操作系统安装恢复分开记录，真机升级/失败恢复后才完成 DoD。所有联网仅由点击产生，网络/配置/签名错误可重试并有明确反馈。

状态：独立工程开发与冒烟完成；Windows 实际升级、MSI/NSIS 恢复与数据库回退未验证，不标波次全完成。
