# M5 分发与发布换手

换手时间：2026-09-27 01:33:35 CST
工作目录：`/home/andares/repos/c-thun/raybend`
快照：`master`，HEAD `060b6dce80e0`，产品版本 `0.1.0`，packageManager `pnpm@12.3.4`。

## 1. 接续目标与授权

崔总先要求评估 M5 与 M4 能否并行、审计已有自动升版脚本及讨论签名/系统信任；随后明确授权「按计划先推进 M5 中能推进的部分，完成后报告需要做哪些」。当前切换 Agent，接续既有授权，继续能独立完成的 M5 收口，最后一次性报告需要崔总执行的事。

**当前结论：M5 W1–W4 的独立工程准备已开发并通过冒烟，M5 发行 DoD 未完成。** 不要把四波全部重做，不要把安装/更新/签名脚本已写等同于真实发行链已跑通。当前没有生成 Windows 发布安装器、签名、tag、push 或上传。

本文件是换手快照，不是第二份持续维护的任务清单。路线状态以 `PLAN.md` 为准，详细契约见 `specs/M5-W1.md` 至 `M5-W4.md`，实施证据见相应记录。后续状态变化须更新正式落点。

## 2. 工作树与最新变化：开工先看这里

1. **必须接当前 checkout，包括未跟踪文件。** 换手核对前共有 173 条 status 记录，其中 67 条未跟踪；大量 M3/M4/M5 改动混在一起。M5 的 `docs/`、`legal/`、`public/legal/`、release helpers、updates.rs、distribution.rs、WelcomeDialog/UpdateDialog 等仍未跟踪。只克隆 HEAD 或新建干净 worktree 会丢掉本轮主体工作。
2. 不做 `git reset/clean/restore`，不全仓格式化，不把并行修改一并提交；本次换手只新增文档。先读现有实现，局部修改。不要手改或回退 pnpm 自己管理的 packageManager 字段。
3. **M4 状态已前进。** `implementations/2026-09-27_m4-finalization.md`（完成时间 01:02:03）记录最终右栏、浏览定稿、外部编辑与 W7 开发及冒烟完成，真实 Windows GUI/画质/性能仍待验收。不能继续沿用 09-26 的「外部编辑/导出尚未实现」判断。原生 updater 现在已检查 `ExternalState.has_unfinished()`，不要再造一份。
4. **Pencil 保存状态已变化。** `design/main.pen` 当前 git 状态为修改，磁盘时间 2026-09-27 00:46:33 CST，大小 495615 字节。09-26 M5 记录和 PLAN/W1 中的「main.pen 仍未保存」是当时结论，不能当成当前事实。此次仅核对文件状态，未通过 MCP 确认 M5 三节点确实包含在这次保存中；下个 Agent 应通过 Pencil MCP 核对。禁止文本读取/搜索加密 `.pen`。
5. **许可证清单未因 M4 失效。** 本次只读核对 `public/legal/third-party.json`：593 项，Cargo.lock 与 pnpm-lock.yaml 的 SHA-256 均与清单 locks 一致。后续依赖变动再刷新，不要仅凭旧测试数字推断当前树全绿。

## 3. 建议阅读入口

按顺序读，不从头重新调研全部里程碑：

- 项目 `AGENTS.md`、`/home/andares/.pi/agent/AGENTS.md`；路线 `PLAN.md` M5 与最新 M4 状态。
- `implementations/2026-09-26_m5-distribution-engineering.md`：本轮代码地图、决策、历史验证与局限。
- `implementations/2026-09-27_m4-finalization.md`、`specs/M4-finalization.md`：M4 最新交接面及真机验收边界。
- `specs/M5-W1.md`、`M5-W2.md`、`M5-W3.md`、`M5-W4.md`：四波现有契约。
- `docs/release.md`：可交给崔总执行的打包、密钥、签名、清单、商店及恢复操作。
- `legal/README.md`：许可原文、补足方式及三项公开发布前复核。
- 如需理解原评审，读 `implementations/2026-09-26_m5-distribution-readiness-review.md` 与 `REVIEW.md` R2；其后授权已发生，不要重新等待并行开发许可。

## 4. W1：升版、Windows 安装与首启

已做：

- `src/lib/release-plan.ts` 与测试复用原升版实现，严格 SemVer/Windows 版本范围、major/minor/patch/test、beta、未知/重复参数与两种 channel 写法。test 不升版，不给 test tag 提示。
- `scripts/release.mjs`、`scripts/lib/release-files.mjs`：产品版本来自 package.json；同步 Cargo workspace 和 Cargo.lock 两个本地包；Tauri version 读取 `../package.json`。事务失败恢复只撤销自己的版本写入，不覆盖期间外部修改。
- 正式/beta 默认阻断脏树；test 允许但明确标记。dry-run 只读。gitHash 捕获的是升版前提交，不冒充随后版本提交的 hash。
- 前端构建一次；test 输出 `dist/test-build`，正式/beta 为 `dist`。`raybend-build.json` 含版本、通道、gitHash、dirty、构建时间、worker 协议、资源哈希等。
- `scripts/lib/release-windows.mjs` 复用 dav1d/WSLENV，发布 target `C:\rb-target\raybend-release`，locked/custom-protocol，Windows cargo-tauri 前置检查，核心 worker 与桌面编译、bundle 命令已备。
- 发布安装器复用主 exe 自重启 RAW worker，未附带容易过期的第二份 worker；`check-win-artifact.mjs` 的 self 模式验证主 exe 内协议。日常 debug 仍走原双 exe 检查，不可随意混用。
- `src-tauri/tauri.conf.json`：NSIS 每用户/英简中、许可资源、禁降级，MSI upgradeCode 固定 `3E3C4B67-6F53-4A99-AD98-421306B894B1`；不要随意改变安装身份。beta 仅 NSIS。没有添加删照片/catalog 的卸载钩子，实际安装器行为未验证。
- `src/shell/WelcomeDialog.tsx`、`src/lib/welcome.ts`、`src/App.tsx`：native 成功读到空库列表且未确认时欢迎；数据库错误不当空库。复用已有建库/登记既有库入口；确认偏好 `raybend.welcome.v1`。
- `src-tauri/src/distribution.rs`、build.rs、Vite/App 的诊断身份：dev/test 允许，正式/beta 前端剔除诊断页面，原生也拒绝 spike。

未验收：真实 bundle、安装/卸载/首启/真实权限与 worker；可选文件关联未做。无证书不阻断工程，显式 `--unsigned` 路线已支持。

## 5. W2：签名更新

已做：

- 采用官方 Rust `tauri-plugin-updater` 2.12.0，未另写签名算法或新增 JS updater 框架。`src-tauri/src/updates.rs`、`src/api/updates.ts`、`src/shell/UpdateDialog.tsx`、`src/lib/update-source.ts` 和对应测试。
- off/stable/beta/custom；**只有主动点击才联网**，缺构建公钥默认关闭。源和公钥绑定，第三方可替换或关闭，不能跳过校验；HTTPS/配置长度原生再次检查，稳定源拒绝预发布。
- 检查、下载并验证 minisign、确认安装分开；配置变化/检查失败丢弃旧候选，并发操作闸门。检查超时 30 秒，下载 300 秒；尚无下载取消和体积上限，不假报已有。
- 原生安装保护：未结束导入（含暂停）、开启/运行的导出、M4 外部编辑作业；前端再防编辑未保存与迁移。复用既有状态，不造第二套任务体系。
- `raybend.updates.v1`；stable 源 `https://github.com/C-Thun/raybend/releases/latest/download/latest.json`；beta 源 `https://raybend.cthun.com/updates/beta.json`。地址是约定，**未验证已有可用线上清单**。
- `RAYBEND_DISTRIBUTION=store` 前后端禁用内置 updater；当前 release CLI 固定 direct，商店构建流水线未实现。

未验收：崔总生成/备份更新私钥、实际公钥配置、HTTPS 更新 JSON 部署、vN→vN+1、断网/坏签名/安装失败与系统恢复。下载失败不启动安装，不能据此宣传操作系统安装失败一定自动回滚。DB 迁移后旧 exe 会受 SchemaTooNew 阻挡，按迁移快照恢复并保留新增数据。

## 6. W3：离线许可、帮助与支持

已做：

- `scripts/generate-licenses.mjs`、`scripts/lib/license-inventory.mjs` 及测试生成 `public/legal/third-party.json`，Windows Cargo normal/build 图保守收录 + pnpm production，排除官网独立技术栈。593 项含版本/SPDX/来源/作者/许可原文及锁摘要，不含开发机绝对路径。
- `legal/` 保存上游精确 commit 原文和 SPDX 标准补足，单列 dav1d 1.5.0、LensFun 校准数据、libwebp COPYING/PATENTS 与源码复用声明。缺失不伪造版权。
- 三项 `spdx-fallback`：libwebp-sys 0.9.6、selectors 0.36.1、simd_helpers 0.1.0；公开发布前复核包内作者/源码文件头/上游原许可。标准 MIT 模板占位符不等于已确认原始版权，见 legal/README。
- `src/shell/AboutDialog.tsx` 复用既有弹窗展示离线帮助/隐私/许可、检索、重试；`docs/user-guide(.en).md`、`privacy(.en).md`、`release.md` 与 README 双语接线。
- `src/lib/support-info.ts` 只白名单复制 build/runtime 摘要，不含照片、目录、用户名与库记录，不自动上传。
- `help.docs` 默认 F1，`help.welcome/licenses/updates` 明确 defaultKey 留空（低频，避免误触），统一 catalog/menu/palette/设置；弹窗内部操作不另造全局热键。
- 官网中英 SmartScreen 文案已纠正，没有保证绕过提示、虚假签名/商店链接或捐赠地址。

下一步明确缺口：M4 现已交付导出与外部 RGB16 TIFF 单向交接，而 user-guide 中仍用「以所用版本发布说明为准」泛化描述。接续可按已实现契约补齐帮助，再更新路线中陈旧的 M4/Pencil 状态。无需重新建帮助体系。捐赠账户未配，属于可选项，不阻断发行。

## 7. W4：签名与最终产物清单

已做：

- 证书存储路线：`RAYBEND_SIGN_CERT_SHA1` 公共指纹，signtool SHA-256 + HTTPS 时间戳；无签名显式 `--unsigned`。远程 SignPath 适配未做，不把 Foundation 私钥想象成可导入本机。
- 独立更新密钥：`RAYBEND_UPDATER_PUBLIC_KEY`、`TAURI_SIGNING_PRIVATE_KEY`、可选密码；`--with-updater` 才要求。已有私钥文件路径可经 wslpath 转 Windows，不打印密钥内容。
- `scripts/finalize-release.mjs`、`scripts/lib/release-artifacts.mjs` 及测试，`pnpm release:finalize` 读取最终安装器/.sig/manifest，签名验证、SHA256SUMS、release-index.json、latest.json/beta.json；无 .sig 不出更新清单。
- 更新目标严格分 `windows-x86_64-nsis` 与 `windows-x86_64-msi`，不提供混装 fallback。最终发布者签名之后再生成 updater .sig 和哈希；清单器不冒充 minisign 验证器。
- `.release/` 与 `release-out/` 已忽略；未运行真实最终产物流程。

签名/信任讨论已形成 docs/release.md：官网 HTTPS、更新签名、Authenticode 发布者和 SmartScreen 信誉不同；不默认购买高价 EV、不保证新证书立即消除提示。SignPath Foundation 免费开源申请与 Store/MSIX 路线仍缺实际账户/审核/身份。

**微软账户费用与政策是时效信息**：上轮文档记载新入口免费并附官方来源；如果接续要给最新开户建议，须再查微软官方当前政策，不把历史判断直接当实时报价。MSIX 商店签名不等于官网 EXE 获得签名；EXE/MSI 商店路线也不能伪装 MSIX。需要真实 Package Identity Name/Publisher 后才接商店包装，现阶段无 MSIX、无认证、无商店地址。

## 8. 已验证证据与边界

**09-26 M5 实施记录中的历史冒烟**（本次换手未重跑这些测试）：

| 检查 | 当时结果 |
| --- | --- |
| pnpm typecheck / lint:colors / lint:arch / lint:i18n | 通过 |
| pnpm test | 1036 passed |
| pnpm test:release | 31 passed；与前端 release-plan 用例有交集，不相加当独立总数 |
| pnpm build 与发行通道前端隔离检查 | 通过；无 SpikeViewport/KitchenSink JS 和 spike_open 入口 |
| WSL cargo check -p raybend-desktop --locked | 通过 |
| cargo test -p raybend-desktop --lib --locked | 93 passed，1 ignored |
| Windows MSVC cargo check，自带 custom-protocol/locked/dav1d 环境 | 通过；是 check，不是 release bundle |
| pnpm licenses:generate | 593 项、锁摘要相符、无本机路径 |
| release test / patch --windows --unsigned --dry-run | 只读计划通过；正式脏树阻断明确 |
| website 独立 typecheck/build | 通过；lint 无错误，有既存 Hero 警告 |

**09-27 M4 最新记录补充**：pnpm test 1059 passed，桌面 Rust 95 passed/1 ignored，核心 1164 passed/5 ignored；前端质量门通过，Windows debug:win/check:win 通过，debug exe 时间 00:58:44 CST，路径 `/mnt/c/rb-target/raybend/debug/raybend-desktop.exe`。记录含合成 DOM/API 回归，不是实际 GUI E2E。引用该记录的结果，不声称本次换手重新验证。

**本次换手新核对**：HEAD/版本/dirty 与未跟踪数量、M4 最新记录、main.pen 文件状态、593 项许可清单与两份锁摘要一致、原生外部编辑安装保护已接入。仅文档变更，未重新编译/跑测试/启动程序或生成发布物。

尚无真实安装、升级、卸载、OS 信任、画质、DPI、大库性能、Windows 外部软件实际打开 TIFF 的发行验收证据。

## 9. 下个 Agent 的推进顺序

1. 读现状与源码，保留并行修改，检查 M4 最新契约对 M5 帮助/更新保护/许可/构建的影响；外部编辑原生保护已经接了，只核查遗漏并补适当单测，不重复实现。
2. 通过 Pencil MCP 验证 main.pen 的欢迎 `eyYHP`、离线许可 `uIuzv`、更新 `OBgSA`（复用原关于 `FPU9M`）实际存在；配对说明 `design/main.md` 已写。确认落盘后再更新 PLAN/W1 的旧保存提示，不因 mtime 变化宣称节点已验证。
3. 完成能独立做的文档/接线/脚本缺口，必要改动配边界单测和实施记录。代码改动后跑相关检查；脚本可用 `pnpm test:release`、版本规划 `pnpm release patch --dry-run` / `pnpm release test --windows --unsigned --dry-run`。**不要把省略 --dry-run 的打包命令交给 Agent 执行。**
4. 不用账户/密钥/真实安装器的部分连续做完。没有发布者证书可以保留无签名直下方案；没有更新密钥可以保持更新关闭。不要为了推进临时制造密钥、服务器、签名账户、CI secret 或 MSIX 身份。
5. 报告「开发完成/冒烟通过」与「未真实验收」两部分，集中说明以下崔总动作；只有真实验收满足时才完成 M5 DoD。

## 10. 最终需崔总执行或决定

- Windows 当前 GUI 确认欢迎/登记既有库、F1/许可检索/隐私/诊断/更新设置，并完成 M4 最新记录的真实流程/画质/导出/外部 TIFF 验收。
- 如启用内置更新，自行生成并安全备份私钥/密码，提供**公钥**构建配置和 HTTPS 更新清单发布位置；不要把私钥放对话或仓库。手动更新可先发行。
- 确定直下无 Authenticode、证书/SignPath、或 Store 方向；选择商店时取得实际 Identity/Publisher 后继续 MSIX 工程，免费签名服务则先完成申请/构建来源要求。捐赠入口可后补。
- 按 docs/release.md 自行生成两版安装包、最终签名/.sig/哈希清单，在干净 Win10/11 验安装→首启→更新及故障→卸载/重装数据保留；保留对应源码和迁移前快照，DB 恢复不能丢新增编辑。
- 验收后自行 tag/push/上传最终安装器与更新清单，触发既有官网 workflow。当前没有授权 Agent 代执行这些操作。

旧会话没有可调用的 plannotator/todo 工具，限制已记在 W1 与实施记录；未虚构评审通过或另开任务代审。接续若工具可用遵循项目单一任务源纪律；review 期间不 commit。不要因旧工具缺失重新索要已授予的开发许可。
