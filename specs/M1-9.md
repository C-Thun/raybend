# M1-9：库设置（齿轮）与离线状态图标化

> 范围：把**已经写好但没人挂载**的「库设置」接上；卡片的在线/离线改成**图标**（不带文字）；
> 点离线图标 = 对所有登记路径重新查找一次。导入模版这一版**纯手输**（体验优化进 FUTURE）。
> 依据：`REPOSITORY.md` §2（身份与多路径、在线/离线、§2.3 重新挂载流程）、`design/main.pen`
> 的 `RepoGear / 库设置` 与 `RepoOffline`。

- [ ] 1. `repositories` 模块导出 `LibrarySettingsDialog`（现在零引用 = 没实装）
- [ ] 2. 卡片右侧动作区：**在线 = 齿轮**（开库设置）、**离线 = 离线图标**（点它重挂载），**都不带文字**
- [ ] 3. 模版保存后刷新库列表（缓存里那份 `importTemplate` 要跟着更新）
- [ ] 4. 画廊加「库卡片」样例（在线/离线各一）+ 冒烟断言：齿轮在、离线的**可见文字为空**、点离线图标真的触发重挂载
- [ ] 5. Rust 测试：**改名模拟插拔**（在线 → 改名 → 离线 → 改回 → 在线 → 换挂载点登记新路径 → 在线）
- [ ] 6. `FUTURE.md` 登记「导入模版的编辑体验（下拉变量/可视化拼装/预览）」；`REPOSITORY.md` 补卡片图标口径
- [ ] 7. 门禁：typecheck / test / smoke / cargo test；前端构建 + Windows 重建（**`pnpm check:win` 必须过**）

## 不做（本单元）

- 库设置里除导入模版以外的项（名称、路径增删、序号计数器）—— 人类指定「只做一个改导入模版功能」。
- 模版编辑的高级体验（变量下拉、拖拽拼装、实时示例）—— 进 `FUTURE.md`。
