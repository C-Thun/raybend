# M4 导出验收前调整（2026-09-26）

状态：开发、单元、冒烟及 Windows debug 产物核对完成；真实环境待验收。崔总本轮已授权直接调整；外部编辑器细节只记录，暂不实施。

- [x] 记录 browse toolsbar right 唯一外部编辑入口、拍摄信息上方定稿下拉及组件级只读显示状态；同步当前格式规范。
- [x] Pencil/右栏移除新建入口，以名称判断新增/更新；四格式 WebP/AVIF/JPG/PNG；尺寸原样/百分比/最大边，默认日期目录模板，旧预设迁移。
- [x] 查明并修复 Tiles 回焦点/切工作流闪烁及定稿消失；刷新保留旧内容、只替换变化、首次明确加载态；选择框置于最上层。
- [x] 单元、可重复 DOM 冒烟与构建核对；实施记录和交付。

尺寸规则：百分比缩小 1–100 整数，100 等于保持不变；最大边 1–65535 整数，仅缩小不放大。数字框短宽，单位固定在右侧。新预设默认 WebP、原尺寸，模板 `:CYEAR-:CMONTH-:CDAY/:FILENAME`。
旧预设通过 v1→v2 显式迁移保留名称/ID/路径等设置；旧 TIFF 转为同样无损 RGB16 的 PNG。TIFF 编码能力保留在内部，仅供外部编辑入口，普通导出不再接受 TIFF。

完成记录：`implementations/2026-09-26_m4-export-refinement.md`。
