# M4-W4/W5：五格式导出与内存后台队列

状态：开发、单元与冒烟完成，Windows debug 已重建核对；真实环境待崔总验收。依据崔总 2026-09-26 指令，连续完成两波并解决回归失败。

- [x] 复用现有 RGB16 出图、导入模板、原子发布与官方编码器；字段×格式矩阵和回读测试。
- [x] 五格式、命名预览/子目录/竞争不覆盖、元数据与预设文件导入导出。
- [x] Pencil 补预览、预设交换、失败重试与四预设上限状态，随后接入界面。
- [x] Rust 内存队列独立线程、有界四预设、最老先执行、失败继续、停止/重置隔离与幂等。
- [x] App 订阅权威状态、启停/重试/进度/闪烁与命令接入；无队列持久化。
- [x] 修复 browse/UI/export 回归失败，完整质量门与 Windows 调试版核对。
- [x] 实施记录与 PLAN 收口，向人类交付完整导出验收入口。

元数据矩阵：拍摄字段写入五格式原生 EXIF；关键词/版权/作者/说明写入原生 XMP（JPEG APP1、TIFF tag700、PNG iTXt、WebP XMP chunk、AVIF mime item）；JPEG 同时写 UTF-8 IPTC APP13。全部方向=1、输出尺寸更新、剔除缩略图/旧偏移/MakerNote；TIFF/PNG RGB16无损，JPEG/WebP/AVIF RGB8量化后编码。

新增确认：执行前按定稿哈希验证存在；修改或删除定稿/照片则跳过标失效并继续。校对 PLAN 行为表，取消选中只用 Esc，保留范围控制。

验证与限制见 `implementations/2026-09-26_m4-w4-w5-export-runtime.md`；W6 外部编辑器与 W7 整体验收仍另行收口。

2026-09-26 二次修订覆盖上述旧入口：预设文件交换暂不启用；目标预览和独立失败/失效清单移除。预设仅列表载入与按名称新增/变更，失败信息和单项重试在队列条目；哈希失效自动跳过并移除。最新任务见 `specs/M4-export-cleanup.md`。
