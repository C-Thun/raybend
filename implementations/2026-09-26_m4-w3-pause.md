# M4 导出缺陷修复与 W3 高精度出图（暂停交接）
完成时间：2026-09-26 17:35:42 CST

按用户要求收尾暂停，接下来讨论 export.pen 的 UI 问题。本记录是阶段交接，不代表 W3 已完成。

## 改动范围

- `src/workspaces/export/ExportWorkspace.tsx`、`src/lib/export-model.ts` 与单测：有照片时空态返回 null，使共享 PhotoGrid 正常挂载并触发分页取定稿；库后台刷新仅首次载入显示加载提示，并隔离过期响应。
- `src/features/browse/BrowsePanels.tsx`：共用库卡按稳定 ID 渲染，后台数据对象更新不再重建卡片。
- `src/i18n/zh-CN.ts`、`design/export.pen` 与同名说明：中文界面用「定稿」，英文与程序标识保持原样。画稿仅通过 Pencil MCP 更新说明和空队列文案。
- `develop/pipeline.rs`、`sample.rs`、`sharpen.rs`、`lut.rs`、`geometry.rs`：泛化已有算法，显示阶段输出 RGB16，RGB8 API 为最终量化适配；不复制数学。
- `display/output.rs`：唯一 profile/几何/缩放配方，缓存复用；严格完整尺寸取图保留 PNG/TIFF 16 位输入，RAW 用隔离 worker 完整线性解码，禁止预览与占位退路。
- `thumbnail/render.rs`：接入共同配方，管线版本 13 → 14 让旧派生缓存失效。
- `export.rs`：稳定资产角色解析移动后的源文件，捕获 profile 不变，源签名在执行前后与发布前检查；提供完整尺寸 RGB16 TIFF 编码/单文件输出。
- `fs_atomic.rs`：复用临时文件机制，以同目录 hard_link 原子发布新文件，竞争时只有一个成功且不覆盖已存在目标。文件系统不支持 hard_link 时明确失败，不降级为覆盖写。
- `src-tauri/src/export.rs` 与 `lib.rs`：薄壳单稿 TIFF IPC，解析镜头/LUT，数据库锁在解码前释放。
- `scripts/check-export-boot.mjs` 与 `package.json`：新增可重复的有数据 DOM 回归工装，复用 CDP 驱动；目前未通过，待继续调通。

## 验证

已验证（冒烟/单元）：
- `pnpm typecheck`：通过。
- `pnpm test`：1009 通过。
- `cargo test -p raybend --lib --quiet`：1145 通过、4 忽略，约 11 秒；包含 16 位渐变贯穿空间阶段/曲线/几何/缩放、TIFF 精确读回、源字节保持、移动解析、非法输入与并发无覆盖发布。
- `cargo check --workspace`：通过（含新增 TIFF IPC）。

尚未完成：
- `pnpm check:export`：工装当前通过 DOM click 未成功切入 export，停留在 browse，未验证有数据页面；不能将此结果视为产品功能通过。
- 完整 lint/构建、Windows 重建和产物核对未执行；当前 Windows exe 尚不包含本次修改。
- 真实照片、颜色、GUI 视觉、DPI、性能体感未由人类验收。

## 决策与遗留

W3 未标完成，W4–W7 未推进。源签名复用文件元数据与稳定 file ID，未另建内容哈希通道。
新增 TIFF IPC 供后续 worker/外部编辑复用，当前未开放独立 UI 用户动作，因此不另登记同义命令或默认热键；现有导出命令与热键继续复用。
未修改共享 Shift 选择实现，也未接管其他任务对 editor、路线与远期文档的并行修改。
继续前先完成画稿讨论；之后调通有数据回归、补质量门和 Windows 冒烟，再收口 W3。
