# M3-W7：M3 开发与冒烟收口
完成时间：2026-09-26 06:03:42 CST

## 范围与文件

- 补齐 W6c 遗漏的命名定稿对比：`design/editor.pen` 新增 `Components / Editor / 对比来源菜单`，同名说明、`plans/M3-W6c.md` 与 `plans/M3-W7.md` 同步。编辑状态栏复用已有对比来源下拉，SOOC、RAW 与本照片命名定稿共用一处入口。命名定稿从自身 1920 AVIF 快照读取，切换不会修改当前 profile、latest 或撤销历史。
- `crates/raybend/src/develop/reference.rs` 给已显影 RGB 快照分配唯一引用帧 ID；`src-tauri/src/issues.rs` 复用独立预览缓存；`src-tauri/src/editor.rs` 新增异步读取和 GPU 对比帧提交。照片路径与递增选择序号双重门禁阻止旧加载结果覆盖新选择；`src/workspaces/editor/EditorWorkspace.tsx` 在 SOOC 编辑时允许选择命名定稿，并限制旧请求错误提示。`src/api/editor.ts`、`src/api/types.ts` 和命令注册同步。
- W7 核对并同步 `AGENTS.md`、`BROWSE.md`、`DESIGN.md`、`FUTURE.md`、`IMAGING.md`、`PLAN.md`；`docs/issue-xmp-contract.md` 明确未来 XMP 序列化的版本化契约。`scripts/ui-smoke.mjs` 修正动态反差的实际面板位置，并令脚本对检测问题返回非零。`crates/raybend/examples/migration-drill.rs` 使用当前迁移版本验证旧库升级。
- W6a/W6b/W6c 的原实施记录分别保留；本记录只记 W7 收口与 W6c 遗漏项，不把真实 GUI 体验当作已验收。

## M3 DoD 核对

1. RAW/JPG 同界面无损编辑：编辑栈在 catalog，原图只读；原文件哈希在真实照片上的前后核对留给 Windows 人类 E2E。
2. 常规调整：影调、色彩、曲线、降噪、锐化、镜头、裁切的前后端链路与单测通过；真实交互帧率和颜色由人类裁决。
3. CUBE LUT：导入、应用开关、移除及缺文件降级已在 W6c 实现；七个 `C:\src\resource` CUBE 通过解析测试。
4. SOOC/issue：定稿、浏览切换、撤销/重做、编辑对比与独立 384/192/1920 快照已接入；XMP 契约见 `docs/issue-xmp-contract.md`，实际 XMP 写入属于后续波次。
5. 缩略图与看图：latest 和命名定稿走现有统一显影入口及不同缓存键；真实照片视觉正确性留给人类真机。

## 验证（Agent 冒烟）

- 前端 `pnpm typecheck`、`pnpm test`（954 项）、`pnpm lint:arch`、`pnpm lint:i18n`、`pnpm lint:colors`、`pnpm build` 全通过；`pnpm smoke:ui` 的 `problems` 为空。
- Rust `cargo check -p raybend-desktop` 通过；`cargo test -p raybend --lib` 1084 通过、3 忽略；`cargo test -p raybend-desktop --lib` 85 通过、1 忽略。补充了引用帧非法尺寸、唯一 ID、旧序号/换照片拒绝的单测，并修复测试样本漏填 LUT 字段。
- `pnpm migrate:drill /tmp/raybend-m3-w7-migrate` 通过：catalog v1→v11、迁移前快照、损坏库/未来版本门禁、备份轮转。`git diff --check` 通过。
- 最终 `pnpm debug:win` 通过：Windows debug 主程序与 RAW worker 编译，`check:win` 验证嵌入的 4 个前端资源及 worker 协议；Windows cargo 耗时 1m26s。构建产物位于 `C:\rb-target\raybend\debug\`，不是正式安装包。
- WSL 合成性能基线和离屏像素证据见 `plans/M3-W7.md`；这些数字不能推断 Windows 真机帧率或色彩。

## 边界与遗留

- 命名定稿对比取 1920 长边快照；在 1:1 放大时左侧清晰度受快照分辨率限制。正式导出仍从 profile 实时显影。
- 人类按 `plans/M3-W7.md` 在 Windows 用真实照片、两档 DPI 核对原文件哈希、LUT、切稿/对比、颜色、镜头、帧率与内存；Agent 未声称完成这些 E2E。
- `cargo fmt --all --check` 仍因仓库既存的跨文件格式差异失败，输出涉及大量未改文件，未为了格式化而批量重写源码。
- 曾尝试仅把 `crates/raybend/examples/migration-drill.rs` 的格式噪声还原为 HEAD 排版并保留三处逻辑改动；自动审批拒绝该写入，理由是可能丢失 W6/W7 或用户修改。未绕过审批；当前文件保留功能正确但较大的格式 diff，迁移演练已通过。 **后续已解决**：用户改为请求批准后，通过格式等价与原文件哈希校验，获批写回；见 `2026-09-26_migration-drill-format-cleanup.md`。
- 本轮新增“选择定稿作为对比来源”是状态栏局部菜单动作，不适合新增全局命令/默认热键；已有“进入对比”命令照旧由命令注册表管理。
