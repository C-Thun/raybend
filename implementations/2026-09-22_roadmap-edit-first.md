# 路线再重排：编辑提前为 M3，产品化功能后移

完成时间：2026-09-22 13:00:15 CST（人类 2026-09-21 定案，本记录覆盖其后两批文档改动）

## 改动范围（人类 2026-09-21 口述方案，逐条落实）

| 人类原话/要求 | 落实 |
| --- | --- |
| M3 改为编辑功能，真正落地基于 GPU editor | `PLAN.md` M3 整章重写：GPU 显影工作台——同一界面对 RAW/JPG **无损（非破坏性）**编辑、常规调整（亮度/色彩/曲线/降噪/锐化/镜头校正/裁剪/对比）、**CUBE 格式 LUT 导入**、SOOC 及 issue 支持；波次 W0（界面设计前置）/W1（地基：issue 数据模型 + 原生视口）/W2（常规调整管线）/W3（对比、LUT、issue 交互）+ DoD 草案 |
| 原 M3 改为 M4，因为要有 issue 才能做导出 | M4 = 实时一致性 + 导出；导出来源契约**按 issue/SOOC 口径重写**（画廊式选 issue 落在 W3） |
| 原 M4、M5 写入 FUTURE | `FUTURE.md` 新增 **I 节**（组织、检索与互操作）与 **J 节**（多仓、缓存治理与数据安全）；H1/H4/H6/H8/H10 状态改未排期 |
| 加一条注明把 Ctrl+K 同时应用于照片搜索 | H8 与 I 节均补注（人类 2026-09-21） |
| （补充要求）多仓那块要注明包括数据的备份支持 | J 节标题与正文明确「**含数据备份支持**」，并列出定期策略/恢复 UI/指引 |
| 原 M6 改为 M5，四大模块完成就可以发版先用 | M5 = 分发与发布，定位句写入章节目标与路线总览 |
| （补充要求）只有 import 和 browse 有设计，未设计界面的模块开工前先做界面设计环节 | 「设计阶段」章节新增**硬规矩**；登记 `design/edit.pen`（M3 开工前置）与 `design/export.pen`（M4-W3 前置）；M3 设 W0 设计波次；纯后端波次（M4-W1）豁免 |

## 涉及文件

- `PLAN.md`：路线总览、明确不做清单（图像编辑移出）、新增 §1.2 再重排依据、设计阶段章节、
  M2→M3 闸门注、M3/M4 整章、删原 M5 章、原 M6→M5、决策记录与变更记录各加一行；
- `FUTURE.md`：一致性改指 M4-W1；「issue 与 SOOC」标已排期 M3；D 节口径更新（不再是第二阶段）；
  C4/C7/D2/D5/G16 触发点改为 M3；新增 I/J 节；变更记录加行；
- `AGENTS.md`：§1 定位「第一阶段不做编辑」→「编辑已进入第一阶段主线（M3）」；§8 问题 #1
  处理时机改 M3 开工时按 D2/D3 候选取舍；
- `design/browse.pen`：**补落盘**——f410726 提交时「菜单（标题栏）」「快捷键设置」两帧只在
  Pencil 内存画布里（MCP 的修改不直接写盘，落盘要编辑器保存），本次随保存落盘补交；
  顶层帧清单核对为原 11 帧 + 这 2 帧，无其它改动。

## 关键决策与理由

1. **「无损」的口径写死为非破坏性**：编辑只写编辑栈（DB/XMP），原始文件零改写（DoD 用校验和验证）；
2. M3 波次把 **issue 数据模型放在 W1 与原生视口并列**——它是编辑、导出、浏览显示三方共同的地基，
   也是人类「没有它导出是空中楼阁」判断的落地；
3. 算法（去马赛克/降噪/镜头校正）与新增依赖（lensfun 等）不预先定死，开工时按 `FUTURE.md`
   D2/D3 候选与 §2.9 纪律讨论；
4. AGENTS.md 本批与胶片带会话的一行 §11.4 规则（SplitHandle 复用）混在同一文件，随本批一起提交
   （该会话的 src 改动仍由其自行收尾提交，未被本批触碰）。

## 验证

- 纯文档 + 画布落盘改动，无代码/schema/依赖变化；`grep` 核对无旧编号残留
  （G8 的 M6 指向已改 M5、A2 的「M0–M6 期间」改为编号无关措辞）；
- 顶层帧清单 diff：HEAD 11 帧 → 工作区 13 帧，新增即两帧本身；
- 未跑编译/测试（无代码改动）。

## 遗留与下一步

- **M3 开工前置 = 人类的编辑工作区界面规划**（W0），Agent 侧不动工；
- 胶片带把手会话（2026-09-21 00:44）的 src/BROWSE/DESIGN/check-browse-boot 仍未提交，待其收尾；
- `design/browse.md` 与 .pen 的两帧说明一致（f410726 已含），无需补文档。
