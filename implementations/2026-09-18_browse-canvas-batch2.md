# 画布批次（W2 阶段 0.3）：状态 tile 重画 / Tab 第三态 / 标签弹窗 / 按实现同步

完成时间：2026-09-18 16:23:48 CST

计划：`plans/M2-W2.md` 阶段 0.3（0.3.1–0.3.6）
文件：`design/browse.pen`（画布）、`design/browse.md`（说明书）
截图：`C:\src\tmp\raybend-canvas-2026-09-18\`（6 张，文件名已改成看得懂的名字）

---

## 一、做了什么

| 步 | 内容 |
| --- | --- |
| 0.3.1 | `States / 浏览标记与选择`：7 个选择态 tile 按新外形重画（正方 150×150、圆角 6、内边距 3、照片保比例居中、覆盖式信息条 26 高）；锚点那格补 `$brand` 1px **inner** 描边 |
| 0.3.2 | `States / 浏览状态（…）`：**补「③ Tab 关胶片带（左右两列在）」**（原来只画了 ② 关左右）；帧名/标题改成四格口径 |
| 0.3.3 | **新帧 `Components / 标签弹窗`**：单张（搜索下拉 + 标签带叉）/ 批量（无叉 + 一句提示）两态并排 |
| 0.3.4 | 复核并同步既有帧：筛选结果区补 **排序控件**（`拍摄时间 ▾`）与 **「任一 / 全部」分段控件**；对比帧的胶片带从 **12 张改成 2 张**（规则是「只显示参与对比的图」，原来画多了） |
| 0.3.5 | 补 **「④ 未选目录的空态」** 一格（点库不铺图、树根是 `photos/` 之内、`_RAW` 不显示） |
| 0.3.6 | 导出 6 张截图交人类过目（见上） |

**面用法按实现改了一遍**（人类口径「已实现的部分以实现为准」）：
`TilePhoto` / 网格 mini tile → `$surface-main`（原为 `$surface-track`）；搜索条 / chips / 预览底 /
直方图底 / 命令面板输入行 /「查看所有库」半截条 → `$surface-bar`；状态栏锁徽标 → `$surface-layer`。
改完只剩**凹槽轨道**与「取消」次级按钮在用 `$surface-track`（与 `Button variant="secondary"` 一致）。

## 二、说明书同步（`design/browse.md`）

* §1 帧表：补两个新帧、改名 `States / 浏览状态（…）`；
* §2.2 / §2.5：目录树没有「整个库」入口、对比胶片带 2 张；
* §2.3.1：把这一轮的按实现同步逐条写下来（含 `$surface-track` 的合法用途清单）；
* §5 待决表：`Tab` 循环（→ **并列三态**）与筛选与/或（→ **任一/全部 分段控件**）标为已决；
* **§5.00 扩为四格**（命令面板 / 筛选 / Tab 两态 / 未选目录空态）、**新增 §5.01 左列目录树口径**、
  **§5.2 标签弹窗**、**§5.3「与实现冲突时以实现为准」的判据（长期有效）**。

## 三、验证

画布逐帧截图核对（无塌陷、无溢出、无断图）；`Get(..., ctx.problems)` 无越界报告；
导出 PNG 六张已落在 `C:\src\tmp\raybend-canvas-2026-09-18\`。

## 四、看完截图后的两处修正（人类 2026-09-18 反馈）

1. **tile 信息条的文件名重复了扩展名**：画布上写的是 `P0001.JPG`（全名）+ 右端 `JPG` 标签，
   同一行说了两遍。**实现里本来就剥掉了**（`Tile.tsx` 的 `displayName()`：末尾 `.` + 1~5 位字母数字就剥），
   所以是画布错 —— 已按实现把 **45 个 `TileName` 的后缀剥掉**。
   （状态栏 / 右栏信息里的文件名**不剥**，那里没有独立的后缀标签，剥了反而丢信息。）
2. **`Shell / Browse / Libs Expanded` 跟上新口径**（原来那一帧还是旧规则）：在列表下面补了
   **`DirTree / 缩到最小`** 那一条（一行提示 + 两行树行，高 88）；
   同时把缩起态的**伪卡片改成满高卡片（靠顶、上留 8px）** + 列表容器钉在 **3.5 张卡**（高 234）——
   这样「查看所有库」六个字完整露在上半截里，不再被下边缘切到（人类看截图时提的那点）。

导出更新：`C:\src\tmp\raybend-canvas-2026-09-18\01-shell-browse-tiles.png`、`07-libs-expanded.png`。

## 五、遗留

* 浅色主题与宽松档的派生帧仍未出（主稿只有深色·紧凑，老问题，见 `design/browse.md` §5 待决表第 5 条）。
* 画布的像素只是**结构**示意：真实尺寸以 `tokens.css` 与实现为准（`DESIGN.md` §8.4）。
