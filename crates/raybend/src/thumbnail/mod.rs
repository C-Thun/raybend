//! 缩略图与预览（M0-5 落地）。
//!
//! 计划承载：
//!
//! - 三个尺度：`GRID`(256–512) / `SCREEN`(视口分辨率) / `FULL`(1:1)；
//! - 缓存键 `(asset_uid, kind, render_signature)`，其中 `render_signature`
//!   含 `pipeline_ver` —— 算法升级后旧缓存自动变孤儿并被回收，无需写失效迁移；
//! - 淘汰策略（容量上限 + frecency + 保留期 + `pinned`）与孤儿回收在 M5 落地。
//!
//! **红线**：删掉整个缓存目录后，功能降级但必须完全可用。
//! 存储形态的取舍见 M0-5 的实施记录。
//!
//! **本模块目前只有说明** —— M0-1 阶段不引入图像编解码依赖。
