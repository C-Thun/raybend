//! 目录索引与本地数据库（M0-4 落地）。
//!
//! 计划承载：
//!
//! - `rusqlite` 连接管理与 **单写者 actor** —— SQLite 只允许一个写者，
//!   所有写操作经由专属线程串行化，读走连接池；
//! - 迁移骨架（`PRAGMA user_version` + 顺序迁移）与版本闸门；
//! - `app.db`（全局）与 `catalog.db`（每仓）的表结构。
//!
//! 架构决定见 `AGENTS.md` §6.4；基准数据见 M0-4 的实施记录。
//! **本模块目前只有说明** —— M0-1 阶段不引入 `rusqlite` 依赖。
