//! 库根目录层面的操作。
//!
//! 与 [`crate::store`] 的分工：`store` 管的是**库的目录结构与数据库内容**
//! （catalog.db 的位置、库身份、导入模版），本模块管的是**用户可见的目录本身**
//! —— 也就是目录树行尾 `⋯` 菜单那两个动作（`BROWSE.md` §4.3）。
//!
//! - [`dirs`]：新建子目录 / 删除空目录（含名字校验与深度空判定）

pub mod dirs;
