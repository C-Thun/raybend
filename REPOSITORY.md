# raybend 库与导入规格（REPOSITORY.md）

> 本文件是**库（library）与导入流程**的业务事实来源。来自用户 2026-09-15 的完整口述，
> Agent 负责整理、指出冲突与待确认项，**不擅自改语义**。
>
> 相关：`AGENTS.md` §6.4（存储架构，本文件细化它）、`DESIGN.md`（界面）、
> `plans/M1-2.md`（数据底座）、M1-3（扫描）/ M1-5（导入工作区）/ M1-6（导入执行）。
>
> 建立时间：2026-09-15

---

## 1. 库的物理结构

库 = 用户指定的一个目录（库的根目录）。库根下的结构**由 raybend 管理**：

```text
<库根>/
├── catalog.db              # 库数据库（库身份、资产、导入模版、序号计数…）
└── photos/                 # 导入进来的照片
    ├── 2026-08-15/
    │   ├── MYP0001.png     # 位图按模版改名后放这里
    │   └── _RAW/
    │       └── MYP0001.ORF # 同名 RAW 放同级的 _RAW/ 下（也按模版改名）
    └── 2026-09-02/
        └── ...
```

要点：

1. **`catalog.db` 直接在库根**（不是藏在 `.raybend/` 子目录里）—— 用户明确要求。
2. **`photos/` 是导入的落地目录**；库根下除此之外不放别的由我们创建的东西。
3. **`_RAW/` 是个保留目录名**：与照片**同级**，放同名 RAW。它出现在每一层落地目录下
   （如 `photos/2026-08-15/_RAW/`），而不是全局只有一个。
4. 库**不强制**目录里只有位图 —— 用户可能自己往里放东西。
   目录里「有啥就是啥」，raybend 不因此报错，只是**登记到库里的**是它自己导入的那些。

> ⚠️ 这条相对 `AGENTS.md` §6.4 早先写的 `.raybend/{repo.json,catalog.db,index.db}` **已经变更**，
> 该小节已按本文件更新。`index.db`（派生索引）暂时不建 —— 等真的需要可重建索引时再加，
> 位置仍是库根（`<库根>/index.db`），不进 `photos/`。

---

## 2. 库身份、多路径与在线/离线

这是本文件最复杂的一部分，用户明确要求支持两件看似矛盾的事：

- **同路径不同库**（同一个目录下换过 catalog.db）
- **同库多路径**（同一个库出现在多个目录，例如同一块盘的不同挂载点、或库被复制/U 盘换盘符）

### 2.1 真相源：中央 `app.db` 记录 + 每库 `catalog.db` 内的唯一 ID

| 位置 | 记录什么 |
| --- | --- |
| 中央 `app.db` | **所有已登记库**：库唯一 ID、名称、导入模版缓存、**该库登记过的每个路径**及其最近状态 |
| 每个 `catalog.db` | **自己的唯一 ID**（`repository_meta.repository_id`）+ 库内数据 |

路径挂载了哪个库，靠「**读该路径下 `catalog.db` 里的 ID**」与 `app.db` 的登记比对得出 ——
而不是靠路径字符串。这正是「同路径不同库」能成立的机制。

### 2.2 状态判定（**统一封装成一个方法**）

用户要求：库的所有操作都要过一层状态检查，统一处理「目录/文件刚失效」的情况。

```text
resolve_repository(repository_id) -> LibraryResolution
  ├─ Online { root, catalog_path }   # 某个已登记路径下确实有这个 ID 的 catalog.db
  ├─ Offline { tried_paths }         # 登记过的路径都不含该 ID 的 catalog.db
  └─ Missing                          # 从未在线过（新建但还没落地）
```

规则：

- 判定顺序：先查**上次已知在线的路径**（快），再遍历该库登记过的其它路径。
- 任何对库的操作（读列表、导入、缩略图、写元数据）都先走 `resolve_repository`；
  解析失败一律转为**离线**而不是报错 —— 界面显示离线徽标，操作给出可读提示。
- 判定结果**缓存**（带失效时间），避免每次点击都遍历路径。

### 2.3 离线库的重新挂载流程（用户指定）

```text
库显示离线（例如移动硬盘没插）
  → 用户插入驱动器
  → 点击离线图标
  → 对该库**所有已登记路径**做一次查找（找 catalog.db 且 ID 匹配）
  → 找到 → 记录「当前挂在哪个目录」→ 转为在线
  → 找不到 → 仍是离线（并给出「未找到该库」的提示）
```

### 2.4 添加库的两个分支（用户指定）

```text
用户选中一个目录，点击「添加库」：
  ├─ 该目录下**已有** catalog.db
  │    → 读出其中的 repository_id
  │    ├─ app.db 里已有这个 ID → 这不是新建，而是**登记一条新路径**（同库多路径）
  │    └─ app.db 里没有这个 ID → 把该库整体登记进来（连同它已有的资产）
  └─ 该目录下**没有** catalog.db
       → 询问是「新建库」还是「该目录属于某个已知库」
         · 新建：生成新 ID、建 catalog.db、登记路径
         · 已知库：把这条路径登记到那个库名下（用于离线库换挂载点）
```

- 「一个路径被标记了但没有 catalog.db」→ 该库**显示为离线**，并可在同一路径下**创建库**
  （即补建 catalog.db）。
- **同一路径允许登记给多个库**（`UNIQUE(path_folded, repository_id)`，而不是路径唯一）。

### 2.5 库唯一 ID 的生成方式（**待你定，见 §6 问题 1**）

你给的参考实现是 `/opt/zzd/shop/luclin/Generators/Gid.php`（配套 `BaseFlow.php`）：

```text
Gid = base62( 时间戳(减 SINGULARITY、精确到 10µs、hex 补足 14 位)
            + shard(默认 '00')
            + 随机数(randomSize=6 字节 → 12 位 hex) )
      → 定长 18 字符（不足左补 '0'，超出截断）
校验：长度 18 + 全在 base62 字符集 + 反解回 hex 后能取出原 shard
另有 toUpper：转 base36 大写、定长 20（用于大小写不敏感的场景）
```

**关于长度（用户 2026-09-15 的补充提问：「我记得是 12 位」）**——
我按代码逐段数了一遍，**当前默认输出是 18 位**：

```text
hex 段 = 14（时间戳）+ 2（shard）+ 12（randomSize=6 字节）= 28 个 hex 字符 = 112 bit
base62 所需位数 = 112 × log10(2)/log10(62) ≈ 18.8  → 按 lengthLimit=18 截断
```

12 / 16 位是**更短配置**下的结果（`lengthLimit` 与 `randomSize` 都是构造参数）：

| 想要 | 需要的 hex 位数 | 一种配法 | 随机熵 |
| --- | --- | --- | --- |
| 12 位 | ≈18（71 bit） | 时间戳 14 + shard 0 + 随机 2 字节（4 hex） | 16 bit ⚠️ 偏少 |
| 16 位 | ≈24（95 bit） | 时间戳 14 + shard 2 + 随机 5 字节（10 hex） | 40 bit |
| **18 位（默认）** | 28（112 bit） | 时间戳 14 + shard 2 + 随机 6 字节（12 hex） | **48 bit** |

- 时间段的容量：56 bit × 10µs ≈ **2280 年**，够用。
- 随机段：48 bit ≈ 2.8×10¹⁴ 种 —— 对「一个用户建的几个库」来说碰撞概率可忽略。
- **本项目要的是「库 ID」，全局唯一天花板很低**（一台机器上几十个库），
  所以长度本身不是问题；`uuid`（36 位文本）更长但零实现成本。

**Agent 的评估**：

| 维度 | Gid（你的方案） | UUID v7（标准方案） |
| --- | --- | --- |
| 时间有序 | ✅（hex 段左补零 → 字典序即时间序） | ✅（RFC 9562，毫秒时间前缀） |
| 去中心化唯一 | ✅（随机段） | ✅ |
| 分片信息 | ✅（shard 段，为分布式 DB 准备） | ❌（不需要） |
| 实现成本 | 需要移植大数 base62/base36 转换 + 校验（Rust 里要自己写 bigint 或引 `num-bigint`） | `uuid` crate 一行 `Uuid::now_v7()` |
| 互操作 | ✅ 与你这套现有系统可直接互换 | 需要转换 |
| 长度 | 18（base62） | 36（带横线文本）/ 22（base64url） |

**结论（供你裁决）**：本项目是**单机应用、无分片、无跨节点协调**，`shard` 段的收益接近零；
时间有序与去中心化唯一这两点 UUID v7 同样满足，且零移植成本。因此**默认取 UUID v7**。
但如果你希望 raybend 的库 ID 与你现有系统**互通**（例如将来把库登记到你的服务端），
那应该忠实移植 Gid —— 这一点只有你能定，见 §6 问题 1。

---

## 3. 导入模版

### 3.1 默认模版与示例

```text
默认：:CYEAR-:CMONTH-:CDAY/MY:FILENAME
```

例：创建时间为 2026-08-15、名为 `P0001.png` 的文件被导入后：

```text
photos/2026-08-15/MYP0001.png
```

- 模版里的 `/` 是**目录分隔符**，会生成子目录。
- 落地目录的起点是 `<库根>/photos/`；模版描述的是 `photos/` **之内**的相对路径。
- 模版是**库级设置**，存在该库的 `catalog.db` 里（可随库迁移），
  同时在 `app.db` 缓存一份供离线显示。

### 3.2 变量表（`:` 前缀，替换即可）

| 变量 | 含义 | 宽度 | 例 |
| --- | --- | --- | --- |
| `:CYEAR` | 创建年份 | 4 位 | `2026` |
| `:CMONTH` | 创建月份 | 2 位 | `08` |
| `:CDAY` | 创建日 | **2 位**（见 §6 问题 2） | `15` |
| `:CWEEK` | 一年中的第几周 | **2 位**（见 §6 问题 2） | `33` |
| `:FILENAME` | 文件主名（**不含扩展名**，扩展名原样透传） | — | `P0001` |
| `:BRAND` | 相机品牌 | — | `NIKON` |
| `:MODEL` | 相机型号 | — | `Z7II` |
| `:SEQ000` | 序号，3 位 | 3 | `001` |
| `:SEQ0000` | 序号，4 位 | 4 | `0001` |
| `:SEQ00000` | 序号，5 位 | 5 | `00001` |

**解析规则（重要，用户特别说明）**：变量名按**已知名字的前缀匹配**，其后是字面量。

```text
模版片段 `:FILENAMEGO`  →  FILENAME + 字面量 `GO`
即：P0001 → P0001GO（GO 加在文件主名末尾，而不是当成变量名的一部分）
```

因此解析器要按「已知变量名集合」做最长前缀匹配，未匹配的 `:` 视为字面量（并给出校验警告）。

### 3.3 序号（`:SEQnnn`）

- 序号在**最终落地目录**（例 `photos/2026-08-15/`）范围内计数，计数存在**该库的 `catalog.db`** 里。
- **不同宽度各自独立计数**：同一目录下 `SEQ000` 与 `SEQ0000` 互不干扰（它们的键不同）。
- **从 1 开始**；**写满后回绕到开头**（用户明确：不纠结）。
- 回绕与并发导致的重名，由 §3.4 的重名规则兜底。
- 序号键要包含模版里**序号之前**的部分吗？—— 不需要：用户要的就是「目录级计数」。

### 3.4 重名处理

```text
目标文件名已存在 → 追加 `_XX`（两位，从 01 开始）
                  两位用完 → 继续涨位数（_100、_101…）
```

- **为什么两位**：两位以内不影响按字符排序；这个数量级足够用。
- 重名可能来自两个地方：① 模版改动后新旧命名撞车；② 序号回绕。
- 追加的位置：**扩展名之前**（`MYP0001_01.png`）。

### 3.5 模版校验（给用户即时反馈）

- 变量名不认识 → 警告（并列出可用变量）。
- 结果路径含非法字符（Windows：`<>:"|?*` 等）→ 报错并指出位置。
- 模版末尾若为 `/` 或为空 → 报错。
- 有没有序号一样的两个模版变量？允许（同宽度会连号），但会提示。

---

## 4. 导入流程

### 4.1 位图与 RAW 的分流（用户指定）

对**同一张照片**（同名不同扩展名，如 `P0001.png` + `P0001.ORF`）：

| 情况 | 处理 |
| --- | --- |
| 只有位图 | 位图进目标目录 |
| 只有 RAW | RAW **正常进目标目录**（不是进 `_RAW/`） |
| 两者都有 | 位图进目标目录；**RAW 进同级的 `_RAW/`**，且**也按同一模版改名** |

例（模版 `:CYEAR-:CMONTH-:CDAY/MY:FILENAME`）：

```text
source:  P0001.png + P0001.ORF（拍摄于 2026-08-15）
target:  photos/2026-08-15/MYP0001.png
         photos/2026-08-15/_RAW/MYP0001.ORF      ← 同名，只差目录
```

- catalog.db 里**可以**关联这两者（便于「编辑位图时自动找同名 RAW」），但**不强制**：
  用户可以自由移动/改名文件，`_RAW/<同名>` 只是一种**约定**。
- 编辑位图时默认尝试载入 `_RAW/` 下的同名 RAW 来解码编辑，**用户可切换为「仅编辑位图」**。

### 4.2 目录透传（同步子目录）

- 导入模版先生成**落地目录**（如 `photos/2026-08-15/`）。
- 若开启了「包含子目录」，则把 source 侧的**子目录结构透传**到落地目录之下，
  子目录内的文件名**同样按模版规则改名**，并登记到库中对应的透传子目录里。

```text
source:  /photo/2026-08-15/婚礼/P0001.png
target:  photos/2026-08-15/婚礼/MYP0001.png     ← 模版只作用于文件名，目录结构透传
```

> **待确认**：透传的子目录是否也参与「落地目录」的生成？（即模版只对第一层生效还是每层都套）
> 见 §6 问题 3。

### 4.3 避免重复导入

- 默认勾选「避免重复导入」（`design/main.md` §3.3）。
- 判据：**文件身份**（`(volume_serial, file_id128)`，见 `AGENTS.md` §7.3）+ 内容大小/mtime 兜底；
  导入后身份记录在 `catalog.db` 的 `asset_files` 上。
- 对已在库中的文件，**默认跳过**；若要强制再导入，界面提供显式开关（M1 可先不做）。

### 4.4 导入的进度与反馈（**用户 2026-09-15 明确要求**）

导入是**真正建立数据**的一步（写 `assets` / `asset_files` / 序号计数、把文件复制进库、排缩略图任务），
因此必须有**独立的导入进度弹窗**（不是角落里的一行字）：

| 要素 | 要求 |
| --- | --- |
| 形态 | **弹窗**（模态），导入进行中不可被误关；关掉＝取消（要二次确认） |
| 总体进度 | **当前阶段 / 总阶段**（扫描 → 复制 → 登记 → 缩略图），加总进度条与百分比 |
| 计数 | 已导入 / 跳过 / 失败，**实时更新** |
| 当前项 | 正在处理的文件（文件名 + 库内目标路径），让人知道「它没卡住」 |
| 错误清单 | 失败项可展开查看原因（权限、磁盘满、格式不支持…），**可导出** |
| 控制 | **暂停 / 继续 / 取消**（取消后已导入的部分**保留**，界面上要说清这一点） |
| 结束态 | 完成摘要（成功/跳过/失败）+「在库中查看这些照片」的跳转；失败项给出重试入口 |

- 单个文件失败（权限、磁盘满、格式不支持）→ 记入错误清单，**继续导入其余文件**；
- 整个导入可暂停/继续/取消；取消后已导入的部分**保留在库中**（不做事后回滚，
  因为文件已经落盘；M1-6 需在界面上说清这一点）；
- 缩略图生成是**导入之后的后台任务**，进度弹窗里只体现为最后一个阶段（不阻塞导入完成）。

> **设计稿状态**：⚠️ 画布上**还没有**这个弹窗 —— 见 `design/main.md` §7 待决项（M1-6 开工前补）。

### 4.5 导入失败与中断

- 单个文件失败（权限、磁盘满、格式不支持）→ 记入错误清单，**继续导入其余文件**；
- 整个导入可暂停/继续/取消；取消后已导入的部分**保留在库中**（不做事后回滚，
  因为文件已经落盘；M1-6 需在界面上说清这一点）。

---

## 5. 右侧库列表上的新元素（界面）

在 M1-5 的 `RepositoryList` 上追加（`design/main.md` §3.3 的扩展）：

| 元素 | 行为 |
| --- | --- |
| **在线状态徽标** | 在线：正常面；**离线**：灰化 + 离线图标（可点击 → §2.3 的重新挂载探测） |
| **小齿轮** | 打开「库设置」弹窗（M1 里**只有一项**：修改导入模版，含变量提示与实时预览） |
| 新建库 | 走 §2.4 的两个分支；若目标目录已有 catalog.db，默认是「登记已有库」而不是覆盖 |
| 库路径 | 一个库可能有多条登记路径，列表里显示**当前在线的那条**；离线时显示上次已知路径 + 离线标记 |

> 这一节的界面细节要回到 Pencil 出稿（`design/main.md`），本文件只记录行为。

---

## 6. 待你确认

1. **库唯一 ID 用哪种**（§2.5）：
   - (a) **UUID v7**（RFC 9562，时间有序，`uuid` crate 一行）—— Agent 倾向
   - (b) **忠实移植你的 Gid**（18 位 base62、含 shard、带 `toUpper` 互操作形式）——
     若你希望库 ID 与现有 Luclin 体系互通就选它（我会连带移植大数进制转换与校验，并写单测）
2. **`:CDAY` 与 `:CWEEK` 的宽度**：你说的是「4 位」，但常识是 2 位（`15`、`33`）。
   - (a) **2 位**（`08`/`15`、`01`…`53`）—— 倾向
   - (b) 就按 4 位（`0015`、`0033`）
3. **目录透传与模版的作用范围**：透传的子目录里，模版是**只作用于文件名**（推荐），
   还是**每层目录都重新套一遍模版**（会生成嵌套的日期目录）？
4. **序号回绕后**：如果回绕又撞上已存在的文件，是①继续往后找空位（`_XX` 兜底），
   还是②直接放弃该文件并报错？倾向①（用户说了"重名时加尾号解决"）。
5. **`photos/` 这个名字**要不要做成库级可配置（有人喜欢 `originals/` 之类）？
   倾向 M1 固定 `photos/`，写进 FUTURE。

---

## 7. 对现有文档/计划的影响（Agent 已处理/待处理的落点）

| 影响 | 处理 |
| --- | --- |
| `AGENTS.md` §6.4 的旧布局（`.raybend/` 子目录 + `repo.json`）与本文件冲突 | **已按本文件更新**（catalog.db 在库根、photos/ 落地、ID 记在 catalog.db 内） |
| 数据底座（M1-2）的 schema | `plans/M1-2.md` 增补：库记录按 §2.1 的「ID + 多路径 + 状态」设计；导入模版与序号计数进 catalog.db |
| M1-5（导入工作区） | 右列库卡片加「在线状态 / 齿轮 / 多路径」三件事 |
| M1-6（导入执行） | 按 §3–§4 实现：模版解析、序号、重名、位图/RAW 分流、目录透传 |
| `FUTURE.md` | 登记：库文件加密（用户要求留到 future 并写明目的）、`photos/` 可配置名、Gid 互操作（若将来要对接） |
