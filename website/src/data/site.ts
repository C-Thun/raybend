/**
 * 站点常量：站点地址与所有对外链接（**唯一来源**，组件里不要另写 URL）。
 * 仓库是 `C-Thun/raybend`，默认分支是 `master`。
 */

export const SITE_URL = 'https://raybend.cthun.com/';

export const REPO_URL = 'https://github.com/C-Thun/raybend';
export const RELEASES_URL = `${REPO_URL}/releases`;
export const ISSUES_URL = `${REPO_URL}/issues`;
/** README 里「从源码构建」一节的入口 */
export const README_URL = `${REPO_URL}#readme`;
/** 许可全文（仓库根 `LICENSE`，AGPL-3.0-only） */
export const LICENSE_URL = `${REPO_URL}/blob/master/LICENSE`;
export const LICENSE_NAME = 'AGPL-3.0-only';
