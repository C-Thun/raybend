# M2-W1 渲染可行性验证 · Windows 逐项表

> **这份表是给崔总的**（`AGENTS.md` §2.8：涉及真实 GPU、DPI、多显示器、体感的验证只能由人在真机上做）。
> **2026-09-19 修订**：先过下面的“0.1 三步复测”，再做完整清单。
> 旧版将 Windows 的显示缩放 1.25 当作 WebView DPR，遗漏 110% 文字缩放，
> 导致灰块、裁剪边界与鼠标一起错位。已填答案保留为旧版历史，不表示新版已验收。
> 数学往返为零只能证明互逆，不能证明画面对齐。
> 开发原理与截图分析见 [原生视口开发指导](../docs/native-viewport-coordinate-guide.md)。

---

## 0. 开始之前

- [X] **准备**：一台 Windows 机器（Win10/11）；至少一个可用 GPU 驱动；如果有多显示器/可改缩放，那部分也能一起验
- [X] 在 WSL 里跑一键脚本：

  ```bash
  pnpm spike:win
  ```

  它会做四件事：重建前端产物 → 构建 Windows 产物（`C:\rb-target\raybend`）→ `check:win` 核对
  → 带 `RAYBEND_SPIKE=1` 启动应用（**启动时自动开出 spike 窗口**）。
- [X] 确认 spike 窗口起来了。窗口标题是「raybend 渲染 spike（M2-W1）」，中间洞口内能看到**灰色测试图**；Fit 留白处能看到桌面/下层窗口，四周是深色面板。

> **窗口没起来 / 灰图完全看不见 / 留白处不透 / 报错**：先别往下做，截图 + 把窗口里的红字（如果有）发回来。
> 这一条本身就是 A.2 的第一个结论。

---

## 0.1 本次只先复测这三步（约一分钟）

先正常关闭旧 debug 应用，再运行 `pnpm spike:win`。Windows 的文字大小 **110% 保持不变**。

| 步骤 | 预期外观 | 如果不符合 |
| --- | --- | --- |
| 点“复位” | 完整灰色测试图居中；四角彩标可见；上下或左右留白对称 | 停止，不用继续整套表 |
| 点“1:1” | 灰图覆盖中间洞口，右侧和底部不再露出宽条；十字位于洞口中央 | 停止，不用做性能测试 |
| 准星压在白块中心，停一下，再来回滚轮 | 读数接近 3000/2000，十字心留在光标下 | 点“写报告”，交回坐标快照与截图 |

- [X] 新版三步均通过（由崔总填写）

左栏现在明确显示 **WebView DPR** 与显示器缩放：当前环境预期分别约 **1.375 / 1.25**，
两者不同是正常的。“CSS”来自真实 `innerWidth/innerHeight`；旧版 1278×817 是反算值，不能作证。
“写报告”会额外保存 `C:\rb-target\spike-report\spike-coordinates.json`，不必再手抄坐标。

**透明的范围**：灰色区域就是 GPU 画的测试图片，不应该透明。仅 Fit 留白或图片被拖开处会露桌面。
1:1 下四角标记不在视野里是正常的；如果主动拖动过，十字也可以离开洞口中心，先复位再检查。

---

## 1. 逐项检查表

每一项都按 **怎么做 → 看什么 → 填什么** 写。程序能答的已经在窗口左栏显示出来了，
你只需要把「填什么」那一列抄进报告（右栏的输入框）。

### 1.1 透明挖洞成立（无黑底、无白边）

| | |
| --- | --- |
| **怎么做** | 先点复位/Fit，再把 spike 窗口拖到资源管理器上，让图像上下的留白区盖住它 |
| **看什么** | 图像之外的留白应露出下层窗口，灰色测试图片本身仍不透明；拖动时不应闪烁 |
| **填什么** | 右栏勾「透明区是透明的」；如果不是，写清是「黑底」还是「白边」还是「闪」 |

- [] 透明成立，无闪烁
- [X] 左栏 `alpha` 那一行记的是什么 → 抄进报告
      （`Inherit` 仅表示使用平台合成语义，不等于透明已验证；截图后端实际为 Vulkan。
      各后端分别记录，以留白处的实际合成为准。）

答: 写的是`Inherit`，跨屏拖动dpi变化时有闪烁，其他正常

### 1.2 1:1 档位下的像素锐利度

| | |
| --- | --- |
| **怎么做** | 点右栏「1:1」；把鼠标移到图像正中心（那个小十字上） |
| **看什么** | 中心应当是一个**方方正正的小白块**（一个图像像素＝一个屏幕像素），不糊、不发虚 |
| **填什么** | 右栏勾「1:1 档位下白块像素级锐利」 |

- [X] 1:1 下白块是方的、锐的
- [X] 顺手看一眼：左上/右上/右下/左下四个角应当是**红 / 绿 / 蓝 / 黄**（顺序不能错，错了就是镜像或翻转）
      ⚠️ **这一条要在「适配 Fit」档看，不能在 1:1 档看** —— 1:1 的语义就是「只看图心那一片」，
      四个角按定义在画面外（人类 2026-09-18 在 1:1 下找它，那是找不到的，判据写错了，已改）。
      Fit 档下每个角落的标记约 10 CSS px（洞口放大到整个中间列之后），颜色一眼可辨。

### 1.3 坐标同步（「图会不会跟不上鼠标」）

| | |
| --- | --- |
| **怎么做** | ① 把鼠标放在图像中心那个白块上，看左栏「命中测试」；② 然后**快速**来回滚轮缩放十几下，再放回中心点 |
| **看什么** | ① 放中心时左栏应当显示 **✅ 命中图像中心**；② 快速缩放之后，鼠标下的那一块图像**不能漂走**（这就是 RapidRAW 踩过的「差 1 像素图就跟不上鼠标」） |
| **填什么** | ① 数字（图像坐标）与是否出现 ✅；② 有没有漂移 |

- [X] 中心处出现 ✅，图像坐标接近 `3000.0, 2000.0`
      **怎么做**：光标现在是**十字准星**（原来是手形，会盖住瞄准点 —— 那是个真问题，已改），
      把准星的**交点**压在图像中心那个白块上。左栏「命中测试」会实时报图像坐标，
      读数进到 `3000.0 ± 2 / 2000.0 ± 2` 时白字变粗并显示 ✅。
      把**选中的那一刻的读数**抄进报告（哪怕差几像素也照抄，那就是坐标同步的实测值）。
- [X] 快速缩放十几下后没有可见漂移
      **怎么做**：先把准星交点稳在十字心上（左栏 ✅ 出现了就说明稳住了），**手别动**，
      连续滚轮十几下。
      ⚠️ 缩放锚点是**光标**（这是产品要的行为），所以准星没压住十字时，十字会随缩放在
      「离光标更远/更近」的方向上移动 —— 那不是漂移，是锚点本来就在光标上。
      判据是：**准星压在十字心上时**，缩放前后十字一直待在准星交点下。
- [X] 抄一下左栏「洞口(物理)」那一行（新增的读数） 答: `都在截图里了`
      **怎么判**：乘的是 **WebView DPR**，不是显示器缩放。当前环境 `1.375 ≠ 1.25`。
      两行数相乘一致只是算术检查，还须与实际边界对照；不能据此排除单位错误。
- [X] 左栏「数学往返偏差」那一行是多少 → 抄进报告（应当是很小的数） 答: `都在截图里了`

### 1.4 DPI 缩放（100% / 125% / 150%）

| | |
| --- | --- |
| **怎么做** | 关掉 spike 窗口（右栏或窗口关闭键都行）→ 系统设置把缩放改成 **125%** → 重跑 `RAYBEND_SPIKE=1` 直接开窗（见下面的命令）→ 对 150% 再来一次 |
| **看什么** | 每档都要：① 窗口内容不模糊、不留黑边；② 记录 WebView DPR 与显示器缩放；保持文字 110% 时，前者为后者的约 1.1 倍；③ 再把鼠标放中心点，仍然 ✅ |
| **填什么** | 三档各填一行：dpr、是否 ✅、有没有异常 |

```bash
# 只想重开窗口、不重新构建：
RAYBEND_SPIKE=1 /mnt/c/rb-target/raybend/debug/raybend-desktop.exe
```

| 系统缩放 | dpr（左栏） | 中心 ✅ | 异常 |
| --- | --- | --- | --- |
| 100% | 未测 | | （人类实测的是 125% 与 200% 两档，比 100/125/150 更严苛） |
| 125% | **1.375**（=1.25×1.1） | ✅ 2999.9, 1999.9 | 无 |
| 150% | 未测 | | — |
| **200%** | **2.200**（=2.0×1.1） | ✅ 3000.1, 2000.3 | 无；洞口 CSS 300,57,757,752 → 物理 660,126,1666,1654 逐项吻合 |

> 两档的 `洞口(物理) = 洞口(CSS) × WebView DPR` 都成立（757×2.2 = 1665.4 ≈ 1666），
> 数学往返偏差 ≤ 4e-4 物理像素。**这一条就是本次修复的验收判据。**

### 1.5 多显示器（有几台就做，只有一台就跳过）

| | |
| --- | --- |
| **怎么做** | 把 spike 窗口从主屏拖到另一台显示器上（两台缩放比例**不同**最好，比如 100% 与 150%） |
| **看什么** | 拖过去之后：① 图像仍然正确填满洞口、不模糊不变形；② 左栏 `dpr` 与 `显示器` 跟着变；③ 没有错位/闪烁 |
| **填什么** | 右栏勾「跨显示器拖动无错位/闪烁」；写清两台屏的缩放各是多少 |

- [X] 跨屏后 dpr 跟着变，画面正确

### 1.6 窗口状态（resize / 最大化 / 全屏 / 最小化恢复）

| | |
| --- | --- |
| **怎么做** | 依次：拖边框改尺寸 → 最大化 → 还原 → 最小化 → 从任务栏恢复 → 全屏（F11 或最大化按钮看有没有）→ 还原 |
| **看什么** | 每一步之后图像都要**立刻正确**（不拉伸、不错位、不留白条）；最小化恢复后仍然正常（最小化时不该崩） |
| **填什么** | 有问题的步骤写出来；全都没问题就勾「四项都正常」 |

- [X] resize 正常
- [X] 最大化/还原正常
- [X] 最小化→恢复正常（恢复后仍然出图）
- [X] 全屏正常

### 1.7 命中测试（透明区吃不吃鼠标）

| | |
| --- | --- |
| **怎么做** | 在洞口（透明区）里：按住左键拖动 → 滚轮缩放 → 点击；再在洞口**外面**的面板上点几下按钮 |
| **看什么** | 洞口的拖动/滚轮都要**有效**（这是产品架构：指针事件由界面接住再转成意图）；面板按钮也要正常响应 |
| **填什么** | 有没有哪一处「点了没反应」或者「误触发」 |

- [X] 洞口拖动有效
- [X] 洞口滚轮缩放有效
- [X] 面板按钮正常

### 1.8 后端回退（三个后端各跑一次）

| | |
| --- | --- |
| **怎么做** | 依次跑下面三条命令（每条都会重建+开窗；只改后端时要重建 exe 吗？不用，`RAYBEND_SPIKE=1` 直接开窗即可，但改了环境变量要重开进程） |

```bash
# 关掉应用，然后分别跑：
WGPU_BACKEND=dx12   RAYBEND_SPIKE=1 /mnt/c/rb-target/raybend/debug/raybend-desktop.exe
WGPU_BACKEND=vulkan RAYBEND_SPIKE=1 /mnt/c/rb-target/raybend/debug/raybend-desktop.exe
WGPU_BACKEND=gl     RAYBEND_SPIKE=1 /mnt/c/rb-target/raybend/debug/raybend-desktop.exe
```

| | |
| --- | --- |
| **看什么** | 左栏「后端」那一行分别是 `Dx12` / `Vulkan` / `Gl`；三者都要能出图（`gl` 有可能起不来——**那也是结论**） |
| **填什么** | 三个后端各填：起来了没有、后端名、有没有异常 |

| 后端 | 起来了吗 | 左栏显示 | 备注 |
| --- | --- | --- | --- |
| dx12 | | | |
| vulkan | | | |
| gl | | | |

答: `试了类似 WGPU_BACKEND=dx12 pnpm spike:win 这种方法启动，上面提供的启动方法接收不到信息根本不启动gpu调试界面，但是即使用这种方法启动gpu调试界面，也全部是Vulkan后端，并不能切到其他后端`

> **2026-09-19 定位：这是脚本的 bug，不是 wgpu 的问题。**
> WSL→Windows **只转发 `WSLENV` 里列出的变量**，而 `scripts/spike-win.mjs` 的**启动步**没把
> `WGPU_BACKEND` 并进去 → 变量被互操作层丢掉 → 报告里如实写着
> 「`WGPU_BACKEND`：(未设置 → 走默认优先级)」，于是永远走 Vulkan。
> **已修**（脚本启动步并入 `WSLENV`）：现在 `WGPU_BACKEND=dx12 pnpm spike:win` 是真 dx12。
> 下次真机顺手把 dx12 / vulkan / gl 三条各跑一次即可（`gl` 起不来也算结论）。
> 注：`RAYBEND_SPIKE=1` 一直好用，是因为脚本同时传了 `--spike=1` 这个**参数**，参数不受互操作限制。

### 1.9 设备丢失与恢复

| | |
| --- | --- |
| **怎么做** | 点右栏「演练丢失」→ 看画面（应当停住/报错）→ 点「恢复」 |
| **看什么** | 恢复之后图像**重新画出来**了；右栏「设备丢失」列表里有「恢复成功」字样 |
| **填什么** | 恢复成没成；不成的话把列表里那几行发回来 |

- [ ] 恢复成功，图重新出现

答: `点击演练丢失和恢复时顶部均报错：创建 surface 失败: surface 校难失败（看wgpu的报错日志）`


### 1.10 性能基线（6000×4000 纹理 + 平移缩放）

| | |
| --- | --- |
| **怎么做** | ① 点「脚本平移 3s」；② 点「脚本缩放 3s」；③ 点「开始采集（手操）」，然后自己拖 + 滚轮折腾 5 秒，点「结束采集」 |
| **看什么** | 右栏表格里每个场景的 p50 / p95 / fps / CPU 两列 |
| **填什么** | 四个场景的 p50、p95、fps、CPU p50 抄进报告（表格会自动进报告，这里只是让你心里有数） |

- [ ] 平移 p50 ≈ 16.7ms（60Hz 上锁 vsync，正常）
- [ ] 缩放 p50 同上
- [ ] 手操时**跟手**（主观判断，勾右栏的「主观帧率」）
- [ ] CPU 那一列有没有接近帧间隔的（接近就说明是 CPU 瓶颈而不是等 vsync）

答: `见截图`

数字（Intel Arc 集显 · Vulkan · Fifo）：

| 场景 | 样本 | p50 | p95 | fps | CPU p50 |
| --- | --- | --- | --- | --- | --- |
| scripted-pan | 99 | 30.89 ms | 31.78 | 31.7 | **0.41 ms** |
| scripted-zoom | 98 | 30.94 ms | 31.89 | 31.5 | **0.42 ms** |
| manual | 676 | **16.42 ms** | 30.59 | **67.3** | 8.94 ms |

> **那两个 31 fps 是 harness 的假象，不是渲染极限**：脚本模式每 16 ms 醒一次
> （`spike_viewport.rs` 的 `Duration::from_millis(16)`），与 16.67 ms 的 vsync 形成拍频，
> 呈现间隔稳定落在 2×16.45 = 30.9 ms。手操那条没有定时器，p50 直接 16.42 ms。
> **CPU 0.41 ms/帧** 说明完全不是 CPU 瓶颈；手操那条 8.94 ms 主要来自改窗口时的 surface 重配。
> 结论：6000×4000 纹理的平移/缩放对这条链路是轻负载。

### 1.11 网格帧率与内存（M0-6 遗留项，顺手一起测）

> M0-6 当时只在 WSL 上量了算法吞吐，**帧率与内存要在 Windows/WebView2 上以真机数字为准**。

| | |
| --- | --- |
| **怎么做** | 主窗口里切到「浏览」工作流 → 打开一个有照片的库 → 快速上下滚动 5 秒；按 **F12** 打开 DevTools（debug 版可用）→ Performance 录一段滚动 |
| **看什么** | 平均 FPS、有没有长帧；Memory 面板看 JS heap 峰值 |
| **填什么** | 平均 FPS、JS heap 峰值（MB）、是否跟手 |

| 项 | 数字 |
| --- | --- |
| 平均 FPS | |
| JS heap 峰值 | MB |
| 跟手吗 | |

答: `目前库内照片不多没测，但是我在import里试了感觉挺流畅的`

> 记为：**无正式数字，主观流畅**。要真数字需要先有一个几百上千张的真实库（`PLAN.md` M0-6 行仍挂这条）。

---

## 2. 填报告并交回

- [X] 在 spike 窗口**右栏底部**勾那三项、填「主观帧率」与备注
- [X] 点「**写报告**」。报告落在 `C:\rb-target\spike-report\`

  ```bash
  # 在 WSL 里看：
  cat /mnt/c/rb-target/spike-report/spike-report.md
  ```

- [X] 把 `spike-report.md` 的内容发回来（或者直接说「报告已生成，在 xxx」）
- [X] 另外用一句话回答：**本次三步复测及后续已测项通过了吗？（整套架构结论由实测和工程限制共同判断）**

答: 报告在`/mnt/c/rb-target/`下面；我认为基本通过，你再复核一下，有些细节，只要不是严重错位什么的，可以往前推进，注意现在已经明确这个只用在editor，所以M2严格来说是不涉及这块功能的。

> **Agent 侧复核（2026-09-19）**：✅ 同意「基本通过」——透明挖洞、坐标契约、性能三项都成立，
> 两档 DPI 的洞口换算逐项吻合、命中中心 3000±1。三处遗留都**不是架构问题**：
> ① 设备恢复（真因＝ `recover()` 跨设备复用 layout → panic 打死渲染线程；**主因已修**，
> 诊断也改成了「出图即撤横幅 + 错误带帧号」，等下次真机复跑确认）；
> ② 后端回退三选一（脚本 `WSLENV` bug 已修，可直接重跑）；
> ③ 跨屏过渡闪烁（已知，产品化按 geometry revision 收）。
> 全程记录：`implementations/2026-09-19_windows-spike-verified-and-device-loss.md`；
> 坐标契约摘要进 `AGENTS.md` §7.9，完整指南 `docs/native-viewport-coordinate-guide.md`。

---

## 3. 我会拿这些数字做什么

收到报告后，Agent 侧会：

1. 把数字写进 `implementations/` 与 `PLAN.md` 的 M0 表格（M0-2 / M0-6 两行）；
2. 任何一项不过，就按 `AGENTS.md` §2.11 判「能绕过」还是「绕不过去」：
   能绕过 → 记进 `ASSISTANCE.md` 的「绕过去了的问题」并继续；
   绕不过去 → 升级为阻塞项，并评估退路（`FUTURE.md` A3 的 Tauri CEF 运行时 / 独立子窗口）；
3. 结论无论好坏都写下来 —— **这次 spike 的目的就是尽早知道这条路通不通**。

---

## 附：本表用到的命令速查

```bash
pnpm spike:win                       # 全套：构建 → 核对 → 开窗
RAYBEND_SPIKE=1 /mnt/c/rb-target/raybend/debug/raybend-desktop.exe   # 只开窗
WGPU_BACKEND=vulkan RAYBEND_SPIKE=1 …                               # 换后端
pnpm check:win                       # 只核对产物是否过期
cat /mnt/c/rb-target/spike-report/spike-report.md                   # 看报告
```
