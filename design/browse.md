# design/browse.pen 说明书

> 与 `design/browse.pen` 同名配对（`AGENTS.md` §5.1）。
> **规格**在 `BROWSE.md`（交互与业务），**视觉规则**在 `DESIGN.md`；本文件只记录**这张画布的内容与表达不出来的东西**。
>
> 建立时间：2026-09-15　状态：**七帧已出，待用户审核**

---

## 1. 本画布承载的内容

| Frame 名 | 节点 id | 主题 | 内容 |
| --- | --- | --- | --- |
| `Shell / Browse / Tiles` | `o2OJy2` | 深色·紧凑（**主稿，其余从它派生**） | 三列：库目录选择器 / tiles 网格 / 信息栏 |
| `Shell / Browse / View` | `PRpZa` | 深色·紧凑 | 看图：照片区 + 胶片带 + 右栏（预览含视口框 + 直方图）+ 看图专用底部状态 |
| `Shell / Browse / Compare` | `Idwro` | 深色·紧凑 | 对比：两图并排（第一幅带基准描边）+ 胶片带**只显示对比图**（主色细边框） |
| `Shell / Browse / Libs Expanded` | `c045KW` | 深色·紧凑 | 库列表展开态：吃掉目录树区域、可滚动显示全部库 |
| `States / 浏览标记与选择` | `ZEqxG` | 深色·紧凑 | 标记三态（无选中 / 单选 / 多选不一致）、标签与两级锁、选择逻辑七态 |
| `Components / 浮层` | `F9YGT` | 深色·紧凑 | 右上角两条 toast（成功=主色边框 / 警告=danger 边框）+ 中央模态（创建子目录，含重名报错） |

> **派生纪律**：`View` / `Compare` / `Libs Expanded` 都是从主稿**复制**后改的（`View` 从 `Tiles`，`Compare` 从 `View`），
> 三行外壳与左列、右列因此与主稿**逐像素一致**。改主稿后这三个帧要同步复检。

---

## 2. 关键设计点（界面表达不出来的）

### 2.1 三行外壳

与 `design/main.md` §2 一致（titlebar 40 / flowbar 52 / toolsbar 40，紧凑档）。
`toolsbar` 在**浏览模式**下装配（`BROWSE.md` §3）：

```text
[筛选 可按下开关]  [旗标][移除所有旗标][★3/5][●色标×6][喜欢]  |  [标签]  [锁]
```

- **筛选**开关：形态与「吸附」一致（关=平的，开=主色底）；打开后它**后面所有控件**变成筛选语义；
- **标星**：示例是 3 星（前 3 颗主色、后 2 颗 `$fg-3`）；
- **标色**：5 个实心色点 + 1 个**空心圈**（无色）；点亮的那颗内嵌 `check`；
- 组与组之间只用间距（20px）分隔，**不加竖线** —— 平面布局、线条极少是既定风格。

### 2.2 左列：库目录选择器（`BROWSE.md` §4）

- **搜索条** → **紧缩库列表** → **库内目录树**，三段纵向；
- **3.5 截断**：三个库卡片（60）+ 一条**半截条**（30），半截条里只写「查看所有库」；
- **库卡片**：图标块 / 库名 + 路径 / 数量 / **齿轮**；选中卡 = `$state-selected` 底 + 主色图标块；
- **目录树**：选中行主色底 + 主色文件夹图标；左侧 chevron 表展开态（**点名字=选中，点箭头=展开**，两个动作）；
  行尾 `⋯` 是「删除空目录 / 创建子目录」入口。

### 2.3 中列：tiles 与控制条

- tile = 图区（150×112）+ 字幕条（26）两行；tile 上可出现的标记：**左上角色标点**、**右上角锁**、字幕右侧星；
- **锚点环**：最后一幅被点中的图，在「选中底」之外再加 **1px 主色内描边**（`$brand`，inner）——
  回答「选了一堆，现在右栏看的是谁」（`BROWSE.md` §5.10）；
- **控制条与导入模式逐项对齐**（32 高）：`计数` — 弹性 — `当前目录 + 文件名` — 弹性 — `按时间`（时钟图标 + 文字，
  80×28 按下式方块）— `缩放图标`（14px）— `缩放滑块`（**96×14 外框、内部 2px 轨道**，已选段填主色）。
  当前目录写作 `库名 / 最后一级目录 · 文件名`（**不放完整路径**；文件名用更亮的 `$fg-2`）。

### 2.3.1 底面用法（2026-09-15 用户纠正，**重要**）

**背景只用「深色 / 浅深」两档来回换**（外加选中/悬停等状态色），**不要引入第三档黑**：

| 面 | 用在哪 | 不用在哪 |
| --- | --- | --- |
| `$surface-main`（深色 `#202226`） | 两侧栏、flowbar / toolsbar 条带；**照片占位区**（tile 图区 / 胶片带缩略 / 对比画幅） | — |
| ~~`$surface-track`~~ | —— | 建稿初期误把库卡片也用了 track，导致「库是黑的」；已全部改回 `$surface-bar` |
| `$surface-bar`（浅深 `#2A2D33`） | **工作区中央内容面**；面板内的插入块（搜索条 / 预览 / 直方图 / chip / 输入框） | — |
| `$surface-track`（`#17191C`） | **只做凹槽轨道**：分段控件底槽、缩放滑块轨道 | **不得**用于照片区、搜索框、缩略图、chip —— 本轮就是踩了这个 |
| `$surface-layer`（`#33373E`） | 菜单 / 弹窗 / 提示气泡 / **浮在照片上的小徽标**（如 tile 的锁） | — |

- ⚠️ 这条与现存的 `main.pen` **不一致**：那边 `PhotoThumb` 用的是 `$surface-track`（导入稿 18 个 tile）。
  需要同一轮把 `main.pen` 也改成 `$surface-main`，否则两稿观感不统一。
- 相关的选择态：tile 被选中时，**整块（含图区）铺 `$state-selected`**，不只是字幕条。

### 2.4 右列：信息栏（`BROWSE.md` §6）

- **tiles 模式**：EXIF（可滚动）→ 标签 → 文件（含可编辑的作者/描述）→ 地理（5 行，均可编辑）；
- **看图模式**：**预览**（全图缩略图 + **当前视野框**，1px 主色描边）+ **直方图**（24 根柱）→ 其余同上（EXIF 让位给预览+直方图）；
- **排版纪律（用户 2026-09-15 强调）**：平面布局、线条极少 ⇒ **识别度靠排版**：
  段落间距 `20`、段内行距 `6`、段标题 `15px / $fg-2 / 600`、字段名 `12px / $fg-3`、字段值 `14px / $fg-1`。

### 2.5 看图与对比

- 看图：中列 = 照片区（上）+ **胶片带**（下，104 高，缩略 96×80）；
- **底部状态栏与 tiles 不同**：**左侧**文件名 + 紧跟其后的**锁徽标**；**右侧**该图标记（星 / 色标 / 旗标 / 喜欢）；
- 对比：两图等宽并排；**第一幅带主色内描边**（它是画幅基准，其余图按它的比例取中间区域同步位移）；
- 对比下胶片带：**只显示参与对比的图**，整条胶片带加 **1px 主色细边框**表示进入该状态（`BROWSE.md` §5.5）。

### 2.6 浮层（`BROWSE.md` §8）

- **toast**：`titlebar` 正下方、右上角；**带边框**，边框色表状态（成功 `$brand` / 警告 `$danger`），
  底 `$surface-layer`；示例带一个「撤销」按钮；**不遮挡操作**；
- **中央模态**：`$scrim` 遮罩 + `$surface-layer` 卡片（本例 380 宽），标题 17px、字段 32 高、
  错误文案用 `$danger`、右下角「取消 / 创建」。

---

## 3. 图标：画布用 lucide 替身，实现用 Tabler

**画布只支持 lucide / feather / Material Symbols(3 种) / phosphor，没有 Tabler**，而实现用 Tabler
（`AGENTS.md` §7.7）。所以画布上用 **lucide 当替身**（同为 24px/2px 描边），名字映射如下：

| 用途 | 画布（lucide / Material） | 实现（Tabler） |
| --- | --- | --- |
| 应用图标 | `aperture` | `aperture` |
| 搜索 | `search` | `search` |
| 库 | `library` | `building-library` |
| 齿轮 / 库设置 | `settings` | `settings` |
| 文件夹 / 展开 | `folder` / `chevron-down` | `folder` / `chevron-down` |
| 行尾更多 | `ellipsis` | `dots` |
| 星（点亮） | `star` + 主色 | **`star-filled`** |
| 锁 | `lock` | `lock` |
| 标签 | `tag` | `tag` |
| 喜欢 | `thumbs-up` | `thumb-up` |
| **筛选** | Material `filter_alt` | `filter` |
| **移除/排除（禁行图标）** | Material `do_not_disturb_on` | `ban`（`AGENTS.md` §11.3 的统一图标） |
| 按时间 | Material `schedule` | `clock` |
| 工作流四段 | `folder-down` / `image` / `sliders-horizontal` / `upload` | 同实现（见 `flow.ts`） |

---

## 4. 画布操作约束（本轮实测，写下来免得下次再踩）

| 约束 | 说明 |
| --- | --- |
| **变量必须先于节点创建** | 节点先建、变量后建 ⇒ `$...` 引用被**静默烤成 `#000000`**（整屏全黑）。补救＝变量建好后再 `Update(id, {fill:"$..."})` 重挂 |
| **`Insert` 的新子节点会被布局引擎摆位** | 但它需要一次重算：**同一调用内读到的 `bounds` / `problems` 是陈旧的**，判断要以**截图**为准 |
| **flexbox 容器里 `x` / `y` 被忽略** | 位置只能靠顺序（`Move(id, parent, index)`）、间距、填充来控制 |
| **优先 `Copy` 现成节点** | 本轮 `View` / `Compare` / `Libs Expanded` 与库卡片都是复制后改的，比从零 `Insert` 更快也更不容易坏 |
| **画布没有 Tabler** | 见 §3 |
| **主题** | `theme: {mode:"dark", density:"compact"}` 是合法节点属性，子树继承；它只决定「解析哪个变体」，救不了被烤死的引用 |
| **`alignItems` 只有 `start/center/end`** | 没有 `stretch`（写了会直接报错并回滚整批） |
| **`filePath` 只对当前打开的文件生效** | 不能跨文件读写（`main.pen` 与 `browse.pen` 互不可见） |

---

## 5. 本稿的待决项（2026-09-17 按实现收敛）

> **状态口径**：`已决` = 实现里已经落地、以代码与 `DESIGN.md` 为准（画布若不同，改画布）；
> `未决` = 还没定，且其中多数要等 `browse.pen` 补完帧才能画（见 `plans/M2.md` 阶段 0）。

| # | 事项 | 状态 | 依据 / 现在按什么做 |
| --- | --- | --- | --- |
| 1 | 色标 5 色的正式取值 | **已决** | `DESIGN.md` §1.4：**两主题各一套**（不是同一组色复用），令牌 `--label-*`，并附对比度实测。本稿早先那组取值作废 |
| 2 | 「混合态」的视觉 | **已决** | `DESIGN.md` §12.11 第 1 条：无值＝全暗（`fg-3`）；取值一致＝显示该值；**混合＝半亮（`fg-2` + 70%）+ 一个短横**。硬要求：混合**不能**看着像无值（否则点一下会清掉一批已打标的照片） |
| 3 | `Tab` 三态的循环语义 | **未决** | 属 `browse.pen` 待补的三帧之一（阶段 0.2）。`BROWSE.md` §10 问题 3 仍挂着：②关左右 与 ③关胶片带 是并列循环还是叠加 |
| 4 | 筛选条件的与/或 | **已决（引擎）/ 未决（界面）** | 引擎两种都支持，**默认 Or**（`store/query.rs` 的 `Combinator`，M2-W1 阶段 3）。界面上的切换控件留到 W2；画布补的「筛选开启结果区」那一帧按 Or 画 |
| 5 | 浅色主题与宽松档 | **未决** | 主稿只有**深色·紧凑**一版。补不补派生帧等主稿定稿；`main.pen` 的派生做法可直接照搬 |

### 5.00 浏览三态（`States / 浏览三态（命令面板 / 筛选 / Focus）`，2026-09-17 新增，M2 阶段 0.2）

三帧都是**紧凑状态帧**（只画该状态特有的部分与它相对网格的位置关系，完整外壳见 `Shell / Browse / Tiles`；
尺寸一律以实现为准，`DESIGN.md` §8.4）：

| 帧 | 画了什么 | 依据 |
| --- | --- | --- |
| ① 命令面板 | `⌘K` 搜索行 + 5 条命令样例（其中一条指向态用辅色底）+ 键位说明 | `BROWSE.md` §8 浮层 |
| ② 筛选开启后的结果区 | 「筛选」开关按下（主色底）+ 右侧标记控件 + **条件 chips**（`3★ 以上 ×` / `红色 ×` / `已喜欢 ×` / 共 42 张）+ 结果网格条 | `BROWSE.md` §3.1；条件间**默认「或」**（引擎两种都支持，界面切换控件归 W2） |
| ③ `Tab` 关左右 Focus 态 | 简化外壳：顶部三条不变、**左右两列隐藏**、网格铺满整宽 | `BROWSE.md` §10 问题 3 —— 与「关胶片带」是**并列**还是**叠加**仍**未定**，定了再画循环顺序 |

### 5.0 tile 两态对照（`Components / Tile 两态对照`，2026-09-17 新增，M2 阶段 0.3）

**这张画框解决的是「同一张 tile 在库内 / 库外到底差什么」** —— 差异只有两处，
都是「这张照片有没有库内属性」造成的：

| | 库内 | 库外（导入正在扫的外部目录） |
| --- | --- | --- |
| 顶部标记条（星标 / 色标 / 旗标） | **有** | **整个不渲染**（不是渲染成空条） |
| 底部条右端的锁位 | **有** | **不渲染** |
| 底部信息条（文件名 + 类型） | 有 | 有 |
| 正方外框 / 照片保比例居中 / RAW 角标 | 有 | 有 |

画框里刻意把两张都设成 **RAW 且带角标**，让「上下文」成为唯一变量 ——
否则一左一右既换了上下文又换了格式，看的人分不清差异是谁带来的。

实现依据（不是设计稿拍的）：`Tile.tsx` 里顶部条与锁位都包在 `<Show when={inLibrary()}>` 里，
`inLibrary()` 由 `context` prop 决定（`"library"` / `"source"`）。

### 5.1 画布与实现的关系（**tiles 已按实现同步，2026-09-17**）

**状态：已同步**（M2 阶段 0.1 完成）。`browse.pen` 里两个网格画框的 36 个 tile 都按落地规格重画：

| 项 | 现在画布上是什么 |
| --- | --- |
| 外形 | **正方外框** 150×150，圆角 6（= `--tile-radius`），内边距 3（= `--tile-pad`） |
| 照片位 | 保比例居中，三种比例交替（3:2 / 2:3 / 16:9），自带 6px 圆角 |
| 信息条 | **覆盖在外框底边**（`inset-x-0 bottom-0`，全宽 26 高，`$scrim` 底，文字一律 `$fg-1`），左端文件名 + **右端扩展名标签** |
| RAW 角标 | 贴**照片框**右下角（不是外框），`$brand` 底 + 6px 圆角 + `$fg-on-brand` 文字 |
| 两个网格画框 | `Shell / Browse / Tiles` 与 `Shell / Browse / Libs Expanded` **都改了**（同一网格的两态，不能只改一个） |

> **信息条与 RAW 角标是互斥的**：实现里信息条只在指向/聚焦/选中时出现，而角标恰好在那三种状态下消失。
> 画布上是**静态**的、两者都画（否则没法同时交代两者的几何）—— 看画布时不要当成「会同时出现」。

**还没同步的**：`States / 浏览标记与选择` 那 7 个状态 tile 仍是旧外形（140×90 长方）。
它们的重画与补帧归 M2 阶段 0.2–0.3；画布上已加**醒目注释**说明「仅示意、已落后于实现」，
按人类 2026-09-17 给的口径，允许用注释过渡（`AGENTS.md` 的计划批注）。

**尺寸口径不变**：画布上的具体像素只作**结构**示意，实际以 `tokens.css` 与实现为准（`DESIGN.md` §8.4）。
