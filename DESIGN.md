# raybend（光伴）视觉体系规范（DESIGN.md）

> 本文件是**视觉与配色的唯一事实来源**。界面设计（Pencil `.pen`）与前端实现都必须遵循本文件。
> 设计稿纪律见 `AGENTS.md` §5.1；界面设计阶段的产出放在 `design/`。
>
> 建立时间：2026-09-15
> 状态：**基础色板已定**；具体界面的尺寸档位、密度与控件细节待美术设计阶段定案。

---

## 1. 配色体系

### 1.1 基础色板（用户指定，不可随意替换）

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| **品牌主色** | `#8DB8AA` | 界面**关键物件**：主操作、当前选中、关键强调 |
| **品牌辅色** | `#EECC55` | 界面**辅助点缀**：次级强调、特殊标记、少量点缀 |
| **浅色** | `#F6F8F4` | light 主题的**基础背景** |
| **深浅色** | `#EDF0E9` | light 主题的**第二层背景**（基于浅色，细微差异、更深一点） |
| **深色** | `#202226` | dark 主题的**基础背景** |
| **浅深色** | `#2A2D33` | dark 主题的**第二层背景**（基于深色，细微差异、更浅一点） |
| **中间色** | `#B6B0AF` | **注释性文字**与次级信息（驼色） |

> 「深浅色」与「浅深色」是由浅色/深色**按同一色相派生的相邻层级**，只做细微差异 ——
> 目的是用**纯色块拼接**区分界面区域，而不是靠描边或阴影（见 §4）。

### 1.2 色板使用次序

1. **品牌主色**用得最克制：一屏之内只出现在「当前焦点/当前选择/主操作」上。
2. **品牌辅色**是点缀，不承担主信息，也不与主色抢注意力。
3. **中间色**只用于注释性文字与次级信息，**不用于大面积色块**。
4. 除此之外的一切都是**中性面（浅/深系列）**的拼接。

---

## 2. 主题组装规则

两套主题**用同一套色板**，只是角色互换。这样切换主题不需要第二套设计。

### 2.1 dark

- 背景 = **深色 `#202226` + 浅深色 `#2A2D33`** 拼接（两级面）
- 文字基本为**浅色系**（浅底上的深色在这里就是「浅」的一侧）
- 灰字（注释、次级信息）用**中间色 `#B6B0AF`**

### 2.2 light

- 背景 = **浅色 `#F6F8F4` + 深浅色 `#EDF0E9`** 拼接（两级面）
- 文字基本为**深色系**
- 灰字（注释、次级信息）用**中间色 `#B6B0AF`**

### 2.3 两级面怎么分

- **基础面**（浅色 / 深色）：主工作区 —— 视口、照片网格、大块内容
- **第二层**（深浅色 / 浅深色）：围绕工作区的结构 —— 面板、工具条、状态栏
- 需要第三级时（如控件 hover / 输入框底），从当前主题的第二层再派生一级（同样只做细微差异），并在设计稿里注明

---

## 3. 文字颜色规则

1. **正文与标题**：按所处背景取**浅色或深色**（dark 主题用浅、light 主题用深）。
2. **注释性文字**（说明、单位、次要元数据、禁用态提示）：用**中间色 `#B6B0AF`**。
3. **与图标关联的文字**：一般**与图标同色**。例如黑底上的白图标，其文字也是白色。
4. **例外**：当图标使用品牌色、且该图标有**强关联文字**时，文字也用品牌色。

---

## 4. 界面风格规则

| 规则 | 内容 |
| --- | --- |
| **不做毛玻璃** | 不使用 `backdrop-filter` 之类的模糊/半透明材质 |
| **不做阴影** | 不用 `box-shadow` 表达层次；层次一律靠**面与面的色彩差** |
| **小倒角** | 绝大部分情况使用小圆角；大圆角只在极少数需要明确「胶囊」语义的控件上出现 |
| **无边线设计** | **面板与面板之间用纯色块区分，不上分隔线**。描边仅保留给「必须限定边界」的输入类控件与焦点环 |

**无边线设计的直接后果**：§1.1 里「深浅色 / 浅深色」的差异幅度必须**足够可辨**。
这是这两级面存在的原因 —— 它们不是装饰，而是**替代分隔线的结构手段**。
美术阶段若发现差异过小导致区域糊成一片，优先**加大这两级面的差异**，而不是改回描边。

---

## 5. 令牌映射（Tailwind v4）

主题通过 `<html data-theme="dark|light">` 切换；语义令牌引用运行时变量，一套工具类同时服务两套主题。

```css
@theme {
  /* 语义面 */
  --color-surface-base: var(--surface-base);   /* 基础面 */
  --color-surface-raised: var(--surface-raised); /* 第二层 */

  /* 文字 */
  --color-fg-1: var(--fg-1);   /* 正文/标题 */
  --color-fg-muted: var(--fg-muted); /* 注释性文字 = 中间色 */

  /* 品牌 */
  --color-brand: #8DB8AA;
  --color-brand-2: #EECC55;
}

:root[data-theme="dark"] {
  --surface-base: #202226;
  --surface-raised: #2A2D33;
  --fg-1: #F6F8F4;      /* 深底取「浅」的一侧 */
  --fg-muted: #B6B0AF;
}

:root[data-theme="light"] {
  --surface-base: #F6F8F4;
  --surface-raised: #EDF0E9;
  --fg-1: #202226;      /* 浅底取「深」的一侧 */
  --fg-muted: #B6B0AF;
}
```

**默认主题 = `dark`**（相片软件在暗房环境下的行业惯例）。
「是否跟随系统主题」由美术阶段决定，本文件暂不规定。

> 现状提示：`src/index.css` 里目前是 M0-1 阶段随手建的**临时深色令牌**（与本规格不一致），
> 美术设计阶段按本文件重建，不要把它当作参考。

---

## 6. 对比度校验与已知问题

对关键组合做过实际计算（WCAG 相对亮度公式）：

| 组合 | 对比度 | 结论 |
| --- | --- | --- |
| 中间色 `#B6B0AF` on 深色 `#202226` | **≈ 7.45 : 1** | ✅ 优秀，作注释文字完全可用 |
| 中间色 `#B6B0AF` on 浅色 `#F6F8F4` | **≈ 2.00 : 1** | ⚠️ **低于 WCAG AA**（正文需 4.5:1，大号字需 3:1） |

### 已知问题 1：中间色在 light 主题下作正文级文字偏淡

- **现象**：按 §2.2 / §3.2 的规则，light 主题的注释文字也用 `#B6B0AF`，实测对比度仅 2.0:1，阅读吃力。
- **影响**：注释、单位、次要元数据在浅底上会「发飘」，在非理想显示器/强环境光下更明显。
- **可选处理**（待美术阶段定夺，不擅自更改用户指定的色板）：
  1. **派生一个「中间色的深变体」专供 light 主题的文字使用**，保持同一色相。
     实测 `#6E6866` 在 `#F6F8F4` 上约 **5.12 : 1**，可过 AA。
  2. 或者规定：`#B6B0AF` 在 light 主题下**只用于非文字用途**（图标二级色、分隔性细节），文字改取深色降一档。
  3. 或者接受现状（把注释文字当作纯装饰级信息，不承载必要内容）。
- **状态**：⏳ 待决（已登记 `ASSISTANCE.md`）

> 深色主题无此问题，可以完全按用户指定的方案执行。

---

## 7. 待决问题（美术设计阶段处理）

| # | 问题 | 说明 |
| --- | --- | --- |
| 1 | 中间色在 light 主题下的文字用途 | 见 §6 已知问题 1 |
| 2 | 是否需要第三级面 | 控件 hover / 输入框底是否需要独立派生级，还是复用第二层 |
| 3 | 尺寸档位与密度 | 字号档、控件高度、行高、面板宽度等（本文件尚未规定） |
| 4 | 小倒角的具体数值 | 「小」是 2px 还是 4px，需要看实际渲染效果 |
| 5 | 焦点环与选中态的具体表达 | 在「无边线 + 无阴影」约束下，用品牌主色块 / 描边 / 反色，需要试 |
| 6 | 是否跟随系统主题 | 默认 dark 已定，跟随系统与否待定 |

---

## 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-15 | 初版：确定基础色板（用户指定）、双主题组装规则、文字颜色规则、界面风格四条规则、令牌映射与对比度校验 |
| 2026-09-15 | 品牌辅色由 `#775588`（紫）改为 `#EECC55`（金黄），用户指定 |
