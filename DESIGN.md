# raybend（光伴）视觉体系规范（DESIGN.md）

> 本文件是**视觉与配色的唯一事实来源**。界面设计（Pencil `.pen`）与前端实现都必须遵循本文件。
> 设计稿纪律见 `AGENTS.md` §5.1；界面设计产出放在 `design/`。
>
> 建立时间：2026-09-15 ｜ 最后修订：2026-09-15
> 状态：**基础色板 + 令牌体系 + 设计规则已定**；主/辅色待界面出图后由用户微调。

---

## 1. 配色体系

### 1.1 基础色板（用户指定，不可随意替换）

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| **品牌主色** | `#8DB8AA` | 界面**关键物件**：主操作、当前选中、关键强调 |
| **品牌辅色** | `#EECC55` | 界面**辅助点缀**：次级强调、特殊标记、少量点缀 |
| **浅色** | `#F6F8F4` | light 主题的**基础背景** |
| **深浅色** | `#EDF0E9` | light 主题的**第二层背景**（基于浅色派生，更深一点） |
| **深色** | `#202226` | dark 主题的**基础背景** |
| **浅深色** | `#2A2D33` | dark 主题的**第二层背景**（基于深色派生，更浅一点） |
| **中间色** | `#B6B0AF` | **注释性文字**与次级信息（驼色） |

> ⚠️ **主色与辅色会在界面出图后由用户按实际效果微调**。目标：**在深浅两种模式下都能清晰看到**。
> 因此所有代码与设计稿**只引用令牌名、不硬编码色值**（见 §9），改色只改一处。

### 1.2 派生规则：面的深度阶梯

用户给了两级面，但实际界面需要**四级**才能表达嵌套（例如：标题栏本身是一级面，标题栏里的凹槽开关又需要更深一级）。

派生规则只有一条：**「更深」= 沿同一色相远离中间灰**。深浅两主题方向一致（都是变深），
因为「凹槽」在物理上永远比周围暗。

| 阶梯 | 令牌 | dark | light | 典型用途 |
| --- | --- | --- | --- | --- |
| 最深 | `--surface-track` | `#17191C` | `#E1E5DB` | **凹槽轨道**（分段控件的底槽） |
| 基础 | `--surface-main` | `#202226` 深色 | `#F6F8F4` 浅色 | 主工作区条带 |
| 第二层 | `--surface-bar` | `#2A2D33` 浅深色 | `#EDF0E9` 深浅色 | 结构性条带 |
| 浮层 | `--surface-layer` | `#33373E` | `#FFFFFF` | 菜单、弹窗、提示气泡 |

> `--surface-track` 与 `--surface-layer` 是从用户色板**派生**的，改动需在本文件登记（见 §15 变更记录）。
> 两者必须同时**深于 `--surface-main` 与 `--surface-bar`**（track）或同时**浅于**（layer），
> 这样同一个令牌在两种宿主条带上都成立。

### 1.3 色板使用次序

1. **品牌主色**用得最克制：一屏之内只出现在「当前焦点 / 当前选择 / 主操作」上。
2. **品牌辅色**是点缀，不承担主信息，也不与主色抢注意力。
3. **中间色**只用于注释性文字与次级信息，**不用于大面积色块**。
4. 除此之外的一切都是**中性面**的拼接。

---

## 2. 表面分层地图（区域 → 令牌）

这是**去线设计的核心**：块与块之间不画分隔线，靠**面的色差**分组。

| 区域 | 令牌 | dark | light |
| --- | --- | --- | --- |
| **titlebar**（应用标题行） | `--surface-bar` | `#2A2D33` 浅深色 | `#EDF0E9` 深浅色 |
| **flowbar**（工作流行） | `--surface-main` | `#202226` 深色 | `#F6F8F4` 浅色 |
| **toolsbar**（工具菜单行） | `--surface-main` | `#202226` 深色 | `#F6F8F4` 浅色 |
| ┗ 其**内容居中对齐**（不靠右） | — | — | — |
| **工作区两侧栏**（如导入的 Source 列、库列） | `--surface-main` | `#202226` 深色 | `#F6F8F4` 浅色 |
| **工作区中央**（内容面，如导入的图片区） | `--surface-bar` | `#2A2D33` 浅深色 | `#EDF0E9` 深浅色 |
| **分段控件的轨道** | `--surface-track` | `#17191C` | `#E1E5DB` |
| **菜单 / 弹窗 / 提示气泡** | `--surface-layer` | `#33373E` | `#FFFFFF` |

### 2.1 为什么是这个排布

用户的判断是：**所有相邻块都做色差会让界面斑斑驳驳**。所以真正的规则是**交替，而不是处处对比**：

```text
--- 条带区 ---
titlebar    bar     ┐
flowbar     main    │  条带区：bar / main 交替一次
toolsbar    main    ┘  （flowbar 与 toolsbar 同面 → 视觉上连成一体）
─────────
工作区左列   main    ┐
工作区中央   bar     │  工作区：侧栏 main / 中央 bar
工作区右列   main    ┘  （中央被两侧"包住"，形成聚焦）
```

- **flowbar 与 toolsbar 同面** → 两者合成一个视觉整体，不再细分。
- **工作区中央用 `bar`、两侧用 `main`** → 中央自然聚焦，且左右侧栏与上方 toolsbar（同为 `main`）连成一体。

### 2.2 反例（禁止）

- 禁止在相邻块之间加分隔线（见 §6「无边线设计」）。
- 禁止为了「区分得更明显」而给每一块都换一种面 —— 会造成斑驳感。
- 禁止在同一层级内混用不同派生来源的面（例：中央用 light 的 `bar` 却用 dark 的 `main`）。

---

## 3. 主题组装规则

两套主题**共用同一套色板**，只是角色互换；**一套设计稿通过切换主题变量同时产出深浅两版**（见 §9.2）。

| | dark | light |
| --- | --- | --- |
| 基础面 | 深色 `#202226` | 浅色 `#F6F8F4` |
| 第二层 | 浅深色 `#2A2D33` | 深浅色 `#EDF0E9` |
| 正文文字 | 浅色系（`#F6F8F4`） | 深色系（`#202226`） |
| 注释文字 | 中间色 `#B6B0AF` | 中间色（见 §13 已知问题） |

**默认主题 = `dark`**（相片软件在暗房环境下的行业惯例）。

**主题切换方式 = 手动开关，不跟随系统**（用户指定，见 `design/shell.md`）。

---

## 4. 文字颜色规则

| # | 规则 |
| --- | --- |
| 1 | **正文与标题**：按所处背景取浅色或深色（dark 主题用浅、light 主题用深）。 |
| 2 | **注释性文字**（说明、单位、次要元数据、禁用态提示）：用**中间色 `#B6B0AF`**。 |
| 3 | **与图标关联的文字**：一般**与图标同色**。例如黑底上的白图标，其文字也是白色。 |
| 4 | **例外**：当图标使用品牌色、且该图标有**强关联文字**时，文字也用品牌色。 |
| 5 | **品牌色块上的文字**：统一用深色 `#202226`（主色 `#8DB8AA` 与辅色 `#EECC55` 都足够亮，深字可读）。 |

---

## 5. 状态与交互色（全局反馈规则）

**这是一条全局规则，所有元素一律遵守**（用户 2026-09-15 指定）：

```text
鼠标指向          →  辅色底
切实被点击后      →  主色底
已有主色底时指向  →  保持不变（不回落成辅色底）
```

| 状态 | 底 | dark | light |
| --- | --- | --- | --- |
| **hover（指向）** | **辅色底** | `#EECC5529`（辅色 16%） | `#EECC552E`（辅色 18%） |
| **selected / active（点击后）** | **主色底**（小控件=实色） | `#8DB8AA` | `#8DB8AA` |
| **selected（大面积：行/卡片）** | **主色底**（低透明度） | `#8DB8AA38`（22%） | `#8DB8AA40`（25%） |
| **selected + hover** | 保持主色底，不变辅色 | — | — |
| **focus 焦点环** | 品牌主色描边 **1px** | `#8DB8AA` | `#8DB8AA` |
| **disabled** | 前景降为次级色，**面不变** | `#B6B0AF` | 见 §13 |

### 5.1 两条设计意图

1. **指向与点击用不同色相**（辅色黄 vs 主色绿）。这比单纯改变深浅强得多 ——
   用户一眼能分辨「我只是划过」与「我已经选定」。
2. **「实色 vs 低透明度」按面积分档，不是两套语义**：
   - **小控件**（分段控件胶囊、磁铁方块、图标按钮）→ **主色实色底**，文字用 `fg-on-brand`
   - **大面积**（列表行、树行、卡片）→ **主色低透明度底**

   理由：§1.3 要求「主色用得克制」。一屏里若出现多处**大面积实色主色**，界面会变成绿色的。
   两者都是「主色底」，只是浓度不同。

> `.pen` 中用 `$state-hover`（辅色底）与 `$state-selected`（主色底）两个令牌表达，
> 改色只需改令牌。

### 5.2 浓度必须是配置项 + 稳定的推导算法

> **用户明确要求**：主色后面可能会改，所以浓度**不能写死**，要么进配置，要么提供
> **稳定的实时转浓度算法**。

**禁止**写出 `#8DB8AA38` 这种「主色 + 固定 alpha」的字面色值。正确做法是**把颜色与浓度拆开**：

```css
/* 浓度是独立参数，颜色由令牌提供 */
--tint-ratio-selected: 0.22;   /* 可主题化 */
--tint-ratio-hover:    0.16;

/* 叠底用 color-mix 实时合成，而不是预先算好的字面色 */
--state-selected: color-mix(in oklab, var(--brand)   calc(var(--tint-ratio-selected) * 100%), transparent);
--state-hover:    color-mix(in oklab, var(--brand-2) calc(var(--tint-ratio-hover)    * 100%), transparent);
```

这样改 `--brand` 一处，所有选中底自动跟着变，**浓度参数保持不动**。

#### 若需更严格的「感知稳定」

固定 alpha 的局限：主色从中间调换到极亮/极暗时，同样 22% 会看起来差别很大。
若需要更严的稳定性，用**以亮度为目标的反推**：

```text
目标：选中底与其宿主面（surface-main / surface-bar）的 CIELAB 明度差 ΔL* ≈ 10～14
做法：构建时用小脚本对 --brand 求相对亮度，反推 alpha 写入 --tint-ratio-selected
```

两层都留位：**先用固定 alpha + `color-mix`（零成本），需要时再上亮度反推**。

---

## 6. 界面风格规则

| 规则 | 内容 |
| --- | --- |
| **不做毛玻璃** | 不使用 `backdrop-filter` 之类的模糊/半透明材质 |
| **不做阴影** | 不用 `box-shadow` 表达层次；层次一律靠**面与面的色彩差** |
| **小倒角** | 绝大部分情况使用小圆角；大圆角只在极少数需要明确「胶囊」语义的控件上出现（如工作流切换） |
| **无边线设计** | **面板与面板之间用纯色块区分，不上分隔线**。描边仅保留给「必须限定边界」的输入类控件、焦点环，以及 `easy copy` 的悬停高亮框 |

**无边线设计的直接后果**：§1.2 的深度阶梯差异幅度必须**足够可辨**。
若美术阶段发现差异过小导致区域糊成一片，优先**加大阶梯差异**，而不是改回描边。

---

## 7. 字体

### 7.1 选型

| 用途 | 首选 | 授权 | 备选 |
| --- | --- | --- | --- |
| **拉丁文 / 数字** | **Inter** | SIL OFL 1.1（可自由嵌入分发） | Geist |
| **中日韩（CJK）** | **Noto Sans SC**（思源黑体同源） | SIL OFL 1.1（可自由嵌入分发） | — |
| **等宽（路径、EXIF 数值对齐）** | JetBrains Mono | SIL OFL 1.1 | Inter 的 tabular 数字 |

- Inter：<https://rsms.me/inter/>
- Noto Sans SC：<https://fonts.google.com/noto/specimen/Noto+Sans+SC>
- JetBrains Mono：<https://www.jetbrains.com/lp/mono/>

**为什么不用 MiSans（更现代的观感）**：MiSans 声明「全球免费商用」，但其许可协议明确禁止
**「单独将字体或其组件对外租赁、再许可、给予、出借或进一步分发字体软件」**
（<https://hyperos.mi.com/font/zh/faq/> 与协议 PDF：<https://hyperos.mi.com/font-download/MiSans字体知识产权许可协议.pdf> ）。
桌面应用**内嵌字体**是否构成「再分发字体软件」存在解释风险。
本项目对许可一贯从严 —— 因此**默认用 OFL 字体**；MiSans 作为候选登记在 `ASSISTANCE.md` 由用户决定。

### 7.2 使用规则

- **数字密集处（EXIF、尺寸、像素数、进度、计数）必须用 tabular 数字**（`font-variant-numeric: tabular-nums`）。
  等宽数字是相片软件的基本功：一排参数上下对齐才读得舒服。
- **字重只用 3 档**：Regular(400) / Medium(500) / Semibold(600)。避免 300 以下的细体（小字号发虚）。
- **字号不随密度档位变化**（用户明确要求，见 §8）。
- **中英混排**：拉丁字体在前、CJK 字体在后组成 font stack，数字与英文走 Inter，汉字走 Noto Sans SC。

---

## 8. 尺寸与密度（`紧凑` / `宽松`）

整个 UI **只有两档密度**，由标题栏右侧的横向多选组件切换。

### 8.1 硬规则

1. **只影响「元素之间的间距类维度」**：padding、gap、行高、控件高度、圆角、面板内边距。
2. **不影响文字与图标尺寸**：字号、字重、图标尺寸两档完全相同。
3. **不是整体缩放**：禁止用 `transform: scale()` 或等比例缩放实现。两档是**两套独立的间距值**，
   某些维度可以不成比例（例如行高差距比内边距差距更大），这样两档的「手感」才真的不同。
4. **足够大的差距**：两档在主要间距维度上应有 **约 1.5–2×** 的差距，切换时必须一眼可辨。

### 8.2 两档的具体值

| 维度 | 紧凑 | 宽松 | 倍率 |
| --- | --- | --- | --- |
| 基础间距单位 | `4px` | `8px` | 2× |
| 元素间 gap | `4px` | `8px` | 2× |
| 条带横向内边距 | `8px` | `16px` | 2× |
| 条带纵向内边距 | `4px` | `8px` | 2× |
| **titlebar 高度** | `32px` | `40px` | 1.25× |
| **flowbar 高度** | `44px` | `60px` | 1.36× |
| **toolsbar 高度** | `32px` | `40px` | 1.25× |
| 列表行高 | `24px` | `32px` | 1.33× |
| 按钮内边距（横×纵） | `6×4px` | `12×8px` | 2× |
| 圆形/方形小圆角 | `4px` | `6px` | 1.5× |
| 面板内边距 | `8px` | `16px` | 2× |
| 字号 | `12/13/14/16px` | **相同** | 1× |
| 图标尺寸 | `16/20/24px` | **相同** | 1× |

### 8.3 设计稿中用到的具体尺寸（两档对照，待宽松档依此实现）

| 元素 | 紧凑（当前稿） | 宽松 |
| --- | --- | --- |
| 面板宽度（左 Source / 右 库） | `320` / `300` | `360` / `340` |
| 标题行高 / Exif 字号 | `12px` | `12px`（不变） |
| 分段控件胶囊内边距 | `[0,14]` | `[0,20]` |
| 分段控件轨道内边距 | `3` | `4` |
| 分段控件胶囊高度 | `28` | `34` |
| 圆形勾选框 | `14px` | `16px` |
| 列表行起始缩进 | `10px` | `16px` |
| **移除/排除按钮点击区** | `22×22` | `26×26` |
| **移除图标** | `18px` | `20px` |
| 已选目录横条高 | `56` | `68` |
| 开关轨道 | `24×14`（圆点 `10`） | `30×18`（圆点 `14`） |
| 磁铁方块 | `28×28`（图标 `16`） | `34×34`（图标 `18`） |
| 图片 tile | `150×112` + 字幕 `22` | `150×112` + 字幕 `28` |
| 导入按钮高 | `32` | `40` |
| 库卡片高 | `56` | `68` |

> **移除类按钮的最小点击区不小于 `22×22`**（用户反馈「太小了，操作要舒服」）。
> 实现时若视觉图标想保持小，用透明的 padding 把点击区撑到该尺寸即可。
>
> 密度档位在**实现侧**用 `[data-density]` 切换（所有尺寸令牌都能切）；
> 在 **Pencil 侧**则**只能表达颜色**（`mode` 轴可靠，子树自动继承），
> **尺寸不可用变量** —— 实测 `width`/`height` 传 `"$bar-title-h"` 会被**静默丢弃**（不报错但值不生效）。
> 因此设计稿的尺寸是按**紧凑档硬写**的，「宽松」档需另出一版。详见 `design/main.md` §6.2。

---

## 9. 令牌的统一定义点（唯一事实来源）

**要求**：颜色定义除了在 Pencil 里，在**实际前端中也要有统一定义点，方便修改**。

### 9.1 三层链路（只有一个源头）

```text
DESIGN.md（本文件，人类可读的规格与色板）
      ↓ 手写一次，之后只改本文件
① 设计侧：Pencil 变量（design/*.pen 的 SetVariables）
② 实现侧：src/styles/tokens.css 的 CSS 自定义属性（:root[data-theme=...]）
      ↓ @theme 映射（不重复定义色值，只做别名）
③ Tailwind v4 语义工具类（bg-surface-bar / text-fg-1 …）
```

**唯一需要手工同步的地方是 ①↔②**（设计工具与代码之间无法自动共享变量）。
这条链路的纪律是：

- **色值只出现在本文件与 `tokens.css` 两处**，且 `tokens.css` 的注释必须指向本文件对应小节。
- **Pencil 变量名 = CSS 变量名 = Tailwind 工具名**（去掉前缀后逐字相同），不允许各起别名。
- **组件代码禁止硬编码色值**；只允许引用语义工具类。

### 9.2 令牌命名

| 令牌 | Tailwind 工具 | 说明 |
| --- | --- | --- |
| `--surface-main` | `bg-surface-main` / `text-surface-main` | 基础面 |
| `--surface-bar` | `bg-surface-bar` | 第二层条带 |
| `--surface-track` | `bg-surface-track` | 凹槽轨道 |
| `--surface-layer` | `bg-surface-layer` | 浮层 |
| `--fg-1` | `text-fg-1` | 正文/标题 |
| `--fg-2` | `text-fg-2` | 次级文字 |
| `--fg-3` | `text-fg-3` | 注释/禁用 |
| `--fg-on-brand` | `text-fg-on-brand` | 品牌色底上的文字 |
| `--brand` | `bg-brand` / `text-brand` | 品牌主色 |
| `--brand-2` | `bg-brand-2` / `text-brand-2` | 品牌辅色 |
| `--state-hover` | `bg-state-hover` | 悬停叠加 |
| `--state-active` | `bg-state-active` | 按下叠加 |
| `--state-selected` | `bg-state-selected` | 选中叠加 |

**密度令牌**用同样的思路，但挂在 `[data-density]` 上：

| 令牌 | 紧凑 | 宽松 |
| --- | --- | --- |
| `--space-unit` | 4px | 8px |
| `--bar-title-h` | 32px | 40px |
| `--bar-flow-h` | 44px | 60px |
| `--bar-tool-h` | 32px | 40px |
| … | 见 §8.2 全表 | |

### 9.3 当前实现状态

> `src/index.css` 里目前是 M0-1 阶段随手建的**临时深色令牌**（与本规格不一致）。
> 界面实现阶段按本文件重建为 `src/styles/tokens.css`，**不要**把现有的当参考。

---

## 10. 组件化策略

**规则：先定义一批最小基础组件，再在开发过程中定义大组件。**

### 10.1 第一批：最小基础组件（设计阶段就要定稿）

放在 `design/components.pen` + `design/components.md`。每个组件必须给出**全部状态**：
`默认 / hover / 激活 / 选中 / 禁用 / 空态 / 加载态`（按适用取用），以及**两档密度 × 两主题**的表现。

| # | 组件 | 说明 |
| --- | --- | --- |
| 1 | `Button` | 主要 / 次要 / 幽灵 / 危险 四种样式 |
| 2 | `IconButton` | 纯图标按钮，方/圆两种 |
| 3 | `Checkbox` | 方形勾选（树选择用） |
| 4 | `RadioCircle` | **圆形主色勾选框**（Recent / Source 列表用，见 §12.4） |
| 5 | `SegmentedControl` | 横向多选（凹槽 + 移动色块），紧凑/宽松开关与工作流切换共用 |
| 6 | `Input` | 文本框（唯一保留描边的控件之一） |
| 7 | `Tooltip` | 提示气泡，`easy copy` 的「点击复制/已复制」复用它 |
| 8 | `Menu` | 下拉菜单（标题栏菜单用） |
| 9 | `Dialog` | 模态弹窗（建库、`easy destroy` 确认用） |
| 10 | `PathText` | **`shortpath` 路径缩写**组件（见 §12.3），带长度上限参数 |
| 11 | `Tile` | 网格基本单元（图标 tile / 图片 tile 的公共骨架） |
| 12 | `TreeNode` | 树形行（展开箭头 + 图标 + 名称 + 勾选框） |
| 13 | `Panel` | 面板容器（带标题行、可折叠、可拖拽分段） |
| 14 | `SplitHandle` | **三段拖拽分隔条**（`...` 三点的细空白区，见 §12.5） |
| 15 | `ScrollBar` | 细滚动条 |
| 16 | `Badge` | 计数/状态小标签 |

### 10.2 第二批：大组件（开发过程中逐步定义）

| # | 组件 | 组成 |
| --- | --- | --- |
| A | `TitleBar` | IconButton + Menu + SegmentedControl(主题) + SegmentedControl(密度) + 窗口三键 |
| B | `FlowBar` | SegmentedControl(工作流) + `ExifStrip` + 开关组 |
| C | `ExifStrip` | 分组 + `easy copy` + `PathText` 式排版 |
| D | `ToolsBar` | 按工作流装配的按钮组（无内容时整行隐藏） |
| E | `SourcePanel` | `RecentList` + `SourceTree` + `SelectedTiles` + `SplitHandle`×2 |
| F | `RecentList` | `RadioCircle` + `PathText` + 行内删除 |
| G | `SourceTree` | `TreeNode` 递归 + 驱动器图标 |
| H | `SelectedTiles` | `Tile` 自动换行流 + 移除角标 |
| I | `PhotoGrid` | `Tile` 虚拟化流 + 底部 `GridControlBar` |
| J | `GridControlBar` | 缩放滑块 + 计数 + 排序（后续） |
| K | `RepositoryList` | 库卡片列表 + 建库按钮 + 导入按钮 + 勾选项 |

### 10.3 组件规范要求

- 每个组件在 `.pen` 里作为 **reusable frame**，实例化时只覆盖内容与状态。
- 组件在 `.md` 里必须写明：**用途 / 结构 / 全部状态 / 交互 / 快捷键 / 关联 i18n key**。
- 命名用英文 PascalCase（`SegmentedControl`），与代码里的组件名一致。

---

## 11. 国际化（语言包系统）

**要求**：加入语言包系统，支持中英界面。

### 11.1 规则

1. **界面文案零硬编码**：所有可见文字必须走 i18n key。
2. **设计稿上显示中文**（项目工作语言），但每个文案在 `design/*.md` 里**同时记录英文**。
3. 用户在设计中若用**英文精确描述**某个术语，Agent 必须**译成中文用于界面**，
   同时在 `.md` 里**原样保留英文**（因为英文常是更精确的技术表述，实现时要能对上）。
4. **中文不做机械翻译**：UI 文案要按中文习惯重写（例：`Click to copy` → `点击复制`，而不是 `点击为了复制`）。
5. key 名用英文小写点分层级，与组件层级对齐。

### 11.2 key 命名

```text
<titlebar.menu.help>            = 帮助            Help
<titlebar.menu.help.about>      = 关于            About
<titlebar.theme.toggle>         = 切换主题        Toggle theme
<titlebar.density.compact>      = 紧凑            Compact
<titlebar.density.loose>        = 宽松            Loose
<flow.import>                   = 导入            Import
<flow.browse>                   = 浏览            Browse
<flow.edit>                     = 编辑            Edit
<flow.export>                   = 导出            Export
<flow.tool.snap>                = 吸附            Snap
<exif.camera>                   = 机型            Camera
<exif.lens>                     = 镜头            Lens
<exif.focal>                    = 焦距            Focal length
<exif.aperture>                 = 光圈            Aperture
<exif.shutter>                  = 快门            Shutter speed
<exif.iso>                      = 感光度          ISO
<exif.dimensions>               = 尺寸            Dimensions
<exif.megapixels>               = 像素            Megapixels
<exif.format>                   = 格式            Format
<common.easy_copy.hint>         = 点击复制        Click to copy
<common.easy_copy.done>         = 已复制          Copied
<common.easy_destroy.confirm>   = 确定要移除吗？   Remove this?
<common.remove>                 = 移除            Remove
<source.recent>                 = 最近            Recent
<source.tree>                   = 来源            Source
<source.selected>               = 已选目录        Selected
<source.include_subdirs>        = 包含子目录      Include subdirectories
<grid.exclude>                  = 排除            Exclude
<grid.fit>                      = 适合屏幕        Fit to screen
<grid.actual_size>              = 实际大小        Actual size
<repo.title>                    = 库              Libraries
<repo.create>                   = 新建库          New library
<repo.import>                   = 导入            Import
<repo.avoid_duplicates>         = 避免重复导入    Skip duplicates
```

> 表中 `<...>` 是**示意写法**（便于在 Markdown 里展示），实际 key 不带尖括号：
> `titlebar.menu.help`。语言包文件：`src/i18n/zh-CN.ts` / `src/i18n/en-US.ts`。
>
> 本表是**主列表**，随设计推进追加；新增 key 必须同时补中英两版。

---

## 12. 通用交互范式

这些是**跨模块复用**的设计规范，不是某个界面的局部实现。

### 12.1 `easy copy`（悬停即知可复制）

**目的**：让「一组相关信息」可被一次性复制，且用户能**看出哪些信息是一组**。

| 阶段 | 表现 |
| --- | --- |
| **悬停某个组** | 该组加一圈**淡淡的细边框**（固定用**品牌主色**，透明度约 40%），同时内容上方**紧挨着**弹出 `点击复制` 标签式小窗（窄 padding） |
| **弹出动画** | 由下向上弹出（`translateY` + 淡入） |
| **点击** | 复制整组内容到剪贴板；`点击复制` 小窗**再向上弹**，紧接着下方**又弹出一个** `已复制` 提示 |
| **离开** | 边框与提示淡出 |

**分组是内容本身的结构**，不是鼠标划过的顺序。例：`相机型号+镜头` 一组、`曝光参数（焦距/光圈/快门/ISO）` 一组、
`尺寸+像素+格式` 一组。悬停到某一组时，**同组的边框同时亮起**，用户能明确感知分组边界。

**实现参考**：`/home/andares/repos/zzd/vue-admin/src/components/project2/P2CopyText.vue`
（Vue 实现：`P2Tip` 提供 hover 提示 + `flip()` 切换到 `已复制`，`navigator.clipboard.writeText` 复制；
样式需按本规范改写）。Solid 版需自行实现 `P2Tip` 的等价物（提示气泡 + 状态翻转）。

### 12.2 `easy destroy`（破坏性操作的快速通道）

**规则**：所有**移除/排除**类操作，默认弹出确认框；**按住 `Shift` 再点击可跳过确认**。

- 语义统一叫 **`移除`**（从当前集合里拿掉），**不是破坏性删除** —— 图标一律用**禁行标志**
  （**填充圆 + 中间一条横杠**），**不是叉号**。
- 「移除」「排除」与「删除到磁盘」是三个不同动作，后者第一阶段不做，
  必须在界面文案上区分清楚。三者的完整区分见 `AGENTS.md` §11.3。
- **批量排除入口在 `toolsbar`**（内容居中），不在图片 tile 上：

  | 状态 | 表现 |
  | --- | --- |
  | 有选中且选中项**未**被排除 | `批量排除`（普通态） |
  | 有选中且选中项**已**被排除 | `反排除`（**主色底**，符合 §5） |
  | 无选中 | **禁用态**（前景降到次级色） |

- **为什么把排除从 tile 移到 `toolsbar`**：排除真正有用的场景是**批量**的
  （「这一天的都不要」），而不是一张一张点。移到顶部后，操作变成
  **先选择（可按天 / 按时间片一键全选）→ 再按顶部排除**，并且天然支持**反排除**。
- **移除按钮点击区不小于 `22×22`**（§8.3）。

### 12.7 按时间分组（照片网格的第二种模式）

**入口**：`GridControlBar` 里、缩放滑块**左边**的 `按时间` 按钮 ——
可按下式（**同磁铁方块的形态**，不是左右切换的圆点开关）：

| 状态 | 表现 |
| --- | --- |
| 未按下 | **无底色**（与所在条同色），图标 + 文字用次级色 |
| 按下 | **主色实色底** + `fg-on-brand`（符合 §5 全局规则） |

**分组规则（两级）**：

```text
一级：按拍摄日期分组（本地日期）→ 一天一组
二级：在该天内按拍摄时间升序，相邻两张间隔 > 1 小时 → 断为新片
```

- **阈值 `1 小时` 必须是配置项**，不要写死在代码里。
- 拍摄时间缺失的照片：归入「未知时间」组放在最后，**不参与时间片切分**。

**UI 三层**：

| 层 | 内容 |
| --- | --- |
| **日组标题** | 日期 + 星期；右侧「**全选当天**」 |
| **片标题（小字）** | **`09:15 – 11:42`** 形式的**首尾时间范围** + 张数 +「**全选此段**」 |
| **缩略图行** | 与普通网格一致。**无排除徽标** —— 排除已移到 `toolsbar` |

- **为什么片标题用「首尾时间范围」而不是张数/时长**：用户是在回忆「我那次拍的是什么」，
  时间范围是最强的记忆线索（「上午那段」「傍晚那段」）。
- **日组标题与片标题都可点击全选**，然后配合 `toolsbar` 的批量排除使用 ——
  这正是「按天 / 按时间范围选择一批照片排除或反排除」的实现方式。
- 演示版面：`design/main.pen` 的 `Demo / 按时间分组 + 批量排除`（`omXCA`）。

### 12.8 可按下按钮（`ToggleBlock`）的统一样式

用于「按下后切换某个模式」的按钮（**吸附**、**按时间** 等），形态一致：

| | 未按下 | 已按下 |
| --- | --- | --- |
| 底 | **无底色**（与宿主面同色，即「平的」） | **主色实色底** |
| 内容 | 次级色 | `fg-on-brand` |

- **不是左右切换的圆点开关（switch）** —— 那是「包含子目录」这类属性开关的形态，
  两者不要混用。
- 「未按下时是平的」在无边线体系里天然成立，不需要边框或描边。

### 12.3 `shortpath`（路径缩写）

`Recent` 与 `已选目录` 里显示的路径不是完整路径，而是一个缩写形式：

```text
盘符 + 中间各级目录的首字母 + 最后一级目录的全名
```

- Windows 例：`D:\Photos\2024\Vacation\Japan\Kyoto` → `D:\P\2\V\J\Kyoto`
- Linux / macOS 下**第一级与最后一级写全**，中间同样取首字母。

**生成规则（两层算法）**：

1. **第一层**：只缩写中间各级目录（首字母逐级拼接），保留首尾完整。
2. **若仍超长**：**放弃第一层的结果，整体重新缩**（即首尾也参与缩写）。
   —— 这一步是必须的：如果直接在第一层结果上继续截断，当首级或末级目录本身很长时，
   已生成的 `...` 可能在某一侧「戳出来」，产生完全错误的截断。

**参数**：生成组件必须支持传入**长度上限**。超长时中间首字母部分缩成 `...` 以**保留两头**。

**完整路径**通过 `Tooltip` 在悬停时给出。

### 12.4 选择状态的两套表达（不要混用）

| 表达 | 含义 | 组件 | 位置 |
| --- | --- | --- | --- |
| **圆形主色勾选框** | 「已加入待导入」——加入下方的已选目录区 | `RadioCircle` | 列表条目**左侧** |
| **整行主色底**（`$state-selected`） | 「当前正在浏览」——中间图片区显示的就是它 | 行背景 | 整行 |

- **勾选**：可多选（多个目录都加入待处理）。
- **选中**：全应用**同一时间只有一个**目录被「选中」（因为中间图片区只能展示一个目录的内容）。
- 两套状态**互相独立**：一行可以同时是「已勾选」与「未选中」，也可以同时是两者。

#### 12.4.1 选中状态的跨面板同步（以及一个不要做的行为）

**要做的**：`最近` 与 `来源树` 的**选中状态是同一个状态**。

- 例：当前选中了 `D:\Photos` —— 若「最近」里也有这条目录，它**必须也显示为选中**。
- 反之亦然：「最近」里点选一条目录，若该目录在树里可见，树里那条也同步为选中。
- 「最近」中**其他不相关的条目不会**有任何选中态 —— 不要让人误以为多个目录同时被选中。

**不要做的（容易不自觉地实现错）**：

> ❌ **不要因为某目录被选中就强制展开树到那一层。**

以前很多软件会这样做：在 A 处选中目录，B 处的树结构即使没展开到它，也会**自动展开**去展示选中态。
**本项目不要这个行为。** 理由：展开与否**只跟用户的展开操作相关**，否则会出现「我只是在最近里点了一下，树却自己跳开了一大堆层级」的干扰。

**实现含义（很重要，它简化了实现）**：

```text
树的展开状态 = 纯用户操作状态，不受选中状态影响
选中背景色 = 渲染每一行时，判断「该行目录 == 全局选中目录」再决定
```

也就是说：**树的某一层没被展开时，那些行根本不存在于视图中，自然也不会显示选中态；
当用户展开到那一层时，渲染时才判定并如实上色**。不需要任何「为了显示选中而展开」的逻辑。

### 12.5 三段式面板与拖拽分隔

左侧 Source 面板分为上中下三段，**比例可由用户拖动调整**。

- 分隔条是**很细的空白区域**，形态是一排 `...` 三点式（用户指定）。
- 位置常在**上接内容区、下接块标题行**之间。
- 拖拽时给出 `row-resize` 光标与 hover 高亮（用 §5 的 hover 色）。

### 12.6 Tile 流换行布局（数学规划）

**规则：除最后一行外，每行列数相同；不做居中、不做拉伸填空，剩余空间作为右边距。**

给定：容器内容宽 `W`、tile 宽 `cellW`、列间距 `gap`（由密度档位决定）

```text
N = max(1, floor((W + gap) / (cellW + gap)))     // 每行列数（除最后一行）
L = W − (N·cellW + (N−1)·gap)                    // 右侧剩余空间
```

- **行内对齐 = 左对齐**（`justify-content: flex-start`）。
- **右边距 = `L`**（剩余空间全部留白，不分配给 tile 宽度，也不居中）。
- **tile 尺寸固定**（由缩放档位决定，不随容器宽度变化）—— 这正是「除最后一行外每行列数相同」的前提。
- `cellW` 由底部控制条的缩放滑块决定；`gap` 由密度档位决定。

> **为什么不允许拉伸**：拉抻填充会让 tile 尺寸随窗口宽度连续变化，破坏「网格是均质的照片阵列」这一认知，
> 也让用户在缩放窗口时不断看到照片变大变小。留白则稳定、且让视线有喘息处。

---

## 13. 对比度校验与已知问题

对关键组合做过实际计算（WCAG 相对亮度公式）：

| 组合 | 对比度 | 结论 |
| --- | --- | --- |
| 中间色 `#B6B0AF` on 深色 `#202226` | **≈ 7.45 : 1** | ✅ 优秀，作注释文字完全可用 |
| 中间色 `#B6B0AF` on 浅色 `#F6F8F4` | **≈ 2.00 : 1** | ⚠️ **低于 WCAG AA**（正文需 4.5:1，大号字需 3:1） |
| 主色 `#8DB8AA` 上的深色文字 `#202226` | **≈ 7.9 : 1** | ✅ 可用 |
| 辅色 `#EECC55` 上的深色文字 `#202226` | **≈ 11.3 : 1** | ✅ 优秀 |

### 已知问题 1：中间色在 light 主题下作正文级文字偏淡

- **可选处理**（待定夺，不擅自更改用户指定的色板）：
  1. 派生「中间色的深变体」专供 light 主题文字，实测 `#6E6866` 在 `#F6F8F4` 上约 **5.12 : 1**，可过 AA；
  2. `#B6B0AF` 在 light 主题下**只用于非文字用途**；
  3. 接受现状。
- **状态**：⏳ 待决（`ASSISTANCE.md` A4）

---

## 14. 待决问题（美术设计阶段处理）

| # | 问题 | 说明 | 状态 |
| --- | --- | --- | --- |
| 1 | 中间色在 light 主题下的文字用途 | 见 §13 | ⏳ A4 |
| 2 | ~~是否需要第三/四级面~~ | **已解决**：需要四级，见 §1.2 | ✅ |
| 3 | ~~尺寸档位与密度~~ | **已解决**：见 §8 | ✅ |
| 4 | 小倒角的具体数值 | 暂定 4px（紧凑）/ 6px（宽松），看渲染效果 | ⏳ |
| 5 | 焦点环与选中态的具体表达 | 暂定见 §5，需出图验证 | ⏳ |
| 6 | ~~是否跟随系统主题~~ | **已解决**：不跟随，手动开关 | ✅ |
| 7 | 字体最终选型 | 默认 Inter + Noto Sans SC；MiSans 待用户定 | ⏳ A6 |
| 8 | 主/辅色的最终色值 | 出图后由用户按效果微调 | ⏳ |
| 9 | 选中态用低透明度底还是纯色块 | 见 §5 说明 | ⏳ |

---

## 15. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-15 | 初版：基础色板（用户指定）、双主题组装规则、文字颜色规则、界面风格四条规则、令牌映射与对比度校验 |
| 2026-09-15 | 品牌辅色由 `#775588`（紫）改为 `#EECC55`（金黄），用户指定 |
| 2026-09-15 | **大扩充**：新增派生深度阶梯（四级面）、表面分层地图、状态与交互色、字体、双档密度与具体值、令牌三层链路与命名、组件化策略（16 个基础组件 + 11 个大组件）、i18n 语言包与 key 主列表、通用交互范式（easy copy / easy destroy / shortpath / 双选择表达 / 三段拖拽 / Tile 流换行算法） |
| 2026-09-15 | 修正 §8.2：Pencil 侧尺寸**不可用变量**（实测被静默丢弃），主题轴只能表达颜色；密度档设计稿需按硬写尺寸另出 |
| 2026-09-15 | **全局反馈规则定案**：指向=辅色底、点击=主色底（§5 重写）；新增 §8.3 两档尺寸对照与「移除按钮点击区 ≥22×22」；新增 §12.4.1 选中状态跨面板同步 + **明确禁止「为显示选中而强制展开树」** |
| 2026-09-15 | 新增 §5.2 **浓度必须是配置项 + `color-mix` 实时合成 + （可选）亮度反推算法**；§12.2 排除改到 `toolsbar` 居中批量操作；新增 §12.7 **按时间分组与时间片**（阈值 1 小时可配）；新增 §12.8 **`ToggleBlock` 统一样式** |
