# raybend（光伴）视觉体系规范（DESIGN.md）

> 本文件是**视觉与配色的唯一事实来源**。界面设计（Pencil `.pen`）与前端实现都必须遵循本文件。
> 设计稿纪律见 `AGENTS.md` §5.1；界面设计产出放在 `design/`。
>
> 建立时间：2026-09-15 ｜ 最后修订：2026-09-15
> 状态：**基础色板 + 令牌体系 + 设计规则已定**；主/辅色待界面出图后由用户微调。

---

## 1. 配色体系

### 1.1 基础色板（用户指定，不可随意替换）

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| **品牌主色** | `#52C6AB` | 界面**关键物件**：主操作、当前选中、关键强调（2026-09-15 用户微调） |
| **品牌辅色** | `#F0B033` | 界面**辅助点缀**：次级强调、特殊标记、少量点缀（2026-09-15 用户微调） |
| **浅色** | `#F6F8F4` | light 主题的**基础背景** |
| **深浅色** | `#EDF0E9` | light 主题的**第二层背景**（基于浅色派生，更深一点） |
| **深色** | `#202226` | dark 主题的**基础背景** |
| **浅深色** | `#2A2D33` | dark 主题的**第二层背景**（基于深色派生，更浅一点） |
| **中间色** | `#B6B0AF` | **注释性文字**与次级信息（驼色） |

> ⚠️ **主色与辅色会在界面出图后由用户按实际效果微调**。目标：**在深浅两种模式下都能清晰看到**。
> 因此所有代码与设计稿**只引用令牌名、不硬编码色值**（见 §9），改色只改一处。

### 1.2 派生规则：面的深度阶梯

用户给了两级面，但实际界面需要**四级**才能表达嵌套（例如：标题栏本身是一级面，标题栏里的凹槽开关又需要更深一级）。

派生规则只有一条：**「更深」= 沿同一色相远离中间灰**。深浅两主题方向一致（都是变深），
因为「凹槽」在物理上永远比周围暗。

| 阶梯 | 令牌 | dark | light | 典型用途 |
| --- | --- | --- | --- | --- |
| 最深 | `--surface-track` | `#17191C` | `#E1E5DB` | **凹槽轨道**（分段控件的底槽） |
| 基础 | `--surface-main` | `#202226` 深色 | `#F6F8F4` 浅色 | 主工作区条带 |
| 第二层 | `--surface-bar` | `#2A2D33` 浅深色 | `#EDF0E9` 深浅色 | 结构性条带 |
| 浮层 | `--surface-layer` | `#33373E` | `#FFFFFF` | 菜单、弹窗、提示气泡 |

> **背景只用两档（用户 2026-09-15 明确）**：`--surface-main`（深色 / 浅色）做**底**，
> `--surface-bar`（浅深色 / 深浅色）做**块与条带** —— 深色主题下配浅色字，浅色主题下配深色字。
> **除此以外不要再引入第三档「背景黑/背景白」**：`--surface-track` 只做**凹槽轨道**
> （分段控件底槽、滑块轨道），`--surface-layer` 只做**浮层**（菜单 / 弹窗 / 气泡 /
> 浮在照片上的小徽标）。照片占位区用 `--surface-main`（不是 track）。
>
> ⚠️ **踩过的坑**：Pencil 画布里若**先建节点、后建变量**，`$surface-*` 引用会被**静默烤成
> 字面 `#000000`** —— 看起来就像「设计里有一大块背景黑」。补救＝变量建好后再 `Update(id,{fill:"$..."})`
> 重挂；**新画布固定顺序：先 `SetVariables` → 再画节点**（`design/browse.md` §4）。
>
> `--surface-track` 与 `--surface-layer` 是从用户色板**派生**的，改动需在本文件登记（见 §16 变更记录）。
> 两者必须同时**深于 `--surface-main` 与 `--surface-bar`**（track）或同时**浅于**（layer），
> 这样同一个令牌在两种宿主条带上都成立。

### 1.3 色板使用次序

1. **品牌主色**用得最克制：一屏之内只出现在「当前焦点 / 当前选择 / 主操作」上。
2. **品牌辅色**是点缀，不承担主信息，也不与主色抢注意力。
3. **中间色**只用于注释性文字与次级信息，**不用于大面积色块**。
4. 除此之外的一切都是**中性面**的拼接。

---

## 2. 表面分层地图（区域 → 令牌）

这是**去线设计的核心**：块与块之间不画分隔线，靠**面的色差**分组。

| 区域 | 令牌 | dark | light |
| --- | --- | --- | --- |
| **titlebar**（应用标题行） | `--surface-bar` | `#2A2D33` 浅深色 | `#EDF0E9` 深浅色 |
| **flowbar**（工作流行） | `--surface-main` | `#202226` 深色 | `#F6F8F4` 浅色 |
| **toolsbar**（工具菜单行） | `--surface-main` | `#202226` 深色 | `#F6F8F4` 浅色 |
| ┗ 其**内容居中对齐**（不靠右） | — | — | — |
| **工作区两侧栏**（如导入的 Source 列、库列） | `--surface-main` | `#202226` 深色 | `#F6F8F4` 浅色 |
| **工作区中央**（内容面，如导入的图片区） | `--surface-bar` | `#2A2D33` 浅深色 | `#EDF0E9` 深浅色 |
| **分段控件的轨道** | `--surface-track` | `#17191C` | `#E1E5DB` |
| **菜单 / 弹窗 / 提示气泡** | `--surface-layer` | `#33373E` | `#FFFFFF` |

### 2.1 为什么是这个排布

用户的判断是：**所有相邻块都做色差会让界面斑斑驳驳**。所以真正的规则是**交替，而不是处处对比**：

```text
--- 条带区 ---
titlebar    bar     ┐
flowbar     main    │  条带区：bar / main 交替一次
toolsbar    main    ┘  （flowbar 与 toolsbar 同面 → 视觉上连成一体）
─────────
工作区左列   main    ┐
工作区中央   bar     │  工作区：侧栏 main / 中央 bar
工作区右列   main    ┘  （中央被两侧"包住"，形成聚焦）
```

- **flowbar 与 toolsbar 同面** → 两者合成一个视觉整体，不再细分。
- **工作区中央用 `bar`、两侧用 `main`** → 中央自然聚焦，且左右侧栏与上方 toolsbar（同为 `main`）连成一体。

### 2.2 反例（禁止）

- 禁止在相邻块之间加分隔线（见 §6「无边线设计」）。
- 禁止为了「区分得更明显」而给每一块都换一种面 —— 会造成斑驳感。
- 禁止在同一层级内混用不同派生来源的面（例：中央用 light 的 `bar` 却用 dark 的 `main`）。

---

### 1.4 色标（color label）—— **一套六色，两主题共用**（人类 2026-09-19 定）

| 色标 | 取值 | 对深色条带 `#2A2D33` | 对浅色条带 `#EDF0E9` | 与主色 `#52C6AB` 的 OKLab 距离 |
| --- | --- | --- | --- | --- |
| 红 | `#E86156` | 4.12 ✅ | 2.91 ⚠️ | 28.7 |
| 黄 | `#C9A90C` | 6.02 ✅ | 1.99 ❌ | 17.2 |
| 绿 | `#6DB453` | 5.46 ✅ | 2.20 ❌ | 10.5 |
| **青** | `#009EB3` | 4.30 ✅ | 2.79 ⚠️ | **13.1** |
| 蓝 | `#5283E0` | 3.73 ✅ | 3.21 ✅ | 22.6 |
| 紫 | `#AD67DC` | 3.78 ✅ | 3.17 ✅ | 29.3 |
| 无色 | 1px `--line-2` 描边的空心圈 | —— | —— | —— |

令牌：`--label-red / -yellow / -green / -cyan / -blue / -purple`（`tokens.css`，**只在 `:root` 里定义一次**）。

**为什么改成「一套」**（人类 2026-09-19）：原先两主题各一套 5 色，共 10 个值要维护，
而且两套的色相并不对齐（蓝 247° vs 247°，但绿 145° vs 144° 亮度差 0.11）——
同一个色标在两侧切换时「像换了个颜色」。统一成一套之后，主题切换只影响底与前景，色标本身稳定。

**六色是怎么定的**（都有计算依据，脚本口径：OKLab 空间 + WCAG 对比度）：

1. **加青**：插在绿与蓝之间（H 211°）；
2. **给青让位**：绿从 H 145° 挪到 **138°**（偏黄绿），蓝从 H 247° 挪到 **262°**（偏靛蓝）——
   青与左右邻居的距离因此分别有 **17.0 / 12.0**（其余相邻对 11.7 ~ 22.0）；
3. **青不与主色撞车**：主色是 L .75 / H 175° 的薄荷绿；青取 **L .64 / H 211°**，
   两者 OKLab 距离 **13.1** —— 是六个里离主色第二近的（最近的是绿 10.5，见下条）；
4. **一套两主题**：亮度只能取中段（L .62 ~ .74）。太亮在浅底上看不见、太暗在深底上看不见 ——
   单一套色能同时照顾两侧的可行带就这么宽，实测如表。

> **已知取舍**：黄在浅色条带上只有 **1.99:1**、绿 **2.20:1**（WCAG 对非文字图形要 3:1）。
> 这是「一套色 + 黄绿天生亮」的必然结果 —— 要压到 3:1 就得把它们压成褐/墨绿，
> 那在深色主题里会脏得认不出是黄绿。**按现状接受**，并且：
> **色标只靠色块本身表达，一律不加描边**（人类 2026-09-19：本界面是**无边线设计**）。
> ⚠️ 本文件曾要求「浅色主题必须加 1px 深描边」——**该条已作废**，不要再按它实现。

**低浓底纹**（tile 外框那层淡色）——「是不是由正确的算法生成」的正式答案：

- 它**不是**一组写死的「变淡色」，而是**实时合成**：
  `--label-*-tint: color-mix(in oklab, var(--label-*) var(--label-tint-mix), transparent)`
  ——`transparent` 参与 OKLab 混合时按**预乘**处理，结果就是「该色 X% 不透明度」；
- 浓度 `--label-tint-mix` **按主题各一档：深色 16% / 浅色 20%**。
  理由是可算的：同一个 α 在深底与浅底上离面色的距离差很多（浅底偏淡、深底偏浓），
  这两档实测让两侧的「与面色差」都落进 **ΔE 5.1 ~ 7.7** —— 看得出来，又不抢照片；
- 合成结果（供肉眼核对）：深色 `红 #47373A / 黄 #404037 / 绿 #35413A / 青 #2E3E46 / 蓝 #313A4C / 紫 #3D374B`；
  浅色 `红 #F1D5CB / 黄 #E6E2C6 / 绿 #D4E4CC / 青 #C9E0DE / 蓝 #CDDBE9 / 紫 #E0D5E8`。

**`toolsbar` 色标控件的选中态 = 双层环**（人类 2026-09-19）：

```text
  选中：  ┌ 外圈 2px 主色（--brand，全局「点击后=主色底」一脉）
          ├ 内圈 1px 面色（--label-ring-inset = --surface-main，即所在面的颜色）
          └ 本体的色标色
```

内圈的作用是**把主色与色标本色隔开**，让两者不直接相接。代价是加内圈后色标本体
看起来小了一圈 —— 这是**预期的**（内圈占的正是本色那一圈），不是画错了。

> ⚠️ 黄 `#C9A90C` 与品牌辅色 `#F0B033` 仍是同色系近邻（都是黄），所以**不要让黄标色块落在辅色底上**。
> 缓解手段**不是描边**（见上条），而是实现里已有的两道屏障 —— 改色标组件时别把它们拆掉：
> - tile 的底色优先级是「选中（主色）> 颜色标记 > 指向（辅色）」（`Tile.tsx`）——
>   带色标的 tile 自身不会出现辅色 hover 底；
> - `toolsbar` 的色标点是纯色圆点，自身不带 `hover:bg-state-hover`（带 hover 底的是旁边的星标按钮）。

**实现落点（唯一来源）**：取值表与类名映射在 `src/lib/color-labels.ts`；
Rust 侧的合法值在 `store/marking.rs` 的 `COLORS`（校验用，**不重复面板顺序**）。

---

## 3. 主题组装规则

两套主题**共用同一套色板**，只是角色互换；**一套设计稿通过切换主题变量同时产出深浅两版**（见 §9.2）。

| | dark | light |
| --- | --- | --- |
| 基础面 | 深色 `#202226` | 浅色 `#F6F8F4` |
| 第二层 | 浅深色 `#2A2D33` | 深浅色 `#EDF0E9` |
| 正文文字 | 浅色系（`#F6F8F4`） | 深色系（`#202226`） |
| 注释文字 | 中间色 `#B6B0AF` | 中间色（见 §13 已知问题） |

**默认主题 = `dark`**（相片软件在暗房环境下的行业惯例）。

**主题切换方式 = 手动开关，不跟随系统**（用户指定，见 `design/shell.md`）。

---

## 4. 文字颜色规则

| # | 规则 |
| --- | --- |
| 1 | **正文与标题**：按所处背景取浅色或深色（dark 主题用浅、light 主题用深）。 |
| 2 | **注释性文字**（说明、单位、次要元数据、禁用态提示）：用**中间色 `#B6B0AF`**。 |
| 3 | **与图标关联的文字**：一般**与图标同色**。例如黑底上的白图标，其文字也是白色。 |
| 4 | **例外**：当图标使用品牌色、且该图标有**强关联文字**时，文字也用品牌色。 |
| 5 | **品牌色块上的文字**：统一用深色 `#202226`（主色 `#52C6AB` 与辅色 `#F0B033` 都足够亮，深字可读，实测 7.60:1 / 8.32:1）。 |

---

## 5. 状态与交互色（全局反馈规则）

**这是一条全局规则，所有元素一律遵守**（用户 2026-09-15 指定）：

```text
鼠标指向          →  辅色底
切实被点击后      →  主色底
已有主色底时指向  →  保持不变（不回落成辅色底）
```

| 状态 | 底 | dark | light |
| --- | --- | --- | --- |
| **hover（指向）** | **辅色底** | 辅色 16% | 辅色 18% |
| **selected / active（点击后）** | **主色底**（小控件=实色） | 主色实色 | 主色实色 |
| **selected（大面积：行/卡片）** | **主色底**（低透明度） | 主色 22% | 主色 25% |
| **selected + hover** | 保持主色底，不变辅色 | — | — |
| **focus 焦点环** | 品牌主色描边 **1px** | 主色（见 §13 已知问题 2） | 见 §13 已知问题 2 |
| **disabled** | 前景降为次级色，**面不变** | `#B6B0AF` | 见 §13 |

### 5.1 两条设计意图

1. **指向与点击用不同色相**（辅色黄 vs 主色绿）。这比单纯改变深浅强得多 ——
   用户一眼能分辨「我只是划过」与「我已经选定」。
2. **「实色 vs 低透明度」按面积分档，不是两套语义**：
   - **小控件**（分段控件胶囊、磁铁方块、图标按钮）→ **主色实色底**，文字用 `fg-on-brand`
   - **大面积**（列表行、树行、卡片）→ **主色低透明度底**

   理由：§1.3 要求「主色用得克制」。一屏里若出现多处**大面积实色主色**，界面会变成绿色的。
   两者都是「主色底」，只是浓度不同。

> `.pen` 中用 `$state-hover`（辅色底）与 `$state-selected`（主色底）两个令牌表达，
> 改色只需改令牌。

### 5.2 浓度必须是配置项 + 稳定的推导算法

> **用户明确要求**：主色后面可能会改，所以浓度**不能写死**，要么进配置，要么提供
> **稳定的实时转浓度算法**。

**禁止**写出 `#52C6AB38` 这种「主色 + 固定 alpha」的字面色值（主色会改，写死就僵住了）。正确做法是**把颜色与浓度拆开**：

```css
/* 浓度是独立参数，颜色由令牌提供 */
--tint-ratio-selected: 0.22;   /* 可主题化 */
--tint-ratio-hover:    0.16;

/* 叠底用 color-mix 实时合成，而不是预先算好的字面色 */
--state-selected: color-mix(in oklab, var(--brand)   calc(var(--tint-ratio-selected) * 100%), transparent);
--state-hover:    color-mix(in oklab, var(--brand-2) calc(var(--tint-ratio-hover)    * 100%), transparent);
```

这样改 `--brand` 一处，所有选中底自动跟着变，**浓度参数保持不动**。

#### 若需更严格的「感知稳定」

固定 alpha 的局限：主色从中间调换到极亮/极暗时，同样 22% 会看起来差别很大。
若需要更严的稳定性，用**以亮度为目标的反推**：

```text
目标：选中底与其宿主面（surface-main / surface-bar）的 CIELAB 明度差 ΔL* ≈ 10～14
做法：构建时用小脚本对 --brand 求相对亮度，反推 alpha 写入 --tint-ratio-selected
```

两层都留位：**先用固定 alpha + `color-mix`（零成本），需要时再上亮度反推**。

---

## 6. 界面风格规则

| 规则 | 内容 |
| --- | --- |
| **不做毛玻璃** | 不使用 `backdrop-filter` 之类的模糊/半透明材质 |
| **不做阴影** | 不用 `box-shadow` 表达层次；层次一律靠**面与面的色彩差** |
| **小倒角** | 绝大部分情况使用小圆角；大圆角只在极少数需要明确「胶囊」语义的控件上出现（如工作流切换） |
| **无边线设计** | **面板与面板之间用纯色块区分，不上分隔线**。描边仅保留给「必须限定边界」的输入类控件、焦点环，以及 `easy copy` 的悬停高亮框 |

**无边线设计的直接后果**：§1.2 的深度阶梯差异幅度必须**足够可辨**。
若美术阶段发现差异过小导致区域糊成一片，优先**加大阶梯差异**，而不是改回描边。

---

## 7. 字体

### 7.1 选型

| 用途 | 首选 | 授权 | 备选 |
| --- | --- | --- | --- |
| **拉丁文 / 数字** | **Inter** | SIL OFL 1.1（可自由嵌入分发） | Geist |
| **中日韩（CJK）** | **Noto Sans SC**（思源黑体同源） | SIL OFL 1.1（可自由嵌入分发） | — |
| **等宽（路径、EXIF 数值对齐）** | JetBrains Mono | SIL OFL 1.1 | Inter 的 tabular 数字 |

- Inter：<https://rsms.me/inter/>
- Noto Sans SC：<https://fonts.google.com/noto/specimen/Noto+Sans+SC>
- JetBrains Mono：<https://www.jetbrains.com/lp/mono/>

**为什么不用 MiSans（更现代的观感）**：MiSans 声明「全球免费商用」，但其许可协议明确禁止
**「单独将字体或其组件对外租赁、再许可、给予、出借或进一步分发字体软件」**
（<https://hyperos.mi.com/font/zh/faq/> 与协议 PDF：<https://hyperos.mi.com/font-download/MiSans字体知识产权许可协议.pdf> ）。
桌面应用**内嵌字体**是否构成「再分发字体软件」存在解释风险。
本项目对许可一贯从严 —— 因此**默认用 OFL 字体**；MiSans 作为候选登记在 `ASSISTANCE.md` 由用户决定。

### 7.2 使用规则

- **数字密集处（EXIF、尺寸、像素数、进度、计数）必须用 tabular 数字**（`font-variant-numeric: tabular-nums`）。
  等宽数字是相片软件的基本功：一排参数上下对齐才读得舒服。
- **字重只用 3 档**：Regular(400) / Medium(500) / Semibold(600)。避免 300 以下的细体（小字号发虚）。
- **字号阶梯**（`tokens.css` 的 `--fs-0…--fs-5`，组件里用 `text-fs-*`，**不要**再写 `text-[11px]`）：

  | 令牌 | 字号 | 用途 |
  | --- | --- | --- |
  | `--fs-0` | 12px | 极小：单位、角标 |
  | `--fs-1` | 13px | 注释、标签、列表次要行 |
  | `--fs-2` | 14px | 控件文字、次级正文 |
  | `--fs-3` | 15px | **正文基准**（应用名、菜单、EXIF 值） |
  | `--fs-4` | 17px | 区块标题 |
  | `--fs-5` | 20px | 大标题、弹窗主标题 |

  > **用户 2026-09-15 明确**：「工业化不是字小」—— 初版偏小，整体 **+2 号**；高 DPI 时代
  > 没有必要把字小当行业特征。字号**不随密度变化**（§8.1）。
- **字号不随密度档位变化**（用户明确要求，见 §8）。
- **中英混排**：拉丁字体在前、CJK 字体在后组成 font stack，数字与英文走 Inter，汉字走 Noto Sans SC。

---

## 8. 尺寸与密度（`紧凑` / `宽松`）

整个 UI **只有两档密度**，由标题栏右侧的横向多选组件切换。

### 8.1 硬规则

1. **只影响「元素之间的间距类维度」**：padding、gap、行高、控件高度、圆角、面板内边距。
2. **不影响文字与图标尺寸**：字号、字重、图标尺寸两档完全相同。
3. **不是整体缩放**：禁止用 `transform: scale()` 或等比例缩放实现。两档是**两套独立的间距值**，
   某些维度可以不成比例（例如行高差距比内边距差距更大），这样两档的「手感」才真的不同。
4. **足够大的差距**：两档在主要间距维度上应有 **约 1.5–2×** 的差距，切换时必须一眼可辨
   （2026-09-16 两档一起收紧后，这条依然成立：gap 3↔6、内边距 6↔12 都是 2×）。

### 8.2 两档的具体值

| 维度 | 紧凑 | 宽松 | 倍率 |
| --- | --- | --- | --- |
| 基础间距单位 | `4px` | `8px` | 2× |
| 元素间 gap | `3px` | `6px` | 2× |
| 条带横向内边距 | `6px` | `12px` | 2× |
| 条带纵向内边距 | `3px` | `6px` | 2× |
| **titlebar 高度** | `36px` | `44px` | 1.22× |
| **flowbar 高度** | `48px` | `62px` | 1.29× |
| **toolsbar 高度** | `36px` | `44px` | 1.22× |
| 列表行高 | `24px` | `30px` | 1.25× |
| 按钮内边距（横×纵） | `5×3px` | `10×6px` | 2× |
| 圆形/方形小圆角 | `4px` | `6px` | 1.5× |
| 面板内边距 | `6px` | `12px` | 2× |
| 字号 | `12/13/14/15/17/20px`（见 §7.2） | **相同** | 1× |
| 图标尺寸 | `16/20/24px` | **相同** | 1× |

> **2026-09-16 修订（人验收反馈）**：两档**各温和收紧约 10%** —— 上表是收紧后的值，
> 真机（高分屏）下不再显得松垮。**不参与缩放**的几项及理由：
> 圆角（观感基准）、开关与勾选框（有可点/可见下限，§8.3）、`--remove-hit`（不小于 22×22 的硬规则）、
> 滚动条（太细就抓不住）、面板宽度（那是**信息宽度**不是间距，收窄只会让树少看一列）。

### 8.4 尺寸以**实现**为准，设计稿只提供结构（2026-09-16 定）

`design/main.md` §6.2 实测：Pencil 的 `width` / `height` **不接受变量引用**（传 `$bar-title-h`
会被静默丢弃），所以「密度轴」在画布里无法表达 —— 稿子只能按某一档硬写。

于是定下口径（人明确要求）：

1. **两档尺寸的真相源是 `src/styles/tokens.css`**（`[data-density="compact|loose"]` 两个块）；
   改尺寸只改那里，**不再回头同步 Pencil**；
2. **Pencil 稿负责**：界面结构、区域划分、控件放置、状态（默认/hover/激活/禁用/空态）与交互说明 ——
   这些才是设计稿不可替代的价值；
3. 因此画布与实现出现**纯尺寸**差异时，不算「偏离设计」，不需要先改 `.pen`（§5.1 的
   「先改设计再改代码」只约束**结构/状态/交互**层面的偏离）。

### 8.3 设计稿中用到的具体尺寸（历史对照；**尺寸以实现为准**，见 §8.4）

| 元素 | 紧凑（当前稿） | 宽松 |
| --- | --- | --- |
| 面板宽度（左 Source / 右 库） | `320` / `300` | `360` / `340` |
| Exif 信息区字号 | `--fs-1`（13px） | **相同**（字号不随密度变，见 §8.1） |
| 分段控件胶囊内边距 | `[0,14]` | `[0,20]` |
| 分段控件轨道内边距 | `3` | `4` |
| 分段控件胶囊高度 | `30` | `36` |
| 圆形勾选框 | `16px` | `18px` |
| 列表行起始缩进 | `10px` | `16px` |
| **移除/排除按钮点击区** | `24×24` | `30×30` |
| **移除图标** | `18px` | `20px` |
| 已选目录横条高 | `60` | `72` |
| 开关轨道 | `28×16`（圆点 `12`） | `34×20`（圆点 `16`） |
| 磁铁方块 | `32×32`（图标 `16`） | `38×38`（图标 `18`） |
| 图片 tile | **9 档可变**，默认 `208×139`；画布示意用 `150×112` | 同左；字幕条 `26` → `32` |
| 导入按钮高 | `36` | `44` |
| 库卡片高 | `60` | `72` |

> **窗口三键**：`52×40`（紧凑）/ `62×48`（宽松）—— 与 titlebar 同高、贴着右边缘。
> **工作流切换器**（flowbar 专属，`src/shell/FlowSwitcher.tsx`）：胶囊高 `34`/`40`、横向内边距 `18`/`24`，
> 比通用分段控件大一档 —— 它是外壳的招牌部件，**不与「紧凑 / 宽松」那种小开关共用组件**
> （用户 2026-09-15 明确：大模块切换的概念不该跟小小的界面松紧调整挂钩）。
>
> **移除类按钮的最小点击区不小于 `24×24`**（用户反馈「太小了，操作要舒服」；随字号上抬由 22 提到 24）。
> 实现时若视觉图标想保持小，用透明的 padding 把点击区撑到该尺寸即可。
>
> 密度档位在**实现侧**用 `[data-density]` 切换（所有尺寸令牌都能切）；
> 在 **Pencil 侧**则**只能表达颜色**（`mode` 轴可靠，子树自动继承），
> **尺寸不可用变量** —— 实测 `width`/`height` 传 `"$bar-title-h"` 会被**静默丢弃**（不报错但值不生效）。
> 因此设计稿的尺寸是按**紧凑档硬写**的，「宽松」档需另出一版。详见 `design/main.md` §6.2。

---

### 8.5 z 轴阶梯（浮层谁在谁上面）

**唯一事实来源**：`tokens.css` 里的 `--z-*` 令牌 + `index.css` 里「按 Ark 的 data 属性赋值」的那一段。

| 层 | 令牌 | 值 | 谁用它 |
| --- | --- | --- | --- |
| 外壳三条内部的分层 | `--z-bar` | 10 | 工作流切换、分段控件的文字/指示块 |
| 跟随触发器的浮层 | `--z-popover` | 40 | 菜单、提示气泡、下拉、日期选择器 |
| 模态遮罩 | `--z-scrim` | 60 | 弹窗背后的压暗层 |
| 模态本体 | `--z-modal` | 70 | 弹窗面板 |
| 右上角提示 | `--z-toast` | 80 | toast（比弹窗还高：它可能出现在弹窗之上） |

**纪律**：

1. 浮层的 z **只在这两处定义**，组件里不写字面量、也不写 `z-50` 这类工具类；
2. 新增浮层组件时，到 `index.css` 里加一行「哪个 `data-scope`/`data-part` → 用哪个令牌」；
3. 需要「一个浮层压在另一个浮层上」时，**用更高的令牌**，不要靠 DOM 顺序。

> **为什么不能用 Tailwind 的 z 工具类**（2026-09-16 踩坑记录）：
> Ark/Zag 的浮层元素带**内联** `z-index: var(--z-index)`，而 `--z-index` 由 Zag 的层级栈赋值、
> 并且同样是**内联变量** —— 内联变量压过样式表，所以 `z-50`、`z-(--z-popover)`、
> 任意属性写法**全部无效**（计算值一律 `auto`，菜单会被下面 `z-10` 的按钮压住）。
> 唯一入口是用 `!important` 覆盖那个内联变量 —— 这就是 `index.css` 里那三处 `!important` 的由来。

---

### 8.6 可拖动把手：只给左边、比例要记（2026-09-16 定）

**原则**：工作区里**只给左侧边界加可拖动的把手**，右侧不给。

| 位置 | 可拖？ | 为什么 |
| --- | --- | --- |
| 左列（导航 / 来源 / 目录）的右边界 | ✅ | 不同人、不同屏幕对它的宽度需求差别很大 |
| 左侧内部的分段（如「最近 / 来源」） | ✅ | 同上 |
| 中列与右列之间 | ❌ | 右侧（库 / 检查器）的宽度由**内容**决定，做成可拖只会「越拖越乱」 |

**尺寸纪律**（`src/lib/layout-prefs.ts`）：

1. 记的是**相对比例**，不是像素 —— 窗口大小变了之后比例仍然对；
2. **拖拽结束才落盘**（`onResizeEnd`），拖动过程由 Ark 自己管，不写存储；
3. 存 **`localStorage`**：布局是设备级偏好（换台机器该重来一次），也避免首帧跳变；
4. 比例之外还要有**像素下限**（如左列 `minSize: "220px"`）—— 比例管「拖」，像素管「窗口特别小」；
5. 存坏值不许把界面搞坏：读取时一律夹到范围内（`sanitizeLayout`）。

---

## 9. 令牌的统一定义点（唯一事实来源）

**要求**：颜色定义除了在 Pencil 里，在**实际前端中也要有统一定义点，方便修改**。

### 9.1 三层链路（只有一个源头）

```text
DESIGN.md（本文件，人类可读的规格与色板）
      ↓ 手写一次，之后只改本文件
① 设计侧：Pencil 变量（design/*.pen 的 SetVariables）
② 实现侧：src/styles/tokens.css 的 CSS 自定义属性（:root[data-theme=...]）
      ↓ @theme 映射（不重复定义色值，只做别名）
③ Tailwind v4 语义工具类（bg-surface-bar / text-fg-1 …）
```

**唯一需要手工同步的地方是 ①↔②**（设计工具与代码之间无法自动共享变量）。
这条链路的纪律是：

- **色值只出现在本文件与 `tokens.css` 两处**，且 `tokens.css` 的注释必须指向本文件对应小节。
- **Pencil 变量名 = CSS 变量名 = Tailwind 工具名**（去掉前缀后逐字相同），不允许各起别名。
- **组件代码禁止硬编码色值**；只允许引用语义工具类。

### 9.2 令牌命名

| 令牌 | Tailwind 工具 | 说明 |
| --- | --- | --- |
| `--surface-main` | `bg-surface-main` / `text-surface-main` | 基础面 |
| `--surface-bar` | `bg-surface-bar` | 第二层条带 |
| `--surface-track` | `bg-surface-track` | 凹槽轨道 |
| `--surface-layer` | `bg-surface-layer` | 浮层 |
| `--fg-1` | `text-fg-1` | 正文/标题 |
| `--fg-2` | `text-fg-2` | 次级文字 |
| `--fg-3` | `text-fg-3` | 注释/禁用 |
| `--fg-on-brand` | `text-fg-on-brand` | 品牌色底上的文字 |
| `--brand` | `bg-brand` / `text-brand` | 品牌主色 |
| `--brand-2` | `bg-brand-2` / `text-brand-2` | 品牌辅色 |
| `--danger` | `bg-danger` | **危险色**（目前只用于窗口关闭键悬停底） |
| `--fg-on-danger` | `text-fg-on-danger` | 危险色底上的前景 |
| `--state-hover` | `bg-state-hover` | 悬停叠加 |
| `--state-active` | `bg-state-active` | 按下叠加 |
| `--state-selected` | `bg-state-selected` | 选中叠加 |
| `--scrim` | `bg-scrim` | 浮层遮罩（Dialog 背景压暗） |
| `--tile-bar-scrim` | `bg-(--tile-bar-scrim)` | tile 信息条的主题中性蒙层（深色压暗 / 浅色提亮；见 §12.9） |
| `--ring-easy-copy` | `border-ring-easy-copy` | `easy copy` 悬停细边框（品牌主色 40%） |
| `--focus-ring` / `--focus-ring-w` | `border-focus-ring` | 焦点环（品牌主色 / 1px） |
| `--scrollbar-thumb` / `--scrollbar-thumb-hover` | —（CSS 内部使用） | 细滚动条滑块（由 `fg-3` / `fg-2` 派生） |

> **不以字面色出现的派生值**（同 §5.2 的思路）：`--scrim`、`--ring-easy-copy`、
> `--scrollbar-thumb*`、`--tile-bar-scrim` 全部由池内颜色用 `color-mix()` 实时合成。
> 这样「主色/前景色改动时它们自动跟着走」，无需新增任何字面色值。
> 两主题的遮罩都必须是**压暗**：dark 用最深的 `surface-track` 派生，light 用 `深色`（`fg-1`）派生 ——
> 浅色主题若用浅面派生会把背景洗白，看起来不像弹窗。
>
> `--tile-bar-scrim` 是**另一个方向**（§12.7.1）：它铺在**照片**上，
> 深色主题压暗（`surface-track` 65%）、浅色主题**提亮**（`surface-layer` 72%），
> 浓度按「照片是纯白/纯黑像素」这个最坏情况算到过 WCAG AA（实测 5.14:1 / 8.03:1）。

**密度令牌**用同样的思路，但挂在 `[data-density]` 上：

| 令牌 | 紧凑 | 宽松 |
| --- | --- | --- |
| `--space-unit` | 4px | 8px |
| `--bar-title-h` | 40px | 48px |
| `--bar-flow-h` | 52px | 68px |
| `--bar-tool-h` | 40px | 48px |
| … | 见 §8.2 全表 | |

### 9.3 当前实现状态

> ✅ **已落地（2026-09-15，M1-1 起）**：`src/styles/tokens.css` 按本文件重建完成，
> 是**令牌的唯一实现处**（`src/index.css` 只做导入与 Tailwind 映射）。
> 字号阶梯、条带高度、控件尺寸、品牌色、`danger` 都在里面，且有 `pnpm lint:colors` 守着
> 「组件里不许出现字面色值」这条纪律。
>
> ⚠️ **Pencil 画布仍是旧值**（编辑器侧不可达，`ASSISTANCE.md` A7）——
> 逐条待同步清单见 `design/main.md` §9；**同步之前以实现 + 本文件为准**。

---

## 10. 组件化策略

**规则：先定义一批最小基础组件，再在开发过程中定义大组件。**

### 10.1 第一批：最小基础组件（设计阶段就要定稿）

放在 `design/components.pen` + `design/components.md`。每个组件必须给出**全部状态**：
`默认 / hover / 激活 / 选中 / 禁用 / 空态 / 加载态`（按适用取用），以及**两档密度 × 两主题**的表现。

| # | 组件 | 说明 |
| --- | --- | --- |
| 1 | `Button` | 主要 / 次要 / 幽灵 / 危险 四种样式 |
| 2 | `IconButton` | 纯图标按钮，方/圆两种 |
| 3 | `Checkbox` | 方形勾选（树选择用） |
| 4 | `RadioCircle` | **圆形主色勾选框**（Recent / Source 列表用，见 §12.4） |
| 5 | `SegmentedControl` | 横向多选（凹槽 + 移动色块），紧凑/宽松开关与工作流切换共用 |
| 6 | `Input` | 文本框（唯一保留描边的控件之一） |
| 7 | `Tooltip` | 提示气泡，`easy copy` 的「点击复制/已复制」复用它 |
| 8 | `Menu` | 下拉菜单（标题栏菜单用） |
| 9 | `Dialog` | 模态弹窗（建库、`easy destroy` 确认用） |
| 10 | `PathText` | **`shortpath` 路径缩写**组件（见 §12.3），带长度上限参数 |
| 11 | `Tile` | 网格基本单元（图标 tile / 图片 tile 的公共骨架） |
| 12 | `TreeNode` | 树形行（展开箭头 + 图标 + 名称 + 勾选框） |
| 13 | `Panel` | 面板容器（带标题行、可折叠、可拖拽分段） |
| 14 | `SplitHandle` | **三段拖拽分隔条**（`...` 三点的细空白区，见 §12.5） |
| 15 | `ScrollBar` | 细滚动条 |
| 16 | `Badge` | 计数/状态小标签 |

### 10.2 第二批：大组件（开发过程中逐步定义）

| # | 组件 | 组成 |
| --- | --- | --- |
| A | `TitleBar` | IconButton + Menu + SegmentedControl(主题) + SegmentedControl(密度) + 窗口三键 |
| B | `FlowBar` | SegmentedControl(工作流) + `ExifStrip` + 开关组 |
| C | `ExifStrip` | 分组 + `easy copy` + `PathText` 式排版 |
| D | `ToolsBar` | 按工作流装配的按钮组（无内容时整行隐藏） |
| E | `SourcePanel` | `RecentList` + `SourceTree` + `SelectedTiles` + `SplitHandle`×2 |
| F | `RecentList` | `RadioCircle` + `PathText` + 行内删除 |
| G | `SourceTree` | `TreeNode` 递归 + 驱动器图标 |
| H | `SelectedTiles` | `Tile` 自动换行流 + 移除角标 |
| I | `PhotoGrid` | `Tile` 虚拟化流 + 底部 `GridControlBar` |
| J | `GridControlBar` | 缩放滑块 + 计数 + 排序（后续） |
| K | `RepositoryList` | 库卡片列表 + 建库按钮 + 导入按钮 + 勾选项 |

### 10.3 组件规范要求

- 每个组件在 `.pen` 里作为 **reusable frame**，实例化时只覆盖内容与状态。
- 组件在 `.md` 里必须写明：**用途 / 结构 / 全部状态 / 交互 / 快捷键 / 关联 i18n key**。
- 命名用英文 PascalCase（`SegmentedControl`），与代码里的组件名一致。

---

## 11. 国际化（语言包系统）

**要求**：加入语言包系统，支持中英界面。

### 11.1 规则

1. **界面文案零硬编码**：所有可见文字必须走 i18n key。
2. **设计稿上显示中文**（项目工作语言），但每个文案在 `design/*.md` 里**同时记录英文**。
3. 用户在设计中若用**英文精确描述**某个术语，Agent 必须**译成中文用于界面**，
   同时在 `.md` 里**原样保留英文**（因为英文常是更精确的技术表述，实现时要能对上）。
4. **中文不做机械翻译**：UI 文案要按中文习惯重写（例：`Click to copy` → `点击复制`，而不是 `点击为了复制`）。
5. key 名用英文小写点分层级，与组件层级对齐。

### 11.2 key 命名

```text
<titlebar.menu.help>            = 帮助            Help
<titlebar.menu.help.about>      = 关于            About
<titlebar.theme.toggle>         = 切换主题        Toggle theme
<titlebar.density.compact>      = 紧凑            Compact
<titlebar.density.loose>        = 宽松            Loose
<flow.import>                   = 导入            Import
<flow.browse>                   = 浏览            Browse
<flow.edit>                     = 编辑            Edit
<flow.export>                   = 导出            Export
<flow.tool.snap>                = 吸附            Snap
<exif.camera>                   = 机型            Camera
<exif.lens>                     = 镜头            Lens
<exif.focal>                    = 焦距            Focal length
<exif.aperture>                 = 光圈            Aperture
<exif.shutter>                  = 快门            Shutter speed
<exif.iso>                      = 感光度          ISO
<exif.dimensions>               = 尺寸            Dimensions
<exif.megapixels>               = 像素            Megapixels
<exif.format>                   = 格式            Format
<common.easy_copy.hint>         = 点击复制        Click to copy
<common.easy_copy.done>         = 已复制          Copied
<common.easy_destroy.confirm>   = 确定要移除吗？   Remove this?
<common.remove>                 = 移除            Remove
<source.recent>                 = 最近            Recent
<source.tree>                   = 来源            Source
<source.selected>               = 已选目录        Selected
<source.include_subdirs>        = 包含子目录      Include subdirectories
<grid.exclude>                  = 排除            Exclude
<grid.fit>                      = 适合屏幕        Fit to screen
<grid.actual_size>              = 实际大小        Actual size
<repo.title>                    = 库              Libraries
<repo.create>                   = 新建库          New library
<repo.import>                   = 导入            Import
<repo.avoid_duplicates>         = 避免重复导入    Skip duplicates
```

> 表中 `<...>` 是**示意写法**（便于在 Markdown 里展示），实际 key 不带尖括号：
> `titlebar.menu.help`。语言包文件：`src/i18n/zh-CN.ts` / `src/i18n/en-US.ts`。
>
> 本表是**主列表**，随设计推进追加；新增 key 必须同时补中英两版。

---

## 12. 通用交互范式

这些是**跨模块复用**的设计规范，不是某个界面的局部实现。

### 12.1 `easy copy`（悬停即知可复制）

**目的**：让「一组相关信息」可被一次性复制，且用户能**看出哪些信息是一组**。

| 阶段 | 表现 |
| --- | --- |
| **悬停某个组** | 该组加一圈**淡淡的细边框**（固定用**品牌主色**，透明度约 40%），同时内容上方**紧挨着**弹出 `点击复制` 标签式小窗（窄 padding） |
| **弹出动画** | 由下向上弹出（`translateY` + 淡入） |
| **点击** | 复制整组内容到剪贴板；**同一个**小窗重放一次「向上弹出」并把文案**翻转**成 `已复制`（原地替换，不是并排两句） |
| **离开** | 边框与提示淡出 |

> **为什么是「翻转」而不是「再弹一个」**（2026-09-16 人类指出实现与设计不符）：
> 曾经把 `点击复制` + `已复制` 排成气泡里的两行，结果是两张脸都不好看 ——
> ① 看到的是一张同时写着两句话的卡片；② 气泡长高一倍，在 `flowbar` 这种贴着窗口顶部的地方
> **上方放不下**，定位器把它翻到下方，看着像「在别处弹了个浮层」。
> **一个气泡、一句文案**同时解决了这两件事（也顺带把「不会因变高而翻面」变成结构性保证）。

**分组是内容本身的结构**，不是鼠标划过的顺序。例：`相机型号+镜头` 一组、`曝光参数（焦距/光圈/快门/ISO）` 一组、
`尺寸+像素+格式` 一组。悬停到某一组时，**同组的边框同时亮起**，用户能明确感知分组边界。

**实现参考**：`/home/andares/repos/zzd/vue-admin/src/components/project2/P2CopyText.vue`
（Vue 实现：`P2Tip` 提供 hover 提示 + `flip()` 切换到 `已复制`，`navigator.clipboard.writeText` 复制；
样式需按本规范改写）。Solid 版需自行实现 `P2Tip` 的等价物（提示气泡 + 状态翻转）。

### 12.2 `easy destroy`（破坏性操作的快速通道）

**规则**：所有**移除/排除**类操作，默认弹出确认框；**按住 `Shift` 再点击可跳过确认**。

- 语义统一叫 **`移除`**（从当前集合里拿掉），**不是破坏性删除** —— 图标一律用**禁行标志**
  （**填充圆 + 中间一条横杠**），**不是叉号**。
- 「移除」「排除」与「删除到磁盘」是三个不同动作，后者第一阶段不做，
  必须在界面文案上区分清楚。三者的完整区分见 `AGENTS.md` §11.3。
- **批量排除入口在 `toolsbar`**（内容居中），不在图片 tile 上。

  **它只有一个按钮，没有独立的「反排除」按钮。** 它是个**反转（toggle）操作**：

  ```text
  对当前选中的照片：
    未排除  →  排除
    已排除  →  取消排除
  ```

  即**逐张反转排除状态**。选中区里混合着已排除与未排除时，一次点击会把两者对调，
  而不是全部推向同一方向。

  | 状态 | 表现 |
  | --- | --- |
  | 有选中 | `批量排除`（普通态，可点） |
  | 无选中 | **禁用态**（前景降到次级色） |

  > 不要做成「按钮文字随选中项状态变成『反排除』」—— 那是个错误设计（已被否定）。
  > 语义固定是「反转」，文字也就固定是「批量排除」。

- **为什么把排除从 tile 移到 `toolsbar`**：排除真正有用的场景是**批量**的
  （「这一天的都不要」），而不是一张一张点。移到顶部后，操作变成
  **先选择（可按天 / 按时间片一键全选）→ 再按顶部排除**，
  一次点击即可完成「把选中的未排除项排除、已排除项恢复」这件事。
- **移除按钮点击区不小于 `24×24`**（§8.3）。

### 12.7 按时间分组（照片网格的第二种模式）

**入口**：`GridControlBar` 里、缩放滑块**左边**的 `按时间` 按钮 ——
可按下式（**同磁铁方块的形态**，不是左右切换的圆点开关）：

| 状态 | 表现 |
| --- | --- |
| 未按下 | **无底色**（与所在条同色），图标 + 文字用次级色 |
| 按下 | **主色实色底** + `fg-on-brand`（符合 §5 全局规则） |

**分组规则（两级）**：

```text
一级：按拍摄日期分组（本地日期）→ 一天一组
二级：在该天内按拍摄时间升序，相邻两张间隔 > 1 小时 → 断为新片
```

- **阈值 `1 小时` 必须是配置项**，不要写死在代码里。
- 拍摄时间缺失的照片：归入「未知时间」组放在最后，**不参与时间片切分**。

**UI 三层**：

| 层 | 内容 |
| --- | --- |
| **日组标题** | 日期 + 星期；右侧「**全选当天**」 |
| **片标题（小字）** | **`09:15 – 11:42`** 形式的**首尾时间范围** + 张数 +「**全选此段**」 |
| **缩略图行** | 与普通网格一致。**无排除徽标** —— 排除已移到 `toolsbar` |

- **为什么片标题用「首尾时间范围」而不是张数/时长**：用户是在回忆「我那次拍的是什么」，
  时间范围是最强的记忆线索（「上午那段」「傍晚那段」）。
- **日组标题与片标题都可点击全选**，然后配合 `toolsbar` 的批量排除使用 ——
  这正是「按天 / 按时间范围选择一批照片排除或反排除」的实现方式。
- 演示版面：`design/main.pen` 的 `Demo / 按时间分组 + 批量排除`（`omXCA`）。

### 12.8 可按下按钮（`ToggleBlock`）的统一样式

用于「按下后切换某个模式」的按钮（**吸附**、**按时间** 等），形态一致：

| | 未按下 | 已按下 |
| --- | --- | --- |
| 底 | **无底色**（与宿主面同色，即「平的」） | **主色实色底** |
| 内容 | 次级色 | `fg-on-brand` |

- **不是左右切换的圆点开关（switch）** —— 那是「包含子目录」这类属性开关的形态，
  两者不要混用。
- 「未按下时是平的」在无边线体系里天然成立，不需要边框或描边。

### 12.3 `shortpath`（路径缩写）

`Recent` 与 `已选目录` 里显示的路径不是完整路径，而是一个缩写形式：

```text
盘符 + 中间各级目录的首字母 + 最后一级目录的全名
```

- Windows 例：`D:\Photos\2024\Vacation\Japan\Kyoto` → `D:\P\2\V\J\Kyoto`
- Linux / macOS 下**第一级与最后一级写全**，中间同样取首字母。

**生成规则（两层算法）**：

1. **第一层**：只缩写中间各级目录（首字母逐级拼接），保留首尾完整。
2. **若仍超长**：**放弃第一层的结果，整体重新缩**（即首尾也参与缩写）。
   —— 这一步是必须的：如果直接在第一层结果上继续截断，当首级或末级目录本身很长时，
   已生成的 `...` 可能在某一侧「戳出来」，产生完全错误的截断。

**参数**：生成组件必须支持传入**长度上限**。超长时中间首字母部分缩成 `...` 以**保留两头**。

**完整路径**通过 `Tooltip` 在悬停时给出。

### 12.4 选择状态的两套表达（不要混用）

| 表达 | 含义 | 组件 | 位置 |
| --- | --- | --- | --- |
| **圆形主色勾选框** | 「已加入待导入」——加入下方的已选目录区 | `RadioCircle` | 列表条目**左侧** |
| **整行主色底**（`$state-selected`） | 「当前正在浏览」——中间图片区显示的就是它 | 行背景 | 整行 |

- **勾选**：可多选（多个目录都加入待处理）。
- **选中**：全应用**同一时间只有一个**目录被「选中」（因为中间图片区只能展示一个目录的内容）。
- 两套状态**互相独立**：一行可以同时是「已勾选」与「未选中」，也可以同时是两者。

#### 12.4.1 选中状态的跨面板同步（以及一个不要做的行为）

**要做的**：`最近` 与 `来源树` 的**选中状态是同一个状态**。

- 例：当前选中了 `D:\Photos` —— 若「最近」里也有这条目录，它**必须也显示为选中**。
- 反之亦然：「最近」里点选一条目录，若该目录在树里可见，树里那条也同步为选中。
- 「最近」中**其他不相关的条目不会**有任何选中态 —— 不要让人误以为多个目录同时被选中。

**不要做的（容易不自觉地实现错）**：

> ❌ **不要因为某目录被选中就强制展开树到那一层。**

以前很多软件会这样做：在 A 处选中目录，B 处的树结构即使没展开到它，也会**自动展开**去展示选中态。
**本项目不要这个行为。** 理由：展开与否**只跟用户的展开操作相关**，否则会出现「我只是在最近里点了一下，树却自己跳开了一大堆层级」的干扰。

**实现含义（很重要，它简化了实现）**：

```text
树的展开状态 = 纯用户操作状态，不受选中状态影响
选中背景色 = 渲染每一行时，判断「该行目录 == 全局选中目录」再决定
```

也就是说：**树的某一层没被展开时，那些行根本不存在于视图中，自然也不会显示选中态；
当用户展开到那一层时，渲染时才判定并如实上色**。不需要任何「为了显示选中而展开」的逻辑。

### 12.5 三段式面板与拖拽分隔

左侧 Source 面板分为上中下三段，**比例可由用户拖动调整**。

- 分隔条是**很细的空白区域**，形态是一排 `...` 三点式（用户指定）。
- 位置常在**上接内容区、下接块标题行**之间。
- 拖拽时给出 `row-resize` 光标与 hover 高亮（用 §5 的 hover 色）。

### 12.6 Tile 流换行布局（数学规划）

**规则：除最后一行外，每行列数相同；不做居中、不做拉伸填空，剩余空间作为右边距。**

给定：容器内容宽 `W`、tile 宽 `cellW`、列间距 `gap`（由密度档位决定）

```text
N = max(1, floor((W + gap) / (cellW + gap)))     // 每行列数（除最后一行）
L = W − (N·cellW + (N−1)·gap)                    // 右侧剩余空间
```

- **行内对齐 = 左对齐**（`justify-content: flex-start`）。
- **右边距 = `L`**（剩余空间全部留白，不分配给 tile 宽度，也不居中）。
- **tile 尺寸固定**（由缩放档位决定，不随容器宽度变化）—— 这正是「除最后一行外每行列数相同」的前提。
- `cellW` 由底部控制条的缩放滑块决定；`gap` 由密度档位决定。

> **为什么不允许拉伸**：拉抻填充会让 tile 尺寸随窗口宽度连续变化，破坏「网格是均质的照片阵列」这一认知，
> 也让用户在缩放窗口时不断看到照片变大变小。留白则稳定、且让视线有喘息处。

### 12.9 tile 信息条（覆盖在照片上）

tile 指向/选中时浮出的两条信息 —— **底部**：文件名 + 扩展名标签 + 加锁位；
**顶部**（库内才有）：星标 / 颜色点 / 旗标。它们是**覆盖层**，
锦在**外框**边缘（不是照片边缘，否则图片比例一变就上下跳），由外框圆角裁到；
照片尺寸**不因它们改变**。

**底纹 = 两层叠加**（为什么不是「同色改浓度」，见下表）：

| 层 | 内容 | 作用 |
| --- | --- | --- |
| 底 | 外框自己的状态底纹（指向=辅色 / 选中=主色 / 颜色标记色） | 让信息条与外框是**同一个色系**，不是贴上去的一张白纸；落在画面之外的留白区时可看见 |
| 面 | `--tile-bar-scrim`（主题中性蒙层） | **照片内容是任意的**：只用状态色改浓度的话，对比度会随状态漂，亮天空的照片上必然读不清 |

- 令牌在 `tokens.css` 的**主题层**（不随密度变）：深色用 `surface-track` **65% 压暗**、
  浅色用 `surface-layer` **72% 提亮**（§9.2）。
- 浓度按**最坏情况**（照片该处是纯白 / 纯黑像素）算到过 WCAG AA：
  实测 **5.14:1（深色）/ 8.03:1（浅色）**—— `pnpm smoke:ui` 每次都会把这两个数真测一遍，低于 4.5 直接报错。
- **信息条上的文字一律 `fg-1`**（不分次级色阶）：底是照片，压了蒙层也只能保证一档对比度，
  再用 `fg-2` 就会在亮照片上掊到 **2.55:1**（深色主题）这个级别；层级改由**字号**表达
  （文件名 `--fs-1`、扩展名 `--fs-0`）。
- 不做毛玻璃（§6：「不使用 `backdrop-filter`」），半透明平涂即可。

---

### 12.10 网格状态水印（载入 / 空 / 错误 / 提示）

照片网格没有内容可画时（扫描目录 + 读文件头缓存的那几秒、目录里没有照片、读不了、
还没选目录/库），**整块区域**浮一个大图标 + 一句话 —— 人类 2026-09-17 的口径：
「像印在背景底纹上」，**不要**拿个主色图标贴上去。

组件：`src/components/ui/StateWatermark.tsx`；动画：`styles/motion.css` 的 `rb-watermark-*`。

| 项 | 口径 |
| --- | --- |
| 容器 | **没有**。无卡片、无边框、无底色 —— 一套容器就从「印痕」变成「浮在中间的控件」 |
| 图标 | 64px、`stroke-width={1}`（细笔画像蚀刻），颜色 `fg-3` |
| 文字 | `--fs-2`、颜色 `fg-2`、居中、最长 24rem（错误信息可能很长，要能换行） |
| 静态浓度 | 整块 `opacity: .9`（不能再低了：`fg-2` 字段值在浅色主题下已经只剩约 4:1，再降就读不了） |
| 载入动效 | 呼吸 `3.6s`（`opacity .55 ↔ .95`）+ 高光扫过 `5.2s`，都只动 `opacity`/`transform` |
| 延时出现 | `120ms`（`rb-watermark-reveal`）—— 命中会话缓存时不闪一帧水印 |
| 减少动态效果 | 全部停下；**外层要落到 `opacity: 1`**（不能停在「还没出现」那一帧） |
| 动作（如重试） | 不参与呼吸，浓度恒定（它是可点的东西，得能读、能瞄） |

> ⚠️ **动效时长有两套口径**（`styles/motion.css` 里也写了）：
> **反馈类**（弹出/淡入）≤ 150ms —— 「我收到了」要立刻；
> **状态指示类**（本节的呼吸与扫光）是**秒级慢循环** —— 快了就像卡顿闪烁。
> 本节的新东西属于后者，不要拿前者去套。

---

### 12.11 M2-W1 落地的几条界面定案（2026-09-17）

这几条是**实现时定下来的**，画布还没画到（`design/browse.pen` 的 tiles 与三帧是 W1 收尾的事），
先记在这里，免得下次重画时又凭感觉：

| # | 定案 | 理由 |
| --- | --- | --- |
| 1 | **三态标记控件**：无值＝全暗（`fg-3`）、取值一致＝显示该值、**混合＝半亮（`fg-2` + 70% 不透明度）+ 一个短横** | 混合**绝不能**看着像无值 —— 那样点一下会把一批已打标的照片清空（语义在 `lib/marking-state.ts`，有测试） |
| 2 | **浏览 `toolsbar` 的筛选模式**：顶部一个开关，开了之后后面的标记控件由「设置」变「筛选」（点 3 星＝只看 3 星）。关掉时**同时清空筛选条件** | 否则界面看着没筛却少着照片。旗标不进筛选（它只活在内存里，按旗标过滤要动网格可见集合，归 W2） |
| 3 | **目录树行尾 `⋯` 菜单**：删除空目录（非空时**选项在但是暗的**）+ 创建子目录（中央模态，重名/非法名由 Rust 挡） | 菜单项「一直在、非空时禁用」比「非空时消失」好：用户能看出这个能力存在，也知道为什么现在点不了 |
| 4 | **删除要确认、且不给 `Shift` 快通道** | `easy destroy` 的 `Shift` 那条是给「从集合里移除」定的（§12.2）；删空目录与回收站删除**真在磁盘上留痕**，一次确认值得 |
| 5 | **导入工作流右列标题＝「导入目标库 / Destination library」** | 只写「库」读不出「这是要导入进去的那个」；左侧是来源、右侧是去处，标题要把这层关系说出来 |
| 6 | **`?spike=1` 的渲染诊断页不属于产品界面** | 它是开发期工具（与陈列室同性质），文案走硬编码不走语言包 —— i18n 守门脚本对 `src/dev/` 豁免。发布前的处理登记在 `FUTURE.md` C6 |

---

## 13. 对比度校验与已知问题

对关键组合做过实际计算（WCAG 相对亮度公式）：

| 组合 | 对比度 | 结论 |
| --- | --- | --- |
| 中间色 `#B6B0AF` on 深色 `#202226` | **≈ 7.45 : 1** | ✅ 优秀，作注释文字完全可用 |
| 中间色 `#B6B0AF` on 浅色 `#F6F8F4` | **≈ 2.00 : 1** | ⚠️ **低于 WCAG AA**（正文需 4.5:1，大号字需 3:1） |
| 主色 `#52C6AB` 上的深色文字 `#202226` | **7.60 : 1** | ✅ 可用（品牌色块上的文字） |
| 辅色 `#F0B033` 上的深色文字 `#202226` | **8.32 : 1** | ✅ 优秀 |

### 已知问题 2：浅色主题下「主色当文字 / 描边」对比度不足

`#52C6AB` 是**填充色**（品牌色块），不是文字色。实测：

| 组合 | 对比度 | 结论 |
| --- | --- | --- |
| `#52C6AB` on 深色条带 `#2A2D33` | **6.59 : 1** | ✅ 深色主题里可以直接当文字/描边 |
| `#52C6AB` on 浅色 `#F6F8F4` | **1.96 : 1** | ⚠️ **不能**当正文或 1px 描边（如焦点环） |

**可选项**（与问题 1 同一类取舍，待你定夺）：

1. 派生 `--brand-ink`：专供浅色主题的文字/焦点环，候选与实测 ——
   `#3FAE94` → 2.55:1、`#359C84` → 3.15:1、`#2E8C76` → 3.83:1、**`#1F7A64` → 4.88:1（过 AA）**；
2. 浅色主题下焦点环改用 `--fg-1`（深色，肯定看得见），主色只做填充；
3. 接受现状（焦点环在浅色下偏淡）。

- **状态**：⏳ 待决 —— **默认按候选 2 走**（浅色主题焦点环改用 `--fg-1`，主色只做填充），
  以免键盘可用性受损；若要更贴合品牌观感则取候选 1 的 `#1F7A64`（4.88:1 过 AA）。
  见 `ASSISTANCE.md` **D1**（不阻塞，但浏览模式设计定稿前需要你拍板）。

### 已知问题 1：中间色在 light 主题下作正文级文字偏淡 —— ✅ 已处理

- **处理方式（采纳候选 ①）**：**浅色主题的 `--fg-2` 用「中间色的深变体」`#6E6866`** ——
  在 `#F6F8F4` 上实测 **5.12 : 1**，过 WCAG AA。深色主题继续用 `#B6B0AF`（7.45 : 1，优秀）。
- **落地位置**：`src/styles/tokens.css` 的 `:root[data-theme="light"]` 段，注释里注明了出处；
  用户指定的原色板 `#B6B0AF` **未被更改**，只是浅色主题下换用它的深变体。
- **状态**：✅ 已解决（2026-09-15，从 `ASSISTANCE.md` A4 归档）

---

## 14. 看图 / 对比 / 提示（M2-W2 定案）

### 13.1 直方图（2026-09-19 定稿：52 点平滑曲线 + 显式分层取色）

* **采样**：**52 个点**（0..255 每 5 级一个点、51 格）。点数由前端向 Rust 请求
  （`getHistogram(path, HISTOGRAM_SAMPLES)`，后端按 `value * bins / 256` 分桶）——
  前端仍然不碰像素（`AGENTS.md` §6.1 的红线），只是把「要多少个采样点」说清楚。
* **曲线**：点与点之间走**单调三次插值**（Fritsch–Carlson 限制器）——
  平滑但**不会过冲**（Catmull-Rom 之类会冒出比两端都高的鼓包，直方图上看着像多了一撮数据）。
  实现在 `features/browse/histogram.ts`（纯函数、有单测：51 段、不过冲、单调段不下降）。
* **颜色**：就是**色标那六色**（§1.4），不另立一套：
  单通道 = 红 / 绿 / 蓝；两两重叠 = 黄（绿+红）/ 青（蓝+绿）/ 紫（红+蓝）；
  三色重叠 = `--hist-triple`（**偏亮的中间灰**，深色 `#A9AFA9` / 浅色 `#7E847E`，
  **不是纯白** —— 纯白在深色主题刺眼、在浅色主题与面板底分不开）。
* **怎么上色**：**不靠混合模式**。把每一列按高度排序切成 7 个区域（三色重叠 / 两两重叠 / 单通道），
  每层填自己的颜色。理由：`mix-blend-screen` 只在通道是**纯红绿蓝**时才混得出标准的黄/青/紫，
  一旦通道色改成色标色（都带灰度），混出来的就发脏、还与规定色对不上。
  显式分层 ⇒ 颜色就是令牌里那个色，改色只改令牌，且**可断言**（冒烟数 7 条路径）。
* **背景**：**很浅的细密虚线**（`--hist-grid`），纵向 4 等分（3 根）+ 横向上下 2 等分（1 根）。
  用 DOM 画而不是 SVG `stroke-dasharray`：SVG 被 `preserveAspectRatio="none"` 拉伸时
  虚线段长会跟着变形（竖线拉长、横线压扁）。
* **组件**：`components/ui/Histogram.tsx` —— **只吃归一化后的采样**（`bars`），
  没有内部状态、不碰 IPC。这样编辑模块将来每帧重算（拖曝光/对比）只要传新 `bars` 就换一帧。
* **归一化**：按**三通道合并**的峰值（各通道各自归一化会把噪声放大成满格，看起来像偏色）。
* **空态**：取不到数据时画一条空直方图 + 一句提示，**不报错**。

### 13.2 看图态的右栏（预览 + 视野框）

* 预览是**整张缩略**，上面一个 **1px `$brand` 的视野框**表示当前看得见的那块；
* 框与照片必须用**同一个变换**：`visibleRect()` 是唯一算法，视图只按百分比摆
  （放开自己再推一遍，放大后一定会错位）；
* 尺寸还没读到（`natural = 0`）时**不画框**，而不是画一个乱跳的矩形。

### 13.3 对比

* **最多 4 张**，且只对比**最近选中的 4 张**（人类 2026-09-19 定；窗口按选择先后滚，
  锚点必含，超出时最早的那张被挤出去）。不支持超过 4 张。
* **摆放**：2 张一排 / 3 张一排三个 / **4 张 2×2**（人类 2026-09-19 定）。
* **画幅基准 = 对比窗口里最早选中的那张**：其余图在自己的像素里**居中扣**出与它同比例的区域；
  窗口内**按选择先后**从左到右排。
* **第一幅带 1px `$brand` 内描边**（它是基准）。
* **同步**：缩放同一个倍率；位移**按画幅的百分比**（不是像素）—— 两张图像素尺寸本来就不同，
  同一个像素位移在两边观感完全不同。
* 对比态下胶片带只显示参与对比的图，整条加 **1px `$brand` 细边框**；点一张 = 移出对比。

### 13.4 提示（toast）

* 位置：`titlebar` 正下方、右上角；层级 `--z-toast`（永远最上面）。
* **不遮挡操作**：容器 `pointer-events-none`，只有卡片本身收事件。
* 带边框，边框色表状态：**成功 `$brand` / 失败 `$danger` / 中性 `$line-2`**；底 `$surface-layer`。
* 停留约 **5 秒**、同时最多 **3 条**（新的顶掉最老的）；带「撤销」的动作把撤销挂在卡片自己身上。
* 只在**有事可说**时出现：改成功不必报，被锁挡住、什么都没改、删失败这类必须说。

### 13.5 侧栏宽度可拖拽（浏览工作区）

* 左右两列都**可拖拽调宽**（220–520px），宽度**存像素**不存比例 ——
  侧栏是人手定的尺寸，改窗口大小不该让它跟着变宽窄。
* 手柄是两点宽的三点细条（与导入工作区同一套 `SplitHandleDots`），
  指向态辅色底、拖拽中主色底；**键盘方向键也能调**（每次 8px），焦点框走全局规则。
* 看图「关左右」（`Tab` 第二态）时手柄与列一起收起来；第三态（`view only`）**连胶片带一起收** ——
  三个状态是**并列**的（不是层层叠加），见 `BROWSE.md` §5.4。

### 13.6 弹性动画（**有意例外**，人类 2026-09-18 口述）

* 库列表的**展开 / 收缩**约 **200–260ms + 弹性曲线**，**允许动高度** ——
  这是 `DESIGN.md` 全局「不动高度、只动颜色」纪律的**唯一例外**。
* 理由：它是「一整块区域在长 / 缩」，靠颜色变化表达不了；而且时长压得短，
  不会让人等。例外的范围**只限这一处**，别推广到别的面板。

## 15. 待决问题（美术设计阶段处理）

| # | 问题 | 说明 | 状态 |
| --- | --- | --- | --- |
| 1 | 中间色在 light 主题下的文字用途 | 见 §13 | ⏳ A4 |
| 2 | ~~是否需要第三/四级面~~ | **已解决**：需要四级，见 §1.2 | ✅ |
| 3 | ~~尺寸档位与密度~~ | **已解决**：见 §8 | ✅ |
| 4 | 小倒角的具体数值 | 暂定 4px（紧凑）/ 6px（宽松），看渲染效果 | ⏳ |
| 5 | 焦点环与选中态的具体表达 | 暂定见 §5，需出图验证 | ⏳ |
| 6 | ~~是否跟随系统主题~~ | **已解决**：不跟随，手动开关 | ✅ |
| 7 | 字体最终选型 | 默认 Inter + Noto Sans SC；MiSans 待用户定 | ⏳ A6 |
| 8 | ~~主/辅色的最终色值~~ | **已定稿（2026-09-15）**：主色 `#52C6AB` / 辅色 `#F0B033`（用户按实际观感微调） | ✅ |
| 9 | 选中态用低透明度底还是纯色块 | 见 §5 说明 | ⏳ |

---

## 16. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-15 | 初版：基础色板（用户指定）、双主题组装规则、文字颜色规则、界面风格四条规则、令牌映射与对比度校验 |
| 2026-09-15 | 品牌辅色由 `#775588`（紫）改为 `#EECC55`（金黄），用户指定 |
| 2026-09-15 | **大扩充**：新增派生深度阶梯（四级面）、表面分层地图、状态与交互色、字体、双档密度与具体值、令牌三层链路与命名、组件化策略（16 个基础组件 + 11 个大组件）、i18n 语言包与 key 主列表、通用交互范式（easy copy / easy destroy / shortpath / 双选择表达 / 三段拖拽 / Tile 流换行算法） |
| 2026-09-15 | 修正 §8.2：Pencil 侧尺寸**不可用变量**（实测被静默丢弃），主题轴只能表达颜色；密度档设计稿需按硬写尺寸另出 |
| 2026-09-15 | **全局反馈规则定案**：指向=辅色底、点击=主色底（§5 重写）；新增 §8.3 两档尺寸对照与「移除按钮点击区 ≥22×22」；新增 §12.4.1 选中状态跨面板同步 + **明确禁止「为显示选中而强制展开树」** |
| 2026-09-15 | 新增 §5.2 **浓度必须是配置项 + `color-mix` 实时合成 + （可选）亮度反推算法**；§12.2 排除改到 `toolsbar` 居中批量操作；新增 §12.7 **按时间分组与时间片**（阈值 1 小时可配）；新增 §12.8 **`ToggleBlock` 统一样式** |
| 2026-09-15 | 修正 §12.2：**没有独立的「反排除」按钮** —— `批量排除` 就是个**反转操作**（未排除的排除、已排除的取消），按钮文字与外观恒定 |
| 2026-09-15 | 新增 §9.2 派生令牌：`--scrim`（遮罩）/ `--ring-easy-copy`（§12.1 的 40% 主色细边框）/ `--focus-ring` / `--scrollbar-thumb*`，均用 `color-mix()` 派生，**不新增字面色值**；新增 §8.3 派生尺寸 `--scrollbar-w` |
| 2026-09-19 | **新增 §14「看图 / 对比 / 提示」**（M2-W2 定案）：直方图（当日晚些改为 **52 点平滑曲线 + 色标六色显式分层**，见 §13.1）、视野框与 `visibleRect`、对比的 4 张上限与 2×2 摆放、toast 规范、侧栏宽度可拖拽、库列表弹性动画作为**唯一有意例外**。原 §14/§15 顺延为 §15/§16 |
| 2026-09-15 | **新增 `--danger` 危险色**（`#BF4E30`，微微偏橙的锈红，用户指定）+ `--fg-on-danger`（`#F6F8F4`）。两主题共用同一值：它靠色相与饱和度跳出来，不靠明度；与深色条带 2.86:1、与浅色条带 4.20:1，其上浅色图标 4.52:1（过 AA）。解决了 §15 待决项 4「窗口关闭键要不要红」 |
| 2026-09-15 | **用户微调品牌色**：主色 `#8DB8AA` → **`#52C6AB`**，辅色 `#EECC55` → **`#F0B033`**。实测：主色上的深色文字 **7.60:1**、辅色上的深色文字 **8.32:1**（都过 AA）；深色主题里主色当文字/描边也有 6.59:1。**新增已知问题 2**：主色在浅色主题下只有 1.96:1，不能当正文或 1px 焦点环，候选项与实测见 §13 |
| 2026-09-15 | **新增 §1.4 色标（color label）**：用户指定两主题各一套 5 色（深色套 `#E7766F/#EAC565/#89E98E/#7CB7EC/#A886ED`，浅色套 `#B8524C/#CCA53E/#56C85C/#5891C5/#805ACD`），令牌 `--label-*` 入 `tokens.css`；附实测对比度与**浅色下必须加 1px 深描边**的纪律（浅色套黄/绿/蓝当色块仅 ~2:1）—— ⚠️ **描边那条已于 2026-09-19 作废**（无边线设计），其余结论仍有效 |
| 2026-09-15 | **字号整体 +2 号并收进令牌**（§7.2 的 `--fs-0…--fs-5`，正文基准 15px）：用户明确「工业化不是字小」，高 DPI 时代没必要把字小当行业特征。条带高度、行高、胶囊高度、勾选框、开关、磁铁方块、按钮高、卡片高等间距类尺寸一并上抬（§8.2/§8.3 已同步）。**工作流切换器独立成 `src/shell/FlowSwitcher.tsx`**：大模块切换不与小开关共用组件；`SegmentedControl` 退回「小而安静」的通用控件（圆角由胶囊改为 `rounded-ui`，形态上与前者区分） |
| 2026-09-17 | 品牌资产到位：标题栏 20px 用**只有徽章的 `logo-small.png`**（带字版本小尺寸下糊成一团），「关于」与打包图标用带字的 `logo.png`。新增**启动闪屏**（800×600 透明无边框、随机中/英、不抢焦点）—— 它同时是「Windows 上 webview 能否真透明」这条风险项的最小验证（`design/main.md` §2.4） |
| 2026-09-17 | **§12.1 纠正**：`已复制` 是**原地替换** `点击复制`（一个气泡、一句文案），不是「再弹一个」—— 并排两行会让气泡长高、在贴着窗口顶部的地方被翻到下方。另：tile **排除态** = 照片变透明 + 正中中性禁行图标（`design/main.md` §3.2.1）；`--tile-radius` 3 → **6px** |
| 2026-09-16 | **新增 §12.9 tile 信息条（覆盖在照片上）** 与令牌 `--tile-bar-scrim`：底纹 = **外框状态底纹 + 主题中性蒙层**两层（只用同色改浓度的话对比度会随状态漂，亮天空照片上必读不清）。深色 `surface-track` 65% 压暗 / 浅色 `surface-layer` 72% 提亮，按「照片是纯白/纯黑像素」的**最坏情况**算到过 AA（实测 5.14:1 / 8.03:1，`pnpm smoke:ui` 每次复测）；**信息条上的文字一律 `fg-1`**（再用 `fg-2` 在亮照片上只有 2.55:1，层级改由字号表达）。同步 §9.2 表与「派生值」注 |
| 2026-09-19 | **§1.4 作废「浅色主题必须加 1px 深描边」**（人类 2026-09-19 明确：本界面是**无边线设计**，色标只靠色块本身、不加描边；实现端早已按此落地，spec 未同步）。保留实测对比度表与「黄 vs 辅色 1.16:1」两条**事实**，但缓解手段改为依赖既有的两道屏障（tile 底色优先级「颜色标记 > 指向」、`toolsbar` 色标点无 `hover:bg-state-hover`），**不再用描边** |
