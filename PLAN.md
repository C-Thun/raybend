# raybend — 开发计划（PLAN.md）

> 规划结构：**Milestone（阶段）→ Wave（波次）**。Milestone 是能力层的边界，Wave 是 milestone 内可独立交付的小步。
> 本文件只做**路线与边界**，每个 milestone 的具体实现方案在开工前单独讨论后再细化。
> 远期方向（不做排期）见 `FUTURE.md`；纪律与关键真相见 `AGENTS.md`。
>
> 建立时间：2026-09-15 00:18:03 CST

---

## 0. 规划原则

1. **先打通四大模块核心流程**。按 2026-09-21 已定路线，M3 先做编辑与 issue 地基，M4 形成「导入 → 浏览/评级/筛选 → 编辑 → 导出」闭环；高级组织检索与全面产品化后置。
2. **风险前置**：把可能推翻架构的事（渲染合成、RAW 解码、SQLite 并发、缩略图吞吐）全部放进 M0 验证，不允许带着未知直接开工产品代码。
3. **每个 milestone 都要能独立交付价值**，而不是「一次性铺完所有底层」。
4. **基础设施只做当前工作单元需要的部分**（例如缓存淘汰策略留到 M5，M1 只要有能跑的缓存即可），避免过度设计。
5. **一个 writer 原则**：人机协作期间，避免同一时段多路修改同一文件。

### 0.1 规划纪律（2026-09-15 修订）

- **一次规划只覆盖一个工作单元**（一个波次，或一个 milestone 里连续可交付的几波）。
- **本文件（PLAN.md）只做路线级描述**：每个 milestone 含哪些波次、完成定义是什么。
- **具体实现方案在每个工作单元开工前单独规划**，写入 `specs/<milestone>-<单元>.md`（如 `specs/M0-1.md`），并提交 plannotator 评审。
- **不提前细化未开工的工作单元**：项目变大时前置规划必然失真，开工时按当时情况重新规划。
- **已归档的旧规划**放在本文件附录（如附录 A），**明确标注「仅供参考、不是待办清单」**。

---

## 1. 路线总览

| Milestone | 目标（一句话） | 波次数 | 交付物 |
| --- | --- | --- | --- |
| **M0** | 可行性验证（按模块按需前置） | 8 | spike 结论按需产出 + 可运行的空壳 |
| **设计阶段** | 各 milestone 的界面前置（非独立 milestone） | — | `design/*.pen` + 同名 `.md` |
| **M1** ✅ | 相片仓、目录与导入 | 6 | 能建仓、导入 5000 张、网格出缩略图、重启不丢、崩溃可续 —— **2026-09-16 交付，等人类验收** |
| **M2** ✅ | 浏览与评估（核心体验） | 3 | 10 万张库中流畅筛选与全键盘评片 —— **2026-09-21 交付，人类验收通过** |
| **M3** | **编辑：GPU 显影工作台（第一阶段主战场）** | 3（草案，开工前重评） | 同一界面对 RAW/JPG 无损编辑：常规调整 + CUBE LUT + SOOC/issue |
| **M4** | **可试用闭环：实时一致性 + 导出** | **7 + W0（2026-09-26 已确认，W2 开发与冒烟收口）** | 实时清单 + issue 画廊/预设 + 全尺寸/16-bit 出图 + 五格式/元数据 + 内存后台队列 + 外部编辑 + 收口 |
| **M5** | 分发与发布 | 4（其中品牌/官网/构建路径已前置） | 安装包、自动更新、首启体验、发布文档 —— **四大模块完成即发版先用** |
| **后续（未排期）** | 组织检索互操作、多仓/缓存/数据安全、AI、跨平台 | — | 见 `FUTURE.md` I/J 节及其余条目 |

**明确不做（近期）**：AI 修图、视频管理、云端同步、插件系统、macOS/Linux 适配、打印与完整色彩管理（ICC/HDR 管线）。
以上登记在 `FUTURE.md`，不在 PLAN 的排期内。图像编辑**已进入主线**（M3，2026-09-21 重排）。

### 1.1　2026-09-20 路线重排依据

> **历史取舍记录**：本表的 M3/M4/M5/M6 去向已被 §1.2 的 2026-09-21 定案替代，不是当前排期。

这次不是按旧标题机械往下做，而是按 `implementations/` 里的真实交付重新划边界：

| 原计划能力 | 已经提前落地的部分 | 仍然缺的产品闭环 | 新去向 |
| --- | --- | --- | --- |
| 原 M3 标签 | 全局平面标签词典、单张增删、批量添加、自动补全、筛选、撤销 | 层级、改名/合并/删除、管理界面 | M4-W2 |
| 原 M3 搜索 / 时间线 | FTS5 trigram + 短词 `LIKE`、组合查询、分面、10 万条基准、时间分组与全量 timeline | 面向照片信息的搜索 UI、保存查询、时间密度导航 | M4-W1 / W4 |
| 原 M4 导出 | 只有外壳入口与命令占位，**没有导出引擎** | 编码、命名、元数据、预设、队列、批量验证 | **提前为 M3 主线** |
| 原 M5 缓存 | 缩略图缓存、签名失效、frecency GC 与统计 | 容量/路径设置、调度、可视化与一键清理 | M5-W2 |
| 原 M5 备份恢复 | 迁移前快照、7 份轮转、版本闸门、故意损坏恢复演练 | 定期策略、启动检查、恢复 UI 与用户指引 | M5-W3 |
| 原 M5 任务 / 隔离 | 持久化 jobs、重试/退避/取消、导入崩溃续跑、RAW 独立进程与崩溃演练 | 跨工种统一调度、资源限流、任务中心、日志与诊断包 | M5-W2 / W3 |
| 原 M5 多仓 / 离线 | 库注册表、多路径、在线/离线解析、缺失标记、重新挂载基础 | 多仓同时打开、跨仓查询、拔插自动恢复的完整体验 | M5-W1 |
| 原 M6 品牌 / 构建 | 品牌、图标、闪屏、Windows 构建核验、官网与 Pages 工作流 | 安装、更新、签名、首启与用户文档 | M6 |

### 1.2　2026-09-21 再重排：编辑提前，产品化后移（人类定案）

上一轮重排（§1.1）追求「先闭环可试用，编辑后置」。人类复盘后推翻了这个取舍：

> 这是一个**开源项目**，不是上来就要卖钱的商业产品——**持续迭代可接受，不必一次拿出全面产品**。
> 第一阶段的重点是**打磨核心功能**。而 editor 的**多 issue 是本系统的重点设计**（`FUTURE.md`「issue 与 SOOC」），
> 没有它的导出是空中楼阁——导出界面本质是「选哪个 issue 导出」。

| 变化 | 依据 |
| --- | --- |
| **M3 = 编辑**（GPU editor：RAW/JPG 同界面无损编辑、常规调整、CUBE LUT、SOOC/issue） | 核心差异化优先；导出/工作区等上层建筑都悬在 issue 模型上，先定地基 |
| 原 M3（实时一致性 + 导出）顺移为 **M4**；导出来源契约按 **issue/SOOC 口径**重写 | 「要有 issue 才能做导出」（人类原话） |
| 原 M4（组织、检索与互操作）与原 M5（多仓、缓存治理、数据安全）**写入 `FUTURE.md`（I/J 节），不排期** | 过于追求产品化；开源项目持续迭代可接受 |
| 原 M6 分发与发布 → **M5**，定位：**四大模块（导入/浏览/编辑/导出）功能完成即可发版先用** | 尽早让真实用户上手，不追求功能齐全再发布 |

**不变的部分**：M1/M2 已交付的底座不动；实时一致性仍是 M4 首波；设计稿先行纪律（§5.1）与
「新增大型依赖先讨论」（§2.9）对 M3 照样生效。

**不能误判为完成**：有平面标签不等于标签体系收尾；有 FTS/查询引擎不等于全局搜索产品；
有 timeline 数据不等于地图/时间线视图；有 GC 函数不等于缓存治理；有迁移演练不等于自动恢复；
有 RAW worker 不等于完整任务系统；「导出」工作流按钮存在更不等于能导出文件。

**节奏变化**：原先先做完整 M3 再做导出，会让地图/相似图等增强能力挡在首个闭环前面。
新顺序先补本地应用最关键的磁盘实时一致性，再做导出并形成可试用版本；组织检索的高级能力随后展开。
地图底图若需要新增大型依赖，仍按 `AGENTS.md` §2.9 先讨论；不会为了赶 M4 暗中引入。

---

## M0 —— 可行性验证（改为「按模块按需前置」）

**目标**：用最小代价把「会推翻架构的未知」打掉。

> **2026-09-15 重构**：原本 M0 是「做完 7 个波次才进 M1」的一次性门禁。用户提出**功能优先**后改为：
> **spike 不再是关卡，而是各模块开工前的前置条件** —— 哪个模块要用，就做哪一个。
> 这既保住了「风险前置」的本意，又不会让人等完 7 个 spike 才看到功能。（依据：`AGENTS.md` §5.4 规划纪律）

| 单元 | 内容 | 服务于 | 何时做 | 状态 |
| --- | --- | --- | --- | --- |
| M0-1 | 仓库基线与 Windows 构建路径 | 全部 | — | ✅ 完成（2026-09-15） |
| M0-4 | 存储基准（SQLite / FTS5 中文 / 并发 / 备份） | M1 导入 | **M1 开工前** | ✅ 分批并入 M1-2 / M1-7 / M2-W1：单写者、FTS、备份恢复、10 万条查询均有实测 |
| M0-5 | 缩略图吞吐（编码格式 / 并发度 / 存储形态） | M1 导入 | **M1 开工前** | ✅ 并入 M1-3：真实样本吞吐、两段式缩放、SQLite 缓存与命中基线已落地；并发 worker 池留作优化而非架构未知 |
| M0-6 | 前端骨架与虚拟化（`VirtualGrid`） | M1 导入 | **M1 开工前** | 🔄 网格已产品化；`perf:browse` 已跑 10 万条（筛选 34ms、JS 堆 97–134MB），最终 2560×1440 / 60fps 仍待 `perf:win` 真机裁决 |
| M0-3 | RAW 解码——**内嵌预览路径** | M1 导入 | **M1 开工前** | ✅ 2026-09-17 并入 M2-W1 完成 |
| M0-3b | RAW 解码——**完整解码路径** | M2 查看器 | M2 开工前 | ✅ 2026-09-17 并入 M2-W1 完成 |
| M0-2 | 渲染可行性（wgpu 透明挖洞）★最高风险 | **编辑视口**（M2 的看图走 DOM，不用它） | **编辑里程碑开工前** | ✅ **2026-09-19 真机验收通过**：透明挖洞成立（Fit 留白看得见下层）、1:1 像素锐利、四角认向正确、命中中心 3000±1、数学往返偏差 ≤4e-4 px、窗口状态（resize/最大化/最小化/全屏）全正常、跨屏 dpr 跟随正确。**两档 DPI 均成立**：125% → dpr **1.375**；200% → dpr **2.200**（= 显示器缩放 × 文字缩放 110%）。**性能**：6000×4000 纹理平移/缩放 CPU **0.41ms/帧**、手操 p50 **16.42ms（67fps）**。**遗留均已了结或已定性**：设备恢复 ✅ 2026-09-19 复跑闭环（结构性修 ＋ 离屏回归测试 ＋ 真机帧数证据）；后端探测当时仅跑通 Vulkan；**2026-09-25 已验证 DX12 + DirectComposition 并固化为 Windows 编辑器默认**，旧 spike 结果不代表硬件仅支持 Vulkan；坐标过渡见 geometry revision（`AGENTS.md` §7.9），越屏闪烁的呈现隔离修复见 `docs/native-viewport-coordinate-guide.md` §8。全程见 `implementations/2026-09-19_windows-spike-verified-and-device-loss.md`，坐标契约进 `AGENTS.md` §7.9 |
| M0-7 | 架构定稿与 crate 边界 | M1 收尾 | M1 收尾时 | ✅ 现有 `raybend` 核心库 + 薄 Tauri 壳成立；按实际复杂度暂不拆空 crate，IPC 契约用 DTO 双侧守卫 |

**历史取舍**：M0-2 当时可以后移，是因为导入模块（文件树 + 缩略图网格 + 库管理）不碰 GPU 直绘；
后来 M2 看图先走 DOM，wgpu spike 仍在 2026-09-19 完成了真机验收。它现在服务于未来显影视口，
不再是 M2 的未决风险；退路（独立子窗口 / Tauri CEF）仍保留在 `AGENTS.md` §6.1。

**原开工前置均已解除**：RAW 样本与 Windows 渲染验证已经取得。后续涉及真实照片画质、GPU、DPI、
安装与 E2E 的裁决仍需人类真机配合，并直接在对话里提出。

---

## 设计阶段（每个 milestone 的界面前置，不是独立 milestone）

**状态**：🔄 进行中（2026-09-15 起）

| 产出 | 内容 | 状态 |
| --- | --- | --- |
| `DESIGN.md` | 令牌体系、四级面与表面分层地图、双档密度、组件化策略、i18n、通用交互范式 | ✅ 已定 |
| `design/main.pen` + `.md` | 外壳、基础组件、导入工作区及其状态（合并在同一主画布） | ✅ 已落地并用于 M1 实现 |
| `design/browse.pen` + `.md` | 浏览 / 看图 / 对比 / 标签 / 命令面板 / 菜单 / 快捷键设置等状态帧 | ✅ 已用于 M2；2026-09-20/21 反向同步完毕（`browse.md` §5.5） |
| `design/editor.pen` + `.md` | 编辑工作区：视口、调整面板、裁剪、对比、LUT、issue 定稿/切换 | 🔄 **进行中（2026-09-23）**：主稿 + 裁切/旋转/对比/空态/Tab 三档/ToolsBar 三段式/浮层 共 8 帧已落；issue 面板为最小可用形态，待人类过目（文件名人类 2026-09-23 定：**统一用 `editor`**） |
| `design/export.pen` + `.md` | 导出工作区：画廊选 issue、预设、队列与错误清单 | 🔄 **已出稿待人类过目（2026-09-26）**：主稿 + 导出运行中 + ToolsBar 三段式与三态 + 预设卡与按钮 + 浮层 + 空态与边界 共 6 帧；规格来自 `prompts/exports.pd`，口径与 8 条待决项见 `design/export.md` |

**硬规矩（人类 2026-09-21 定，状态于 2026-09-26 核对）**：导入/浏览/编辑已有设计；导出已出稿待确认，外部编辑器弹窗尚未出稿。任何模块涉及**未设计或未定案的界面**，
都必须先做「**结合功能设计的界面设计环节**」（人类主导功能意图与规划 → Pencil 落稿 → 人类定案），
定案后才写对应界面代码；实现中若要偏离设计，先改 `.pen` 再改代码（`AGENTS.md` §5.1）。
仅后端/纯逻辑、且不新增界面的波次（如 M4-W1 一致性编排）不受此约束。

---

## M1 —— 相片仓、目录与导入

**目标**：建立可信的数据底座，完成「建仓 → 导入 → 缩略图可见 → 重启仍在一份」这条最小闭环。

### M1-W1　相片仓与全局库

- 仓的定义与生命周期：创建、登记（`app.db` 注册表）、改名、移除（不删照片）、只读卷
- `<仓>/.raybend/repo.json` 结构定稿；`<仓>/.raybend/` 的隐藏与保护策略
- 全局库 `app.db` 的表设计（设置、仓库注册表、任务队列、缓存索引镜像）

### M1-W2　catalog schema 与迁移框架

- 表族定稿：`assets` / `asset_files` / `metadata_core` / `metadata_ext` / `keywords` / `tagged` / `collections` / `develop_stack` / `edit_revisions` / `jobs` / `fts`
- **文件身份**：`(volume_serial, file_id128)` 主键 + 路径规范化策略（NFC 存储 + 原始名保留 + 大小写折叠列）见 `AGENTS.md` §7.3
- 迁移框架（`user_version` + 顺序迁移）、版本闸门（库版本高于程序则拒绝打开）
- **快照备份**：迁移前自动 `VACUUM INTO`，保留 7 份
- 写并发模型落地：单写者 actor + 读连接池 + `PRAGMA` 调优

### M1-W3　扫描与变更追踪

- 文件系统扫描（并行、可中断）、按目录批量顺序读以照顾 HDD
- 增量扫描：基于 `(device, file_id)` 与 `(size, mtime)` 的差异检测；重命名/移动的识别（不丢编辑）
- `notify` 实时监听与「攒批处理」策略（避免大批量写入风暴）
- 缺失文件与离线卷的标记（不删记录，标记缺失）

### M1-W4　元数据与缩略图管线

- EXIF 抽取（时间、机型、镜头、ISO、快门、光圈、焦距、朝向、GPS）+ 未知 tag 的保留策略
- **内嵌预览提取优先**（快速出图）→ 后台真实解码替换（`AGENTS.md` §7.4）
- 缩略图生成 worker + 最小缓存读写（`GRID` 档，SQLite BLOB）
- 缩略图任务的持久化队列雏形（崩溃可续）

### M1-W5　导入 UI

- 导入面板（源选择、目标仓、重命名规则、是否复制/移动/就地引用、重复检测策略）
- 导入进度与统计（成功/跳过/失败/错误清单可导出）
- 可暂停/继续/取消；错误不打断整批
- 导入第一步只写「元数据 + 内嵌预览」，让进度条立刻动起来

### M1-W6　M1 收尾　✅ 2026-09-16 完成

- [x] 崩溃恢复演练：杀进程后重启，队列与库状态一致
      → `pnpm crash:drill`（600 张与 **5000 张**两档都跑过；修复了演练抓到的「崩在复制完未登记时会多出一份」）
      → `implementations/2026-09-16_M1-7_crash-recovery-drill.md`
- [x] 实施记录补齐 `implementations/`
- [x] 性能基线记录（5000 张导入耗时、缩略图生成吞吐）
      → `implementations/2026-09-16_M1-7_perf-baseline.md`
- [x] 迁移 / 备份 / **故意损坏**演练（DoD 里的第四条，原计划挂在 M5，提前做掉）
      → `pnpm migrate:drill` → `implementations/2026-09-16_M1-7_migration-backup-drill.md`

### M1 完成定义（DoD）　✅ 2026-09-16 全部达成（Agent 侧证据如下；最终确认权在人类验收清单）

- [x] **能创建仓并把 5000 张照片导入，重启后状态完整**
      5000 张合成图 11.27 秒导入完；重新打开库再导一遍，**5000 张全部判重跳过**（说明身份、来源与序号状态都在）
      → `implementations/2026-09-16_M1-7_scale-5000.md`
- [x] **网格能看到缩略图，滚动不卡顿**
      缩略图管线 18.9 张/秒（P95 62.8 ms）、缓存命中 0.09 ms/张；5 万张时窗口计算 72.6 µs/次（比一帧低两个数量级）。
      ⚠️ **「卡不卡」是体感 —— 仍须人类在真机确认**
- [x] **杀进程后重启可继续未完成的任务**：`pnpm crash:drill`（600 张 + 5000 张两档）
- [x] **迁移与备份机制经过一次演练（含人为损坏后的恢复）**：`pnpm migrate:drill`（v1→v3 迁移、7 份轮转、损坏拒绝打开不静默重建、快照恢复、未来版本闸门）
- [x] **有一份实测性能基线数据** → `implementations/2026-09-16_M1-7_perf-baseline.md`

#### 人类验收清单（M1 收尾前，Agent 到点会提醒）

| # | 事项 | 怎么做 |
| --- | --- | --- |
| 1 | **设计系统目视**（两主题 × 两密度 × 全状态） | `pnpm dev` → 浏览器开 `http://localhost:1420/dev/kitchen-sink`，顶部可当场切主题/密度/语言 |
| 2 | **外壳验收 8 项**（拖拽、双击最大化、边缘缩放、菜单悬停、三键、主题密度持久化、`toolsbar` 随工作流显隐、高 DPI 清晰度） | 跑起应用逐项操作；清单见 `specs/M1-4.md` §7 |
| 3 | **缩略图观感 + 悬停/选中的信息条可读性**（重点：亮天空、白墙这类**亮照片**上的文件名） | 拿**你自己的一批真实照片**（最好 JPG + RAW 都有）当来源导进一个新库，再看网格与 1:1 之外的表现。**指向/选中几个 tile**，看信息条底纹是否真的出现、字是否读得清（深色 65% 可能偏重、浅色白条可能偏抢眼 —— 觉得不对就说）。注意 `/mnt/c/src/tmp/pic` 已清理，需重新指一个目录 |
| 3b | **排除这条链路**（导入时「这张不要」） | 在网格里选中几张 → `toolsbar` 的「批量排除」→ 看照片**变透明 + 正中一个禁行图标**、上方统计的**张数减掉了**、右边出现「已排除 N 张」。再**切到别的目录**转一圈回来 —— 排除不该丢。最后点「导入」：进度弹窗的总数应当**不含**被排除的那些 |
| 3c | **tile 倒角**（3px → 6px）与 `easy copy` 的翻转 | 网格里看倒角是否清楚；悬停 flowbar 右侧的 EXIF 信息组 → 出「点击复制」→ 点一下 → 应当**原地**变成「已复制」（不是并排两行、也不该跑到下方） |
| 3d | **语言切换**（帮助菜单 → 在「关于」上面） | 点一下应当整体切到英文，而那条菜单项本身显示的是**目标语言的名字**（中文界面显示 `English`、英文界面显示 `中文`）；重启应用后语言应当还在 |
| 3e | **品牌 logo**（占位图标已换成真 logo） | 看标题栏最左那个 20px 的小徽章（只有徽章、没有字那版）—— **两种主题**下都要看清；再开「帮助 → 关于」，那里用的是带字的完整 logo。**不用管它清不清晰**，先看是不是那个图标、有没有被裁掉边角 |
| 3f | **启动闪屏**（新） | 双击 exe 起应用：应当**先**弹一张 800×600 的图（透明背景、**没有边框也没有按钮**、不进任务栏、不抢焦点），主窗口在它**后面**准备；**闪屏至少停 3 秒**（就算主窗口早就绪也等满），之后闪屏消失、主窗口出现。**多起几次**：中文版和英文版应当都能抽到（各约一半）。⚠️ 这条同时是「Windows 上 webview 能否真透明」的**首次真机验证** —— 若闪屏是**黑底/白底一块矩形**，说明透明没生效，请把现象告诉我（这可能牵动渲染架构，见 `AGENTS.md` §6.1/§8） |
| 4 | **M1 端到端手感**：建库 → 选来源 → 导入（含进度弹窗、暂停/取消、错误清单、结束摘要）→ 网格出图 → 库设置改模版 | 跑 Windows 版：`/mnt/c/rb-target/raybend/debug/raybend-desktop.exe`（2026-09-17 01:30 构建，已内嵌当期前端）。步骤见下 |

**第 4 项的走法（约 10 分钟）**：

1. 启动 exe（在 WSL 里直接跑，别经 `cmd.exe /c start`，见 `AGENTS.md` §5.3）：

   ```bash
   (cd /mnt/c/rb-target/raybend/debug && ./raybend-desktop.exe >/dev/null 2>&1 &)
   ```

2. 左列「库」区 → **新建库**，挑一个空目录（Windows 路径，如 `D:\raybend-test`）；
3. 左侧来源面板选一个**照片目录**（勾选要导入的子目录）→ 看工具栏的「导入」能否点；
4. 点「导入」→ **进度弹窗应当出现**（M1-6 修过一个「弹窗弹不出来」的 bug，这里重点看它）；
   中间可以试**暂停 / 继续 / 取消**（取消会二次确认，并写明「已导入的会保留在库里」）；
5. 结束后应当有**摘要**与「在库中查看这些照片」按钮 → 点它跳到浏览、网格出缩略图；
6. 右列库卡片上的**齿轮** → 改导入模版 → 「预览」应当边打字边变；
7. **导入到一半把窗口关掉**（或任务管理器结束进程）再重开：库应当还能打开、照片不重复；
8. 顺带看：主题/密度切到反面各扫一眼（与第 1 项的陈列室对照）。

> 这些**不阻塞开发**（Agent 继续往下做），但**M1 移交验收时必须由人过一遍**。

---

## M2 —— 浏览与评估（核心体验）

> **波次已重排（2026-09-17）**：原来 6 个波次（W1 网格 / W2 评级 / W3 查看器 / W4 筛选 / W5 命令面板 / W6 收尾）
> 合并为 **3 波**，按**功能**切而不是按层切 —— 目标是「每一波结束时人都能看到东西」。
> 详细方案与取舍见 `specs/M2.md`。
>
> **前置的状态**：
>
> - **设计已通过**（2026-09-17 七帧 → 2026-09-18 画布按实现同步 + 补帧：8 帧，见 `design/browse.md`）；
>   **画布与实现冲突时以实现为准**（人类 2026-09-18 定）；
> - **Windows GPU 验证后移**（2026-09-18 人类定）：**M2 的看图走 DOM 路线**（把已有的前端 Viewer 扩成
>   浏览看图 / 胶片带 / 对比），原生 wgpu 视口与逐项验证改到**编辑里程碑开工前**做。
>   `AGENTS.md` §6.1 的接口纪律不变（视口状态集中在一处、前端不碰像素），将来换渲染层只换视图。

**目标**：把「看片、挑片、打分」这件事做到比 ON1 更顺滑 —— 这是 raybend 的第一战场。

### M2-W1　地基 + 浏览网格　✅ **正式结束（人类 2026-09-19 签字）**

- **RAW 真解码**（rawler + worker 进程隔离 + 内嵌预览优先）→ 缩略图管线占位图下线
- **catalog 查询引擎**：范围 / 筛选 / 排序 / 分页 + 分面 + 全文索引维护（10 万条实测 P95 全 < 60ms）
- **标记写入 + 撤销栈 + 旗标（内存）+ 回收站删除**
- **IPC 11 条命令 + 契约两侧同步**
- **前端数据层**（api 封装 / browse store / 三态纯逻辑）
- **浏览工作区三列接线**：库目录选择器 + 网格 + 信息栏 —— 切到「浏览」就能看到库里的照片
- ✅ **一度后移的那件事已了结**：渲染 spike 的**人类逐项验证** 2026-09-19 在真机上跑完
  （两档 DPI + 性能基线，见 `implementations/2026-09-19_windows-spike-verified-and-device-loss.md`）。
- ✅ **画布补帧与状态图已完成**（W2 阶段 0.3，2026-09-18，见 `design/browse.md`）。

### M2-W2　看图 / 对比 / 标记 / 筛选　✅ **2026-09-20 完成**

- 看图（**DOM 路线**：扩展已有的前端 Viewer）、胶片带、2–4 图对比（比例同步 + 按百分比位移）
- `toolsbar` 全量装配：三态标记控件（评分 / 色标 / 喜欢 / 旗标 / 锁）+ 标签弹窗 + 筛选开关
- 撤销 / 重做 UI、toast 与中央模态
- 筛选与排序 UI、右栏看图态（预览含视口框 + 直方图）
- **闸门已解除**（2026-09-18）：看图走 DOM，不等 Windows GPU 验证；方案见 `specs/M2-W2.md`

### M2-W3　命令面板 / 快捷键 / 收尾　✅ **2026-09-21 结案（人类目视验收通过）**

- ✅ 命令面板（模糊搜索 + 显示快捷键 + 最近使用）、快捷键可自定义 / 导入导出 / 冲突检测
- ✅ 菜单极简兜底（File/Edit/View/Window/Help），所有菜单项同时是可搜索命令
- ✅ 10 万条性能路径：Rust 查询最慢一条 P95 28.29ms；`perf:browse` 感知筛选数据路径 37.5ms，
  JS 堆约 97–139MB。无头软件渲染的滚动数字只作诊断，不拿来冒充真机 60fps
- ✅ `scripts/perf-win.mjs`（真机采样脚本）已落地并验证（探针全链路跑通，2026-09-21）；
  Windows 产物已重编核验（`pnpm debug:win` 含 `check:win`）；Pencil 已补菜单/快捷键设置两帧
- ✅ 人类真机采样完成（2026-09-21）：主屏 2560×1441@1.375 与 200% 屏 3073×1826@2.2 均
  **120fps**（p50 8.3ms，DoD 60fps 余量一倍）；筛选端到端 20–22ms；人类目视验收通过
  （后续问题由人类反复跑测发现，不逐条记档）
- 详见 `specs/M2-W3.md` 与 `implementations/2026-09-20_m2-w3-command-palette-shortcuts-menus.md`

### M2 完成定义（DoD）　✅ **全部达成（2026-09-21）**

- ✅ 10 万张库中筛选响应 < 100ms（感知即时）—— Rust P95 最慢 28.29ms；`perf:browse` 数据路径 37.5ms；
  真机端到端 20–22ms
- ✅ 网格滚动稳定 60fps（目标分辨率 2560×1440）—— 真机双屏实测 **120fps**（p50 8.3ms）
- ✅ 全键盘可完成「看片 → 打分 → 下一张」的完整评片流 —— 人类目视验收通过（2026-09-21）
- ✅ 命令面板覆盖所有主要操作，且每项显示快捷键 —— 命令/菜单/快捷键同源一份注册表

### M2 → M3 闸门（已过闸，2026-09-21）

- M2 于 2026-09-21 正式关门（人类目视验收通过，DoD 四条全部达成）；
- 人类已重排路线（§1.2）：**M3 = 编辑（GPU 显影工作台）**；M3 开工前需人类完成**编辑工作区的界面规划**，
  并按 §5.1 设计稿先行（`design/` 出 `.pen` + `.md`，人类定案后才写代码）；
- 若后续发现会污染数据或推翻交互契约的问题，仍先回补 M2；普通视觉微调集中成一轮修复。

## M3 —— 编辑：GPU 显影工作台（第一阶段主战场）

> 2026-09-26：W0–W7 的开发与 Agent 冒烟已完成；真实照片、Windows DPI/色彩/交互性能和原文件哈希仍按 `specs/M3-W7.md` 由人类真机验收。

**目标**：把本产品的差异化核心落地——同一套界面对 RAW 与 JPG 做**无损（非破坏性）编辑**，
并把 **issue / SOOC** 从概念变成可用能力。「无损」指编辑只写编辑栈（DB/XMP），
**原始文件永不被改写**。

> 波次为路线级描述，**开工前按 §5.4 纪律另写详细计划并过评审**（`specs/M3-Wn.md`）。
> **开工前置 = 编辑工作区界面设计**（W0，已完成）。
>
> **波次已重切（2026-09-23，人类定）**：依据 = `design/editor.pen` + `design/editor.md`（已定案）
> + `prompts/editor.pd`（功能口述，**尚未写完**）。重切原则沿用 M2 的成功经验 ——
> **每一波结束时人都能看到 / 用到东西**；并额外把两类工作分开推进：
>
> * **「人看了才知道对不对」的**（布局 / 面板 / 开关 / 空态 / 命令）→ W1 先把骨架全立起来；
> * **「做出来才知道能不能用」的**（GPU 视口 / 显影管线 / 算法）→ W2 起的像素与算法。
>
> 两者互不阻塞：W1 交付的洞口契约（DOM 上报矩形 + DPR）正是 W2 渲染线程的输入。
>
> **与旧草案的差别（记一笔，免得后来者找不着）**：原 W1 把「issue 落库 + 原生视口」捆在一起、
> 原 W2「常规调整管线」、原 W3「对比 / LUT / SOOC」；现在拆成六波，
> 并把「界面骨架」「视口像素」「调整管线」「LUT 与 issue 交互」「三个视口工具」按**可见成果**分开。

### M3-W0（前置）　编辑工作区界面设计　✅ 2026-09-23 完成

- 人类主导功能意图（`prompts/editor.pd` 口述）→ Pencil 落稿：`design/editor.pen`（10 个顶层帧）
  + 同名 `design/editor.md`（结构 / 状态 / 交互 / Ark UI 可行性复核 / 命令接入评估 / i18n key / 待决项）
- 连带定案：`DESIGN.md` §14.8 编辑视口覆盖线（`--overlay-line` / `--overlay-halo`）、
  §14.9 右栏 = **三个同时在场的页签组**、§14.10 拉杆统一样式（+ 新令牌 `--slider-track`，
  2026-09-23 追加 **50% 半透**）；`AGENTS.md` §11.1 `toolsbar left/right` 术语、§11.5「取消在左 / 确认在右」
- 人类已过目两轮（`implementations/2026-09-23_editor-design-draft.md`、`_round2.md`、`_slider-track-half-opacity.md`）
- **遗留待决项**（不阻塞 W1，见 `editor.md` §7）：右栏是否给 editor 单独一档宽度、左栏宽度数值、
  旋转时框外是否压暗、禁用态画法、issue 列表信息量、要不要给「旋转后裁切」单独画帧

### M3-W1　编辑工作区骨架与洞口契约

**结束时可看到**：切到「编辑」是**一块真的工作区** —— 左 LUT 面板、中 视口 + 胶片带 + 照片状态栏、
右三组页签；`Tab` 三档切换、左右面板开关、空态四态；命令面板里能搜到编辑动作。
**视口里还没有照片像素**（W2 接管）——所以中列先是「装照片的那个框」，参数拉杆也还没有真实效果（W3 接管）。

- **外壳**：`ToolsBar` 升级成**左 / 中 / 右三段式**（左右贴边 + 中段居中 + 挤压时渐变淡出）——
  改**现有**组件，不新造一份（`prompts/editor.pd` 明确要求）
- **Tab 三档**：editor 三档（`左右+胶片带+view` → `左右+view` → `仅view`）、
  import 同步改成三档（中间那档是 `胶片带+view`）；**browse 四档不动**。抽成纯函数 + 单测
- **左列 LUT 面板**：固定宽、不可拖、默认开且状态持久化（版本化设备级偏好）；
  分类一级折叠（同时只展开一个）、新建分类；**LUT tile 与导入在 W4 接入**（按钮先禁用并写明）
- **右列三组页签**：`总览·定稿·信息` / `影调·色彩·清晰度·镜头` / `曲线`；
  拉杆按 `DESIGN.md` §14.10 用新的 `SliderRow`（标签上行、值同行居右、轨道等宽、双极/单极填充、`--slider-track` 半透）
- **洞口契约**：视口 DOM 上报 CSS 矩形 + DPR + 视口尺寸给 Rust（**只上报原始事实**，`AGENTS.md` §6.1 红线 2），
  Rust 存下来并可回读（产品侧命令 + 契约测试）；W2 的 GPU 线程直接吃它
- **空态四态**：无库 / 库无导入目录 / 目录无照片 / 有照片未选中（视口水印 + 右栏禁用）
- **命令注册表接入**：切 LUT 面板、裁切 / 旋转 / 对比（W5 生效）、Tab 三档循环、回浏览看网格
- i18n 中英双份；`pnpm typecheck` / `test` / `lint:colors` / `lint:arch` / `lint:i18n` / `build` 全绿

**明确不做**（留给后面波次，别当成遗漏）：视口像素、任何调整的真实效果、LUT 导入与应用、
issue 落库、三个工具的框线

### M3-W2　GPU 编辑视口产品化（含真实照片）　✅ 2026-09-23 完成

**结束时可看到**：编辑视口里出现**真实照片**（RAW 内嵌预览 / 完整解码两条路），可缩放、平移、1:1、适合窗口。

- 透明 WebView 覆盖原生 `wgpu` 底板；Windows 产品默认 DX12 + DirectComposition visual + Opaque（2026-09-25 修复越屏拖动闪烁）。平台呈现策略经最小 `PresentationAdapter` 接入，见 `ARCHITECTURE.md` §2.1；`AGENTS.md` §7.9 的坐标契约继续适用
- 渲染线程：**捕获 panic + 重启 + 上报**、设备丢失恢复、layout 与设备绑定、
  跨屏 / DPR 变更用带 revision 的整包布局事务
- 照片进纹理：解码走现有 `raw::worker`（隔离进程），上传与降采样在 Rust；前端只发交互意图
- 视口状态（zoom / pan / fit / rotation / clip / dpr）由 Rust 独有；命中测试在 Rust
- 与胶片带联动：换照片 = 换纹理（这一波先看原图，不带编辑栈）

> 交付细节与验收清单见 `implementations/2026-09-23_m3-w2-gpu-viewport.md`。
> 两条从这里带走的**新事实**（下一波会用到）：
> ① wgpu 在 sRGB 目标上把**清屏值当线性值**（`Srgb8::to_clear_color` 里有实测记录，
> `examples/editor-offscreen.rs` 守着它）；② 洞口底色由 wgpu 画、DOM 链在出图时让开，
> 于是**清屏作用于整张 surface** —— DOM 里的缝露出的是洞口底色而不是桌面。

### M3-W3　编辑栈与常规调整管线

**结束时可看到**：右栏 `影调` / `色彩` / `曲线` 拉杆真的改变画面（RAW 与 JPG 同一套），可撤销、可重置、可持久。

- **issue / 编辑栈数据模型落库**（catalog 迁移走既有框架，`AGENTS.md` §2.16）；SOOC 写死语义；
  XMP sidecar 的 issue 表示有明确契约（`docs/issue-xmp-contract.md`；真源仍是 DB）
- **Rust 显影管线**（scene-referred，`FUTURE.md` D1）：曝光 / 反差 / 高光 / 黑区、色温 / 饱和度 / 自然饱和度、
  曲线（含通道）；参数是管线的一级，**不是**在 8bit 显示空间上做
- 前端拉杆 ↔ IPC ↔ 管线：拖动中实时预览（渲染线程不阻塞 UI），松手才落库
- 编辑后缩略图与看图反映编辑结果（`render_signature` 失效语义延伸到编辑栈）
- 去马赛克与画质算法的取舍（`FUTURE.md` D2）在本波定案并记录

### M3-W4　清晰度 / 镜头

- `清晰度`：杂点降噪 / 杂色降噪 / 锐化强度 / 锐化范围（单极拉杆）；
  `镜头`：当前配置状态 + 全库品牌/焦段搜索 + 启用校正 + 畸变 / 暗角量与范围 / 红蓝独立色差
- 编辑内对比的**数据侧**（SOOC / RAW vs 当前结果）先落，框线交互在 W5

> **波次已重切（2026-09-24，人类定）**：`.cube` LUT 与 issue 两件事从 W4 拿出来，
> 与一次**数据结构重整**合并成新的 W6；原来的 W6（收口）顺移为 W7。
> 理由：LUT 与多 issue 都要挂到「编辑栈 / 变更集 / 渲染缓存」这套地基上，
> 而 W3 的 `latest` 单存储位只是**先把地基跑通**的第一版 —— 与其在 W4 里打补丁，
> 不如在 W6 把地基一次性整到位，再上这两块功能。

### M3-W5　裁切 / 旋转 / 对比三工具（视口交互）

- 三工具互斥（同一时刻只有一个控制块）；线型 = `DESIGN.md` §14.8 的 4px「深-浅-深」，
  裁切主色井字 / 旋转辅色井字
- 裁切：四角 + 四边中点把手、框不可越界（**旋转后裁切时边界是斜画布边缘**）、
  右栏控制块（比例下拉 / 反转 / 取消比例限制 / 横纵输入 / 取消与确认）、三种退出方式（控制块取消 / 再点按钮 / `Esc`）
- 旋转：水平内接展示框（**框 = 成片边界**）、角度拉杆、画面内拉水平线自动摆正
- 对比：对分线 + 双侧外粗线把手、statusbar 左 `SOOC`（可换 issue）/ 右「当前结果」
- 工具内部的命中测试与拖动意图全在 Rust（前端不发坐标数学）

### M3-W6a　可选基础曲线与机型档案

> 2026-09-26 按人类的新方案修订：W6a/W6b 独立于 LUT 和 issue 实施；后者仍留在 W6c，须等待专门文档。

- 基础曲线是**可选**的显示域曲线，作用于 RAW 的 sRGB 编码之后、用户曲线之前；SOOC 编辑不使用。用户四通道曲线不受基础曲线选择影响。
- 仅当照片 EXIF 有完整的 `camera_make` + `camera_model` 才提供基础曲线功能；不按 ISO 或品牌兜底，不内置通用曲线。
- `app.db` 按机型保存多份拟合档案，`catalog.db` 的编辑栈保存所选档案 ID 与曲线快照。两个库均走现有版本迁移。旧编辑栈迁到「不使用基础曲线」。
- 曲线组件中增加只读叠线，下面显示当前基础曲线选择；列表仅有本机型档案和「不使用基础曲线」。新照片初始未选择。SOOC 侧隐藏整块功能。

### M3-W6b　显式自动调整与档案学习

- 工具栏的「自动调整」放在裁切左侧，只在当前 RAW 编辑有配对位图、机型信息齐全且未锁定时启用；不在载图、导入或每次进入编辑时自动计算。
- 点击后解码缩略级 RAW 与直出位图，在 Rust 中做亮度分位数拟合。以固定采样的亮度曲线加斜率差衡量同机型档案相似度；阈值内沿用旧曲线并累计样本数，阈值外新建档案。当前编辑栈保存曲线快照，旧照片的渲染不因档案变化而漂移。
- 同一次操作用保守的分位数规则建议曝光、反差和饱和度，并尝试镜头匹配。调整仅写基础曲线与现有影调、色彩、清晰度、镜头参数；用户曲线保持原样。
- 镜头选择仍显示所选镜头或「选择镜头配置文件」，打开弹窗时刷新推荐候选；不把选择按钮叫「自动识别」。
- 本波必须有单元测试和编译冒烟；真实 Windows 图像视觉效果与性能由人类 E2E 确认。

### M3-W6c　CUBE LUT + issue（多定稿）+ 编辑数据结构重整（原 W6 内容）

**结束时可看到**：LUT 能导入 / 分类 / 应用到画面 / 从栈里移除（含 LUT 预览 tile）；
一张图可以有多个 issue（定稿、命名、列表、浏览侧选显示哪个），`SOOC` / `RAW` / `latest` 都统一进 issue 模型。

- **LUT 兼容范围（人类 2026-09-25 定：只两种）**：
  * **`.cube`**（1D / 3D / shaper）—— 要处理对四件事：**Red 变化最快**的行序（Adobe 规范：
    等价 C 索引 `r + N*g + N*N*b`）、`DOMAIN_MIN` / `DOMAIN_MAX`（输入域不一定是 [0, 1]）、
    1D shaper + 3D 的组合（1D 先应用）、尺寸不定（17³ / 33³ / 65³ 都要能读；33³ 是创意 LUT 的常见档）；
  * **HaldCLUT**（PNG / TIFF）—— 同一条三线性插值，只换「从图里取表」的 loader。
    规格已核实（`rtengine/clutstore.cc::loadFile`）：**图边长 = level³、表边长 = level²**，
    512×512 = level 8 = 64³ 表；查表用三线性插值 + `strength` 与原值混合；
  * **明确不做**：`.3dl` / `.look` / `.csp` / `.cub` / `.itx` / `.mga` / `.spi*` / `.blut` 等影视私有格式；
    `.icc` / `.icm` 里的 LUT 归色彩管理（`FUTURE.md` C1），**不放 LUT 面板**。
- **导入 = 按目录**（人类 2026-09-25 定）：选一个目录，递归扫描**该目录及其下最多 2 层子目录**
  （共 3 层，覆盖「总目录 → 若干 LUT 子目录 → cube + 演示图」这种常见结构）。
- **LUT 封面**：导入时**一次性烘焙**（不是运行时生成）—— 口径见 `design/editor.md` §3.1.1
  （两档 768×576 / 384×288、cover 裁切、WebP 质量 80；来源 = 同名伴生图 / 内置样片套 LUT）。
  没有自带演示图的那些要靠**内置固定样片套 LUT**，所以 **LUT 求值引擎必须先能跑**。
- **存储**：LUT 本体**复制进** `%LOCALAPPDATA%\com.cthun.raybend\luts\`（自包含，不怕用户移动原目录），
  元数据（名称 / 分类 / 原路径 / 封面）进 `app.db`，封面 `*.webp` 与本体同目录。走既有迁移框架（§2.16）。
- 应用为管线一级、从栈中移除
- issue：定稿为新 issue、issue 列表（缩略图 / 名称 / meta）、浏览侧选择显示哪个 issue
- ❗ **同时做一次数据结构重整**（人类 2026-09-24 定，**这一波的核心价值**）：
  * **无损变更的变更集（edit changeset）**：数据结构与存储重新设计 ——
    W3 的 `latest` 只是「一个存储位」，要改成能装多 issue / 多版本、能算差异、能向前兼容的形态；
  * **调整后的缓存快照**：编辑结果的缓存键 / 失效语义 / 快照粒度一起重整（与变更集同批，
    否则「改了参数、缓存还是旧的」这类毒缓存会再犯一次）；
  * **issue 数据结构要打版本号**（`schema_version` 进库）；
  * **既有 latest 与旧 issue 数据必须迁移并保持前后编辑结果一致**（人类 2026-09-26 更新口径）；
    catalog 接入时走已有版本迁移框架，issue schema_version 入库。
- 目标：为后续功能（导出按 issue 取源、局部调整、版本对比）打**坚实的地基**，
  而不是在 W3 那版单存储位上打补丁

### M3-W7　收口

> 2026-09-26：开发与冒烟已收口，数字见 `specs/M3-W7.md`；真实照片、Windows 两档 DPI、交互帧率和颜色由人类真机验收。

- 性能基线：交互帧率（拉杆拖动 / 视口缩放）、大图编辑内存、编译与包体
- 人类真机验收清单（两档 DPI + 真实照片库）；Agent 只做冒烟与离屏像素证据
- 文档同步（`AGENTS.md` / `DESIGN.md` / `BROWSE.md` / `FUTURE.md`）；DoD 逐条核对

### M3 完成定义（DoD，草案）

- RAW 与 JPG 在同一界面完成无损编辑，原始文件零改写（可用校验和验证）
- 亮度/色彩/曲线/降噪/锐化/镜头校正/裁剪可用，交互性能可接受（真机裁决）
- CUBE LUT 可导入、可应用、可从栈中移除
- SOOC 与 issue：可定稿、浏览可切换显示、编辑内可对比；XMP 通道的 issue 表示有明确契约（`docs/issue-xmp-contract.md`）
- 编辑后缩略图/看图正确反映编辑结果

---

## M4 —— 可试用闭环：实时一致性 + 导出

> **2026-09-26 崔总已确认重排路线并授权开工；M4-W1 开发与冒烟完成，真实环境待验收。**
> 原四波路线保存在附录 B（历史参考，不是第二份任务清单）。本节是本次路线提案的唯一正文；
> 按实际开工波次写 `specs/M4-Wn.md`，不提前展开七波的实施步骤；当前工作单元见 `specs/M4-W2.md`。

**目标**：磁盘在程序外变化时不说假话；把 M3 的 SOOC / latest / 命名 issue 可靠地导成文件，
并复用同一出图入口送外部编辑器。四大模块在此闭合，随后进入 M5 发版准备。

### 重排基线（2026-09-26 开工前）：已经有什么、还缺什么

| 能力 | 已有事实（读过实现） | M4 要补的边界 |
| --- | --- | --- |
| M3 地基 | W0–W7 开发与冒烟收口；catalog v11、app v7（含 M3 最新 LUT 哈希收尾）；完整 profile、不可变命名 issue、latest 自动保存、LUT 私有副本、384/192/1920 快照均已接入 | 不重做 issue 模型；真实照片 / Windows DPI / 颜色 / 性能 / 原文件哈希验收仍未据此判为完成 |
| 实时一致性 | 目录展开重读、回焦点刷新、进目录读盘同步计数；`media::{scan,diff}` / `store::assets` 和手动重建已有 | 同步计数不等于 catalog 同步；需范围扫描、安全差分、变更通知、缓存与 FTS 联动 |
| 导出入口 | 工作流与 `view.flow.export`（`Mod+4`）存在 | 无 export workspace、引擎、预设或导出 worker；目前 App 的 export 分支会落到 import fallback |
| 出图 | RAW 独立 worker、线性源图、基础曲线、影调/色彩/镜头/降噪/LUT/几何已有 | CPU `render_develop` 返回 RGB8；锐化、LUT 批处理及成片几何仍接 RGB8。1920 AVIF 是预览，不能拿来做全尺寸或真正保留精度的 16-bit TIFF |
| 队列 | `app.db.jobs` 已持久化；缩略图 worker 有按 kind 认领、重试/退避、running 恢复；导入有自己的 run/item 恢复 | 共用任务操作尚嵌在缩略图模块；需就地抽取供两类 worker 复用，不能把缩略图 worker 当成通用导出调度器 |
| 界面底座 | `BrowseLeftColumn`、`PhotoGrid` / `Tile`、`TilesShell` / `TilesControlBar`、时间分组、`SplitStack` / `SplitHandle`、命令注册表已有 | 按照片聚合 issue 的单元与队列状态是数据/扩展槽；扩充这一套，禁止另造导出网格、目录树、状态栏或把手 |
| 格式依赖 | image 的 JPEG/PNG/TIFF/AVIF 已启用；有损 WebP 已改用官方 libwebp 包装并通过回读颜色测试 | 五格式不是五次调用 save：还缺可变质量、TIFF 精度、元数据写出与回读矩阵 |

**开工前风险（W1 已修复）**：全库重建曾忽略 `ScanOutcome` 的不完整信息，构造 `DiskFile` 时未带文件身份；
W1 已将扫描完整性、范围边界与文件身份纳入同一链路，避免权限失败被当作缺失或可靠移动丢失身份。
原子写入 `fs_atomic` 当前用于可覆盖的派生缓存；导出要在同一实现上增加不覆盖发布/重名预留能力。

### M4-W0（前置）　设计与行为口径确认

**现状**：`design/export.pen` 已出稿，Pencil MCP 已核对其主稿、运行中、工具栏、预设、浮层、空态六帧；
配套说明是 `design/export.md`，原版功能说明实际路径为 **`prompts/exports.pd`**。
外部编辑器弹窗尚未设计。本轮只核对结构与文案，不改画稿、不做 GUI E2E。

- 以原版口述为准：反 Ctrl 多选、每预设独立内存队列、开始/停止是本会话持续运行开关、空队列可运行、
  新入队在上/执行从下往上、完成项保留、全部停止、确认重置；旧计划的批次结束语义据此修订。
- 本节末尾的行为口径已随路线确认；同步到 `design/export.md`，画稿修订属于 W0，W1 不新增界面。
- 校正画稿与说明的尺寸：外壳紧凑 **36/48/36**、宽松 **44/62/44**；右列共用
  `--panel-w-right` **276/288**，不另建 export 的 360 宽；以 `tokens.css` 为准。
- 应用最小窗 **1200×800**：workspace 可用高约 **680/650**，扣 8px 把手后两窗等分仅
  **336/321**，不能同时硬设 384。沿用共享分段布局，按可用高夹取下限与保存比例；上下各自保留 statusbar。
- 需补到 Pencil 的状态：最小窗/宽松密度、超过四个 issue 与长中文名、失败/等待新任务、
  运行中重置、预设未保存；外部编辑器另出 `design/external-editor.pen` + 同名 `.md`。
- 原设计“未选预设就整个设置区空态”会挡住首个预设创建：改为未选时提供空白新建表单，开始按钮禁用。
- 修订只在确认后进行；W1 不新增界面可先开工，W2 和 W6 的界面必须各自先过设计确认。

### M4-W1　库与磁盘实时一致性

**状态（2026-09-26）**：开发与单元/冒烟完成；Windows debug 产物已重建并核对，真实环境待崔总验收。
实施结果见 `implementations/2026-09-26_m4-w1-realtime-consistency.md`，本波未实施导出。

**结束时可用**：在应用外增删/改名/移动照片后，重新进入目录能看到真实照片清单；权限失败和离线不误标全库缺失。

- 在既有扫描/差分/落库链上增加**范围契约**，进入/展开/重读只扫相关目录及其 `_RAW`；
  不把每次展开变成全库递归，不拿局部扫描结果与全库行直接做删除差分。
- 分清“完整扫描为空”“权限/读取失败”“取消/超深截断”“库离线”；只有完整覆盖范围才可标缺失。
  复用库 ID 解析；同卷身份确认的移动/改名保持 asset 与 issue，身份不明不强行猜配。
- 移动事件同时协调旧/新范围，必要时扩大一次有界扫描；同路径内容替换刷新文件身份与源签名。
- catalog 变更后统一刷新元数据、FTS、计数、小图/preview/当前清单；跨库计数失败可补偿，禁止静默假绿。
- 有界监听只触发重扫，攒批合并/同范围单飞；监视 `photos/`，排除 cache、DB、自家临时文件。
  `notify` 目前未引入，已采用 notify 8.2.0 的原生实现，见 `specs/M4-W1.md`。
- 单测覆盖空/单文件、位图+RAW 配对、Unicode/大小写、改名跨目录、并发导入、权限失败、离线恢复和触发风暴。

### M4-W2　导出工作区、issue 画廊与预设

**状态（2026-09-26）**：工作区、预设、issue 选择与快照入队开发完成；单元/导出启动冒烟通过，Windows debug 已重建并核对，真机待验收。实现记录见 `implementations/2026-09-26_m4-w2-export-workspace.md`。

**结束时可看到**：切到导出是真实三列工作区，可选目录、按 issue 多选、切展示范围、创建/修改预设，重启设置仍在。
尚未接入的导出运行按钮明确禁用并说明阶段，不伪造进度或完成状态。W2 交付 issue 入队与各预设私有内存队列。

- 左列复用 browse；双 tiles 各有 statusbar，复用分隔与缩放，尺寸/展示范围/分栏设备偏好独立持久化。
  右列沿用公共宽度，预设卡限高滚动、设置区可滚动、主操作钉底。
- 在**唯一 PhotoGrid/行模型/Tile**上加 issue 附加内容与状态扩展，数据适配器提供照片组与 issue 稳定身份；
  处理多 issue 导致的行高、可见页批量取稿、小图按需取图，避免逐张 IPC 或一次加载全库所有 profile。
- 画廊单选单元是 issue；点照片本体选其可见 issue；日/时间片选中覆盖未取回页。
  反 Ctrl 只改变手势到既有选择模式的映射；Shift 区间与日/时间片整段开关都走共的
  `lib/selection.ts`，不另写第二份选择算法（区间语义人类 2026-09-26 已改为「置为选中」，
  口径见 `BROWSE.md` §5.2.1–§5.2.2）。
- 预设参数保存 `app.db` 的版本化设备设置，稳定 preset ID 与名称分离；相同名称更新目标预设。
  名称、质量、最大边、目录存在/可写、命名模板实时校验；模板复用 `import::template`，中文与 `/` 子目录原样支持。
- 命令与热键同波接入；选中清除/全选优先扩充既有命令，避免再造同义命令。
  取图、元数据、全屏入口适配当前照片/issue，不能靠切稿改写 latest 来展示导出选择。

### M4-W3　统一全尺寸出图与真正的 16-bit TIFF

**结束时可验证**：一个 SOOC、latest 或命名 issue 能从原始源完整显影并写成原尺寸 TIFF；
浮点/16 位合成渐变证明输出未先坍缩为 256 级，裁切/旋转后的尺寸正确，原图哈希不变。

- 抽出共享的“解码 → profile 阶段 → 成片几何 → 输出缩放”入口，缓存、导出、外部编辑只配置消费需求，
  不复制曝光/曲线/LUT/镜头等算法。RAW 完整解码继续复用隔离 worker；严格导出不允许预览或占位图兜底。
- 高精度源图与管线后段保持浮点/16 位直到编码：影调、基础曲线、用户曲线、动态反差、
  降噪、锐化、LUT、镜头、几何、缩放均纳入；已有 RGB8 输出保留为该入口的量化适配，守住 M3 回归。
- TIFF = RGB16 无损成片；8 位 SOOC 没有凭空增加信息，16 位输出的价值是后续处理不再被中间 RGB8 量化限制。
  pipeline 行为变化时同步版本/缓存失效，不让新旧出图签名混用。
- 导出不吃网格 3:1 展示夹取，也不吃 384/192/1920 缓存尺寸。最大边空/0 = 成片原尺寸，仅缩小不放大。
- profile 与源选择按入队快照执行，不改变 browse/editor 当前 profile、撤销历史或选中状态。
  源文件执行前/后签名检查；文件改名按稳定身份解析，内容变更/源缺失清楚报错，不偷偷换基准。
- 基线为 SDR/sRGB；完整 ICC/显示器管理仍是 `REVIEW.md` R1-01 的待决项，不在本提案中自动升级为既定范围。

### M4-W4　五格式编码、命名与元数据

**结束时可验证**：同一出图入口支持 JPEG / TIFF / PNG / WebP / AVIF，质量与尺寸真实生效；
模板建子目录、重名不覆盖，逐格式可回读规定的元数据。

- 范围唯一来源仍是 `IMAGING.md` §1：导出五种，RAW 不导出文件，HEIC 不进入导出格式，JXL 后续。
  TIFF 无损 RGB16、PNG 无损（质量行禁用）；JPEG/WebP/AVIF 用同一质量参数契约，各自适配编码器。
- AVIF 抽取现有 ravif 编码能力的可配置参数，缓存仍使用既定质量 90 / 4:4:4；
  WebP 复用 W6c 修复后的官方 libwebp 路径，不换回曾导致色度丢失的实现。
- 复用导入模板及重名 `_01` 规则；扩充现有原子写入以支持导出目标预留与不覆盖发布。
  拦目录穿越/绝对路径/设备名/非法路径/源目标相同，跨预设竞争也不能互相覆盖。
- **元数据不能只列标题**：开工先做字段×格式矩阵并核实现有编码器写入接口。
  覆盖拍摄 EXIF、关键词/IPTC、版权；修正 Orientation=1 与成片尺寸、移除陈旧内嵌缩略图；
  以中文字段与真实回读单测验证。某格式写不出承诺字段时，明确缺口后讨论适配/依赖，不能静默丢弃。
- 旧计划已有预设导入/导出与可预览要求：本波交付版本化预设序列化/校验及模板目标名、尺寸、元数据策略预览；
  对应入口须先补设计，不另建第二个导出对话框。机器路径导入后重新校验，不能直接开跑。

### M4-W5　内存级多预设队列与真实后台运行

**结束时可用**：W2 已入队的 issue 打开处理开关后按顺序执行；多个预设有独立队列，
切预设/目录/工作流不影响后台任务。退出应用丢弃队列和完成记录，运行开关不持久化。
**2026-09-26 崔总补充定案覆盖旧持久队列安排。**

- 队列、完成记录及 off/on 开关全部内存级；Rust 独立执行线程管理，有界并发，前端仅订阅状态。
  不将导出队列写入 jobs/数据库/localStorage，不实现重启恢复；预设参数与设备展示偏好仍持久化。
- 控制状态与条目状态分开：空队列开启后等待，停止保留本会话队列，再开始继续。失败不阻断其他条目。
- 入队单位为 issue（库/asset/issue + profile 快照），同照片不同 issue 可同时存在；
  每预设私有队列，新项在上，最老待处理项先执行，完成记录保留到重置以防重复。
- 每张预设卡有内容时显示 `<待完成>/<队列总数含完成>`；待完成含 pending/running/failed。
  flowbar 导出文字仅在有条目实际 running 时复用 import 的从左向右闪烁；on + 空队列不播放。
- 任务保存入队时 profile/源签名/预设参数快照；可靠移动按稳定身份解析，源变更或缺失报错。
  同一会话内文件发布/回调幂等，原子写入与无覆盖预留复用 W4；不因取消或重试生成重复产物。
- 多预设开启使用共享有界执行预算，与编辑/缩略图争用时保持 UI 响应。
- 全部停止作用于全部预设；确认重置先安全停工、隔离旧回调再清队列，不删磁盘产物。
  失败清单、单项/批量重试复用 Dialog；不另加历史第三列。

### M4-W6　外部编辑器：纯单向输出

**结束时可用**：浏览/编辑当前结果能经同一引擎写成 16-bit TIFF，再打开选定编辑器；不自动取回。
原 M4-W4 的已确认范围完整搬到本波，不重新讨论其单向定位。

- 前置设计 `design/external-editor.pen` + `.md`；应用选择（默认上次）/多应用登记/路径记忆/进度/取消与确定。
  无登记时确定禁用；“+ 应用”常驻右侧，反复发现与勾选加入，按 exe 路径判重，已登记标“已添加”。
- Windows 可插拔发现 adapter：候选表 × HKCU/HKLM/WOW6432Node App Paths + 已知路径 glob；
  不全盘扫描/全系统枚举。9 个首批候选沿用原定案：Photoshop、Affinity Photo、Affinity（Canva 整合版）、
  GIMP、Krita、PaintShop Pro、Topaz Photo AI、RawTherapee、RapidRAW。
- 路径 metadata 过滤卸载残留；执行时失效报错并自动清登记，默认回退下一项；可预检并 toast。
  便携版手动 exe 兜底沿用 `FUTURE.md`，不把普通模式变成找启动文件。
- TIFF 出图复用 W3/W4，含当前 profile 快照与完整几何；写出成功但唤起失败要给出已保存文件路径。
  不改原 RAW、不自动回收、不新增 Gallery flow、不登记 catalog 产物。
- 应用登记存 `app.db.settings`；ShellExecute/open_path 参数与 MSIX/应用执行别名的兼容边界要前置核实，
  不能以“exe 路径存在”推断九应用都能启动。`tauri-plugin-opener` 尚未安装，按已有候选方案评估后接入。
- 统一命令接 browse/editor 上下文，默认键明确留空（弹窗、秒级出图、非高频）。
  已装软件的调用可做进程级冒烟；各软件真正打开/显示 TIFF 由崔总真机确认。

### M4-W7　整条闭环收口与试用基线

**结束时交付**：四大模块的开发/冒烟证据、故障恢复证据、基线与一份集中真机验收清单；不生成发布安装包。

- 小规模合成单测常态秒级；1 张与 **1000 张小合成图**做显式批量冒烟，记录耗时/内存/失败重试/恢复，
  大尺寸与大库基准单独手动入口，不能拿小图数字推断真实 RAW 性能。
- 系统覆盖源离线/替换、目录权限、磁盘写满模拟、同名竞争、停止/重置竞态、缓存缺失、错误载荷与高版本。
  Windows 与 WSL 的文件发布/重名语义都要有单测；原文件哈希保持不变。
- 前端质量门、核心/外壳单测、迁移演练、Windows 主程序 + RAW worker 构建与 `check:win`；
  共享网格/滑块改动守住节点身份与 fit context 两面回归，不用 GUI 冒烟代替视觉验收。
- 更新规格与实施记录；`FUTURE.md` 当前外部编辑引用已同步为 W6，历史决策保留原编号与日期。
- 崔总真机验收：真实照片三形态、多 issue 五格式、16-bit TIFF、颜色/元数据、最小窗/两密度/DPI、
  多队列长时间运行、外部编辑器、原文件哈希。M3 尚未验的相关项一起核对。

### 已确认的行为口径（2026-09-26 崔总指示开工）

| 项 | 已确认口径 | 影响 / 所属波次 |
| --- | --- | --- |
| 取消选中键位 | 默认 **Esc**，复用现有 `edit.clearSelection`；不改输入控件原生 Ctrl+C | 原口述 Ctrl+C 与复制习惯/输入态冲突，但当前注册表未发现“复制”命令；不是声称已有注册冲突。送入队列可用 Enter，仅画廊有焦点时；运行开关建议 Mod+Enter，其余留空说明理由（W2/W5），实施时过冲突与保留键检测 |
| 队列“列表模式” | 默认小 tile，提供列表切换 | 原口述同时要求小 tile 与列表模式，依设计稿保留开关；不是强制只画行（W2/W5） |
| 未保存设置 | 开始禁用；先显式新增/更新预设，不代为保存；停止始终可用 | 对齐“预设不保存不能导出”，避免按钮偷偷覆盖设置；运行时配置草稿不改变旧任务（W2/W5） |
| 快照与预设更新 | **入队锁定 profile + 已保存预设参数**；更新只影响以后入队项，已完成项不自动重导 | latest 会变，不能到执行时再读最新参数。latest 配置变化可作为新版本入队；同一命名稿/同一 latest 配置重复加入去重（W3/W5） |
| 空队列/停止/重置 | 开启后等新任务；停止=保留队列；重置=停所有预设后清记录；重启恢复记录但不开跑 | 本文没有在 on/off 之外增加第三颗“暂停”按钮；已有停止/开始承载暂停/继续语义（W5） |
| RAW-only 原始项 | `全部`显示 **RAW 原始渲染项**；`仅 SOOC`仅真实位图；`仅定稿`含非空 latest 与命名稿 | 现画稿只列 SOOC/latest/命名稿，会漏未编辑纯 RAW。配对照片默认不额外列 RAW 原始项；永不导出 RAW 文件（W2/W3） |
| 大量 issue 与统计 | issue 小片换行，不把四个当上限；状态栏“照片数/选中 issue 数”，队列按输出项计数 | 必须与共享虚拟行模型一起支持，长中文名省略且可读全名；不能隐藏多出的 issue（W0/W2） |
| 预设管理、导出历史 | 保留旧计划的预设导入/导出和预览；增加最小删除入口（有任务时阻止删除或确认），复制/重排后置；历史先以任务记录查询/错误清单交付，独立历史 UI 后置 | 新增/导入/删除入口先补稿；“从资产追溯”可见入口若必须本轮做，补设计后归 W5（W0/W4/W5） |
| 画稿规范 | 两窗下限随可用高夹取；右列沿用公共 276/288；禁用主按钮用中性表面 | 修正过时尺寸，不因一张示意图改变全工作区宽度；实际视觉由崔总验收（W0/W2） |

**范围控制**：本轮不自行采纳 `REVIEW.md` 尚未批准的 ICC、集合/智能集合、XMP 写回、
旗标持久化等扩围；保持原近期边界。**Shift 选择语义已于 2026-09-26 由人类批准并落地**
（属并行的选择逻辑收敛，不占本轮 M4 范围；见 `BROWSE.md` §5.2 与 `lib/selection.ts`）。
R1-08 出图质量基线与 W7 真机样片对照相关，
但对照工具/样片与达标阈值仍需定案。HEIC 解码仍是已有未完成导入能力，不能因 M4 五格式导出而标成完成。

### M4 完成定义（DoD，2026-09-26 已确认）

- 应用外新增/删除/同卷移动/改名后，重进目录得到真实 catalog 清单；不完整扫描/权限失败/离线不会误标全库缺失。
- 画廊按 issue 选择，支持非空 latest / 命名定稿 / SOOC 与纯 RAW 原始渲染项；显示所选稿不写回 latest。
- 五格式能从正确原源/profile 出图；TIFF 是保留管线精度的 RGB16；最大边、几何、朝向、模板、元数据均有回读证据。
- 每预设队列独立，完成留存/去重、持续开启、停止继续、失败重试、确认重置、重启恢复与两处崩溃窗口均可重复验证。
- 一张与 1000 张合成导出可完成，资源有界且内存不持续增长；原文件零改写，失败不打断其它条目。
- 浏览/编辑能经同一 TIFF 引擎送外部编辑器，多应用记忆/发现/失效清理成立；真实软件打开结果归真机验收。
- 四大模块开发与冒烟完成，相关 M3/M4 真机验收结论单列；M5 再做安装、更新、发布，推送/发布仍由崔总执行。

---

## M5 —— 分发与发布（四大模块完成即发版先用）

**目标**：让不装开发环境的人也能安全安装、升级与求助。**定位（人类 2026-09-21 定）：导入 / 浏览 /
编辑 / 导出四大模块功能完成后就可以发版先用**——开源项目持续迭代，不追求功能齐全再发布。
品牌、闪屏、Windows 构建路径、官网与 Pages 工作流已提前完成；本阶段只做仍缺的发布产品化。

### M5-W1　安装包与首启

- NSIS / MSI 安装包；版本信息、文件关联（可选）、卸载时保留用户数据
- 首次运行体验：欢迎、建库/打开库向导、隐私说明与真实目录权限错误反馈
- 发布构建明确处理 spike 诊断页是否保留，不能无意随包发布

### M5-W2　自动更新

- 稳定/预览通道、签名校验、下载失败与安装失败回滚
- **注意**：AGPL-3.0 项目不要让更新机制强制只接受官方签名；第三方构建必须可以替换更新源或关闭更新

### M5-W3　文档、许可与支持

- 用户文档、常见问题、错误码与诊断包说明、捐赠入口
- `THIRD-PARTY-NOTICES.md` 与应用内许可页完整覆盖 LGPL/GPL/AGPL 组件及来源
- 官网下载/教程从占位内容切到真实版本，但站点发布仍由 GitHub Actions 与人类控制

### M5-W4　代码签名与正式分发（**由人类执行**）

- 证书获取与签名流程、SmartScreen 信誉、发布页与校验和；干净 Win10/Win11 真机安装/升级/卸载 E2E
- 发布物生成、tag、push、上传全部由人类执行；Agent 只准备脚本、校验与说明

### M5 完成定义（DoD）

- 干净的 Win10/Win11 机器上双击安装即可用，卸载不误删照片与 catalog
- 自动更新从 vN 到 vN+1 成功且失败可回滚/恢复
- 第三方许可、隐私说明、诊断与支持路径完整

---

## 2. 决策记录（开工后逐条填充）

| 日期 | 决策 | 结论 | 依据 |
| --- | --- | --- | --- |
| 2026-09-15 | 许可 | **AGPL-3.0-only**（不闭源盈利） | 用户决定；使 rawler(LGPL-2.1) 与 darktable/RawTherapee(GPL-3.0) 的算法均可合法使用，并额外解锁 RapidRAW 与 zenraw（均 AGPL-3.0）的代码复用 |
| 2026-09-15 | 渲染架构 | 原生 wgpu + 透明挖洞（方案 B） | 用户决定；RapidRAW 实证性能 |
| 2026-09-15 | 存储架构 | 全局库 + 每仓 catalog（分层） | 用户决定；可移动/隔离损坏/按仓备份 |
| 2026-09-15 | 缓存位置策略 | `%LOCALAPPDATA%` 默认 + 可按仓改为随仓 | 用户决定 |
| 2026-09-15 | UI 框架 | Solid 1.9.x，不引入 SolidStart | 用户接受；Solid 2 仍 RC 且 Start 面向 v1 |
| 2026-09-15 | Tauri 3.0 迁移准备 | 分层 + 不碰 `tauri::Wry`/feature flag | Tauri 3 alpha 的运行时 crate 化改动 |
| 2026-09-15 | 色彩管理 | 预留，第一阶段不做 | 用户决定 |
| 2026-09-15 | 组件原语 | **Ark UI**（`@ark-ui/solid`）——`splitter` 与 `tree-view` 只有它提供 | 实测对比见附录 A.10 |
| 2026-09-15 | 图标体系 | **Tabler**（`@tabler/icons-solidjs`） | 实测对比见附录 A.10 |
| 2026-09-20 | M2 后路线 | **先实时一致性与导出闭环，再做完整组织检索；原 M5 压缩为 3 波** | M1/M2 已提前交付标签、FTS、缓存 GC、备份演练、持久化任务与 RAW 隔离；原顺序会让地图/相似图挡住首个可试用闭环（已被 2026-09-21 再重排取代） |
| 2026-09-21 | **编辑提前**（人类定案） | **M3 = 编辑（GPU 显影工作台：RAW/JPG 无损编辑 + 常规调整 + CUBE LUT + SOOC/issue）；原 M3（一致性+导出）顺移 M4 且导出按 issue 口径；原 M4/M5 写入 FUTURE；原 M6 → M5，四大模块完成即发版先用** | 开源项目持续迭代可接受，第一阶段重点打磨核心功能；editor 多 issue 是重点设计，没有它的导出是空中楼阁；未设计界面的模块开工前必须先做界面设计环节（人类 2026-09-21 定） |
| 2026-09-24 | M3 波次再切（人类定案） | **CUBE LUT 与 issue 从 W4 拿出，与新 W6（编辑数据结构重整）合并；原 W6（收口）顺移 W7** | LUT 与多 issue 都要挂在「编辑栈 / 变更集 / 渲染缓存」这套地基上；W3 的 `latest` 单存储位只是把地基跑通的第一版，与其在 W4 打补丁，不如在 W6 一次性整到位（变更集数据结构与存储 + 调整后缓存快照 + issue 打版本号；既有 latest 与旧 issue 数据须迁移并保持结果一致） |
| 2026-09-24 | 外部编辑器（人类定案） | **纯单向输出进 M4-W4；候选表首批 9 个（含 RawTherapee/RapidRAW）；应用发现走 Windows 系统级接口（可插拔 adapter），不做全系统枚举** | 取回链路跑不顺（外部编辑器不配合，LR 自身的 Edit-In 回收都长期故障），单向先行；我们 RAW 开发不做极致（至少现在完全不想），专业 RAW 开发送外部 |
| 2026-09-25 | Windows 编辑视口呈现 | DX12 + DirectComposition visual + Opaque；最小 `PresentationAdapter` 预留 macOS/Linux 接入 | 同 HWND 的 GPU 与 Tauri/GDI 绘制竞争；隔离呈现层后原越屏场景真机确认通过，详 `docs/native-viewport-coordinate-guide.md` §8 |

---

## 3. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-15 | 初版：确立 7 个 milestone 与波次划分、决策记录表 |
| 2026-09-15 | 新增 §0.1 规划纪律（一次规划只覆盖一个工作单元）；M0 章节收敛为工作单元索引表；原一次性铺开的 M0 波次规划归档到**附录 A** |
| 2026-09-15 | **M0 重构**：spike 由「一次性门禁」改为「按模块按需前置」——M0-4/5/6/3 服务于 M1，M0-2 后移到 M2 前；新增「设计阶段」章节 |
| 2026-09-17 | **官网支线落地（不在 milestone 序列内）**：`website/` 首页（双语 / 下载区 / 教程占位）+ `.github/workflows/website.yml`（push master / 正式版 release / 手动 → GitHub Pages）。**不属于应用本体的进度**，不给 milestone 排期；方案见 `specs/website-homepage.md`，约定见 `website/AGENTS.md`，待人做的事见 `website/ASSETS.md` |
| 2026-09-20 | **M2 后路线重新基线化**：M3 改为「实时一致性 + 导出」以尽快闭合首个可试用流程；原 M3 与 XMP 合并为 M4；M5 按已提前完成的底座从 5 波压成 3 波；新增 §1.1 明确“已完成地基 ≠ 产品能力完成”的边界 |
| 2026-09-21 | **M2 结案 + 路线再重排（人类定案）**：新增 §1.2；M3 改为**编辑（GPU 显影工作台）**并设 W0 界面设计前置；原 M3（一致性+导出）顺移 M4，导出按 issue/SOOC 口径重写；原 M4（组织检索）与原 M5（多仓/缓存/数据安全，含数据备份支持）移入 `FUTURE.md` I/J 节；原 M6 → M5，定位「四大模块完成即发版先用」；「设计阶段」新增硬规矩：未设计界面的模块必须先过界面设计环节（现有设计仅导入与浏览） |
| 2026-09-24 | **M3 波次再切（人类定案）**：CUBE LUT 与 issue 从 W4 拿出来，与「编辑数据结构重整」合并为新 W6；原 W6（收口）顺移 W7。W6 同时重整：无损变更的变更集数据结构与存储、调整后的缓存快照；issue 数据结构打版本号（现阶段既有 latest 与旧 issue 数据须迁移并保持结果一致） |
| 2026-09-24 | **外部编辑器进 M4（人类定案）**：M4-W2 导出格式清单已含 **TIFF**（16-bit 无损，外部编辑前置）；新增 **M4-W4「外部编辑器：纯单向输出」**（候选表首批 9 个 + 应用发现可插拔 adapter + 弹窗交互规格：默认上次应用 / + 应用发现 / 按路径判重 / 失效报错并自动删登记）；M4 波次 3 → 4 |

| 2026-09-26 | **M4 重排提案（待确认，未实施）**：依据 export 画稿/说明、`prompts/exports.pd` 与 M3-W6c/W7 实际代码，将原四波拆为 W0 口径确认 + W1 一致性 / W2 工作区与预设 / W3 高精度全尺寸出图 / W4 五格式与元数据 / W5 持久队列 / W6 外部编辑 / W7 收口；原路线归附录 B；不自行采纳 REVIEW 待决扩围 |
| 2026-09-26 | **W2 补充定案与实施**：issue 是入队单位；各预设独立队列与处理开关全部内存级，参数与展示偏好持久化；卡片显示待完成/含完成总数；实际 running 才触发 flowbar 文字闪烁。W2 交付选择/快照入队与预设，编码/后台线程仍属 W3–W5；取消选择复用 Esc |

---

## 附录 A：M0 未开工波次的此前规划（归档，仅供参考）

> **为什么在这里**：2026-09-15 的第一次规划把整个 M0 一次性铺开（7 个波次 + 后续设计阶段），
> 实施中暴露两个问题：① 计划文件范围过大，勾选状态随变更失真；② 进度 tracker 挂着大量未开工条目。
> 按 §0.1 的规划纪律，那次规划的 **M0-1 部分已抽出为 `specs/M0-1.md`**，其余部分归档在此。
>
> **⚠️ 不要把下面当成待办清单直接开工。** 它们只记录当时的思路与检查项；
> 真正开工某个波次前，要按**当时的实际情况重新规划**（技术前提、依赖、优先级都可能已变）。
> 完整原稿：`git show cfb84a2:plans/M0.md`

### A.1　平台验证分工（仍然有效）

| 波次 | 在 WSL/Linux 做 | 必须在 Windows 复测 |
| --- | --- | --- |
| M0-2 渲染 | 代码与架构原型 | ✅ 2026-09-19 spike 真机闭环；当时只跑通 Vulkan。2026-09-25 进一步验证 DX12 + DirectComposition 并作为 Windows 产品默认；强制不可用后端时不回退仍属预期 |
| M0-3 RAW | 全部（算法与解码平台无关） | MSVC 编译 + 解码结果抽查 |
| M0-4 存储 | 全部（SQLite 平台无关） | 文件身份（`FileIdInfo`）与路径规范化 |
| M0-5 缩略图 | 全部 | 文件系统行为抽查 |
| M0-6 前端 | 组件开发与迭代 | ⏳ **帧率与内存数字**仍缺（需一个真实大库；2026-09-19 真机只做了主观确认） |

**前置事实**：WSLg 无硬件 GPU（软件渲染回退）→ GPU 相关结论只能在 Windows 取得。

### A.2　M0-2　渲染可行性 spike ★（最高风险）

代码落位：`crates/raybend/src/render/`（wgpu 上下文、视口变换、WGSL）+ `src-tauri/src/spike_viewport.rs`

**窗口策略**：不动主窗口。用 `WebviewWindowBuilder` 另开 `label = "spike-viewport"` 的调试窗口（`transparent: true`），即使透明挖洞彻底失败也不影响主窗口与设计工作。

当时的检查项：

- wgpu surface 能否挂到 Tauri 窗口（`raw-window-handle` 版本一致性 —— 最常见的集成失败点）
- 透明挖洞成立（无 alpha 预乘色偏、无闪烁）
- DPI 与缩放（100%/125%/150% 下 1 图像像素 ↔ 1 物理像素）
- 多显示器（DPR 不同屏间拖动）
- 窗口状态（resize / 最大化 / 全屏 / 最小化恢复）
- 坐标同步（快速缩放是否漂移 —— RapidRAW 踩过「差 1 像素就图跟不上鼠标」的坑）
- 命中测试（透明区是否吞鼠标事件；`pointer-events: none` / `set_ignore_cursor_events`）
- 后端回退与 device lost（`WGPU_BACKEND=dx12|vulkan|gl`；`device.destroy()` 模拟）
- 性能基线（6000×4000 纹理 + 平移缩放的帧时间）
- 结论：能做 / 不能做 / 规避手段；是否需启用退路（独立子窗口 / Tauri CEF）

### A.3　M0-3　RAW 解码 spike

代码落位：`crates/raybend/src/raw/`（`trait RawDecoder` + `RawlerBackend` + worker 进程入口）

- rawler 0.8.0 在 WSL 与 Windows MSVC 下均编译通过（其 `multiversion`（SIMD target-feature）/ `memmap2` / `jxl-oxide` 依赖是最易出平台问题处）
- 格式矩阵：CR3 / NEF / ARW / RAF / RW2 / ORF / DNG 各跑通
- 路径 A（内嵌预览提取）：耗时目标 <10ms/张、内存、拿到的是 JPEG 还是 RGB
- 路径 B（完整解码）：黑电平/白平衡/最简去马赛克 → 线性 f32 RGB；记录耗时与内存峰值
- worker 进程隔离：注入 `panic!` 与损坏文件，验证主进程不受影响（**release profile 是 `panic = "abort"`，必须用 release 各跑一次**）
- 决定 rawler 用上游还是 RapidRAW fork（先用上游）

### A.4　M0-4　存储基准 spike

代码落位：`crates/raybend/src/index/`（rusqlite 连接管理、单写者 actor、迁移骨架）

- 确认 `rusqlite` bundled SQLite 版本 **≥ 3.34**（`trigram` 分词器所需）
- 10 万行合成 catalog：批量插入耗时（每批 500 条 vs 单条）
- 查询基准：日期区间 / 机型 / 评级筛选；`ORDER BY` + 分页（**keyset vs OFFSET**）
- FTS5 trigram 中文实测：中文关键词 / 子串 / 中英混合 / 单字查询
- 跨仓查询：`ATTACH` 3 个库 + `UNION ALL` 延迟
- 并发压测：单写者 actor 下模拟「导入写 + 缩略图写 + 读查询」
- 备份演练：`VACUUM INTO` 耗时与体积、`integrity_check`、故意损坏后的恢复
- Windows 侧抽查：文件身份（`GetFileInformationByHandleEx(FileIdInfo)`）与路径规范化

### A.5　M0-5　缩略图吞吐 spike

代码落位：`crates/raybend/src/thumbnail/`

- 从 M0-3 样本生成 512px 缩略图，端到端计时（5000 张规模）
- 编码格式对比：**QOI / WebP(lossy) / JPEG** 的「编码耗时 / 体积 / 前端解码耗时（`createImageBitmap`）」
- 并发度扫描：物理核数 N、N-1、N/2 三档吞吐曲线
- 存储形态对比：**SQLite BLOB vs 两级分片文件** 的随机读延迟
- 决策输出：编码格式、默认并发度、5000 张导入端到端估算

### A.6　M0-6　前端骨架与虚拟化 spike

代码落位：`src/`（Solid + Tailwind）

- 手写最小虚拟化网格（固定单元格 + 可见窗口 + overscan），**不引入任何组件库**
- 10 万条合成数据：滚动帧率、内存占用、`scrollTop` 抖动处理
- 验证「占位 tile → 位图到位后替换」与不可见时取消解码请求
- 对照实验：`content-visibility` / `will-change` 的有无对帧率的影响
- 决策：**是否需要 canvas tile**（按 `FUTURE.md` §C3 的触发条件；结论写回该文件）
- 产出可复用组件 `VirtualGrid`（M2 直接使用）
- **在 Windows（WebView2）上复测**帧率，以 Windows 数字为准

### A.7　M0-7　架构定稿与移交

- 依据 spike 结论敲定 crate 边界：是否现在拆出 `raybend-raw` / `raybend-cache` / `raybend-jobs`
- 敲定 `raybend-ipc`（specta）契约方式、错误模型、Rust↔TS 类型生成管线
- 回填 `AGENTS.md` 架构章节与版本基线（wgpu 版本）
- 更新 `FUTURE.md`：spike 结论（CEF 退路、canvas tile 判定）置为已评估
- 产出**设计阶段输入清单**：窗口尺寸档位与密度、令牌初稿、视口交互范围、主界面信息量

### A.8　M0 剩余完成定义

- 六个 spike 全部有可复现命令 + 数据 + `implementations/` 记录
- 四个架构未知均有明确结论（渲染成立与否 / RAW 可用性 / 存储目标达成与否 / 缩略图编码与并发）

（已达成部分：WSL 与 Windows 两侧均能起窗口、`check`/`clippy` 全绿；许可/中文名/工具链纪律/去个人化已落实；无模板残留与未使用依赖。）

### A.9　UI 设计阶段（M0 之后、M1 之前）

界面截图 + 逐块口述 → 用 Pencil 在 `design/` 绘制 `.pen` + 同名 `.md` → 同时定案组件库与图标库（已定，见 A.10）。

### A.10　UI 层选型信息（2026-09-15 实测）

**组件层 —— 已定 Ark UI：**

| 维度 | Kobalte | Ark UI |
| --- | --- | --- |
| npm | `@kobalte/core` 0.13.14（另有 2.0.0-alpha.2） | `@ark-ui/solid` 5.39.2 |
| 最近发布 | 2026-09-07 | 2026-09-13 |
| 组件目录数 | 59 | 61 |
| GitHub | 1.85k ★ / 126 open issues | 5.39k ★ / 9 open issues |
| 独有 | 颜色选择器家族、`rating` 星级、`number-field`、`time-field`、`menubar` | **`splitter`**、**`tree-view`**、**`date-picker`**、`tags-input`、`scroll-area`、`drawer` |

关键差异：最需要且最难自研的 `splitter`（可拖拽分栏）与 `tree-view`（标签层级树）**只有 Ark UI 提供**。

**图标层 —— 已定 Tabler：**

| 维度 | Lucide | Tabler | Phosphor | Material Symbols |
| --- | --- | --- | --- | --- |
| 数量 | ~1,600 | **6,184（5,130 描边 + 1,054 填充）** | 1,248 × 6 字重 | 2,500+ |
| 填充态 | ❌ 官方不支持 | ✅ 独立 filled 文件 | ✅ fill / duotone | ✅ FILL 可变轴 |
| Solid 包 | `lucide-solid` | `@tabler/icons-solidjs` | 无 | 无 |

Tabler 是唯一同时满足「数量大 + 描边/填充成对 + MIT + 有 Solid 包 + 活跃」的选项。

**设计令牌的视觉规格见 `DESIGN.md`**（配色、双主题、无边线风格）。


---

## 附录 B：M4 重排前的四波路线（历史参考，不是待办清单）

> 保存 2026-09-26 本次重排前的正文，便于核对原已确认范围。上文七波路线已于 2026-09-26 确认；
> 本附录不用于排期、进度或新实现。旧编号在历史决策与 FUTURE 中暂保留，确认新路线后同步引用。

## M4 —— 可试用闭环：实时一致性 + 导出

**目标**：磁盘在程序外变化时不说假话；以 M3 产出的 issue/SOOC 为口径把选好的照片可靠导出——
四大模块（导入/浏览/编辑/导出）在此闭合，之后即可进入发版准备（M5）。

### M4-W1　库与磁盘实时一致性

- 复用现有 `media::{scan,diff}` 与 `store::assets`，补正式编排：进入/展开/重读目录时后台重新读盘，
  少的补入、多的标记缺失（不物理删记录），移动/改名保持资产身份；库离线时只降级，不误判缺失
- 增量监听与攒批：监听只负责触发重读，真相仍来自重新扫描；限制递归范围与触发频率，避免扫描风暴
- 缩略图、FTS、相片/图片计数与 catalog 变化保持一致；为外部增删、拔盘/插回、权限失败补可重复测试
- 这是原 M1-W3 未落到产品运行时的尾项，也是 `FUTURE.md`「清点人头」的正式排期

### M4-W2　导出引擎与预设（按 issue 口径）

- Rust 侧统一导出入口：尺寸/缩放、质量、命名与目录模板、SDR 基线；前端不写像素算法
  * **格式范围以 `IMAGING.md` §1 为准**（唯一事实源）：位图**导出 5 种**
    （JPEG / TIFF / PNG / WebP / AVIF）、**导入 6 种**（+HEIC）、**RAW 只进不出**、
    JXL 排在后面（导入导出全流程，不替换缓存）、**issue 快照恒 AVIF**；
  * **AVIF 编码器已经在 M3-W3 引入**（缓存图就用它，`image` 的 `avif` feature / ravif）——
    导出只是复用它，不再新增依赖；
  * ⚠️ 「导入 avif / heic」需要另接解码器（`image` 只有 avif 编码器、完全不支持 HEIC）——
    `IMAGING.md` §1.4；
  * **TIFF 是 16-bit 无损**，兼作外部编辑的前置格式（见 M4-W4）
- 导出来源以 **issue / SOOC** 为第一公民：导出选中的 issue（含编辑栈渲染）或 SOOC 原片；
  RAW / 位图 / 位图+RAW 三形态的来源选择沿用既有契约，身份从「照片」细化到「issue」
- 元数据保留策略：EXIF/IPTC/关键词/版权；RAW 原文件永不改写；水印不阻塞首批闭环
- 导出预设可预览、可导入导出；复用现有命令注册表、Dialog、持久化 jobs 与错误清单，不另造一套任务模型

### M4-W3　导出工作区、批量队列与试用收口

- **开工前置**：`design/export.pen` + `.md`（画廊选 issue、预设、队列与错误清单——未设计，先过界面设计环节）
- 导出工作区（画廊式选择导出哪些 issue，`FUTURE.md`「issue 与 SOOC」的原始设想）；
  批量队列可暂停、继续、取消、失败重试，结束后给出摘要与可导出错误清单
- 导出历史可从资产/issue 追溯；文件同名、目标不可写、磁盘写满、源文件途中离线等失败不打断整批
- 1000 张合成/小样本的 Agent 冒烟与内存基线；真实照片的画质、色彩、元数据与使用手感由人类 E2E
- 汇总尚未验收项，形成首个可长期试用的基线；**不在本阶段做安装包发布（那是 M5）**

### M4-W4　外部编辑器：纯单向输出（人类 2026-09-24 定案）

**结束时可看到**：浏览/编辑里能把当前 issue「在外部编辑器中打开」—— 渲染成 16-bit TIFF
存到用户指定位置并唤起外部编辑器；**单向，不取回**（取回链路跑不顺，见 `FUTURE.md`「Gallery / 外部编辑器」）。

- **开工前置**：弹窗界面先过设计环节（`design/external-editor.pen` 或并入 `export.pen`，开工时定）；
- **弹窗**：应用选择（默认选中上次用的；**支持登记多个应用**）+ 导出路径（记忆上次目录）+
  取消/确定（取消在左，§11.5）；**无已登记应用时确定置灰**；
- **弹窗内「+ 应用」**：常驻应用选择最右侧，点击触发**应用发现**，发现结果给用户勾选加入；可反复发现；
- **应用发现**：候选表 × Windows 系统级接口（**可插拔 adapter**，`FUTURE.md`「编辑器发现」）——
  `App Paths` 注册表（HKCU/HKLM/WOW6432Node）+ 常见路径 glob；探测到路径顺手 `fs::metadata` 验证，
  卸载残留直接过滤；**不做全系统枚举**；
- **边界**：登记判重按 **exe 路径**（发现列表中已登记的显示「已添加」）；
  确定时应用 exe 已不存在 → **报错并自动删除该登记**（删的是默认项则回退下一个，删空则确定置灰）；
  弹窗打开时可静默预检失效登记（toast 告知）；
- **候选表首批 9 个**（人类 2026-09-24 定）：Photoshop / Affinity Photo（1.x+2.x 合并识别）/
  Affinity（Canva 整合版；独立 Designer·Publisher **不进**）/ GIMP（2.10+3.x）/ Krita /
  Corel PaintShop Pro / Topaz Photo AI / **RawTherapee** / **RapidRAW** ——
  后两个进入的理由：**我们的 RAW 开发不做极致（至少现在完全不想），专业 RAW 开发送外部**；
  逐条 exe 名与参数模板见 `FUTURE.md` 调研矩阵，落地时逐个实测；
- **送出的是渲染结果 TIFF**（不是原始 RAW）—— RawTherapee/RapidRAW 拿到 TIFF 后其 RAW 专属模块不可用，属预期；
- **命令体系**（§2.15）：登记命令（浏览/编辑上下文可用），默认热键**留空**并写明理由
  （会弹窗、秒级耗时、非高频，不与导出抢键位）；
- 实现手段：`tauri-plugin-opener` 的 `open_path(path, with)`（内部 ShellExecuteW，避引号地狱）；
  编辑器预设存 `app.db.settings`（**设备级**）；**不需要新表、不动 issue 模型、不动 catalog schema**。

### M4 完成定义（DoD）

- 应用外新增/删除/移动照片后，重新进入对应目录能得到真实状态，离线卷不误删 catalog
- 一张与 1000 张导出都能完成；SOOC 与编辑后 issue 都能正确导出；失败可重试、队列可恢复，内存无持续增长
- 「导入 → 浏览 → 编辑 → 导出」不用离开 RayBend 即可走完
- 对 issue/SOOC/RAW/位图的导出来源、尺寸与元数据策略有明确且可测试的契约
- 当前 issue 可一键送外部编辑器：应用发现、多应用记忆、失效登记自动清理符合 M4-W4 规格

---
