# raybend — 开发计划（PLAN.md）

> 规划结构：**Milestone（阶段）→ Wave（波次）**。Milestone 是能力层的边界，Wave 是 milestone 内可独立交付的小步。
> 本文件只做**路线与边界**，每个 milestone 的具体实现方案在开工前单独讨论后再细化。
> 远期方向（不做排期）见 `FUTURE.md`；纪律与关键真相见 `AGENTS.md`。
>
> 建立时间：2026-09-15 00:18:03 CST

---

## 0. 规划原则

1. **先做相片管理全流程，编辑一律后置**。第一个能给别人试用的版本，是一条完整的「导入 → 浏览 → 评级 → 筛选 → 导出」闭环。
2. **风险前置**：把可能推翻架构的事（渲染合成、RAW 解码、SQLite 并发、缩略图吞吐）全部放进 M0 验证，不允许带着未知直接开工产品代码。
3. **每个 milestone 都要能独立交付价值**，而不是「一次性铺完所有底层」。
4. **基础设施只做当前里程碑需要的部分**（例如缓存淘汰策略留到 M5，M1 只要有能跑的缓存即可），避免过度设计。
5. **一个 writer 原则**：人机协作期间，避免同一时段多路修改同一文件。

---

## 1. 路线总览

| Milestone | 目标（一句话） | 波次数 | 交付物 |
| --- | --- | --- | --- |
| **M0** | 可行性验证与项目骨架 | 6 | 5 份 spike 结论 + 可运行的空壳 + 第一批设计稿 |
| **M1** | 相片仓、目录与导入 | 6 | 能建仓、导入 5000 张、网格出缩略图、重启不丢、崩溃可续 |
| **M2** | 浏览与评估（核心体验） | 6 | 10 万张库中流畅筛选与全键盘评片 |
| **M3** | 组织与检索 | 5 | 标签、集合、中文全文搜索、地图/时间线 |
| **M4** | 导出与互操作 | 4 | 批量导出 + XMP 导入导出（含 Lightroom 迁移入口） |
| **M5** | 多仓、缓存治理与数据安全 | 5 | 多仓/离线卷、缓存淘汰面板、备份与恢复 |
| **M6** | 分发与发布 | 4 | 签名安装包、自动更新、首启体验 |
| **后续（未排期）** | 显影/编辑、AI、跨平台 | — | 见 `FUTURE.md` |

**明确不做（近期）**：图像编辑与显影、AI 修图、视频管理、云端同步、插件系统、macOS/Linux 适配、打印与色彩管理。
以上全部登记在 `FUTURE.md`，不在 PLAN 的排期内。

---

## M0 —— 可行性验证与项目骨架

**目标**：用最小代价把「会推翻架构的未知」全部打掉，并得到一个能跑的空壳。

### M0-W0　前置：项目初始化（**由人类执行**）

Agent 在此之前不做任何脚手架操作。建议方向（不强制，按你的习惯调整）：

1. **前端脚手架**：Vite + TypeScript + Solid 1.9.x，**不用 SolidStart**
   - `pnpm create vite . --template solid-ts`（或等价方式）
2. **Tauri 外壳**：根目录生成 `src-tauri`
   - 依赖 `@tauri-apps/cli@^2.11`；`tauri init` 时设 `productName = RayBend`、`frontendDist = ../dist`、`devUrl = http://localhost:1420`
   - `src-tauri/Cargo.toml` 的 `package.name = "raybend-desktop"`
3. **Cargo workspace**：根 `Cargo.toml` 声明 `[workspace]`，建两个成员
   - `crates/raybend`（`package.name = "raybend"`，核心库占位）
   - `crates/raybend-ipc`（可选：IPC 契约与 specta 生成）
4. **样式**：Tailwind CSS 4（`@tailwindcss/vite` 插件方式）
5. **目录占位**：`design/`、`implementations/`
6. **工具链固定**：`rust-toolchain.toml` 固定版本（建议 ≥ 1.95，兼容 Tauri 3 的 MSRV）；包管理器遵循全局约定用 **pnpm**（项目内开发依赖）
7. **`.gitignore`**：`target/`、`node_modules/`、`dist/`、`.pen` 的临时导出物
8. 初始化完成后写一份 `implementations/` 记录（含精确时间），并告诉 Agent「初始化完成，可以开工」

**验收**：`pnpm tauri dev` 能起一个空窗口；`cargo check --workspace` 通过。

### M0-W1　渲染可行性 spike（**最高优先级**）

验证 Windows 上「WebView2 透明挖洞 + 原生 wgpu 直绘」是否稳定可用。

- 透明区域定位、窗口 resize 与 surface 重配置、分数缩放（125%/150%）、多显示器不同 DPR、最大化/全屏、退出全屏后的重绘
- 坐标同步：前端滚轮/拖拽 → Rust 视口变换，检查是否漂移
- GPU 驱动回退与 `device lost` 恢复路径
- **产出**：一份「能做 / 不能做 / 需要哪些规避手段」的结论 + 最小 demo 代码

### M0-W2　RAW 解码 spike

- rawler 在本机 MSVC 工具链下编译通过；跑通 CR3 / NEF / ARW / RAF / RW2 / ORF / DNG 样本
- 量出两条路径的耗时：**嵌入式 JPEG 预览提取**（应当 <10ms/张）与**完整解码 + 基础去马赛克**（作为后台任务的成本基线）
- 验证 worker 进程隔离方案（崩溃不带走主进程）
- 确认默认启用哪些可选后端（rawler 官方版 vs RapidRAW fork，见 `AGENTS.md` §7.5）

### M0-W3　存储基准 spike

- 10 万行虚拟 catalog 的读写基准：批量插入、按日期/机型/评级筛选、排序分页
- FTS5 `trigram` 中文搜索实测（中文关键词、子串、混合中英）
- `ATTACH` 多库 + `UNION ALL` 跨仓查询实测
- 单写者 actor 模型下的并发压测（模拟导入 + 缩略图生成同时写）
- 备份与恢复演练：`VACUUM INTO` 快照、`integrity_check`、故意损坏后的恢复流程

### M0-W4　缩略图吞吐 spike

- 5000 张样本：内嵌预览提取 → 生成 512px 缩略图 → 落库的端到端耗时
- 编码格式对比（QOI / WebP lossy / JPEG）在「生成速度 / 体积 / 前端解码速度」上的取舍
- 并发度曲线（磁盘并发 vs CPU 并发），确认是否会被 HDD 或杀软拖垮

### M0-W5　UI 骨架 spike

- Solid 1.9 + Tailwind 4 起最小界面；照片网格用 **DOM 虚拟化**跑出 10 万条目 60fps
- **组件原语选型定夺：Kobalte vs Ark UI**（对比见 `AGENTS.md` §7.6）→ 结论写入本文件「决策记录」
- **图标体系选型定夺**（Lucide / Tabler / Material Symbols，评估见 `AGENTS.md` §7.7）→ 结论写入本文件「决策记录」
- 确认中文字体与排版基线、`i18n` 的留位方式（建议一开始就上 i18next 或等价方案）
- 用 Pencil 画出第一批界面稿到 `design/`（Library 网格、查看器、导入对话框）

### M0-W6　架构定稿

- 用 W1–W5 的结论敲定 `crates/` 的拆分（是否现在就把 `raybend-raw` / `raybend-cache` / `raybend-jobs` 独立出来）
- 敲定 `raybend-ipc` 的类型生成方式（specta）与错误模型
- 更新 `AGENTS.md` 的架构章节

### M0 完成定义（DoD）

- [ ] 初始化完成，`pnpm tauri dev` 可运行
- [ ] 五份 spike 结论落盘为 `implementations/` 记录，且每条都有可复现命令
- [ ] 组件库与图标选型已定，写入本文件决策记录
- [ ] `design/` 至少有 3 个界面稿（含同名 `.md`）
- [ ] `AGENTS.md` 架构章节与本文件同步更新

---

## M1 —— 相片仓、目录与导入

**目标**：建立可信的数据底座，完成「建仓 → 导入 → 缩略图可见 → 重启仍在一份」这条最小闭环。

### M1-W1　相片仓与全局库

- 仓的定义与生命周期：创建、登记（`app.db` 注册表）、改名、移除（不删照片）、只读卷
- `<仓>/.raybend/repo.json` 结构定稿；`<仓>/.raybend/` 的隐藏与保护策略
- 全局库 `app.db` 的表设计（设置、仓库注册表、任务队列、缓存索引镜像）

### M1-W2　catalog schema 与迁移框架

- 表族定稿：`assets` / `asset_files` / `metadata_core` / `metadata_ext` / `keywords` / `tagged` / `collections` / `develop_stack` / `edit_revisions` / `jobs` / `fts`
- **文件身份**：`(volume_serial, file_id128)` 主键 + 路径规范化策略（NFC 存储 + 原始名保留 + 大小写折叠列）见 `AGENTS.md` §7.3
- 迁移框架（`user_version` + 顺序迁移）、版本闸门（库版本高于程序则拒绝打开）
- **快照备份**：迁移前自动 `VACUUM INTO`，保留 7 份
- 写并发模型落地：单写者 actor + 读连接池 + `PRAGMA` 调优

### M1-W3　扫描与变更追踪

- 文件系统扫描（并行、可中断）、按目录批量顺序读以照顾 HDD
- 增量扫描：基于 `(device, file_id)` 与 `(size, mtime)` 的差异检测；重命名/移动的识别（不丢编辑）
- `notify` 实时监听与「攒批处理」策略（避免大批量写入风暴）
- 缺失文件与离线卷的标记（不删记录，标记缺失）

### M1-W4　元数据与缩略图管线

- EXIF 抽取（时间、机型、镜头、ISO、快门、光圈、焦距、朝向、GPS）+ 未知 tag 的保留策略
- **内嵌预览提取优先**（快速出图）→ 后台真实解码替换（`AGENTS.md` §7.4）
- 缩略图生成 worker + 最小缓存读写（`GRID` 档，SQLite BLOB）
- 缩略图任务的持久化队列雏形（崩溃可续）

### M1-W5　导入 UI

- 导入面板（源选择、目标仓、重命名规则、是否复制/移动/就地引用、重复检测策略）
- 导入进度与统计（成功/跳过/失败/错误清单可导出）
- 可暂停/继续/取消；错误不打断整批
- 导入第一步只写「元数据 + 内嵌预览」，让进度条立刻动起来

### M1-W6　M1 收尾

- 崩溃恢复演练：杀进程后重启，队列与库状态一致
- 实施记录补齐 `implementations/`
- 性能基线记录（5000 张导入耗时、缩略图生成吞吐）

### M1 完成定义（DoD）

- [ ] 能创建仓并把 5000 张照片导入，重启后状态完整
- [ ] 网格能看到缩略图，滚动不卡顿
- [ ] 杀进程后重启可继续未完成的任务
- [ ] 迁移与备份机制经过一次演练（含人为损坏后的恢复）
- [ ] 有一份实测性能基线数据

---

## M2 —— 浏览与评估（核心体验）

**目标**：把「看片、挑片、打分」这件事做到比 ON1 更顺滑 —— 这是 raybend 的第一战场。

### M2-W1　Library 网格

- 虚拟化网格：尺寸档位、密度、行列自适应、滚动位置恢复
- 选中模型（单选/区间/Ctrl 切换/框选）、键盘导航（方向键/Home/End/PageUp-Down）
- 缩略图叠加信息（评分、旗标、色标、文件名、时长态）

### M2-W2　评级与快速操作

- 星级（0–5，含半星可选项）、旗标（Pick/Reject）、色标
- 快捷键全键盘评片流（打分 → 下一张 → 放大 → 退回），可自动前进
- 批量操作（多选打分/加标/加标签），操作可见、可撤销
- 撤销/重做栈（至少覆盖评级与标签）

### M2-W3　查看器（原生 wgpu 视口）

- 视图模式：fit / fill / 1:1 / 自定义缩放；滚轮缩放、拖拽平移、双击切换、键盘辅助
- 多图对比（2 图并排、可扩展到 4 图）
- 叠加层：直方图、EXIF 摘要、对焦区域提示（若有）
- 大图加载策略：先出降采样图 → 后台补全分辨率

### M2-W4　筛选、排序与状态反馈

- 筛选：评分/旗标/色标/日期区间/机型/镜头/ISO/焦段/有无人脸（占位）
- 排序：拍摄时间、导入时间、文件名、评分、机型
- 筛选器 UI（可折叠、可保存为智能集合的前身）
- 状态栏：总数/已筛选数/已选数 + 提示行（修饰键提示，见 `AGENTS.md` §1 设计立场）

### M2-W5　命令面板与快捷键体系

- 命令面板（模糊搜索 + 显示快捷键 + 最近使用）
- 上下文工具栏（选中什么显示什么操作）
- 快捷键可自定义、可导出/导入、冲突检测
- 菜单保持极简（File/Edit/View/Window/Help 的兜底版），所有菜单项同时是可搜索命令

### M2-W6　M2 收尾

- 10 万条目的筛选响应、滚动帧率、内存占用实测
- 评片流程的可用性自查（能不用鼠标走完整流程）

### M2 完成定义（DoD）

- [ ] 10 万张库中筛选响应 < 100ms（感知即时）
- [ ] 网格滚动稳定 60fps（目标分辨率 2560×1440）
- [ ] 全键盘可完成「看片 → 打分 → 下一张」的完整评片流
- [ ] 命令面板覆盖所有主要操作，且每项显示快捷键

---

## M3 —— 组织与检索

**目标**：从「能看」到「找得到、组织得起来」。

### M3-W1　标签体系

- 层级标签（闭包表）、父子移动、重命名、合并、同义词
- 批量打标、标签自动补全、最近使用标签
- 标签面板（树 + 计数 + 拖拽打标）

### M3-W2　集合与智能集合

- 手动集合（静态成员列表，保序）与智能集合（保存的查询）
- 查询 DSL 定稿（可序列化、可版本化、可跨仓）
- 集合的导入导出（供迁移与分享）

### M3-W3　全局搜索

- FTS5 `trigram` 中文 + `porter` 英文双索引策略（`AGENTS.md` §7.2）
- 搜索范围：文件名、标题、说明、关键词、相机/镜头、地点
- 搜索历史与「搜索即筛选」的统一心智模型

### M3-W4　地图与时间线

- `R*Tree` GPS 边界框查询、离线地图或本地缓存底图（若引入地图库需先讨论）
- 时间线：按年/月/日聚合与快速跳转、照片密度可视化

### M3-W5　重复与相似

- 感知哈希（参考 RapidRAW 用 `image_hasher` 的思路）做重复检测与相似分组
- 「同一次连拍/同一次导入」的自动分组（burst grouping）

### M3 完成定义（DoD）

- [ ] 中文关键词搜索可用且返回准确（含子串）
- [ ] 标签与集合的增删改全部可撤销
- [ ] 地图与时间线能定位到具体照片并跳转到网格
- [ ] 重复检测在测试集上的误报可接受

---

## M4 —— 导出与互操作

**目标**：把照片带得出去，也把外面的库（尤其 Lightroom）搬得进来。

### M4-W1　导出引擎

- 尺寸/缩放、格式（JPEG / WebP / JPEG XL / TIFF / PNG）、质量、锐化输出、色彩（SDR 基线）
- 命名规则与目录结构模板（可预览结果）
- 元数据保留策略（EXIF/IPTC/关键词/版权）、水印（可后置）

### M4-W2　导出预设与队列

- 预设的创建/编辑/导入导出；批量导出队列（可暂停、失败重试、错误报告）
- 导出历史记录（可从库中回溯「这张照片导出过哪些尺寸」）

### M4-W3　XMP 互操作

- 写入 sidecar `.xmp`（RAW 永不改动原文件）
- 读取 Lightroom 的 `crs:` / `lr:` 命名空间基础项（曝光、白平衡、裁剪、评级、关键词）→ 作为迁移入口
- 「DB 为真相源、XMP 为通道」的冲突处理规则定稿（`AGENTS.md` §6.4）

### M4-W4　M4 收尾

- 导出 1000 张的端到端实测与失败率统计
- 从 Lightroom 目录导出的 XMP 样本做一次真实迁移演练

### M4 完成定义（DoD）

- [ ] 一张照片从库到导出文件的全流程正确，元数据按策略保留
- [ ] 1000 张批量导出无内存爆涨，失败可重试
- [ ] Lightroom 的 XMP 能导入评级与关键词，不产生重复资产

---

## M5 —— 多仓、缓存治理与数据安全

**目标**：把「长期使用」带来的问题处理干净：多仓、外接盘、缓存膨胀、数据损坏。

### M5-W1　多仓与离线卷

- 多仓并行打开、跨仓搜索（`ATTACH` + `UNION ALL`）
- 外接盘拔插处理：卷缺失时降级展示（缩略图兜底）、只读卷策略
- 仓的移动/复制/归档语义（`repo.json` 的 uuid 与重定位）

### M5-W2　缓存治理

- 缓存索引表、frecency 淘汰、容量上限、保留期、`pinned`
- 孤儿回收（签名过期、资产不存在）、缓存重建流程
- 缓存面板 UI（按仓/类型/大小展示 + 一键清理 + 改路径）

### M5-W3　备份、恢复与完整性

- 定期快照策略与保留份数、异常退出后的自动检查
- `integrity_check` 调度、损坏检测与修复指引
- 云同步盘/网络盘检测与警告（`AGENTS.md` §6.4）

### M5-W4　任务系统完整化

- 持久化队列（优先级、限流、暂停、重试、失败隔离）
- 后台任务的资源占用可视化（用户能知道「它在干什么」）

### M5-W5　崩溃隔离与可诊断性

- RAW 解码 worker 进程化（崩溃不影响主进程）
- 日志分级与滚动、崩溃报告（本地收集，可选上报）
- 诊断包导出（日志 + 环境 + 库状态摘要）

### M5 完成定义（DoD）

- [ ] 缓存目录整体删除后，应用仍可用并能自动重建
- [ ] 故意损坏 catalog 后，能按流程恢复到最后一份快照
- [ ] 拔掉外接盘后界面不崩、不删记录，插回后自动恢复
- [ ] RAW 解码进程崩溃不影响主进程（有演练记录）

---

## M6 —— 分发与发布

**目标**：让不装开发环境的人也能用得上。

### M6-W1　打包

- NSIS / MSI 安装包；图标、版本信息、文件关联（可选）、卸载清理策略（保留用户数据）
- 首次运行体验（欢迎页、建仓向导、隐私说明）

### M6-W2　自动更新

- 更新通道（稳定/预览）、签名校验、失败回滚
- **注意**：AGPL-3.0 项目不要让更新机制强制只接受官方签名（见 `AGENTS.md` §2 相关讨论）

### M6-W3　代码签名与分发（**由人类执行**）

- 证书获取与签名流程、SmartScreen 信誉、发布页与校验和
- 发布物生成、tag、push、上传全部由人操作

### M6-W4　文档与支持

- 用户文档、常见问题、错误码说明、捐赠入口
- 许可证与第三方声明（LGPL 组件的声明页）

### M6 完成定义（DoD）

- [ ] 干净的 Win10/Win11 机器上双击安装即可用
- [ ] 自动更新从 vN 升到 vN+1 成功且可回滚
- [ ] 第三方许可声明完整

---

## 2. 决策记录（开工后逐条填充）

| 日期 | 决策 | 结论 | 依据 |
| --- | --- | --- | --- |
| 2026-09-15 | 许可 | **AGPL-3.0-only**（不闭源盈利） | 用户决定；使 rawler(LGPL-2.1) 与 darktable/RawTherapee(GPL-3.0) 的算法均可合法使用，并额外解锁 RapidRAW 与 zenraw（均 AGPL-3.0）的代码复用 |
| 2026-09-15 | 渲染架构 | 原生 wgpu + 透明挖洞（方案 B） | 用户决定；RapidRAW 实证性能 |
| 2026-09-15 | 存储架构 | 全局库 + 每仓 catalog（分层） | 用户决定；可移动/隔离损坏/按仓备份 |
| 2026-09-15 | 缓存位置策略 | `%LOCALAPPDATA%` 默认 + 可按仓改为随仓 | 用户决定 |
| 2026-09-15 | UI 框架 | Solid 1.9.x，不引入 SolidStart | 用户接受；Solid 2 仍 RC 且 Start 面向 v1 |
| 2026-09-15 | Tauri 3.0 迁移准备 | 分层 + 不碰 `tauri::Wry`/feature flag | Tauri 3 alpha 的运行时 crate 化改动 |
| 2026-09-15 | 色彩管理 | 预留，第一阶段不做 | 用户决定 |
| M0-W5 | 组件原语 | _待定：Kobalte / Ark UI_ | 见 `AGENTS.md` §7.6 |
| M0-W5 | 图标体系 | _待定：Tabler / Lucide / Material Symbols_ | 见 `AGENTS.md` §7.7 |

---

## 3. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-15 | 初版：确立 7 个 milestone 与波次划分、决策记录表 |
