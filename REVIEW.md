# raybend — 定期评审与方向修正（REVIEW.md）

> **定位**：每隔一段时间回顾开发内容、为未来方向做修正的文档。
> **每次明确的 review 行为都对本文件做出「补充 / 更新 / 重写」，用来确定接下来要做的事。**
> 维护规则见 §0；纪律见 `AGENTS.md` §5.6；与其它文档的分工见 §0 第 1 条。
>
> 建立时间 / 首次评审：2026-09-26 13:28:55 CST

---

## 0. 这个文件怎么用（维护规则）

1. **它是「结论账本」，不是「第二份规格」。**
   本文件只记三样：**判断**、**取舍**、**去向**（落到哪个文件的哪一节）。
   一经采纳，**正式改动写进拥有它的那份文件** ——
   `AGENTS.md` / `DESIGN.md` / `BROWSE.md` / `REPOSITORY.md` / `IMAGING.md` / `PLAN.md` / `FUTURE.md`；
   本文件那一项只更新到「**已落地 + 落点**」。
   ⚠️ **禁止把规格正文抄第二份**（`AGENTS.md` §2.12：同一个东西两种表达本身就是 bug）。
2. **不用 `- [ ]` 勾选框。** `AGENTS.md` §5.4 说明勾选项会被 plannotator 进度跟踪，而那个机制
   只属于 `specs/*.md`。本文件的状态**一律写在表格的「状态」列**里
   （`待决` / `已采纳` / `已落地` / `不做` / `搁置`）。
3. **每轮 review 追加一节，不覆盖历史。** 文件顶部的 §3「当前待办总览」是**全文件唯一的汇总视图**，
   每轮重排一次；下面各轮的小节是**证据与理由**，可以长。
4. **不改写旧结论。** 结论变了就写新一节并注明「**取代 `R⟨轮次⟩-⟨序号⟩`**」。历史留着 ——
   「当时为什么这么定」本身是资产，删掉它下次会重新吵一遍。
5. **编号规则 `R<轮次>-<序号>`**（如 `R1-03`），一经分配**不再复用**；作废的标 `不做` 留行。
6. **不是待办清单的替代品。** 本轮要做的事仍按 `AGENTS.md` §5.5 走 `todos` / plannotator progress；
   本文件管的是「**方向对不对**」，不是「今天做哪件」。

---

## 1. 当前待办总览（R1 / R2 · 全文件唯一的汇总视图）

> 优先级：**P0** = 影响"能不能被当成相片管理软件/编辑软件"的准入门槛；
> **P1** = 明确的产品或体验缺口，应在下一到两个里程碑内消化；
> **P2** = 定位/措辞/一致性，成本低但别忘。

| 编号 | 事项 | 类型 | 优先级 | 状态 | 去向（落地处） |
| --- | --- | --- | --- | --- | --- |
| R1-01 | 色彩管理（ICC）从「后期」提到**编辑模块 DoD 门槛** | 产品缺口 | **P0** | **已采纳**（执行待排期） | 方案已定稿：**`specs/color-management.md`**（L1–L4 分级 + 验收判据 + 风险 + 7 项待拍板）；每波开工前另写 `specs/CM-W<n>.md` |
| R1-02 | **强化相片集**（集合 / 智能集合；引擎已有、缺产品形态与界面） | 产品缺口 | **P0** | **已采纳**（形态待讨论） | 人类 2026-09-26 定为 **M5 之后三条主线之一**（`PLAN.md`「M5 之后：三条主线」）。⚠️ **人类已有创意想法、待专门讨论** → 先写意图规格再谈界面与数据模型；**不预设它等于** `FUTURE.md` I 节的「集合与智能集合」 |
| R1-03 | **XMP** —— 目的是**方便导入迁移**（不只是写出去） | 产品缺口 | P1 | **已采纳**（范围待定） | 人类 2026-09-26 定为 **M5 之后三条主线之一**（`PLAN.md` 同上）。契约：`docs/issue-xmp-contract.md`；冲突规则在 `FUTURE.md` I 节。⚠️ 原登记偏「只写」，本次口径把**「读」（把别的软件里的评标/标签/调整迁进来）也纳入** |
| R1-04 | `Shift` 区间语义（**翻转** vs 业界肌肉记忆的**扩展**）复审 | 交互 | P1 | **不做**（人类改判） | 人类 2026-09-26 定：`Shift` 只“置为选中”、**不**做区间取消、**不**支持 `Shift+Ctrl` 同按（退化为单击）；两端算法固定不变 —— 已落地，口径见 `BROWSE.md` § 5.2.1 与 `lib/selection.ts` |
| R1-05 | **旗标不持久化**与用户预期正面冲突 | 交互 | P1 | 待决 | `BROWSE.md` §3.2 |
| R1-06 | 外壳垂直占用（紧凑 **120px** / 宽松 **150px**）按工作流收层 | 交互/视觉 | P2 | 待决 | `DESIGN.md` §2.1、§8.2 |
| R1-07 | AVIF 缩略图**生成节点口径统一**（导入时 vs 进 tiles 时） | 工程 | P1 | 待决 | `IMAGING.md` §3.1/§4/§6 |
| R1-08 | 自研去马赛克/降噪的**质量基线**与「不达标就送外部」的明确判据 | 工程 | **P0** | 待决 | `AGENTS.md` §8 第 1 条、`FUTURE.md` D2/D3 |
| R1-09 | 规格漂移：条带高度 §9.2 与 §8.2 **互相矛盾**，且 `design/*.md` 在用旧值 | 文档纪律 | P1 | **已落地**（2026-09-27） | `DESIGN.md` §9.2 已改对并加「本表由 §8.2 派生」约束；`design/{export,browse,editor,main}.md` 四处改为「画布硬写 X，**实现以 `tokens.css` 为准**」。剩下的是歷史記录（`DESIGN.md` §8.3 历史对照表、`design/main.md` §9 同步执行记录）**故意不改** |
| R1-10 | `FUTURE.md`「issue 是**创新点**」措辞校准 | 定位 | P2 | 待决 | `FUTURE.md`「issue 与 SOOC」 |
| R1-11 | 「Tab 四态 / 无级分栏 / 对比虚拟画布」与「时间片」的定位校准 | 定位 | P2 | 待决 | `DESIGN.md` §13.3/§14、`BROWSE.md` §5.7 |
| R1-12 | 把「库身份用内容定」写成**对外的差异点**（现在只写在内部规格里） | 定位 | P2 | 待决 | `README.md` / 官网 `website/` |
| R2-01 | M5 工程准备与 M4 并行，正式发布验收仍依赖 M4 收口 | 排期 | P1 | 已采纳（工程准备） | 2026-09-26 崔总授权推进；`PLAN.md` M5 进度表与实施记录 |
| R2-02 | 扩展已有 release 链路：统一版本、参数与产物校验、Windows release/bundle | 工程 | P1 | 已采纳（开发/冒烟） | 复用原脚本、dav1d 与 check:win；见 M5-W1/W4 及实施记录 |
| R2-03 | 官网直下保留，同时评估免费商店 MSIX 与开源免费签名 | 分发/信任 | P1 | 待决 | 若采纳，修订 `PLAN.md` M5-W4；先验证 MSIX 可行性，免费签名须申请审核，不先购买 EV |
| R2-04 | 自动更新恢复区分安装失败、启动失败与数据库已迁移，渠道各有更新责任 | 数据安全 | P1 | 已采纳（恢复口径） | `PLAN.md` M5-W2、`docs/release.md`；实际恢复未验收，不另建 schema 通道 |

---

## 2. R1 评审的元信息

| 项 | 内容 |
| --- | --- |
| 时间 | 2026-09-26 13:28:55 CST |
| 触发 | 人类要求：「分析一下当前的产品设计，感觉这个项目在行业中设计思路怎么样」 |
| 范围 | **产品设计**（定位 / 信息结构 / 交互 / 视觉体系 / 数据模型 / 路线取舍）+ **实现侧现状对照** + 行业横向对照 |
| 依据（读过的） | `DESIGN.md`(1335)、`BROWSE.md`(607)、`REPOSITORY.md`(429)、`IMAGING.md`(247)、`PLAN.md`、`FUTURE.md`、`design/{main,browse,editor,export}.md`；实现侧 `src/styles/tokens.css`、`src/features/commands/catalog.ts`(64 条命令 / 31 条带默认键)、`src/workspaces/editor/`、`implementations/2026-09-26_M3-W7-closeout.md` |
| 行业对照对象 | Capture One **Variants / Sessions**、Lightroom **Virtual Copies**、ON1 **Versions**、darktable、digiKam、RawTherapee、RapidRAW |
| **未覆盖** | 真机观感、帧率、色彩正确性、安装与 E2E —— 按 `AGENTS.md` §2.8 归人类；本轮**没有任何**来自真机的证据 |

### 2.1 R1 总体判断（一句话）

**设计成熟度在整个开源相片软件里属于断层第一档；真正的问题不是"想得对不对"，而是三处取舍是否成立，
以及"创新"的自我定位需要校准。**

展开成两句：

- **设计思路**：把"现代 Web 产品"的纪律（设计令牌、组件化、命令注册表、i18n、空态/加载态/禁用态全覆盖）
  搬进一个"暗房工具"的骨架（三行外壳、暗色优先、否定式工具条）。这个组合**在 Windows 桌面端目前没人做好过**。
- **优势的真实来源**：**设计纪律 + 执行自洽**，不是**概念独创**。
  开源相片软件死的从来不是缺创意，是缺纪律 —— 所以这仍然是一副好牌，但对外介绍要用准确的说法。

### 2.2 行业位置（R1 的判定）

| 对手 | 它强在哪 | 判定 |
| --- | --- | --- |
| **darktable** https://www.darktable.org/ | RAW 显影质量与色彩管理 | UI/交互/DAM **高一到两个档次**；显影质量暂时输 |
| **digiKam** https://www.digikam.org/ | 开源里最强的 DAM（标签/搜索/人脸） | 功能面还追不上（集合/层级标签/人脸均未排期）；观感上是"Qt 桌面软件" vs "2025 年的产品" |
| **RawTherapee** https://www.rawtherapee.com/ | 无 catalog，纯转换器，质量高 | 不是同类；**已被定位为外部编辑器** —— 聪明的互补 |
| **RapidRAW** https://github.com/CyberTimon/RapidRAW | 同技术栈（Rust+Tauri+wgpu）、现代观感、AGPL | **最接近的对手**；关键差别：它是 sidecar 流派、**没有 catalog 数据库** |
| **Capture One** https://www.captureone.com/ | 色彩 / 变体 / 联机拍摄 / **Sessions** | 专业度差距最大；但**库模型基本是「现代化改编版的 C1 Sessions」**，方向本身被验证过 |
| **Lightroom Classic** | 生态、云端、镜头库、AI 蒙版 | 能力差得远；但 **UI 确实停在 2015 年** —— 观感与命令体系这两样已经能平推 |

**结论**：「**Windows 优先 + 有 catalog + 现代观感 + 开源**」这个交集**目前是空的**，
定位成立且有空位。三家里没有一家的组合能占住它：RapidRAW 无 catalog，digiKam 不现代，darktable 两边都缺。

---

## 3. R1 建议（按"要不要现在动"分组）

### 3.1 需校准的自我认知（改措辞，不改功能）

#### R1-10　`FUTURE.md`「issue 是创新点」→ 收回一点

`FUTURE.md`「issue 与 SOOC」写着：「这是本应用的一个**创新点**：每张图可以有自然的多定稿版本」。

**这个说法站不住**：issue 在功能上就是行业里已有的东西，只是换了名字。

| 概念 | 归属 | 说明 |
| --- | --- | --- |
| **Variants** | Capture One | 官方文档明确「**一切皆 variant**，原片本身就是一个 variant」—— 与「SOOC 是写死的特殊 issue」**几乎逐字对应** |
| **Virtual Copies** | Lightroom Classic | 一份文件多条编辑路线，可各自导出 |
| **Versions** | ON1 Photo RAW | 保留不同 Develop/Effects/裁剪/元数据 |
| **Snapshots** | LR / C1 | 参数快照，用途重叠 |

参考：Capture One 官方对 variants 的定义
https://support.captureone.com/hc/en-us/articles/360002478437-The-concept-of-variants-in-Capture-One
（Sessions 对比 Catalog https://support.captureone.com/hc/en-us/articles/30041173920029-Sessions-vs-Catalogs-in-Capture-One-how-they-work-and-where-to-store-them
｜ON1 Versions https://www.on1.com/ ）

**真正的差异化不在"发明了 issue"，而在"把 issue 做成第一公民"**，这四条合起来确实比 LR 彻底：

1. **导出工作区以 issue 为选择单元**（`design/export.md` §4.2：可选中单元是 issue 小片，点照片本体 = 整组全选）；
2. **浏览侧右栏「展示定稿」切换**，切完立刻刷新网格缩略图与看图（`design/browse.md`「M3-W6c」）；
3. **SOOC / RAW 是固定源**，且每个命名定稿带「基于 SOOC / 基于 RAW 编辑」标签；
4. **对比工具的左源可以是任意定稿**。

> 对着 C1 只能算**追平并补齐**（C1 甚至能一次导出同一张的多个 variant）。
> **建议措辞**：从「创新点」改为「**把行业里已有、但被做成二等公民的机制提升为第一公民**」——
> 既准确，对外也更容易站住。

**去向**：`FUTURE.md`「issue 与 SOOC」小节；同时把上面四条差异点写进去（那才是可辩护的部分）。

#### R1-11　另外两处同类校准

- **`Tab` 四态循环 / 无级分栏拖拽 / 对比虚拟画布**：C1 与 LR 都有成熟形态。这里比的是**执行质量**，
  不是概念新颖度。对外不必强调"独创"，强调"更顺"。
- **时间片（相邻间隔 > 1h 断为新片）**：这是 Apple Photos「Moments」的思路搬进专业工具，
  **第一次被认真做**。但它是**受众选择**：婚礼/活动/旅拍是加分项，棚拍/静物是噪声。
  建议在 `BROWSE.md` §3.1 的开关说明里写清它服务谁，别默认所有人都会用。

#### R1-12　把「库身份用内容定」写成对外的差异点

`REPOSITORY.md` §2 的库模型（**身份记在 `catalog.db` 里，路径只是挂载点**，
于是「同路径不同库」「同库多路径」「拔盘离线后自动重挂」三件事同时成立）——
**这是我认为整个设计里最该被写进 README 与官网的一条**，而现在它只活在内部规格里。

为什么它值钱：Lightroom 的 catalog 与照片是"指针 + 目标"关系，catalog 丢失或照片搬家就是灾难；
这里有明确的**行业痛点**与明确的**机制性差异**（用内容定身份，和 git 的思路一致）。

**去向**：`README.md` 的产品介绍；官网 `website/` 的功能段（等其内容规划到位时）。

---

### 3.2 产品级缺口（P0/P1）

#### R1-01（P0）色彩管理必须从「后期」提到**编辑模块的门槛**

- **现状**：`AGENTS.md` §6.1「第一阶段不做色彩管理」，只保证 SDR 下不变形。
- **问题**：浏览阶段可以扛，**编辑阶段扛不住**。RAW 解码 → 场景参考管线（`FUTURE.md` D1）→ 显示，
  这条链上缺 ICC 管理时，"用户看到的颜色"不保证等于 sRGB 定义的颜色，更不用说 Adobe RGB / P3。
  C1 / LR / darktable **全都有**完整 ICC 与软打样 —— 这不是可选项，是**相片编辑软件的准入门槛**。
- **缓解因素（要一并记下）**：lcms2 已在 M0 预留；「不变形」这个最低目标写得很清楚；
  管线是 scene-referred 的，接色彩管理比在显示参考管线上补要容易。
- **建议**：把 ICC 从"后期里程碑"提到 **`PLAN.md` M3 完成定义的硬门槛**；至少在编辑视口这条路上
  「输入 → 工作空间 → 显示器」三段全部到位，并给出可验证的判据（不是"看起来对"）。

#### R1-02（P0）集合 / 智能集合的排期决定

- **现状**：`FUTURE.md` I 节整体后移、**未排期**。手动集合 + 保存查询的智能集合都在里面。
- **问题**：一个没有"相册 / 集合"的相片管理软件，用户没法组织照片，只能靠目录 + 标签。
  这会直接改变人们对"这是个 DAM 还是看图工具"的判断。
- **缓解因素**：**引擎底座已经在** —— 平面标签、FTS5 trigram、`query::facets`、时间线全在 M1/M2 落地，
  缺的**只是 UI 与持久化实体**。所以这是一件"成本比看起来低"的事。
- **建议**：在 M4 收口与 M5 发版之间做一次明确的取舍 —— 要么拉回一小波（静态集合 + 保存查询），
  要么在 `PLAN.md` 写明"首个公开版本不含集合，理由是 X"，**不要保持沉默的未排期状态**。

#### R1-03（P1）XMP sidecar 的实际写出

- **现状**：`docs/issue-xmp-contract.md` 契约已定案，**实际写入属后续波次**（`M3-W7` 记录确认）。
- **问题**：这是"**数据不是黑箱**"的对外承诺。缺它，从 LR / darktable 迁移过来的用户
  无法验证"我的评级与调整真的存下来了"，也拿不到逃生舱。
- **建议**：优先做**只写不读**（评标 + issue 参数导出为 sidecar），**读回与冲突解决**留到
  `FUTURE.md` I 节的互操作实现；「DB 为真相源、XMP 为通道」的规则已经在 `AGENTS.md` §6.4 写好了。

---

### 3.3 交互取舍（P1/P2，需要人类拍板）

#### R1-04（P1）`Shift` 区间的「翻转」语义

- **现状**（`BROWSE.md` §5.2）：点 A → `Shift` 点 C → **B、C 翻转**（不含 A），C 成为新锚点。
- **理由原文**：「逻辑简单、无需保存复杂状态、操作又灵活」。
- **问题**：代价被转移到了用户身上。Windows 资源管理器、macOS Finder、LR、Bridge、C1、
  浏览器、Excel —— **所有地方** `Shift+点击` 都是"从锚点扩展选中到此处"。这里是**唯一一个不同的**。
  从审片（culling）角度动机成立（批量改判已选/未选），但偏离需要**极强的视觉反馈**，
  否则会变成"我明明要 B..C，结果 B 被取消了"。
- **建议（二选一，请人类定）**：
  * **A（保守）**：`Shift` = 业界语义的**扩展**，另给 `Ctrl+Shift`（或 `Alt+Shift`）= **区间翻转**；
  * **B（保留）**：维持翻转，但在状态栏/toast 里**明确报出**"翻转 2 张"，并在 `DESIGN.md` §14
    补一条该操作的视觉反馈规格（否则这一条会以 bug 报告的形式回来）。

#### R1-05（P1）旗标不持久化与用户预期冲突

- **现状**（`BROWSE.md` §3.2 / §10 第 6 条）：旗标**只在内存**，关软件即清空，
  「一定程度替代未来的 bucket」。
- **问题**：旗标是摄影工作流里最常被理解为"已标记"的东西。**LR 的 pick/reject 是持久元数据，
  而且是筛选主力**。用户关掉再打开发现全没了，第一反应是 bug 不是 feature。
- **同类先例都在持久化**：LR 的 Quick Collection、Bridge 的 Collections、本项目 `FUTURE.md` H6 的
  picture bucket —— 那些**临时工作集也都是落盘的**。
- **建议（三选一）**：① 恢复上次会话的旗标（落 `app.db.settings`，仍然"不是元数据"）；
  ② 退出时询问是否保留；③ 维持现状但在 UI 上把临时性讲到无法误解
  （例如图标带"会话"角标 + 首次使用时一条 toast 说明）。

#### R1-06（P2）外壳垂直占用：紧凑 120px / 宽松 150px

- **实测值**（`src/styles/tokens.css`）：`--bar-title-h` 36/44 + `--bar-flow-h` 48/62 + `--bar-tool-h` 36/44
  = **120 / 150px**；加上中列自己的 statusbar（32）就是 **152 / 182px**。
- **问题**：2560×1440 上无所谓；在 1920×1200 / 16:10 笔记本上很贵
  （LR Classic 的模块选择条只有一条细带）。
- **建议**：评估"**哪些工作流可以自动收起某一层**"——例如导入时 `flowbar` 的 `ExifStrip`
  几乎无用；`toolsbar` 已经有"无内容整行隐藏"的规则，把它真正用起来。
  ⚠️ 这是对 §2.1 表面分层地图的改动，**先改设计稿再改代码**（`AGENTS.md` §5.1）。

---

### 3.4 工程与文档纪律

#### R1-07（P1）AVIF 缩略图的生成节点口径统一

- **现状**：`IMAGING.md` §3.1-1 说「**导入时**就要生成缩略图」，§8 现状里又说
  「按需：可见 tile 请求时渲染并写缓存」—— **两套口径并存**。
- **判断**：AVIF 的体积优势是真的（第三方基准：约 **−43%**，101kB vs JPEG 177kB），
  解码代价约 **+32%**（13.0ms vs 9.8ms）——见 https://gist.github.com/mvsde/3e42f33723b0cfff296f01bedc0dea94 。
  浏览侧这点解码差异吃得住；**真正的成本在编码**，而编码恰好在**导入路径**上。
- **建议**：明确成一条 —— **导入只登记 + 投递后台缩略图队列**，让"导入 5000 张"的耗时不被
  ravif 编码主导（编码代价已实测，见 `FUTURE.md` C8）。同时在 §6 的状态表里把这一点写死。

#### R1-09（P1）规格漂移：条带高度已经三处不一致（**有证据**）

| 位置 | 值（紧凑 / 宽松） | 状态 |
| --- | --- | --- |
| `DESIGN.md` **§8.2** | `--bar-title-h` 36/44、`--bar-flow-h` 48/62、`--bar-tool-h` 36/44 | ✅ 2026-09-16 按人验收反馈收紧后的**新值** |
| `DESIGN.md` **§9.2**（密度令牌表） | 40/48、52/68、40/48 | ❌ **旧值，未同步** |
| `src/styles/tokens.css`（实现，§8.4 的真相源） | 36/44、48/62、36/44 | ✅ 与 §8.2 一致 |
| `design/export.md` §2（**2026-09-26 最新出稿**） | 「`titlebar` 40 / `flowbar` 52 / `toolsbar` 40（紧凑档）」 | ❌ **用了旧值** |

**即：同一个文件里 §8.2 与 §9.2 互相矛盾，而最新的设计说明书沿用了错的那一份。**
这不是吹毛求疵 —— 它正好印证这套文档体系最大的成本：`DESIGN.md` 1335 行 + 三份大规格
+ 四本 `.pen` 说明书（共约 2500 行），单作者维护必然局部过期。

- **建议**：§9.2 那一列**要么删掉改成"见 `tokens.css`"**，要么加一条显式约束：
  「本表由 §8.2 派生，**改 §8.2 必须同步此处**」；顺手修 `design/export.md` §2。
- **可推广的做法**：把这类"派生表"统一标注派生来源（本仓已有 `design/browse.md` §5.3
  「与实现冲突时的判据」与 `DESIGN.md` §8.4「以实现为准」两个好机制，只是没覆盖到 §9.2）。

#### R1-08（P0）自研去马赛克 / 降噪的质量基线

- **现状**：`AGENTS.md` §8 问题清单**第 1 条**就是「去马赛克与画质算法自研成本高」，
  候选取舍见 `FUTURE.md` D2/D3。
- **判断**：这是**唯一一件"做不好就没有口碑"的事**，而且没有捷径。
  当前对冲是 M4-W4「送外部编辑器」——**这个对冲是对的**，但它只在"用户自己认得出要送出去"时才生效；
  内置出图如果明显输给 darktable，用户第一次打开就形成判断了。
- **建议**：给 M3/M4 的出图定一条**可验证的质量基线**（例如固定几张样片 × 与
  darktable / RawTherapee / 相机直出的对照），并把「**达不到就走外部编辑器路线、内置只保证够用**」
  写成明确的判据 —— 让"不做极致"是一个**决定**，而不是一个**结果**。

---

## 4. 已确认保持（R1 无动作，但记下来免得下次又提一遍）

| # | 项 | 为什么保持 |
| --- | --- | --- |
| 1 | **库身份用内容定**（`catalog.db` 内的 ID；路径只是挂载点）+ 多路径 / 离线解析 | 见 R1-12：业内真痛点 + 机制性差异 |
| 2 | **`flowbar`：把"工作流 = 有序流水线"做成常驻控件** | 这是产品定位级别的选择（代价见 R1-06，但方向不换） |
| 3 | **命令注册表的纪律化接入**（64 条命令 / 31 条带默认键；`AGENTS.md` §2.15 要求新命令必须当场决定热键） | 少见的"防能力腐化"工程约束；不是"学 VS Code" |
| 4 | **无边线 + 四级面 + 明令禁毛玻璃/禁阴影 + 双档密度**，且「色差看不清时优先加大阶梯、不许改回描边」 | 有骨架的约束，不是偏好 |
| 5 | **送外部编辑器的战略**（承认 RAW 不做极致；明确标注 catalog 型软件的"编辑集成"是制造数据损坏） | 全项目最清醒的一条战略判断 |
| 6 | **issue 选中态由 profile 哈希推导**（不存选中 ID） | 「参数一偏离定稿就自动显示工作副本、回到完全一致就自动选中」这个语义**免费成立**，并顺带消掉了"选中态与内容不一致"的脏状态 |
| 7 | `easy copy` / `easy destroy` / `shortpath` / 三点拖拽 / 空态水印 / 流光动效两套时长口径 | 原创度高、边界划得准（尤其 `easy destroy` 只给"从集合里移除"、不给真删磁盘） |
| 8 | **反重复实现的既有机制**（`PhotoViewingStage` / `TilesShell` / `film-strip-prefs` / `untrack` 在插入点） | 已经是本仓最强的工程资产之一，继续按 §2.12 执行 |

---

## 5. 留给下一轮评审的问题（R2 的输入）

1. **R1-01 / R1-08 的取舍结果**：色彩管理与出图质量是否被写进 DoD？没写的话，首个公开版本拿什么说服专业用户？
2. **R1-02 的结果**：集合到底进不进首个公开版本？如果进，静态集合与保存查询哪个先？
3. **M4-W4 落地后回看**：把 RAW 精修送出去这条路，真实用户会不会真的走？（这是 R1 判断里最依赖猜测的一条）
4. **真机证据补齐**：R1 全程**没有任何来自真机的证据**。下一轮评审应当有
   `AGENTS.md` §5.3.2 那份验证清单的真机结果，否则"观感/性能/色彩"这类判断仍然悬空。
5. **文档体量的自我约束**：随着 M4/M5 展开，规格文档总量会继续长。
   是否需要一个"到期归档/裁剪"的机制（例如已实现且稳定的章节压成一行 + 指针）？

---

## 6. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-26 | **建立本文件 + R1 首次评审**（人类要求分析当前产品设计在行业中的位置）：产出总体判断、行业位置判定、12 条待办（P0 三条：R1-01 色彩管理 / R1-02 集合 / R1-08 出图质量基线）、8 条「已确认保持」、5 个留给下一轮的问题。**全部 12 条均为 `待决`，尚未有任何人拍板** |
| 2026-09-26 | **人类拍板 R1-01 / R1-04**（同日，口述）：① R1-01 → **已采纳**，「把色彩管理方案落成完整的执行方案，后面安排执行」→ 已定稿到 **`specs/color-management.md`**；② R1-04 → **不做**（人类改判）：`Shift` 只“置为选中”、不提供区间取消、不支持 `Shift+Ctrl` 同按，**两端算法固定不变**（已落地，口径见 `BROWSE.md` § 5.2.1）。另：`plans/` 目录经人类要求**改名为 `specs/`**（纪律见 `AGENTS.md` § 5.4） |
| 2026-09-26 | **人类定 M5 之后的优先级：三条主线** —— 色彩管理 / XMP（方便导入迁移）/ 强化相片集，其余仍留 `FUTURE.md`（原话入 `docs/user-requirements.md`）。对应本表：R1-01、R1-03、R1-02 **三条全部转为 `已采纳`**；已记录到 `PLAN.md`「M5 之后：三条主线」并同步 `PLAN.md` §2 决策记录（色彩管理那条**取代** 2026-09-15 的「第一阶段不做」）。⚠️ **均未排期**；相片集那一条人类已有创意想法、待专门讨论，**不预设形态** |

| 2026-09-27 | **R1-09 已落地**（文档统一，人类 2026-09-27 授权「如果是文档上你现在就可以统一掉」）：`DESIGN.md` §9.2 的密度令牌表改对（旧值 40/52/40 → **36/44、48/62、36/44**）并加「本表由 §8.2 派生」约束；`design/{export,browse,editor,main}.md` 四处把「画布硬写值」与实现值分开表述。另：**新增 `DESIGN.md` §5.3「两个品牌色各自的职责」** —— 人类澄清「**辅色不是只给指向用**，指向只是适应面很广的 case；辅色的作用是在主色密集处制造差异、提高识别率」，§5 开头的「全局规则」已限定为「管指向/点击用什么色」 |

---

## 7. R2：M5 并行可行性、release 现状与 Windows 分发信任

评审时间：2026-09-26 20:22:16 CST
触发：崔总要求先确认 M5 能否与开发中的 M4 并行，再核对现有自动升版脚本与进度，讨论签名、SmartScreen 与商店费用。
本轮范围为审查和讨论准备，没有启动 M5 实现、升版、打包、签名、tag、push 或上传。

### 7.1 判断与取舍

- **R2-01**：可并行的是工程准备，公开发行与最终安装/升级验收仍等四大模块收口。当前 M4-W3 的最新记录已完成开发与冒烟，W4/W5 的五格式与真实执行、W6 外部编辑尚缺；真实照片与 Windows GUI 验收单列。M5 并非只有安装包，还含首启、更新、许可与支持，不能按已有 debug exe 判为完成。建议先做 W1 的发布链路和安装配置，W3 文档可并行整理；W2 的签名验证/通道逻辑可用合成输入开发；W4 的身份与分发方案应提前选定，实际发布仍由崔总执行。不对当前路线自行改判。
- **R2-02**：已有 release 底座可复用，18 条既有单测通过，但不等于发布闭环。最主要缺口是只写 package.json、只构建前端，与 Cargo/Tauri 的版本来源未同步；其余参数、输出目录和失败恢复证据见本轮实施记录。应原地扩展，避免新造一套升版或 Windows 构建逻辑。
- **R2-03**：优先评估「官网直下 + 商店 MSIX」与 SignPath Foundation 免费签名，不把购买 EV 或 SmartScreen 无警告设为开发开工前置。微软当前新入口开发者注册免费；MSIX 经商店认证后由微软代签，EXE/MSI 商店提交仍需开发者自行签名。Tauri 现成商店教程只覆盖 EXE/MSI，MSIX 需额外包装及数据/子进程/外部编辑兼容验证，不能宣称现有配置改一项即完成。商店信任不自动延伸到官网的 EXE。官网下载来源信任、更新包签名、Windows 发布者签名与 SmartScreen 信誉分别说明。
- **R2-04**：已有迁移在库比程序新时拒绝打开，所以升级后的程序回退不是数据库回退。恢复承诺必须按失败发生的阶段写清楚，复用迁移前快照且避免覆盖升级后新增编辑。官网安装包可使用 Tauri updater 自持密钥；第三方构建可换源与公钥或关闭更新，仍保留验证。商店 MSIX 由商店负责更新，避免两个安装器争写同一份程序。

### 7.2 官方依据（2026-09-26 查阅）

- 微软开发者开户新入口与费用：https://learn.microsoft.com/en-us/windows/apps/publish/partner-center/open-a-developer-account?tabs=individual 。当前文档新流程个人/公司都无注册费用，须从 https://storedeveloper.microsoft.com/ 进入并完成身份/企业验证；本轮未进行实际开户验证。
- 签名选项：https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options 。MSIX 商店代签免费，EXE/MSI 自行签；EV 自 2024 年起不再获得即时 SmartScreen 放行。Artifact Signing 有主体地区限制，不能仅凭每月价格认定中国个人可申请。
- SmartScreen 与 Smart App Control：https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation 。有效签名仍需信誉，自签不能建立公共 Windows 信任；开启 Smart App Control 的机器可能直接拦截不受信任的程序，官网应按不同提示解释，不能笼统保证都有「仍要运行」。
- 免费开源签名：https://signpath.org/ 、https://signpath.org/terms.html 。需符合源仓构建、MFA、签名/隐私政策等条件，申请是否获批尚未确认。
- Tauri updater：https://v2.tauri.app/plugin/updater/ 。更新签名不可关闭，可配置源与公钥；此密钥不等同于 Windows Authenticode 证书。
- Tauri 商店现成路径：https://v2.tauri.app/distribute/microsoft-store/ 。当前教程覆盖 EXE/MSI，不提供直接 MSIX 输出。
- MSIX 包装与数据迁移：https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/packaging/ 。MSIX 与传统安装的数据位置可能不同，须有明确迁移与卸载保留策略。

所有路线建议为 **待决**；已验证只有既有 release 单测、dry-run 与只读代码核对，没有安装/升级/卸载 E2E 或证书/商店账户实测。


### 7.3 授权后的实施跟进

2026-09-26 崔总授权「按计划先推进 M5 中能推进的部分」。工程准备已开发并通过冒烟，参见 `implementations/2026-09-26_m5-distribution-engineering.md`；阶段状态以 `PLAN.md` M5 表为准。R2-03 的具体 SignPath/商业证书/Store 路线仍未拍板，保留官网直下及显式无签名选项，不因证书成本阻断。没有生成真实安装器或宣称 Windows 信任/E2E 完成。
